# asm/nonmatching

Assembly for functions or regions that are not yet matched in C.

As C functions become matching, move the corresponding assembly out of the link
path and keep original reference assembly under `asm/original/`.

