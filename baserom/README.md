# baserom

Local ROM inputs go here and are ignored by git.

For now this repo targets only US Rev 0. The known parent-workspace master input
is:

```text
Ogre Battle 64 - Person of Lordly Caliber (U) [!].v64
```

Do not commit ROM binaries.

