# assets

Asset source forms belong here when they become part of the reproducible decomp
tree.

Suggested layout:

- `sprites/`
- `textures/`
- `models/`
- `audio/`

Bulk raw extraction output should stay in the parent research workspace or
`build/` until curated.

