# After-action report: Highway D-F checkpoint 001 promotion

Status is completed and review-pending. Six frozen results now expand canonical matching C from 23 to 29 exact owners. This prepares one stable promotion subject. No action is required from Joe; the Parent Director must freeze and route independent review.

## Assignment and boundaries

The assignment is `OB64-MC-6LW01-PROMO-DF-CP001-20260804-R1`.

Canonical `main` started clean at `38e9348438cc0255f9bf44a159e42ba0eb5ec056`.

The starting configuration matched SHA-256 `C1221A8FF12270BF20B96E94A159839066F0DCA83FB411BA0B73A7B133AB2513`.

The worker changed only the canonical promotion files and assigned records.

No lane tree, coordination tree, accepted commit, fallback assembly, branch, worktree, commit, remote, editor, emulator, or runtime state changed.

## Promotion result

The final configuration contains 29 matching-C targets.

The first 23 target objects and compiler object match canonical `HEAD` exactly.

The six appended objects match their accepted-result trees.

The appended order follows the frozen checkpoint manifest.

| Symbol | Classification | Assembly-bearing | Bytes |
|---|---|---|---:|
| `func_0002DE10` | `ASSEMBLY_REQUIRED` | true | 64 |
| `func_000238B0` | `JUSTIFIED_HYBRID` | true | 88 |
| `func_001072B8` | `PURE_MATCHING_C` | false | 212 |
| `func_0000B030` | `PURE_MATCHING_C` | false | 128 |
| `func_0011B344` | `PURE_MATCHING_C` | false | 236 |
| `func_00007600` | `PURE_MATCHING_C` | false | 136 |

The checkpoint contributes 864 reviewed bytes.

Every symbol, row, primary ID, section, source, and fallback remains unique.

Pairwise interval analysis found no overlapping target ranges.

The final configuration SHA-256 is `7AD3F59E5B2A9F9254B62727B14EE3189E783DA7D9FF2AB93F0813A17418D8E9`.

## Classification preservation

The worker copied all classification fields from the frozen checkpoint without reinterpretation.

The `func_0002DE10` result remains `ASSEMBLY_REQUIRED` and retains its complete inline-assembly body.

The `func_000238B0` result remains `JUSTIFIED_HYBRID` and retains its accepted `move` macro.

Their worker and reviewer evidence paths still match the frozen SHA-256 values.

The four pure matching-C results remain non-assembly-bearing.

The evidence index records each immutable justification, confirmation, path, and hash.

## Verification summary

Fresh Splat generation passed in the isolated output root.

Fresh Phase 7 conventional build and verification passed.

Fresh Phase 8 matching-C build and verification passed for all 29 targets.

Both phases preserve full-ROM SHA-256 `571E83396BC81E70DA4C0A20313D82DBD7DFE685F2C37418C8E27F927E2CC67A`.

Both phases preserve code-region SHA-256 `40D4E7875BA50F005788611C63CF9C42D9154339B36793556BF045C25B64B409`.

Phase 8 preserves 7,242 primary rows, 7,251 link slices, and 19 overlay reservations.

The preservation record sets `fullRomExact: true` and `originalAssemblyTargetsNotLinked: true`.

All 29 asm-differ results set `exact: true` and `currentScore: 0`.

The evidence index contains the exact commands, prerequisite identities, output hashes, and function contracts.

## Material claims

| Claim | Evidence grade | Review status | Evidence |
|---|---|---|---|
| Six frozen results were integrated from accepted trees. | `Verified` | pending | Target and source identity audit |
| The 23 baseline targets and compiler contract remain preserved. | `Verified` | pending | Baseline object comparison |
| Frozen classifications and assembly evidence remain unchanged. | `Verified` | pending | Checkpoint and hash audit |
| The 29-target conventional and matching-C builds remain exact. | `Verified` | pending | Fresh Phase 7 and Phase 8 reports |
| Gameplay meaning or runtime behavior is proven. | Not claimed | pending | Static matching has no semantic runtime proof |

## Changed surfaces

The technical promotion changes:

- `config/phase8/matching-c.json`.
- `src/lib/mod_s64_tail.c`.
- `src/lib/sprintf.c`.
- `src/lib/func_001072B8.c`.
- `src/boot/boot_resource_lzss_load_entry.c`.
- `src/lib/func_0011B344.c`.
- `src/boot/boot_state_slot_target_peer_record_dispatch.c`.

The worker created the assigned task log, evidence index, after-action report, and handoff manifest.

All changes remain uncommitted and unstaged for Parent intake.

## Failed methods and limits

One patch wrapper failed because shell output truncated a cumulative configuration.

A second wrapper failed because native argument quoting removed JavaScript string delimiters.

Neither wrapper changed repository files. A PowerShell JSON extraction supplied the exact accepted objects.

No required build or verification gate failed.

The static result does not prove gameplay semantics, runtime behavior, editor readiness, or release safety.

## Protocol deviations

No branch, worktree, commit, push, publication, pull request, or task creation occurred.

The worker used one fresh external Phase 7 root and one fresh external Phase 8 root, as assigned.

No setup gate replaced the required conventional and matching-C gates.

## Proposed canonical-document changes

No canonical status or semantic document change is proposed before independent review.

## Evidence index

`C:\Users\Joe\Projects\OgreBattlel64\OB64 Decomp\docs\matching-c\ob64-mc-6lw01-df-cp001-promotion-20260804\evidence-index.md`

## Next action

The Parent Director must freeze this worker result and route fresh independent promotion review.

The worker does not issue an acceptance verdict.
