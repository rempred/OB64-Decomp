# Chunk 26 — CODE-dominant MIXED: FP-heavy char/class/scenario/encounter code + 3 inline data islands (`0x001A1000..0x001B1000`)

Source-ownership pass for the twenty-seventh 64 KiB chunk. **CODE-dominant MIXED**: an incoming
FUNCTION straddler-tail, FP-heavy character/class/scenario/encounter code, three inline DATA
islands, and an outgoing FUNCTION straddler-head. **96 tracked parts**: **81 code + 15 data**.
Byte-exact rebuild preserved (code SHA `40D4E787…B409`, ROM SHA `571E8339…CC67A`). Coverage
`0x1000..0x1B1000` = 1,769,472 B = **62.1041 %** of the 2,849,204-byte evidenced executable
extent; code-only classified = 1,451,576 B (**50.9468 %**).

Pipeline: parent-evidence sweep + byte-exact proret scan → 13 code-slice + 1 data analysis agents
(Workflow) → reconcile (kind-normalize + 7 preamble-orphan/cross-slice folds) → `check_boundaries`/
`check_splits` PASS → 7-agent adversarial swarm. Adversarial moved **2 boundaries** (1 missed
function split, 1 data/code seam) + LOW note/name fixes; all applied and re-gated.

## Region map (ROM-ordered)
| Region | Range (z64) | Bytes | Class | Notable |
|---|---|---:|---|---|
| Incoming straddler-tail | `0x1A1000..0x1A11F8` | 504 | code | `func_001A0264_chunk26tail` |
| CODE 1 (+ D1 island) | `0x1A11F8..0x1A4474` | — | code+data | FP loops; D1 UI labels |
| Embedded DATA island | `0x1A4474..0x1A4C10` | 1,948 | data | ramp LUT + packed records + 0x8021 ptr table + double pool |
| CODE 2 (dispatchers) | `0x1A4C10..0x1A8F2C` | — | code | `func_001A4C10` switch-dispatcher; ESET loader `func_001A6D64` |
| D4 menu DATA island | `0x1A8F2C..0x1A9290` | 868 | data | options-menu strings + record array + ptr tables |
| CODE 3 (big dispatcher) | `0x1A9290..0x1B0F78` | — | code | `func_001A9290` (9.3KB, 23 callees); `func_001AF828` reward-queue writer |
| Outgoing straddler-head | `0x1B0F78..0x1B1000` | 136 | code | `func_001B0F78` |

Most scanner-flagged "float runs" are **FP CODE** (lwc1/div.s/mul.s/…), not float pools — this
chunk is FP-heavy like chunks 18/24. The genuine data is the three islands.

## Incoming FUNCTION straddler (chunk 25 → chunk 26)
`func_001A0264_chunk26tail` `[0x1A1000,0x1A11F8)` — tail of `func_001A0264` whose `addiu $sp,-0x88`
prologue is in chunk 25 at `0x1A0264`. Ends `jr$ra`@`0x1A11F0` + delay `addiu $sp,0x88`@`0x1A11F4`.
`kind:"straddler-tail"`.

## Data islands (3; 15 parts, 2,996 B — `docs/data-index/rev0/chunk26-data-region-inventory.json`)
- **D1** `[0x1A2B6C,0x1A2C20)` (180 B, 4 parts): `rodata_001A2B6C` ASCII `"Soldier"`/`"Thrust"` +
  `table_001A2B80` byte index/ramp LUT + `zero_fill_001A2BD0` + `jumptable_001A2BE0` (16 0x802-band
  jump pointers). Adversarially confirmed 0 prologues/0 `jr$ra`.
- **Embedded island after `func_001A42A4`** `[0x1A4474,0x1A4C10)` (1,948 B, 3 parts): `data_001A4474`
  (0x04..0x0F up/down byte-ramp LUT — the audit's "ramp table") + `data_001A4880` (packed
  0xE7/0xD9/0xFF records with an embedded 0x8021-band 6-pointer table) + `float_001A4BE0` (IEEE-754
  DOUBLE pool: 1.45, 1.2, 2*pi). `func_001A42A4` returns at `0x1A446C`; the island sits between it
  and `func_001A4C10`. Adversary scanned all 489 words: 0 prologues/0 `jr$ra`.
- **D4** `[0x1A8F2C,0x1A9290)` (868 B, 8 parts): `rodata_001A8F2C` options/debug-menu string pool
  (33 strings: "Message speed", "Icon name display", "Legion indicator", "Destination display",
  "Unit report type", "Battle action name", "Battle animation", "Restore defaults", …) +
  `data_001A90B0` (14×0x10 menu-record array) + `table_001A9190`/`table_001A9238` (0x8021/0x8022
  pointer tables) + small byte/index `data_*` + zero-fill.

Decoded ASCII exports: `data/decoded/rev0/strings/chunk26-options-menu-1A8F2C.md`,
`chunk26-ui-labels-1A2B6C.md` (+ matching JSON indexes). Distinguish: **parsed** (ASCII, doubles,
pointer tables) vs **raw-but-classified** (byte LUTs, packed records, menu-record array — field
semantics undecoded).

## Notable code (names stay conservative `func_*`)
- **`func_001A42A4`** `[0x1A4264,0x1A4474)` — FP distance/vector loop; entered via a 3-nop + FP
  read-before-write preamble at `0x1A4264` (parent prologue `0x1A42A4`); self-contained, one `jr$ra`.
- **`func_001A4C10`** `[0x1A4C10,0x1A5388)` — switch-dispatcher (preamble-orphan; internal `jr $v0`
  jump-table @`0x1A51E8` + `j 0x802xxxxx` tail-jumps, all internal).
- **`func_001A6D64`** (parent lead = runtime **ESET/enemy-set loader**; wiki has live PJ64 disasm;
  calls `resource_alloc`; processes char.name). Preamble-orphan entry `func_001A6D5C`.
- **`func_001A9290`** `[0x1A9290,0x1AB72C)` — 9,372 B char-data **dispatcher** (23 callees);
  adversarially confirmed ONE prologue (`addiu $sp,-0xB0`@`0x1A9298`) + ONE `jr$ra`@`0x1AB724`. The
  parent editor's claimed "103×4B jump table at `0x1AB030`" is **executable class-promotion handler
  CODE** here, refuted byte-exactly.
- **`func_001AF828`** — parent lead **reward-queue writer** (`sh` into `0x801F0B50`).

## Outgoing FUNCTION straddler (chunk 26 → chunk 27)
`func_001B0F78` `[0x1B0F78,0x1B1000)` — entry is a 2-word read-before-write preamble
(`lui/lbu $v0`) @`0x1B0F78`, prologue `addiu $sp,-0x18`@`0x1B0F80` (body reads `$v0` via `bne`
@`0x1B0F84`). **NO `jr$ra` in `[0x1B0F78,0x1B1000)`**; continues into chunk 27 as
`func_001B0F78_chunk27tail`. `kind:"straddler-head"`.

## Adversarial pass (7 verifiers: 5 code + 2 data)
2 boundary moves: (1) `func_001A2CA0` split at `0x1A2F38` (a frameless leaf was merged in);
(2) `float_001A4BE0` data/code seam moved `0x1A4C18`→`0x1A4C10` (2 words were the `func_001A4C10`
entry preamble). LOW fixes applied: `func_001AB770`→`func_001AB768` (part-start naming),
"Frameless"→"Framed" note wording, second `jr$v0` dispatcher documented in `func_001AD0E4`. Both
data islands + D1/D4 confirmed 0 prologues/0 `jr$ra`; `func_001A9290` confirmed single; outgoing
straddler confirmed 0 `jr$ra`.

## Method & verification
Parent-evidence sweep + proret scan → slice_extract (13 code slices, D4 carved) → 13 code + 1 data
agents (Workflow) → reconcile (kind-normalize + 7 folds: `func_001A286C`/`func_001A2C20`/
`func_001A32CC`/`func_001A3490`/`func_001A4264`/`func_001A8CCC`/`func_001B0F78`) → `check_boundaries`
PASS (96 parts; 0 of each) + `check_splits` 0 fragments → 7-agent adversarial swarm (2 boundary
moves + LOW fixes) → re-gate → `split_original_mips_part --remove-source`. Gates: `check_manifest`
ALL PASS (27 chunks); `assemble_original_mips` byte-exact (code `40D4E787…B409`); 0 data files with
function/true-entry/boundary wording; data-index JSON valid.

## Caveats / next frontier
- Conservative code names (overlay-relocated; true runtime band `0x801Bxxxx-0x801Cxxxx`, NOT the
  decode-column `0x8021/0x8022`). ESET-loader / reward-writer / dispatcher identities are strong
  parent leads but names stay `func_*`.
- Undecoded data: the ramp-LUT purpose, packed-record field layouts, the 14×0x10 menu-record array
  fields, and the overlay-relocated pointer-table targets.
- **Next frontier `0x001B1000` (chunk 27).** FIRST continue the outgoing **FUNCTION** straddler:
  `func_001B0F78` continues into chunk 27 — emit `func_001B0F78_chunk27tail` `[0x1B1000,0x1B1070)`
  first (ends `jr$ra`@`0x1B1068` + delay `addiu $sp,0x18`@`0x1B106C`).
