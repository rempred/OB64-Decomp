# Chunk 9 — all-code: army-mgmt / display-list builders (`0x00091000..0x000A1000`)

Source-ownership pass for the tenth 64 KiB chunk. **ALL CODE — the first
single-class chunk since chunk 2**: an incoming function-straddler tail + 32
parent-detected framed functions + an outgoing function-straddler head. **34
tracked parts**: **32 code `func_*` + 2 straddler markers (1 tail + 1 head)**, **0
data**. Byte-exact rebuild preserved (code SHA `40D4E787…B409`, ROM SHA
`571E8339…CC67A`). Coverage `0x1000..0xA1000` = 655,360 B = **23.0015 %** of the
2,849,204-byte evidenced executable extent; code-only classified ≈ 524,572 B
(**18.41 %**).

Chunk 9 is heavily **PARENT-DETECTED** (33 parent fns, 31 named) and unusually
clean: every function is single-prologue / single-`jr $ra`, with the only boundary
correction a single 8-byte preamble-orphan fold. An adversarial swarm (4 region
agents + 1 dedicated data-hunter) tried to disprove the all-code claim and every
boundary and **found nothing** (max inline pointer-run 1 word, max zero-run 1 word,
0 undecodable words across all 16,384 words).

## Region map (single region — all code)

| Region | Range (z64) | Bytes | Class | Parts |
|---|---|---:|---|---:|
| STRADDLER+CODE | `0x91000..0x000A1000` | 65,536 | code | 34 (1 straddler-tail + 32 `func_` + 1 straddler-head) |

No interior data region. The naive content scan found 0 pointer-word runs, 0 zero
runs, 0 ASCII runs ≥ thresholds; the dedicated adversarial data-hunter confirmed it
quantitatively (see Method).

## What this code is
The parent DB names all 33 functions `character::char-data consumer`, active states
`army_mgmt` / `class_change_transition` / `mission_briefing`. The adversarial
data-hunter identified the dominant idiom as **F3DEX/RDP display-list builders**:
store-heavy code assembling graphics command words (RDP opcodes `0xFC../0xF5../
0xE8../0xE6../0xF0../0x07..` via `lui/ori` immediate halves, written through a
running pointer at `-0x6460($800F)`). This explains the store-heavy instruction mix
(`sw` 2,982 / `lui` 3,176 / `addiu` 2,131) and the **659 out-of-chunk `j` targets**
— these are relocated overlay tail-jumps (`j 0x801A…/0x801B…`), **NOT function
boundaries** (the same pattern flagged in chunk 8). Conservative `func_*` names are
retained — overlay relocation makes RAM/global/callee identity unverified.

## Incoming straddler (chunk 8 → chunk 9)
`func_00090e54_chunk9tail` `[0x91000,0x912F4)` is the honest tail of `func_00090E54`
(true entry `0x90E54` in chunk 8; head `func_00090e54_chunk8head`). Confirmed from
disasm: no prologue at `0x91000` (first chunk word is `lw $a1,0x18($s2)`, mid-body);
ends `jr $ra`@`0x912EC` + epilogue `addiu $sp,0x30`@`0x912F0`; the next function's
prologue `addiu $sp,-0x30` is exactly at `0x912F4`. Frame `-0x30` matches the
chunk-8 parent prologue's `-0x30`. **NOT an independent function** —
`kind:"straddler-tail"`.

## CODE `0x912F4..0xA0DAC` (32 framed functions)
Parent-detected, contiguous, textbook-clean. Per-part return/prologue profile: every
function = exactly 1 `addiu $sp,-N` prologue + exactly 1 `jr $ra`, 0 internal
returns, 0 inline pointer words. Notable structure:
- **One preamble-orphan (the only boundary correction):** parent labeled a function
  start at `0x95260`, but its true entry is `0x95258` — an 8-byte read-before-write
  load `lui $v1,0x8019`@`0x95258` + `lw $v1,0x6AF8($v1)`@`0x9525C` (global
  `0x80196AF8`, consumed at `0x952AC addiu $s6,$v1,0x1BDA` before any store). Folded:
  `func_000943A0` ends `[0x943A0,0x95258)` (jr $ra@`0x95250` + epilogue@`0x95254`);
  `func_00095258` = `[0x95258,0x96B74)` with an entry label + note.
- **Two internal jump-table dispatchers (NOT function boundaries):**
  `func_00095258` has `jr $v0`@`0x96930` (9-case: `lbu/sltiu 0x9/sll2/lui 0x801F/
  addu/lw -0x6C0($at)` → table at `0x801EF940`); `func_000A0560` has `jr $v0`@`0xA06F0`
  (7-case: `sltiu 0x7/sll2/lui 0x801F/addu/lw -0x698($at)`). **Both target tables live
  in `0x801F…` relocated overlay RAM, not inline ROM** (verified — no inline data
  island). The case code after each dispatch belongs to the same function.
- **Five genuine multi-entry functions** (a secondary entry reached only by
  fallthrough — kept as ONE part each): `func_00091FE0`(sec `0x92410`),
  `func_000934B0`(sec `0x93A28`), `func_00093E74`(sec `0x9437C`),
  `func_0009DB5C`(sec `0x9DC04`), `func_000A02F8`(sec `0xA037C`).
- **No hidden frameless leaves, no over-merges:** prologue count (33) = `jr $ra`
  count (33); the adversarial pass confirmed no return hides inside any "single"
  function (a frameless leaf would need its own return).

Largest functions (all single, validated whole): `func_00095258` 6,428 B,
`func_00091FE0` 5,328 B, `func_0009C90C` 4,688 B, `func_000943A0` 3,768 B,
`func_0009BC80` 3,212 B.

## Outgoing straddler (chunk 9 → chunk 10)
`func_000A0DAC_chunk9head` `[0xA0DAC,0xA1000)` — true entry `0xA0DAC` (prologue
`addiu $sp,-0x30`), body continues **past `0xA1000` to `0x000A118C`** in chunk 10.
Confirmed: NO `jr $ra` anywhere in `[0xA0DAC,0xA1000)`; the last word `0xA0FFC` is
`bne $v0,$zero,…`@`0x14400018`, whose delay slot is the FIRST word of chunk 10.
`kind:"straddler-head"`. **Chunk-10 first action: emit `func_000A0DAC_chunk10tail`
`[0xA1000,0x000A118C)` (tail), then content-scan the rest of `0xA118C..0xB1000`.**

## Method & verification
`promote_original_mips` (seed) → `dump_function_context` (33 parent fns) +
content-scan (`build/scan_chunk9.js`, gitignored: prologues/returns/pointer-runs/
ASCII/zero/`j`-targets) → `plan_chunk` (`--tail-end 0x912F4`) → `slice_chunk
--disasm` (7 slices) → **7-agent analysis swarm** (one per slice; frameless-leaf
recovery, over-merge un-merge, preamble-orphan fold, conservative naming) →
`integrate_chunk` → `check_boundaries` + `check_splits` → **5-agent adversarial
swarm** (4 region refuters + 1 dedicated data-hunter) → `split_original_mips_part
--remove-source`. No tool changes this chunk.

Deterministic invariants over all 34 code parts (`check_boundaries`): 0 fragments,
0 cross-boundary branches, 0 under-splits, 0 delay-slot leaks, 0 straddler-position
errors, 0 inline data islands. Per-part profile: every function single-prologue,
single-`jr $ra`; 0 pointer words chunk-wide. Adversarial data-hunt (whole chunk):
max consecutive `0x80xxxxxx` run = 1 word, max zero run = 1 word, max printable run
= 3 words, 0 of 16,384 words undecodable; both dispatcher tables in relocated RAM.
Gates: `check_manifest` ALL PASS (10 chunks / 1,576 parts); `check_boundaries` PASS;
`check_splits` 0 fragments; `assemble_original_mips` byte-exact (code SHA
`40D4E787…B409`); `verify_setup` PASS (10 / 1,576 / 90); `audit_code_region` OK; 0
data files with function wording; `git diff --check` clean.

## Caveats / next frontier
- Code names are conservative overlay address labels (RAM/global/callee identity
  suspect; the linear decode-comment RAM column is wrong for overlay code). The
  "display-list builder / army-mgmt UI" role is a structural inference, not
  runtime-proven.
- Jump-table dispatcher targets (`func_00095258`@`0x96930`, `func_000A0560`@`0xA06F0`)
  read from `0x801F…` relocated RAM — targets not resolved.
- **Next frontier `0x000A1000` (chunk 10).** FIRST continue the straddler:
  `func_000A0DAC_chunk9head` continues to `0x000A118C` (chunk-10 tail file
  `func_000A0DAC_chunk10tail` `[0xA1000,0x000A118C)`, true entry 0xA0DAC). Chunk 10
  should remain largely parent-detected — but content-scan for interior data first
  (chunks 5–8 each had data; chunk 9 did not — do not assume either way).
