# Chunk 12 — all-code: FP-math + dispatcher-heavy char-data code (`0x000C1000..0x000D1000`)

Source-ownership pass for the thirteenth 64 KiB chunk. **ALL CODE** (no interior
data). **74 tracked parts**: **72 code `func_*` + 2 straddler markers (1 tail + 1
head)**, **0 data**. Byte-exact rebuild preserved (code SHA `40D4E787…B409`, ROM SHA
`571E8339…CC67A`). Coverage `0x1000..0xD1000` = 851,968 B = **29.9020 %** of the
2,849,204-byte evidenced executable extent; code-only ≈ 721,180 B (**25.3118 %**).

Same `character::char-data consumer` / FP-math family as chunks 9–11, but
**dispatcher-heavy**: 20 internal `jr $reg` jump-table dispatchers (tables in `0x801F`
relocated overlay RAM). A 5-agent adversarial swarm (4 region refuters + 1
data-hunter) found 0 data and confirmed all boundaries after 3 preamble-orphan fold
corrections.

## Region map (single region — all code)

| Region | Range (z64) | Bytes | Class | Parts |
|---|---|---:|---|---:|
| STRADDLER+CODE | `0xC1000..0x000D1000` | 65,536 | code | 74 (1 straddler-tail + 72 `func_` + 1 straddler-head) |

## Incoming straddler (chunk 11 → chunk 12)
`func_000C0EDC_chunk12tail` `[0xC1000,0xC132C)` — honest tail of `func_000C0EDC`
(true entry `0xC0EDC` in chunk 11, head `func_000C0EDC_chunk11head`). Confirmed: no
prologue at `0xC1000` (mid-body); ends `jr $ra`@`0xC1324` + epilogue `addiu
$sp,0x70`@`0xC1328`; next function `0xC132C`. `kind:"straddler-tail"`.

## CODE `0xC132C..0xD0B8C` (72 functions)
- **Deferred-prologue function `func_000C132C` `[0xC132C,0xC1578)`:** the parent DB
  labels a start at `0xC1364` (its `addiu $sp,-0x10` prologue), but the TRUE entry is
  `0xC132C` — the function begins with ~56 B of frameless setup (`lui 0x8019/lbu
  0x39D4`, stores `sh 0x1A4/0x18A`, a `bne`@`0xC1360` whose delay slot IS the `addiu
  $sp,-0x10`@`0xC1364`), then runs to `jr $ra`@`0xC1570`. No return between `0xC132C`
  and `0xC1570` → one function; live registers (`$a1`=`*0x80196AF8`, `$t1`/`$t2`)
  cross the deferred prologue. Adversarially confirmed.
- **12 recovered frameless leaves** (parent over-merged/missed), incl. the gap1
  cluster `0xC2328..0xC2530` (3 leaves: `func_000C2330`/`C23DC`/`C248C`) and
  `func_000C9708` (a `addiu $sp,-8` mini-frame copy leaf).
- **~24 preamble-orphans folded** (read-before-write `lui/lw 0x80196AF8`, or
  `lui/lhu` of a `0x800E/0x800F` global, before a parent prologue). 3 were corrected
  in the adversarial pass (`func_000c6bec`/`func_000c71e0`/`func_000c7d00` — the
  analysis agent had left the preamble in the previous function's tail).
- **20 internal `jr $reg` jump-table dispatchers (NOT boundaries):** at `0xC48A4`,
  `0xC4AC8`, `0xC58EC`, `0xC602C`, `0xC67B8`, `0xC6A60`, `0xC6E94`, `0xC7228`,
  `0xC7724`, `0xC9898`, `0xCC870`, `0xCCF64`, `0xCDE3C`, `0xCE044`, `0xCE654`,
  `0xCE6F0`, `0xCE758`, `0xCF7F4`, `0xD0D9C`, `0xD0FBC` — all `sltiu/sll/lui 0x801F/
  addu/lw/jr` (or register-computed RAM base), **target tables in `0x801EF…/0x801F…`
  relocated overlay RAM, not inline ROM** (adversarially confirmed).
- **FP-math code** (e.g. the RGB555 colour-convert loop at gap2 `0xC5630..0xC58B0`,
  and clusters at `~0xC6CExx`, `~0xCC560`): float constants are built in-stream via
  `lui/ori + mtc1` or `ldc1` from relocated RAM — **0 bare float-data words chunk-wide**.

## Outgoing straddler (chunk 12 → chunk 13)
`func_000D0B8C_chunk12head` `[0xD0B8C,0xD1000)` — true entry `0xD0B8C` (prologue
`addiu $sp,-0x30`), body continues **past `0xD1000` to `0x000D110C`** in chunk 13.
Confirmed: NO `jr $ra` in `[0xD0B8C,0xD1000)`; last word `0xD0FFC` = `addiu
$s0,0x33` (mid-stream). `kind:"straddler-head"`. **Chunk-13 first action: emit
`func_000D0B8C_chunk13tail` `[0xD1000,0x000D110C)`.**

## Method & verification
`promote_original_mips` (seed) → `dump_function_context` (61 parent fns, 28 gaps) +
content-scan (`build/scan_chunk.js`, gitignored) → `plan_chunk` (`--tail-end
0xC132C`) → `slice_chunk --disasm` (8 slices) → **8-agent analysis swarm** →
`integrate_chunk` → `check_boundaries` + `check_splits` → **5-agent adversarial
swarm** (4 region refuters + 1 data-hunter; flagged 3 unfolded preamble-orphans, all
fixed) → re-`check_boundaries` → `split_original_mips_part --remove-source`. No tool
changes.

Deterministic invariants over all 74 code parts (`check_boundaries`): 0 fragments,
0 cross-boundary branches, 0 under-splits, 0 delay-slot leaks, 0 straddler-position
errors, 0 inline data islands. Per-part profile: 0 parts with >1 `jr $ra` (no
under-splits), 0 inline pointer words. Adversarial data-hunt (whole chunk): max
pointer-run 0, max zero-run 3 (alignment pads), max printable-run 0, 0 bare float-data
words, 0 undecodable words; all 20 dispatcher tables in relocated RAM. Mid-run +
final gates: `check_manifest` ALL PASS; `check_boundaries` PASS; `check_splits` 0
fragments; `assemble_original_mips` byte-exact; `verify_setup` PASS; 0 data files with
function wording.

## Caveats / next frontier
- Conservative overlay address names (RAM/global/callee identity suspect; linear
  decode-comment RAM column is wrong). Roles are structural + parent-DB inferences,
  not runtime-proven.
- 20 dispatcher target tables read from `0x801F…` relocated RAM — not resolved.
- **Next frontier `0x000D1000` (chunk 13).** FIRST continue the straddler:
  `func_000D0B8C_chunk12head` continues to `0x000D110C` (chunk-13 tail file
  `func_000D0B8C_chunk13tail` `[0xD1000,0x000D110C)`, true entry 0xD0B8C). Chunk 13's
  source-report profile (1,526 nops, only 21 parent function labels) differs sharply
  from chunks 9–12 — content-scan for DATA first; it may be data-heavy or
  frameless-dense.
