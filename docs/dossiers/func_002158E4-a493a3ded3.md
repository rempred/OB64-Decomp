# func_002158E4: Exact Hybrid Promotion and PURE_C Exhaustion

## Status

`func_002158E4` is an exact `HYBRID_C` owner. It is not matching C and does not
contribute to the `PURE_C` count.

- Accepted range: `0x002158E4..0x002159D0` (236 bytes)
- Active source: `src/battle/func_002158E4.c`
- Active source SHA-256: `C3B18C44ADE13DB19119F0AED8EF483C0219BB6DFCFF287B611C5FDBA1F9B698`
- Target and linked SHA-256: `2F0A1C6BC565B80B40015D55DD7944DC2C2C842E488763608D932D9BBCE6A8BD`
- Relocation contract: 27 records, `MATCH`
- Preserved best PURE_C candidate: `A493A3DED358110F479B3B356FFF631E55AF0A687441A1BFD46F4DA7667177A3`
- Preserved PURE_C source: `docs/archive/matching-c-candidates/2026-09-03-func_002158E4-a493a3ded3.c`

## PURE_C exhaustion

The preserved PURE_C candidate has the exact accepted extent and all correct
behavior. Its canonical linked body differs only at offsets `0x18`, `0x1C`,
and `0x20`: KMC saves the allocator return into `$s0` before loading
`D_801CE8BC`, while retail loads the owner first and then performs the save.

The continuation systematically tested these source families:

- direct global arguments versus a one-use owner local;
- owner/context-store statement order and declaration placement;
- distinct allocator-result and long-lived snapshot variables;
- pointer/integer return types, casts, and old-style versus prototyped calls;
- `register`, `const`, pointed-to `volatile`, volatile globals, and volatile
  locals where meaningful;
- comma expressions, nested allocator calls, argument-side assignments, and
  GNU statement expressions;
- one- and two-field aggregate carriers;
- address-taking/aliasing forms;
- unused incoming parameters as preferred-register carriers;
- explicit blocks, duplicated equivalent branches, and goto-shaped lifetimes;
- neighboring retail allocator sequences with the same load-before-save form.

Direct-global, volatile-local, address-taken, aggregate, and duplicated-branch
forms changed the stack frame, saved-register allocation, or wider prologue.
All remaining forms optimized back to the same three-word difference.

Acceptance-compiler probe
`D4DD932B74447A316B8F7348DDB1157B7316DAEC676DD437DAF271331354FE52`
captured the combine, first scheduler, local allocation, global allocation,
second scheduler, and delay-slot passes. It shows both operations ready
immediately after the allocator call; KMC gives the long-lived return-value
save priority over the independent global load. Ordinary C shapes did not
reverse that final scheduler tie.

## Bounded compiler-influence fallback

The active source uses one extended-assembly statement with an empty template.
It ties the allocator value to the long-lived snapshot output and presents the
owner as a simultaneous input. The statement emits no instruction and
implements no behavior; the complete 236-byte target remains compiler output
from the surrounding C. Its sole effect is to make KMC schedule the owner load
before the allocator-result save.

This is nevertheless an assembler mechanism under source policy, so the
translation unit is truthfully `HYBRID_C`. A future PURE_C reopen requires a
new source shape or compiler capability that expresses the same scheduler
dependency without an assembler escape hatch.

## Canonical acceptance

Focused canonical diff:

```text
Source class ............... HYBRID_C
Decoded instruction rows ... EXACT
Raw linked bytes ........... EXACT
Differing bytes ............ 0
Relocation contract ........ MATCH
```

Focused `node tools/verify.js --target func_002158E4` also passed baserom
identity, toolchain, source policy, C ownership, placement, relocations, target
bytes, and full-ROM identity, ending in `RESULT: EXACT BASELINE`.
