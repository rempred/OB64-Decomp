# lib chunk 46 — `0x002E1000..0x002F1000`

Classification: **DATA TERRITORY — source-owned as data parts** (no code). The entire 64 KiB chunk
is non-code data, PAST the evidenced executable-MIPS extent end `0x002B89B4`. It continues the
chunk-45 data tail (`data_002E0D68`) across `0x2E1000`. Owned as honest data parts; **not** code.

- Split JSON: `build/chunk_002E1000-002F1000_splits.json`
- Data index: `docs/data-index/rev0/chunk46-data-region-inventory.json`
- Promoted source: `asm/original/rev0/lib/` (17 parts)

## Composition

**17 parts = 9 data + 8 zero_fill (0 code).** No incoming/outgoing function straddler (data
continuation). One contiguous high-entropy graphics/texture asset region segmented by 8 small
(16–32 B) zero-fill runs separating texture/graphics objects.

- `data_002E1000` `0x2E1000..…` — **continuation of chunk-45 `data_002E0D68`**.
- … 9 `data_` blobs interleaved with 8 `zero_fill_` gaps …
- `data_002EF7F8` `…..0x2F1000` — final data object, runs to the chunk end (continues into chunk 47).

## Data accounting (Data Territory Mode)

- Total **65,536 B** — `data` 65,356 B (raw-but-classified) · `zero_fill` 180 B (parsed, all-zero).
- **raw-but-classified**: the 9 data spans (high-entropy graphics/texture/asset pixel data; not
  field-decoded; possibly compressed). **parsed**: the 8 zero_fill spans. **undecoded-and-unclassified: 0 B.**
- Every byte byte-owned and classified; no byte fallback-only.

## Proof of non-code

- **0 `jr $ra`**, **0 stack prologues** (`0x27BD8xxx..0x27BDFxxx`), **0 `0x80xxxxxx` RAM-pointer
  words** across all 16,384 words — no function structure possible.
- No real ASCII strings (1,564 coincidental printable bytes). No archive/LZSS/MIO0 magic at any part
  start.
- `audit_code_region` non-code tail; only chunk-46 bytes owned (global tail NOT reclassified).

## Parent workspace evidence sweep

Covered by the run-wide `0x2D1000..0x301000` sweep (see review handoff). **Runtime-load evidence**
(`Cutscene Frames/dma_log.txt`): 3 real PI-DMA cart→RAM transfers SOURCE chunk 46 — romoff
`0x2e1110`, `0x2e14c2`, `0x2e1872`, each 0x3c0 B (the 0x3c0 length is a capture-filter artifact, NOT
a record format), loaded to scattered RAM (`0x801206f0`/`0x801255b0`/`0x801260f0`), all
**archive-unmapped** (raw cart data, not LHA-compressed). Confirms this data is raw packed asset bytes
copied at runtime via PI DMA. Asset-shape hint: `scripts/ob64_4a_audit.json` block `0x2e81d8`
(CI4_TILE, claimed 153600 B but overruns the chunk end by ~51 KB — scanner heuristic flooding a
high-entropy run; reconciled = texture-shaped, not a proven block). `scripts/ob64_functions.json`:
**0 functions** in range. No archive entry, no compression magic, no pointer directory. No combat
overlay source (RSR-001). No names, no hook sites.

## Mistakes corrected / hazards handled

- None structural — uniformly non-code. Segmented at the 8 genuine zero-fill runs. No data forced
  into `func_*` files.

## Verification

`check_boundaries` PASS (17 parts, 0 code); `check_splits` 0 fragments; `check_manifest` 17 parts;
`assemble_original_mips` byte-exact (code SHA unchanged); `audit_code_region` extent unchanged.
Adversarial swarm (hidden-code + structure + tiling): clean — see review handoff. Runtime states:
none. Patch-workbench: none.

## Next-run first action (chunk 47, `0x002F1000`)

Chunk 46 ends in DATA. The `data_002EF7F8` high-entropy texture region continues across `0x2F1000`
into chunk 47. Own that continuation first.
