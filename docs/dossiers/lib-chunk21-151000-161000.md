# Chunk 21 — MIXED: class/character-lookup code + trailing high-entropy data (`0x00151000..0x00161000`)

Source-ownership pass for the twenty-second 64 KiB chunk. **MIXED**: an incoming FUNCTION
straddler-tail, a large CODE region (class/character lookup, including the
`classLookup_full` lead), then a trailing high-entropy DATA region (possibly compressed)
ending in an outgoing DATA straddler. **99 tracked parts**: **94 code (incl. 1 incoming
straddler-tail) + 5 data**. Byte-exact rebuild preserved (code SHA `40D4E787…B409`, ROM SHA
`571E8339…CC67A`). Coverage `0x1000..0x161000` = 1,441,792 B = **50.6033 %** of the
2,849,204-byte evidenced executable extent; code-only classified ≈ 1,209,308 B
(**42.4437 %**).

Pipeline: parent context + content scan (code/data boundary `0x15FBF0`) → 7 code-slice + 1
data-region analysis agents (Workflow) → combine → `check_boundaries`/`check_splits` →
4-agent adversarial swarm (3 code + 1 data). **1 disproof** (a missed frameless leaf at
`0x15F838`); fixed. No tool changes.

## Region map (3 ROM-ordered regions)

| Region | Range (z64) | Bytes | Class | Parts |
|---|---|---:|---|---:|
| incoming straddler-tail | `0x151000..0x15105C` | 92 | code | 1 |
| CODE (class/character lookup) | `0x15105C..0x15FBF0` | 60,308 | code | 93 |
| DATA (trailing, high-entropy) | `0x15FBF0..0x161000` | 5,148 | data | 5 |

## Incoming function straddler (chunk 20 → chunk 21)
`func_00150550_chunk21tail` `[0x151000,0x15105C)` — tail of `func_00150550` whose true
entry `0x150550` is in chunk 20. Returns `jr $ra`@`0x151054` + delay `addiu $sp,0x40`@
`0x151058`; the next function starts exactly at `0x15105C` (parent end). `kind:"straddler-tail"`.

## CODE `0x15105C..0x15FBF0` (93 parts)
Overlay-relocated (decode RAM column `0x801C/0x801Dxxxx` is the WRONG linear map; real
runtime slots `0x8020xxxx` band, scenario state). 25 framed prologues + 17 preamble-orphans
(read-before-write folds; each named `func_<startaddr>` with `label`=name) + 23 frameless
leaves the parent over-merged + plain code. Internal `jr $v0` jump-table dispatchers
(`0x153910` → relocated `0x80214CE0`, `0x155554` → `0x80214CF8`) and `j 0x801xxxxx/
0x802xxxxx` overlay tail-jumps kept internal. FP-heavy bodies (cvt.s.w/div.s/cvt.d.s/
c.lt.d/bc1f) are CODE. **`func_001591FC`** is the parent-documented `classLookup_full` lead
(name kept conservative; see parent evidence). `func_001574B8` is the parent's
runtime-override hook lead. No inline data islands.

## Adversarial correction
The code-C verifier disproved one boundary: `func_0015F694` (preamble-orphan, inner
prologue `0x15F6A4`, `-0x30` frame) had a **full epilogue** `jr $ra`@`0x15F830` + delay
`addiu $sp,0x30`@`0x15F834`, then a **distinct no-prologue frameless leaf** at `0x15F838`
(`lui $a3,0x801F` / reads globals `0x801F0DE0`/`0x801F36C4`, ends `jr $ra`@`0x15F95C`). Split
into `func_0015F694 [0x15F694,0x15F838)` + `func_0015F838 [0x15F838,0x15F964)`. code-A/B and
data-A: 0 disproofs.

## DATA `0x15FBF0..0x161000` (5 parts) + outgoing straddler
- The last code function ends `jr $ra`@`0x15FBE8` + delay `addiu $sp,0x18`@`0x15FBEC`; data
  begins exactly at `0x15FBF0` (adversarially confirmed). The region is **HIGH-ENTROPY**
  (random-looking high-byte words — e.g. `0xEA15F31D`; likely compressed) → a `0x00010001`
  fill run (terminator `0xFFFF0001`@`0x15FC44`) → a small entropy tail (`0xDF000000`+zero @
  `0x15FDF0`) → the outgoing straddler.
- **VERIFIED no hidden code:** the entire region `[0x15FBF0,0x161000)` has **0
  `addiu $sp,-N` prologues and 0 `jr $ra` (0x03E00008)** (adversarial data-hunter scan) —
  the linear-map fallacy does NOT recur here.
- **Outgoing DATA straddler `data_0015FDF8_chunk21head`** `[0x15FDF8,0x161000)` — a packed
  small-byte stream (values `0x0F`-`0x2B`, e.g. `0x13131313`) running to the last word
  `0x13131313`@`0x160FFC` with no terminator → continues into chunk 22. `kind:"data"`.

## Parent-workspace evidence (sweep 2026-06-23)
- **`0x001591FC` = `classLookup_full`** — the one hand-named function in this range
  (docs/overlay-system.md:21, ram-snapshot-findings.md, mips-decode.md; runtime RAM
  `0x8020808C`, snapshot-verified; size 632 B; Rev0≠Rev1.1). Recorded as a strong LEAD;
  name stays `func_001591FC`. The symbols_v2 `ram` `0x801C8DFC` is the WRONG linear
  back-map.
- LEADS: `0x001574B8` = primary runtime-override hook candidate (encounter consumer, 2344 B,
  runtime `0x80206348`, CRC-safe); `0x14E538`/`0x1531DC`/`0x15305C` = class/variant lookup
  cluster (mid-function). `0x0015FBF0` = the editor's "overlay stub-cave" (runtime
  `0x8020EA80`) — a patch cave, NOT a function entry; correctly the data boundary. All other
  functions carry only heuristic auto-classifier tags (names stay `func_*`).

## Data inventory (machine-readable: `docs/data-index/rev0/chunk21-data-region-inventory.json`)
**Total data: 5,148 B in 5 parts.** By class: outgoing straddler-head 4,616 B · packed-fill
424 B · high-entropy blob 96 B · packed/unknown 12 B. State: **raw-but-classified** (high
entropy / possibly compressed; no fixed structure decoded). **Undecoded:** the entropy
blob's contents (no strings/pointers/fixed stride evident; follow-up: test against the LZSS
decoder if a header is found). No ASCII/text → no human-readable string export.

## Method & verification
Parent context + content scan (code/data boundary `0x15FBF0` pinned at the last fn's
`jr $ra`@`0x15FBE8`) → 7 code-slice + 1 data agent (Workflow) → combine (`combine_chunk.js`;
data names → `<kind>_<addr>`; kinds normalized) → `check_boundaries` PASS (99 parts; 0
fragment/cross/under/leak/straddler/data-island) + `check_splits` 0 fragments → 4-agent
adversarial swarm (1 missed-leaf fix; data-hunter confirmed no hidden code) → re-gate →
`split_original_mips_part --remove-source`. Gates: `check_manifest` ALL PASS (22 chunks /
2,800 parts); `assemble_original_mips` byte-exact (`40D4E787…B409`); `verify_setup` all
green; `audit_code_region` OK; data-index JSON valid; 0 data files with function wording.

## Caveats / next frontier
- Conservative code names (overlay-relocated, runtime `0x8020xxxx`; `classLookup_full`
  identity recorded as a lead, name stays `func_001591FC`). The trailing high-entropy data
  is undecoded (possibly compressed; no structure evident).
- **Next frontier `0x00161000` (chunk 22).** FIRST continue the outgoing DATA straddler:
  `data_0015FDF8_chunk21head` `[0x15FDF8,0x161000)` (packed small-byte stream, no terminator)
  continues into chunk 22 — emit `data_00161000_chunk22tail` first and prove its end.
