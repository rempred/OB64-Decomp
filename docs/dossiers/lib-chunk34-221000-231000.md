# lib chunk 34 — `0x00221000..0x00231000`

Classification: **MIXED** (code → interior combat-overlay DATA island → code →
outgoing function straddler-head).

- Split JSON: `build/chunk_00221000-00231000_splits.json`
- Data index: `docs/data-index/rev0/chunk34-data-region-inventory.json`
- Promoted source: `asm/original/rev0/lib/` (120 parts)

## Composition

**120 parts = 89 normal code + 1 straddler-tail + 1 straddler-head + 29 data.**

Region map:

- Incoming FUNCTION straddler-tail `func_0021EBBC_chunk34tail` `0x00221000..0x002213DC`
  (continues `func_0021EBBC`, prologue `addiu $sp,-0x2E8` in chunk 33 @`0x0021EBBC`;
  returns `jr $ra`@`0x002213D4` + delay `addiu $sp,0x2E8`@`0x002213D8`).
- CODE region A `0x002213DC..0x00228D6C` — promotion / level-up / class-def / combat
  code (frame-bearing functions + many recovered frameless leaves + read-before-write
  preamble-orphans).
- Interior DATA island `0x00228D6C..0x0022A280` (5,396 bytes) — combat-overlay data:
  0x801D/0x801E-band handler/jump pointer tables, packed GBI/RDP display-list blobs,
  IEEE float/double constant pools, and ASCII message-string pools.
- CODE region B `0x0022A280..0x00230A9C` — more promotion/class-def code incl. the big
  dispatcher `func_0022D14C` (`0x22D14C..0x22EC08`) and `func_0022F580`
  (`0x22F580..0x230A9C`).
- Outgoing FUNCTION straddler-head `func_00230A9C` `0x00230A9C..0x00231000`
  (prologue `addiu $sp,-0x160` @`0x00230A9C`; NO `jr $ra` before the boundary; continues
  into chunk 35 as `func_00230A9C_chunk35tail`, returns `jr $ra`@`0x002317C0`).

## Data island inventory (29 parts, 5,396 bytes)

Bytes by class: table 2,284 · float 1,472 · data 1,460 · rodata 176 · zero_fill 4.
Full per-part index: `docs/data-index/rev0/chunk34-data-region-inventory.json`.

Highlights:

- **0x801D/0x801E pointer tables** (`table_*`) — runtime handler/jump pointer runs for the
  combat overlay (e.g. ~192-entry run @`0x00229AB0`, ~108 @`0x00229F18`, ~60 @`0x0022A0E0`,
  ~41 @`0x0022A1D8`). These are overlay RAM pointer values, NOT linear back-maps.
- **Packed GBI/RDP display-list blobs** (`data_*`) — words like `0xE7000000`/`0xE3001xxx`
  (RDP set-other-modes/fill-style style); stride unresolved.
- **Float/double constant pools** (`float_*`) — ratio ramps (0.1667, 0.333, 0.5 … 1.0),
  scale constants (2.0/5.0/10.0/15.0/20.0/30.0/45.0), `pi` (`0x40490000`), doubles 0.8/1.5.
- **ASCII message strings** (`rodata_*`) — `"%s cannot be used."`, `"%s joined the
  battalion."`, `"%s was not persuaded."`, `"%s became %s."`, `"%s fled."`, `"%s has
  regenerated."`, the `"AddTimeTable:"` label, and a `"NULL"` sentinel.

## Role leads (conservative; names stay `func_*`)

From the parent workspace evidence sweep (`docs/promotion-system.md`,
`growth-system.md`, `combat-function-notes.md`, `overlay-system.md`) — leads only,
not applied as names:

- `func_0021EBBC` (the incoming straddler) is the parent-named **classChangeStateMachine**
  (combat overlay runtime RAM `0x801DB8EC`); its case handlers exit via `j 0x801DDxxx`
  overlay tail-jumps.
- This code runs in the **combat overlay** (runtime band `0x801D0000..0x801FFFFF`); the
  data island sits in the combat working-data band. `levelUpMain` lives at `0x21C140`
  (chunk 33). Parent role tags on these functions: `promotion consumer`,
  `class-def consumer`, `character::char-data consumer` — leads only, validated against
  byte-exact disassembly.

## Mistakes corrected / hazards handled

- Parent DB over-merged tiny frameless leaves and missed several; the analysis swarm
  recovered them (e.g. `func_002247CC`; the cluster `func_0022653C/00226580/002265CC/
  00226654/00226718` and `func_002258CC/00225A20/00225B70`; `func_002285B8/002286EC/
  0022891C`).
- Slice-seam / read-before-write preamble-orphans folded FORWARD: `func_002215D0`
  (`lui 0x801D / lw -0x173C` before prologue @`0x2215D8`), `func_00222CA8`,
  `func_00226AFC`, `func_00228090`, `func_002289C0`, plus `func_00228A88` and
  `func_00228B3C` (both `lui $v1,0x800F / lw -0x6460` = `*0x800F9BA0` DL pointer before
  their prologues). Each previous part ends at the preamble start; label == own name.
- **Preamble-orphan name/label mismatch (assemble hazard, fixed):** the slice-A6 agent
  named the last two preamble-orphans for their *prologue* address (`func_00228A90`,
  `func_00228B44`) while starting the part at the *preamble* (`0x228A88`, `0x228B3C`),
  so the synthesized header glabel collided with the body's parent boundary-candidate
  glabel. Renamed to the preamble-start names `func_00228A88` / `func_00228B3C`. The
  outgoing straddler-head `func_00230A9C`'s redundant header label was dropped (its body
  already carries the parent boundary glabel). Assembly is byte-exact after the fix.
- The 5,396-byte interior gap was treated as a DATA candidate and classified honestly
  (NOT forced into a `func_*` file). Adversarial data skeptics scanned every word for
  prologues/`jr $ra` and found no hidden code.
- Small 8–12B inter-function gaps that are pure alignment `nop` padding stay folded into
  the preceding function's tail (e.g. the 3 nops @`0x224F24`).

## Verification

`check_boundaries` PASS (0 fragment/cross/under/leak/straddler/data-island);
`check_splits` 0 fragments. Adversarial swarm (3 code + 2 data + 1 straddler skeptics).
Byte-exact rebuild preserved (see review handoff).
