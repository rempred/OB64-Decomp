# Chunk 13 — MIXED: char-data code + unit-management UI data (`0x000D1000..0x000E1000`)

Source-ownership pass for the fourteenth 64 KiB chunk. **MIXED** — the first chunk
with interior data since chunk 8. **67 tracked parts**: **27 code (26 `func_*` + 1
straddler-tail) + 40 data**. Byte-exact rebuild preserved (code SHA `40D4E787…B409`,
ROM SHA `571E8339…CC67A`). Coverage `0x1000..0xE1000` = 917,504 B = **32.2021 %** of
the 2,849,204-byte evidenced executable extent; code-only classified ≈ 760,884 B
(**26.70 %**).

Two ROM-ordered regions: CODE `0xD1000..0xDAB18` (39,704 B, dispatcher-heavy
char-data code) then DATA `0xDAB18..0xE1000` (25,832 B, unit/battle-management UI
data). A 3-agent adversarial swarm (1 code + 2 data) found 0 disproofs and confirmed
the code→data boundary and every data classification.

## Region map

| Region | Range (z64) | Bytes | Class | Parts |
|---|---|---:|---|---:|
| STRADDLER+CODE | `0xD1000..0xDAB18` | 39,704 | code | 27 (1 straddler-tail + 26 `func_`) |
| DATA | `0xDAB18..0x000E1000` | 25,832 | data | 40 |

## Incoming straddler (chunk 12 → chunk 13)
`func_000D0B8C_chunk13tail` `[0xD1000,0xD110C)` — honest tail of `func_000D0B8C`
(true entry `0xD0B8C` in chunk 12, head `func_000D0B8C_chunk12head`). Confirmed: no
prologue at `0xD1000` (mid-body); ends `jr $ra`@`0xD1104` + epilogue `addiu
$sp,0x30`@`0xD1108`; next function `0xD110C`. `kind:"straddler-tail"`.

## CODE `0xD110C..0xDAB18` (26 functions)
Dispatcher-heavy char-data code, same family as chunks 9–12. Adversarially
confirmed: 27 code functions ↔ 27 `jr $ra` (1:1, no under-split). Includes:
- **13 preamble-orphans folded** (read-before-write `lui/lw 0x80196AF8` before a
  parent prologue).
- **6 frameless leaves** (`func_000D43D4`, `func_000DA66C`, `func_000DA730`,
  `func_000DA77C`, `func_000DAA84`, + the straddler-tail) — each a true `$sp`-free leaf.
- **~17 internal `jr $reg` jump-table dispatchers** (tables in `0x801F` relocated
  overlay RAM, NOT boundaries).
- **Code→data boundary `0xDAB18`** (CRITICAL, adversarially confirmed): the last
  function ends `jr $ra`@`0xDAB10` + delay `nop`@`0xDAB14`; `0xDAB18/0xDAB1C` are two
  alignment-zero words; `0xDAB20` = `0x54797065` = ASCII "Type" — the data region
  begins. No code resumes past `0xDAB18`; no data before it.

## DATA `0xDAB18..0xE1000` (40 parts, 25,832 B) — unit/battle-management UI data
Classified conservatively (no field semantics invented). 100% data
(adversarially confirmed: 0 prologues, 0 `jr $ra` in the region). Machine-readable
inventory: `docs/data-index/rev0/chunk13-data-region-inventory.json`. By class:

| Class | Bytes | Notes |
|---|---:|---|
| string-pool (`rodata_`) | 984 | 2 pools |
| ram-pointer-table (`table_`) | 7,548 | 10 parts |
| zero-fill (`zero_fill_`) | 216 | 5 parts |
| packed/record (`data_`) | 17,084 | 23 parts (incl. display-list stream + straddler) |

Highlights:
- **`rodata_000dab18`** `[0xDAB18,0xDADD0)` — field labels + item/status pool:
  "Type"/"Stats"/"Ele."/"Qty."/"Cost"/"Alpha."/"No."/"Move"/"Level"/"Atk."/"Def.",
  weapon names (Thrusting Sword…Doll), armor (Small Shield…Accessory), categories
  (Expendable:/Valuable:), status names, formations (Mobile Wall…Soldier Reserves);
  `0x0E`/`0x0F`/`0x10`/NUL framing.
- **`rodata_000db860`** `[0xDB860,0xDB980)` — menu/help pool: "Element:"/"Execute"/
  "Continue"/"Cancel"/"Execute Sort"/"Arrange Manually"/"Buy"/"Returns character to
  reserves.\nUnit disbands…"/"Changes position of characters in the unit."/
  "(Soldier)"/"Character Reserves"/"(Unit Disband)".
- **`table_*` (10)** — RAM-pointer tables in the `0x801A..0x801E` band, including long
  identical-pointer fill runs (e.g. ~83× `0x801DB274`) → sparse handler/pointer
  tables. The coarse-scan "glyph blobs" were mostly these identical-pointer runs.
- **`data_000de250`** `[0xDE250,0xE0BD0)` (~10,624 B) — a display-list/command record
  stream: repeating 8-word record `E7000000/…/060006xx/…/DF000000/00000000` with a
  monotonically incrementing `0x06xxxxxx` offset word.
- **IEEE float blocks** (`data_000dbbe8` @`0xDBBF4`: 40.0/64.0/−32.0/…; `data_000dc600`:
  1.05/1.0/0.95 + double π).
- **Zero-fill** at `0xDAE10`, `0xDBE18` (22 words), `0xDBE98` (15 words), `0xDDFF0`,
  `0xDE234`.

### Outgoing data straddler (chunk 13 → chunk 14)
**`data_000e0bd0_chunk13head`** `[0xE0BD0,0xE1000)` — a high-byte-dense packed/glyph/
compressed blob (65.3% bytes ≥0x80, 0% zero, no terminator) that runs to the chunk
boundary and **continues into chunk 14**. Its start `0xE0BD0` is a real boundary (the
preceding command stream ends with a `DF000000/00000000` terminator at `0xE0BC8`).
**Chunk-14 first action: emit `data_000e1000_chunk14tail` `[0xE1000,?)`** owning the
continuation; determine its end from the bytes in chunk 14.

Data state split (conservative): parsed ≈ 216 B (zero-fill); raw-but-classified ≈
24,544 B (string pools + pointer tables + classified records/display-list/floats);
undecoded ≈ 1,072 B (the packed/glyph straddler + a small packed blob `data_000ddf60`
whose compression/bitmap structure is unresolved).

## Method & verification
CODE: `dump_function_context` (`0xD1000..0xDAB18`) + content-scan → `plan_chunk`
(`--tail-end 0xD110C`) → `slice_chunk --disasm` (3 slices) → 3-agent code analysis
swarm. DATA: content-segmentation → 4-agent data-classification swarm over
`[0xDAB18,0xE1000)` (4 sub-regions). Combine code+data → `check_boundaries` +
`check_splits` → 3-agent adversarial swarm (1 code-boundary + 2 data-classification;
0 disproofs) → `split_original_mips_part --remove-source`. No tool changes.

Gates: `check_manifest` ALL PASS (13 chunks / 1,943 parts); `check_boundaries` PASS
(67 parts; 0 fragment/cross/under/leak/straddler/data-island; data parts exempt from
code checks); `check_splits` 0 fragments; `assemble_original_mips` byte-exact;
`verify_setup` PASS; `audit_code_region` OK; 0 data files with function wording;
data-index JSON parses + ranges match the manifest.

## Caveats / next frontier
- Code names are conservative overlay address labels (RAM/global/callee identity
  suspect). 17 dispatcher target tables read from `0x801F…` relocated RAM — not resolved.
- **Data is conservatively classified** — no field semantics invented. The
  display-list/command record format, the packed/glyph blob structure, and the
  pointer-table → string/handler wiring are NOT decoded (follow-up work). The
  `0x0E/0x0F/0x10` string-framing control bytes are observed, not fully specified.
- **Next frontier `0x000E1000` (chunk 14).** FIRST continue the OUTGOING DATA
  straddler: emit `data_000e1000_chunk14tail` `[0xE1000,?)` — the continuation of the
  packed/glyph blob `data_000e0bd0_chunk13head`; prove its end from the chunk-14
  bytes. Then content-scan the rest of chunk 14 (it may stay data-heavy — chunk 13's
  data could extend, or code may resume).
