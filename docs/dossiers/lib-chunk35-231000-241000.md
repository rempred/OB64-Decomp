# lib chunk 35 — `0x00231000..0x00241000`

Classification: **MIXED** (code → interior float/record DATA island → code →
outgoing function straddler-head). Frameless-leaf / preamble-orphan dense.

- Split JSON: `build/chunk_00231000-00241000_splits.json`
- Data index: `docs/data-index/rev0/chunk35-data-region-inventory.json`
- Promoted source: `asm/original/rev0/lib/` (134 parts)

## Composition

**134 parts = 127 normal code + 1 straddler-tail + 1 straddler-head + 5 data.**

Region map:

- Incoming FUNCTION straddler-tail `func_00230A9C_chunk35tail` `0x00231000..0x002317C8`
  (continues `func_00230A9C`, prologue `addiu $sp,-0x160` in chunk 34 @`0x00230A9C`;
  returns `jr $ra`@`0x002317C0` + delay `addiu $sp,0x160`@`0x002317C4`; frame matches).
- CODE region A `0x002317C8..0x00239B94` — promotion / class-def / FP stat-scaling
  code; ends with two recovered trailing byte-compare frameless leaves.
- Interior DATA island `0x00239B94..0x0023A3A0` (2,060 bytes) — float32 ramp pool,
  packed parameter/record blob, and an 0x801F-band pointer/IEEE-double record table.
- CODE region B `0x0023A3A0..0x00240FF0` — class/character/display-list code; the big
  command dispatcher `func_0023C114` (`0x23C114..0x23D050`); GBI/RDP display-list builders.
- Outgoing FUNCTION straddler-head `func_00240FF0` `0x00240FF0..0x00241000`
  (prologue `addiu $sp,-0x20` @`0x00240FF0`; NO `jr $ra` before the boundary; continues
  into chunk 36).

## Data island inventory (5 parts, 2,060 bytes)

Bytes by class: table 1,360 · data 636 · float 48 · zero_fill 16.
Full per-part index: `docs/data-index/rev0/chunk35-data-region-inventory.json`.

- `zero_fill_00239b94` — 4 align nops before the float pool.
- `float_00239ba4` — 12 IEEE float32 constants, monotone ramp 0.6, 0.8, 1.0, 1.3, 1.6,
  2.0, 2.3, 2.6, 3.0, 3.4, 3.7, 4.0 (`0x3F19999A..0x40800000`).
- `data_00239bd4` — ~636B packed parameter/record/color blob (untyped; stride unresolved).
- `table_00239e50` / `table_00239ec0` — 0x801F-band RAM-pointer / IEEE-double record table
  (repeating pointer groups interleaved with double constants 0.1/1.2/1.5/1.6/0.8/100.0).

## Role leads (conservative; names stay `func_*`)

Combat/class/promotion overlay code (parent role tags `promotion consumer`,
`class-def consumer`, `character::char-data consumer`; `combat` active state) — leads
only, validated against byte-exact disassembly. `func_0023C114` is a large command/
opcode dispatcher (cascaded `slti`/`beq`, `j 0x801E8DAC` tail-jumps).

## Mistakes corrected / hazards handled

- **Slice-seam preamble-orphan at `0x00240D20`** (adversarial HIGH, fixed): the B5/B6
  seam split backward-folded the `lui $v1,0x800F / lw $v1,-0x6460($v1)` (= `*0x800F9BA0`,
  the RDP/GBI display-list pointer) into `func_00240964`'s tail. It is a read-before-write
  preamble for the next function (body reads `$v1` at `0x240D40`/`0x240D50` before any
  write). Corrected: `func_00240964` ends at `0x240D20`; the next part is `func_00240D20`
  (preamble + prologue @`0x240D28`).
- Read-before-write preamble-orphans folded FORWARD: `func_0023A5EC` (`lui 0x8019/lbu`
  before prologue @`0x23A5FC`), `func_0023DAA4`, `func_0023E06C`, `func_0023E3C8`.
- Parent over-merges un-merged into recovered frameless leaves: `func_00234544`
  (frameless `jr $v0` jump-table dispatcher), `func_0023683C`, `func_00237E18`,
  `func_00239B74` + `func_00239B84` (trailing byte-compare leaves), `func_0023DB24`,
  `func_0023E674`, `func_0023E708`, and a cluster of tiny return-stub leaves
  `func_0023B220/00239B234/0023B23C/0023B244` + range-check helper `func_0023B680`.
- The 2,060-byte interior gap classified honestly as DATA (NOT forced into a `func_*`
  file); adversarial data skeptic scanned every word — no hidden code.
- The 60B (`0x23B210`) and 76B (`0x23B678`) "gaps" the run prompt flagged as data
  candidates were proven to be CODE (align nops + recovered frameless leaves) by
  byte-exact disassembly, NOT data.

## Verification

`check_boundaries` PASS (0 fragment/cross/under/leak/straddler/data-island);
`check_splits` 0 fragments (4 informational TINY return-stub leaves, all with `jr $ra`).
Adversarial swarm (3 code + 1 data + 1 region-B-leaf + 1 straddler skeptics): 5 clean +
1 HIGH fixed (the `0x240D20` fold above). Byte-exact rebuild preserved (see review handoff).
