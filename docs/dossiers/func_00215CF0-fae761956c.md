# func_00215CF0: Preserved Matching-Workbench Candidate

## Status

This source is a research candidate. The original assembly remains the accepted owner.

- Candidate: `FAE761956C1D221F00281248C73A96A158FBE3C247F7F8735166089B08D2E975`
- Target: `func_00215CF0` at ROM `0x215CF0`
- Source: `docs/archive/matching-c-candidates/2026-09-04-func_00215CF0-fae761956c.c`
- Source SHA-256: `4258B7B7B3C2D704B113689946D5EB77384CDF42329A0EEAAC8B6C8B06A09F0C`
- Latest scratch class: `length-mismatch`
- Latest scratch score: `28.52`
- Scratch extent: `3236` / `3400` bytes (`809` / `850` instructions)
- Recognized scratch relocations: `293`
- Scratch artifact: `build/matching/runs/807E859C6D2C72F2385934F6D582B7C90EE755C309445184D6E9D42A51C04FE6`

## Preservation reason

Wave 4 blocked PURE_C candidate: restored the 0x100-byte local buffer, three distinct floating-point sentinel comparisons, symbolic globals and calls, exact 0x140 frame/stack offsets, and retail-like explicit state dispatch/shared-tail placement. Scratch output is 3236/3400 bytes (809/850 instructions), score 28.52, with 293 recognized relocations; remaining 164-byte extent and CFG mismatch requires further source-shape reconstruction, so original ASM ownership is retained.

## Continuation result

The Wave 4 continuation reduced the deficit from `420` bytes / `105`
instructions to `164` bytes / `41` instructions. Restoring the missing
`0x100`-byte local text buffer and three floating-point sentinel paths recovered
the retail `0x140` frame. Replacing numeric addresses and provisional library
names with typed symbols eliminated two compiler-hoisted global-base saved
registers, leaving the retail three-register save set and exact stack offsets.

The state selector now lowers as the retail branch chain rather than a dense C
switch, states 8 and 9 retain their no-op behavior, and the three shared state
tails are placed after states 10 and 11. The sentinel blocks use the distinct
retail constants at `D_801E66A8`, `D_801E66B0`, and `D_801E66B8`; their normal
`bc1f` orientation, loads, conversions, comparisons, decrements, and stores are
all present. KMC still chooses a different legal schedule for the decrement and
common store.

After masking only the candidate's complete relocation records and branch
displacements for the bounded shape audit, the first substantive divergence is
at relative offset `0x1F4`: retail fills the jump delay on the shared snapshot
tail with `li $a0, 0x6094`, while this PURE_C lowering emits a nop. Later
differences include the sentinel scheduling described above and unresolved
control-flow/source shape; the object remains 41 instructions short and the
workbench reports 170 blocks versus 177. Register-allocation or relocation-only
tuning is therefore premature. A hybrid body fallback is outside Wave 4 scope,
so the stronger candidate is preserved and original assembly remains the sole
accepted owner.

## Evidence boundary

Scratch object comparison does not prove canonical linker ownership, relocation resolution, target bytes, or full-ROM identity. Resume through the normal diff and verification workflow.

## Workbench-reported first difference

The generic workbench reports an unresolved relocation field first because the
target remains ASM-owned and has no accepted C relocation manifest. This does
not supersede the bounded machine-shape findings above.

```json
{
  "actual": "lui $a2, 0x0000",
  "actualWord": "0x3C060000",
  "expected": "lui $a2, 0x801D",
  "expectedWord": "0x3C06801D",
  "index": 0,
  "offset": 0,
  "pc": 2149394976
}
```
