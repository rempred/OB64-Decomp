# lib chunk 45 — `0x002D1000..0x002E1000`

Classification: **DATA TERRITORY — source-owned as data parts** (no code). The entire 64 KiB chunk
is non-code data, PAST the evidenced executable-MIPS extent end `0x002B89B4`. It continues the
chunk-44 data tail (`data_002CBA58` high-entropy texture region) seamlessly across `0x2D1000` (no
zero gap at the seam). Owned as honest data parts; **not** code.

- Split JSON: `build/chunk_002D1000-002E1000_splits.json`
- Data index: `docs/data-index/rev0/chunk45-data-region-inventory.json`
- Promoted source: `asm/original/rev0/lib/` (15 parts)

## Composition

**15 parts = 8 data + 7 zero_fill (0 code).** No incoming/outgoing function straddler (data
continuation). One contiguous high-entropy graphics/texture asset region segmented by 7 small
(16–28 B) zero-fill runs separating texture/graphics objects.

- `data_002D1000` `0x2D1000..…` — **continuation of chunk-44 `data_002CBA58`** (seamless, no header).
- … 8 `data_` blobs interleaved with 7 `zero_fill_` gaps …
- `data_002E0D68` `…..0x2E1000` — final data object, runs to the chunk end (continues into chunk 46).

## Data accounting (Data Territory Mode)

- Total **65,536 B** — `data` 65,380 B (raw-but-classified) · `zero_fill` 156 B (parsed, all-zero).
- **raw-but-classified**: the 8 data spans (high-entropy graphics/texture/asset pixel data; not
  field-decoded; possibly compressed). **parsed**: the 7 zero_fill spans. **undecoded-and-unclassified: 0 B.**
- Every byte byte-owned and classified; no byte fallback-only.

## Proof of non-code

- **0 `jr $ra`** (`0x03E00008`), **0 stack prologues** (`0x27BD8xxx..0x27BDFxxx`), **0 `0x80xxxxxx`
  RAM-pointer words** across all 16,384 words — no function structure possible.
- No real ASCII strings (3,156 coincidental printable bytes in pixel data, none forming words —
  e.g. `2CCSTCEEEDTE3R3vVETRbD13q`-style soup). No archive/LZSS/MIO0 magic at any part start.
- `audit_code_region` classifies this range inside the data-evidenced non-code tail. This run owns
  ONLY chunk-45 bytes — the global tail is NOT reclassified.

## Parent workspace evidence sweep

Covered by the run-wide `0x2D1000..0x301000` sweep (see review handoff). Asset-shape hints
(corroborate "texture/tile pixel data here", NOT proof of a parsed format — reconciled against bytes:
high-entropy 6.6–7.3 b/byte, no header/magic):
- `scripts/ob64_4f_audit.json`: 4 small (256 B) "RGBA pixel stride-4" sprite/tile blocks clustered at
  the chunk start — `0x2d112c` (SMALL_TABLE), `0x2d119c` (ANIM_KEYFRAME), `0x2d11fa`, `0x2d12a4`.
- `scripts/ob64_4a_audit.json`: a boundary straddler `0x2d0fe2` (RGBA32, 1024 B, ends `0x2d13e2`
  just inside chunk 45). Scanner heuristics only.
`scripts/ob64_functions.json`: **0 valid functions** in range. `scripts/ob64_archive_catalog.json`:
**0 archive entries** (first LHA archive at `0x636784`). No asset-directory pointer table indexes the
range. No compression magic. No combat overlay source covers this range (RSR-001). No symbolic names,
no hook sites.

## Mistakes corrected / hazards handled

- None structural — uniformly non-code. Segmented at the 7 genuine zero-fill runs (16–28 B) rather
  than forcing one 64 KiB blob, for discoverability; split points are real zero spans, not invented.
  No data forced into `func_*` files.

## Verification

`check_boundaries` PASS (15 parts, 0 code → 0 fragment/cross/under/leak/straddler, 0 data-island
warnings); `check_splits` 0 fragments; `check_manifest` 15 parts; `assemble_original_mips` byte-exact
(code SHA unchanged); `audit_code_region` extent unchanged. Adversarial swarm (hidden-code +
structure + tiling skeptics): clean — see review handoff. Runtime states: none used. Patch-workbench:
none (texture/asset data; not patch space).

## Next-run first action (chunk 46, `0x002E1000`)

Chunk 45 ends in DATA. The `data_002E0D68` high-entropy texture region continues across `0x2E1000`
into chunk 46. Chunk 46 `[0x2E1000,0x2F1000)` is more data territory — own that continuation first.
