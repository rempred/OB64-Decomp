# Rev 0 Lib Chunk 28 Source Ownership

Range: `0x001C1000..0x001D1000`

Status: source-owned as tracked original MIPS. Chunk 28 is mixed code/data and
keeps every byte represented by a tracked `asm/original/rev0/lib/` part.

## Region Map

| Range | Owner class | Files | Evidence |
| --- | --- | ---: | --- |
| `0x001C1000..0x001C1024` | incoming function straddler tail | 1 | Tail of `func_001C0FC8`; returns at `0x1C101C`, delay slot `0x1C1020`. |
| `0x001C1024..0x001C2DC0` | code | 24 | Parent function cluster, with preamble folds at `0x1C13E0`, `0x1C27C0`, and `0x1C2938`. |
| `0x001C2DC0..0x001C3300` | data | 8 | 0 prologues, 0 `jr $ra`, dense pointer/table words, zero pad, and GBI/RDP-like command words. |
| `0x001C3300..0x001C5690` | code | 23 | Parent code plus recovered frameless `func_001C3D14`; several 2-word preamble folds. |
| `0x001C5690..0x001C904C` | data | 10 | Stronghold/tutorial text pool, pointer tables, packed command/script blob; 0 prologues and 0 `jr $ra`. |
| `0x001C904C..0x001C91E8` | code | 3 | Tiny overlapping mini-cluster plus two framed functions; kept as code after adversarial review. |
| `0x001C91E8..0x001CE070` | data | 4 | Pointer/id table, packed script blob, `tusc_hc_test come` string, zero pad; 0 prologues and 0 `jr $ra`. |
| `0x001CE070..0x001D0694` | code | 21 | Three recovered frameless helpers at `0x1CE070`, `0x1CE0C4`, `0x1CE114`, then parent code/resource-loader cluster. |
| `0x001D0694..0x001D1000` | outgoing function straddler head | 1 | `func_001D0694` prologue starts here and continues into chunk 29; no return before boundary. |

Part count: 97 total, 73 normal code, 22 data, 2 function straddlers.

## Data Inventory

Machine-readable index:
`docs/data-index/rev0/chunk28-data-region-inventory.json`.

Data bytes: `0x8D84` / 36,228 bytes.

Parsed or directly recognizable bytes: 1,124 bytes:

- zero fill: `0x001C2F44..0x001C2F94`, `0x001CE064..0x001CE070`.
- string pools/pockets: `0x001C5690..0x001C5A5C`,
  `0x001C5C40..0x001C5C58`, `0x001C5CD0..0x001C5D00`,
  `0x001CE050..0x001CE064`.

Raw-but-classified / undecoded bytes: 35,104 bytes. These are byte-owned and
classified, but the packed command/script record formats are not decoded yet.
The largest undecoded spans are:

- `data_001C5D00.s` (`0x001C5D00..0x001C8F90`, 12,944 bytes).
- `data_001C926C.s` (`0x001C926C..0x001CE050`, 19,940 bytes).

Representative decoded strings:

- `0x001C5690`: `Stationing unit will gather information.`
- `0x001C57EA`: `Stronghold Command. Proceed?`
- `0x001C5C40`: `dedasi_000`
- `0x001C5CD0`: `come here 000`
- `0x001CE050`: `tusc_hc_test come \n`

The first data territory (`0x001C2DC0..0x001C3300`) has 149 RAM-like pointer
words, 111 `0x8022/0x8023`-style words, 76 zero words, and 42 GBI/RDP-like
words. The `0x8022/0x8023` values are not treated as simple ROM back-maps.

## Boundary Corrections

- `func_001C0FC8_chunk28tail` confirms the previous chunk's outgoing
  straddler returns inside chunk 28 at `0x001C101C`.
- Parent gap `0x001C3D14..0x001C3DD8` is executable: recovered as
  `func_001C3D14`, a frameless helper returning at `0x001C3E04`.
- Parent gap `0x001CE070..0x001CE0C0` is executable: recovered as
  `func_001CE070`, returning at `0x001CE0BC`.
- Parent gap `0x001CE0C4..0x001CE114` is executable: recovered as
  `func_001CE0C4`, returning at `0x001CE10C`.
- Parent gap `0x001CE114..0x001CE168` is executable as
  `func_001CE114`; parent `0x001CE168` is an interior/tail artifact, not a
  standalone function start.
- `0x001D0694..0x001D1000` is the head of a function that continues into chunk
  29. The next chunk must begin with `func_001D0694_chunk29tail` and confirm
  its return.

## Parent Workspace Evidence

Useful leads:

- `C:\Users\Joe\Projects\OgreBattlel64\scripts\ob64_functions.json`,
  `ob64_symbols_v2.json`, `ob64_overlay_map.json`, and
  `ob64_callgraph_v2.json` supplied function/control-flow seeds only. All
  data/code boundaries were reconciled against local byte-exact disassembly.
- `C:\Users\Joe\Projects\OgreBattlel64\scripts\ob64_multu72_search_output.txt`
  has in-range stride/render leads at `0x001C1708` and `0x001CF770`; both remain
  inside code parts and caused no split changes.
- `C:\Users\Joe\Projects\OgreBattlel64\wiki\battle-turn-queue-trace\disasm_old_pointer_clear_training.txt`
  mentions `[ROM 0x001c1fbc]`. Local disassembly places it inside
  `func_001C1F98`; no boundary or name changed.

Rejected or corrected leads:

- High Attack/runtime artifacts using `0x801D....` PCs or out-of-range z64 hooks
  do not hit this chunk as Rev 0 ROM offsets.
- `0x8022/0x8023` decode-column values are overlay/RAM leads only, not simple
  ROM back-maps.
- `0x01Cxxxxx` archive offsets in parent archive tools are not `0x001Cxxxx`
  code-region offsets.

No patch-workbench metadata was harvested for this chunk. Existing proven
patch-workbench sites are out of range.

## Verification

Pre-split checks:

```powershell
node tools/check_boundaries.js --splits build/chunk_001C1000-001D1000_splits.json --disasm build/original-mips/rev0/code_001C1000_001D1000.s
node tools/check_splits.js --splits build/chunk_001C1000-001D1000_splits.json --disasm build/original-mips/rev0/code_001C1000_001D1000.s
```

Both passed with 97 splits and 0 hard failures. An adversarial explorer also
reported clean boundaries, including the three data territories, the recovered
frameless helpers, `0x001C904C..0x001C9074`, and both straddlers.

## Next Action

Start chunk 29 at `0x001D1000` with the incoming function tail
`func_001D0694_chunk29tail`. The tail returns at `0x001D109C` with delay slot
`0x001D10A0`; fresh chunk-29 code begins at `0x001D10A4`.
