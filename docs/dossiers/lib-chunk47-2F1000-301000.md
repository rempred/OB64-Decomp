# lib chunk 47 — `0x002F1000..0x00301000`

Classification: **DATA TERRITORY — source-owned as data parts** (no code). The entire 64 KiB chunk
is non-code data, PAST the evidenced executable-MIPS extent end `0x002B89B4`. It continues the
chunk-46 data tail (`data_002EF7F8`) across `0x2F1000`. Owned as honest data parts; **not** code.

- Split JSON: `build/chunk_002F1000-00301000_splits.json`
- Data index: `docs/data-index/rev0/chunk47-data-region-inventory.json`
- Promoted source: `asm/original/rev0/lib/` (27 parts)

## Composition

**27 parts = 14 data + 13 zero_fill (0 code).** No incoming function straddler (data continuation);
an OUTGOING data continuation crosses `0x00301000` into chunk 48. One contiguous high-entropy
graphics/texture asset region segmented by 13 small (16–32 B) zero-fill runs separating
texture/graphics objects (more frequent object boundaries than chunks 45-46).

- `data_002F1000` `0x2F1000..…` — **continuation of chunk-46 `data_002EF7F8`**.
- … 14 `data_` blobs interleaved with 13 `zero_fill_` gaps …
- `data_003002E8` `0x003002E8..0x00301000` (3,352 B) — final data object, runs to the chunk end with
  no terminating zero-fill, so it **continues into chunk 48** (the next run's first action).

## Data accounting (Data Territory Mode)

- Total **65,536 B** — `data` 65,276 B (raw-but-classified) · `zero_fill` 260 B (parsed, all-zero).
- **raw-but-classified**: the 14 data spans (high-entropy graphics/texture/asset pixel data; not
  field-decoded; possibly compressed). **parsed**: the 13 zero_fill spans. **undecoded-and-unclassified: 0 B.**
- Every byte byte-owned and classified; no byte fallback-only.

## Proof of non-code

- **0 `jr $ra`**, **0 stack prologues** (`0x27BD8xxx..0x27BDFxxx`), **0 `0x80xxxxxx` RAM-pointer
  words** across all 16,384 words — no function structure possible.
- No real ASCII strings (1,324 coincidental printable bytes). No archive/LZSS/MIO0 magic at any part
  start.
- `audit_code_region` non-code tail; only chunk-47 bytes owned (global tail NOT reclassified).

## Parent workspace evidence sweep

Covered by the run-wide `0x2D1000..0x301000` sweep (see review handoff). No chunk-47-specific
scanner block lands cleanly in `0x2F1xxx` (the 4a CI4 block at `0x2e81d8` overruns from chunk 46 into
this range, but its extent is scanner-flooded heuristic, not a proven boundary — reconciled =
texture-shaped only). `scripts/ob64_functions.json`: **0 functions** in range.
`scripts/ob64_archive_catalog.json`: **0 entries**; no compression magic, no asset-directory pointer
table indexing the range. No combat overlay source (RSR-001). No names, no hook sites. High-entropy
packed graphics/texture asset data, consistent with chunks 45-46.

## Mistakes corrected / hazards handled

- None structural — uniformly non-code. Segmented at the 13 genuine zero-fill runs. No data forced
  into `func_*` files.

## Verification

`check_boundaries` PASS (27 parts, 0 code); `check_splits` 0 fragments; `check_manifest` 27 parts;
`assemble_original_mips` byte-exact (code SHA unchanged); `audit_code_region` extent unchanged.
Adversarial swarm (hidden-code + structure + tiling): clean — see review handoff. Runtime states:
none. Patch-workbench: none.

## Next-run first action (chunk 48, `0x00301000`)

Chunk 47 ends in DATA with an OUTGOING continuation: `data_003002E8` runs to `0x00301000` with no
terminating zero-fill and continues into chunk 48. Chunk 48 `[0x301000,0x311000)` is more data
territory — own that continuation first, then segment honestly. The global non-code tail
`0x002B89B4..0x0063676C` remains a separate, larger reclassification design.
