# func_00215CF0: Exact-Extent PURE_C Research Candidate

## Status

This source is a nonexact `PURE_C` research candidate. The original assembly
remains the sole accepted owner.

- Candidate: `8BB179FD7AB31807515451089CA0190B6723FE4C6133059B8C93F0762E8871C7`
- Target: `func_00215CF0` at ROM `0x215CF0`
- Source: `docs/archive/matching-c-candidates/2026-09-04-func_00215CF0-8bb179fd7a.c`
- Source SHA-256: `0A55DCCCE8BF8EFF117836BB0350A7F89806F7A3C7366C75EDE2FAEA30AAABF7`
- Scratch class: `cfg-mismatch`
- Scratch score: `37.97`
- Scratch extent: `3400` / `3400` bytes (`850` / `850` instructions)
- Scratch CFG blocks: `169` / `177`
- Recognized scratch relocations: `293`
- Scratch artifact: `build/matching/runs/C9586B612C5F719B1CAA709FFEB51C52486302C06B830842BEACF310C5251284`

## Continuation gain

This continuation closed the prior `164`-byte / `41`-instruction deficit
without padding or assembler mechanisms. The missing code was primarily the
outgoing ABI setup for four calls to `func_00054e24`. Each call has ten
arguments, but the earlier reconstruction represented only the four register
arguments. Restoring the six stack arguments on every predecessor path gives
KMC the path-specific setup while allowing it to tail-merge the identical call
sites into the two retail shared calls.

The recovered stack arguments preserve the path-specific coordinates and the
shared `0x1E`, `0x1E`, `0x122`, and `0x46` values. This is structural ABI and
call-site evidence, not an extent-filling device.

The state-3 `0x8000` path was inverted to the retail fallthrough orientation,
the shared call tails were placed at their retail state-7 position, and the
state-8 and state-9 no-op cases were expressed as distinct tests. Finally, a
byte-width local and explicit `0x100` path recovered the retail seven-word
selection shape and the exact `0xD48` extent. KMC still assigns the local result
to `$v1` where retail uses `$v0`, and broader CFG, scheduling, and allocation
differences remain.

The final workbench comparison reports no candidate-only or target-only
instructions, but `691` aligned instruction differences and `169` candidate
blocks versus `177` retail blocks. The result is therefore a substantially
stronger starting point, not a matching claim. A hybrid fallback is outside
the assigned scope for this target.

## Evidence boundary

Scratch object comparison does not prove canonical linker ownership,
relocation resolution, linked target bytes, or full-ROM identity. Because this
candidate is nonexact, it was archived only; no linkage or target configuration
was changed, and the original assembly remains accepted.

## Workbench-reported first difference

The generic workbench reports an unresolved relocation field first because the
target remains ASM-owned and has no accepted C relocation manifest:

```json
{
  "actual": "lui $a2, 0x0000",
  "actualWord": "0x3C060000",
  "expected": "lui $a2, 0x801D",
  "expectedWord": "0x3C06801D",
  "index": 0,
  "offset": 0,
  "pc": 2149394976
}
```
