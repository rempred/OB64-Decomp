# func_002215D0: Preserved Matching-Workbench Candidate

## Status

This source is a research candidate. The original assembly remains the accepted owner. `func_002215D8` is a prologue alias inside this accepted owner, not a separate promotion target.

- Candidate: `EED6C9A9A03BFA88183401B9BCA98F365B3893B46CDD6890B4BDB4844BA4B89A`
- Target: `func_002215D0` at ROM `0x002215D0`
- Requested alias: `func_002215D8`
- Source: `docs/archive/matching-c-candidates/2026-08-25-func_002215D0-c31d905585.c`
- Source SHA-256: `3E65E00DAF5BD1246746C042556F37DA34F64B7C823E19D64769829C0DD7267D`
- Scratch class: `length-mismatch`
- Scratch score: `25.21`

## Preservation reason

Resolver-corrected pure-C reconstruction of the complete 60-state unit/object dispatcher. It recovers the retail frame, stack-object extent, table domain, cursor strides, countdown types, and localized clamp constants. The remaining register/CFG/scheduling gaps are distributed through a 752-instruction body and do not support a small hybrid fragment.

## Evidence boundary

Scratch object comparison does not prove canonical linker ownership, relocation resolution, target bytes, or complete-ROM identity. The unresolved `D_801CE8C8` relocation accounts for the first raw word difference shown by the workbench; this candidate is still nonexact after relocation normalization.

## Accepted target and configured generation coverage

- Accepted owner: ROM `0x002215D0..0x00222190`, 3,008 bytes / 752 instructions.
- Retail shape: 128-byte frame, 215 blocks, 22 calls, one indirect jump, and floating-point operations.
- The complete configured eight-ruleset ensemble was attempted. All eight initial runs stopped at m2c's delay-slot label `.L_00221764before`.
- After analysis-only control-flow repair, structured, goto-preserving, and stack-preserving sources were regenerated. Structured and stack each produced 2,992 bytes / 748 instructions at score `30.30`; gotos produced 2,988 bytes / 747 instructions at score `24.45`.
- The five specialized structured transformations were evaluated against the corrected source and were mechanically inapplicable, so they produced no distinct candidate.

## Placement and control-flow correction

Total Resolver places the function at live `0x801DE300` under the accepted direct-contiguous ROM/DMA slab, with live-minus-ROM delta `+0x7FFBCD30`. Runtime table `0x801E6E10` therefore maps to ROM `0x0022A0E0`, owned by `asm/original/rev0/lib/table_0022a0e0.s`. Its 60 entries have 15 distinct destinations, all within this accepted owner.

The same mapping proves these apparent external tails are internal labels: live `0x801DEE74`, `0x801DEE70`, `0x801DE530`, `0x801DE75C`, `0x801DE7E4`, `0x801DE91C`, and `0x801DE9A8` / `0x801DE9C0` map back inside the accepted ROM range. Corrections were made only in ignored analysis assembly. Accepted boundaries, source ownership, table source, and original assembly were not altered.

The branch-likely and call delay slots around ROM `0x00221734..0x00221760` each increment `$s2` on their respective path. Moving that high-level increment to the common analysis join allowed m2c to recover the loop without claiming that the edited analysis assembly is canonical source.

## Best recovered C

The first corrected structured candidate still contained several m2c type errors. The preserved source fixes them as follows:

- `void **` cursors now advance by one pointer (four retail bytes), not four pointers (16 bytes).
- The `+0x94` countdowns are signed 32-bit values rather than pointer arithmetic.
- Explicit cases `1..60` and an out-of-range default preserve the full retail jump-table domain.
- Four observed stack outputs are elements of one address-escaped eight-word scratch array, naturally producing the exact 128-byte frame and exact ten-register save set.
- `0xFF` / `0x3F` clamp values are scoped to their two active cases, avoiding the nonretail whole-function constant hoist.

The result emits 2,980 bytes / 745 instructions, 213 blocks versus retail's 215, and opcode-sequence LCS 665/752 (`88.43%`). Its line-ending-equivalent working candidate was `C31D90558593594EEEA3F7420F480246139CAE1A9B028790D0D58959E5B941D5`; the tracked archive has the identity recorded above.

The raw positional scorer prefers candidate `D1B003346FA572D13E89673FE6BEE9BFE8AF18DEE6BC66D619731E2414D89A81` at score `30.49`, 2,988 bytes / 747 instructions, exact frame, and opcode LCS 662/752 (`88.03%`). That version is not the preferred source hypothesis because it lets KMC hoist `0xFF` into a function-wide saved register, unlike retail's case-local materialization.

## Semantics and remaining blocker

The recovered function iterates 20 slots and dispatches a 60-state per-unit/object update. It operates on up to three object pointers, state `+0x74`, active mask `+0xA5`, partner index `+0xA6`, and object animation/position fields including `+0x1C`, `+0x28`, `+0x34`, and `+0x94`. Available evidence supports a broad runtime unit/animation/effect coordinator. It does not establish class-change eligibility or class-change-only behavior.

Remaining differences include retail's long-lived `$s5` / `$s8` / `$s7` constants, branch-likely join placement, repeated signed-coordinate update topology, register coloring, and delay-slot scheduling. Assembly sufficient to close those gaps would replace broad portions of the dispatcher, violating the localized-hybrid gate. No hybrid was promoted.

No canonical linkage change, linked diff, verifier, or full-ROM build was run.
