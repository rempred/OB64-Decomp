# lib chunk 37 — `0x00251000..0x00261000`

Classification: **MIXED** (code → interior pointer/struct/float DATA island → code →
outgoing function straddler-head). Command-dispatcher / mission-briefing-combat heavy.

- Split JSON: `build/chunk_00251000-00261000_splits.json`
- Data index: `docs/data-index/rev0/chunk37-data-region-inventory.json`
- Promoted source: `asm/original/rev0/lib/` (180 parts)

## Composition

**180 parts = 170 normal code + 1 straddler-tail + 1 straddler-head + 8 data.**

Region map:

- Incoming FUNCTION straddler-tail `func_00250F9C_chunk37tail` `0x00251000..0x002510E0`
  (continues `func_00250F9C`, prologue `addiu $sp,-0x18` in chunk 36 @`0x00250F9C`;
  returns `jr $ra`@`0x002510D8` + delay `addiu $sp,0x18`@`0x002510DC`).
- CODE region 1 `0x002510E0..0x0025E2BC` — dense message/command-dispatcher handlers
  (`andi $a0,0xFFFF` selector + `beq`/`slti` chains + `j 0x801F/0x802` overlay tail-jumps),
  with many parent over-merges un-merged into recovered frameless leaves.
- Interior DATA island F `0x0025E2BC..0x0025EE90` (3,028 bytes) — a mixed 0x80x-band
  pointer/struct/float record table + a trailing float32 pool.
- CODE region 2 `0x0025EE90..0x00260F30` — incl. recovered frameless FP/compare leaves
  and the command dispatcher `func_0023C114`-style code.
- Outgoing FUNCTION straddler-head `func_00260F30` `0x00260F30..0x00261000`
  (prologue `addiu $sp,-0x20`; `blez`/`beq`/`bne` + `j 0x8020C408` internal tail-jumps;
  NO `jr $ra` before the boundary; continues into chunk 38 as `func_00260F30_chunk38tail`).

## Data island inventory (8 parts, 3,028 bytes)

Bytes by class: data 2,668 · float 200 · table 148 · zero_fill 12.
Full per-part index: `docs/data-index/rev0/chunk37-data-region-inventory.json`.

- DATA F `0x25E2BC..0x25EE90` — a large mixed RAM-pointer/struct record table (0x801F/
  0x8020/0x8022 pointers with interleaved scalar/float fields), shorter sub pointer tables,
  and a trailing IEEE float32 constant pool (`0x25EDC0..0x25EE84`) before zero padding.

## Role leads (conservative; names stay `func_*`)

Mission-briefing/combat module (parent active states `mission_briefing`+`combat`). Region 1
is a large bank of command/message dispatchers that switch on a 16-bit selector and
tail-jump into overlay handlers; region 2 mixes FP vector/geometry helpers. Leads only,
validated against byte-exact disassembly; names stay `func_*`.

## Mistakes corrected / hazards handled

- One interior data island F carved via `build/carve_chunk.js`; the run-prompt's small
  4/20/28/40-byte "gap" leads (`0x25EEF8`, `0x25F6B4`, `0x25F7E8`, `0x25FD90`, etc.) were
  proven **CODE** (frameless FP/compare leaves), not data, and recovered.
- Heavy parent over-merges un-merged: e.g. `func_0025B394` → 1 framed + 5 frameless leaves;
  a 20-function un-merge across `0x25B954..0x25D394`. All validated at `jr $ra`+delay.
- Preamble-orphans folded FORWARD, named for the PART START (no duplicate-label collisions).
- Outgoing straddler-head `func_00260F30` confirmed (no `jr $ra` before `0x261000`).

## Verification

`check_boundaries` PASS (0 fragment/cross/under/leak/straddler/data-island);
`check_splits` 0 fragments. Adversarial swarm (4 code + 1 data + straddler skeptics).
Byte-exact rebuild preserved (see review handoff).
