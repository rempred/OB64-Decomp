# Chunk 25 — CODE-dominant MIXED: char/class/scenario code + a shop-dialogue string pool + inline data islands (`0x00191000..0x001A1000`)

Source-ownership pass for the twenty-sixth 64 KiB chunk. **CODE-dominant MIXED**: an incoming
FUNCTION straddler-tail, two large code regions split by a dialogue STRING-POOL data island,
two small inline data islands inside the code, and an outgoing FUNCTION straddler-head.
**71 tracked parts**: **59 code + 12 data**. Byte-exact rebuild preserved (code SHA `40D4E787…B409`,
ROM SHA `571E8339…CC67A`). Coverage `0x1000..0x1A1000` = 1,703,936 B = **59.8039 %** of the
2,849,204-byte evidenced executable extent; code-only classified = 1,389,036 B (**48.7517 %**).

Pipeline: parent-evidence sweep + byte-exact proret scan → 9 code-slice + 1 data analysis agents
(Workflow) → combine → `check_boundaries`/`check_splits` → 6-agent adversarial swarm. **Tiling
structurally CONFIRMED** (no boundary moves); pre-gate reconciliation resolved 4 cross-slice
preamble folds, 6 preamble-orphan label fixes, and 1 name fix; the adversarial's findings were
all LOW/cosmetic (note transcriptions + a stale `file` field), now fixed.

## Region map (ROM-ordered)

| Region | Range (z64) | Bytes | Class | Parts |
|---|---|---:|---|---:|
| CODE 1 (char/class/scenario; record-builder; dispatcher) | `0x191000..0x19BFF0` | 44,016 | code (+2 inline data islands) | 52 |
| DATA (shop-dialogue string pool + handler-pointer tables) | `0x19BFF0..0x19C760` | 1,904 | data | 10 |
| CODE 2 (+ outgoing straddler) | `0x19C760..0x1A1000` | 18,592 | code | 9 |

Unlike chunks 23–24, the parent DB did **not** mislabel large data here — chunk 25 is genuinely
code-dominant; the data is one string pool + two small inline islands.

## Incoming FUNCTION straddler (chunk 24 → chunk 25)
`func_0018FB30_chunk25tail` `[0x191000,0x191174)` — tail of `func_0018FB30` whose `addiu $sp,-0x78`
prologue is in chunk 24 at `0x18FB30`. Ends `jr$ra`@`0x19116C` + delay `addiu $sp,0x78`@`0x191170`.
`kind:"straddler-tail"`.

## CODE 1 `0x191000..0x19BFF0` (52 parts; 2 inline data islands)
Overlay-relocated (runtime `0x8021` band). Char/class/scenario code. **9 preamble-orphans** (all
adversarially confirmed read-before-write, names = preamble start), incl. `func_001911E8`,
`func_001914D0`, `func_00191E50` (4-word, folded across the e1s1/e1s2 seam), `func_00194624`,
`func_00194AC0`, `func_00194F6C`, and the dispatcher `func_001977E0`. 4 frameless leaves. Notable
functions (names stay conservative `func_*`):
- **`func_0019554C`** = the parent-documented, **in-game-proven record-builder** (builds a 52-byte
  runtime enemy record from an enemydat template; the runtime mod-loader hook is at `0x195584`,
  inside this function, a `sltiu $v0,$s5,0x1E` bounds check). Verified CODE (prologue + `jr$ra`@`0x1957C8`).
- **`func_001960A8`** (5,776 B) — parent lead char/class hub; one prologue + one `jr$ra`.
- **`func_001977E0`** (4,580 B) — parent lead dispatcher/state-machine; preamble-orphan entry
  (`lui/lbu $v1,0x8022`@`0x1977E0` read by `sltiu $v0,$v1,0x10`@`0x197808`); internal `jr $v0`
  jump-table dispatch@`0x197824`.
- **Inline data island `data_001952E8`** `[0x1952E8,0x195410)`: ASCII `"Soldier"/"Thrust"/"Target
  Unit"` + `{C6}`/`{C0}` tokens + an 8-entry `0x8021`-band pointer table; 0 prologues/returns.
- **Inline data island `data_00197738`** `[0x197738,0x1977E0)`: ASCII `"Mitsuiye"`, `"No free
  space on TCharacterEnemyData."`, `"No free space on EnemySolderData."` (sic, byte-exact) + a
  6-entry `0x80215A` pointer table; 0 prologues/returns.

## DATA `0x19BFF0..0x19C760` (10 parts) — shop-dialogue string pool
`rodata_0019BFF0` `[0x19BFF0,0x19C488)` (1,176 B): NUL-delimited **shop dialogue** — `"I shouldn't
expect much from a place like this."`, `"This is all you got?"`, `"What do we need?"`, `"Hmm{T05}
Let's see{T05}"`, `"What's today's special?"` (47 strings; `{T05}` = pause/timing token). Followed
by `data_0019C488`, then **six RAM-pointer tables** (`table_0019C4BC` … `table_0019C6F0`) — these
are **handler/jump-pointer arrays** targeting relocated `0x8021`/`0x8016`-band code/data (NOT
string pointers into the local rodata), and `zero_fill_0019C754` (3 nops). All adversarially
confirmed 0 prologues/returns. Seams byte-exact (`0x19BFF0` after a `jr$ra`+delay; `0x19C760` =
the `func_0019C760` preamble).

## CODE 2 `0x19C760..0x1A1000` (9 parts) + outgoing straddler
Preamble-orphan entry **`func_0019C760`** (`lui/lbu $v1,0x8022`@`0x19C760` read by the
`addiu $sp,-0x68` body@`0x19C768`). Functions incl. `func_0019D64C`, `func_0019E2E0` (5,456 B),
`func_0019F830`, each one prologue + one `jr$ra`.
- **Outgoing FUNCTION straddler-head `func_001A0264`** `[0x1A0264,0x1A1000)` — `addiu $sp,-0x88`
  prologue@`0x1A0264`; **NO `jr$ra` in `[0x1A0264,0x1A1000)`** (adversarially confirmed; last
  chunk `jr$ra` is `0x1A025C`); continues into chunk 26 (parent lead ~3,988 B, ends ~`0x1A11F8`).
  `kind:"straddler-head"`.

## Adversarial pass (6 verifiers)
4 code + 2 data skeptics. **0 boundary disproofs** — the tiling is structurally correct. All 9
preamble-orphans confirmed genuine read-before-write folds (names = preamble starts); the
record-builder, the two huge functions, and the dispatcher each confirmed single prologue + single
`jr$ra`; the three data islands confirmed 0 prologues/returns with byte-exact seams; the outgoing
straddler confirmed 0 `jr$ra`. LOW findings (all fixed): two note ASCII transcription corrections
(`TCharacterEnemyData`, `EnemySolderData`), one stale `file` field on a folded part, and the
string-pool pointer tables reclassified as handler/jump-pointer arrays (not string pointers).

## Data inventory (machine-readable: `docs/data-index/rev0/chunk25-data-region-inventory.json`)
**Total data: 2,368 B in 12 parts.** By class: ASCII strings 1,176 B · RAM-pointer tables 632 B ·
packed/unknown 548 B · zero-fill 12 B. **Parsed:** all three ASCII pools (decoded byte-exact:
`chunk25-dialogue-pool-19BFF0`, `chunk25-ui-labels-1952E8`, `chunk25-debug-strings-197738`) +
the pointer tables (identified by `0x8016/0x8021`-band targets). **Raw-but-classified:** small
value/fill blocks. **Undecoded:** the precise semantics of the handler-pointer-array targets
(overlay-relocated; not back-mappable to ROM here).

## Method & verification
Parent-evidence sweep + proret scan → slice_extract (own byte-exact slices) → 9 code + 1 data
agents (Workflow) → combine (`combine_chunk.js`) → 4 cross-slice preamble folds + 6 label fixes +
1 name fix → `check_boundaries` PASS (71 parts; 0 fragment/cross/under/leak/straddler/data-island)
+ `check_splits` 0 fragments → 6-agent adversarial swarm (0 boundary moves; LOW note/file fixes)
→ `split_original_mips_part --remove-source`. Gates: `check_manifest` ALL PASS (26 chunks /
3,106 parts); `rebuild_from_source_manifest` byte-exact (ROM `571E8339…CC67A`);
`assemble_original_mips` byte-exact (code `40D4E787…B409`); `verify_setup` all green;
`audit_code_region` `no-credible-code-edge-into-tail`; `git diff --check` clean; data-index JSON
valid; 0 data files with function wording.

## Caveats / next frontier
- Conservative code names (overlay-relocated, runtime `0x8021`). The record-builder identity is a
  strong parent-documented lead but the name stays `func_0019554C`. The handler-pointer-array
  targets are undecoded (overlay-relocated).
- **Next frontier `0x001A1000` (chunk 26).** FIRST continue the outgoing **FUNCTION** straddler:
  `func_001A0264` `[0x1A0264,0x1A1000)` has its `addiu $sp,-0x88` prologue in chunk 25 and
  continues into chunk 26 (parent end ~`0x1A11F8`) — emit `func_001A0264_chunk26tail` first and
  confirm its `jr$ra`.
