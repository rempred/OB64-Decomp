# Chunk 18 — ALL CODE: FP-heavy scenario/combat code (`0x00121000..0x00131000`)

Source-ownership pass for the nineteenth 64 KiB chunk. **ALL CODE** — an incoming FUNCTION
straddler-tail, ~93 functions, and an outgoing FUNCTION straddler-head; **0 data**. **95
tracked parts**: 1 incoming straddler-tail + 62 framed prologues + 8 preamble-orphans + 23
frameless leaves + 1 outgoing straddler-head. Byte-exact rebuild preserved (code SHA
`40D4E787…B409`, ROM SHA `571E8339…CC67A`). Coverage `0x1000..0x131000` = 1,245,184 B =
**43.7029 %** of the 2,849,204-byte evidenced executable extent; code-only classified ≈
1,057,476 B (**37.1148 %**).

Pipeline: parent context + content scan (no data signature) → 8 code-slice analysis agents
(Workflow) → combine → `check_boundaries`/`check_splits` → 3-agent adversarial swarm.
**1 disproof** (a missed frameless leaf at `0x12ECDC`); fixed. No tool changes.

## Region map (ALL CODE)

| Region | Range (z64) | Bytes | Class | Parts |
|---|---|---:|---|---:|
| incoming straddler-tail | `0x121000..0x1211F8` | 504 | code | 1 |
| CODE | `0x1211F8..0x130E60` | 64,616 | code | 93 |
| outgoing straddler-head | `0x130E60..0x131000` | 416 | code | 1 |

## Incoming function straddler (chunk 17 → chunk 18)
`func_00120FC4_chunk18tail` `[0x121000,0x1211F8)` — tail of `func_00120FC4` whose true
entry `0x120FC4` is in chunk 17 (an FP-heavy 0x50-frame function: `swc1`/`lwc1`/`sub.s`/
`mul.s`/`div.s`/`trunc.w.s`, loading float constants from out-of-chunk `0x801F0D98/DA0/
DA4` rodata via `lwc1`). Returns `jr $ra` @`0x1211F0`; the next function starts exactly at
`0x1211F8` (parent end). `kind:"straddler-tail"`.

## CODE `0x1211F8..0x130E60` (93 parts)
Overlay-relocated (decode RAM column `0x8019xxxx` is the WRONG linear map; real runtime
slots `0x801D` band, scenario/combat_transition states). 62 framed prologues + 8
preamble-orphans (read-before-write folds; each named `func_<startaddr>` with `label`=name)
+ 23 recovered frameless leaves the parent DB over-merged. Notable: `func_00121F38`
(5,408 B), `func_0012EC6C`/`func_0012ECDC` (the adversarial split — see below). Internal
`jr $v0` jump-table dispatchers (`0x124888`, `0x124AB0`, `0x12680C`, `0x12E238` → relocated
`0x801E/0x801F` tables) and `j 0x801Cxxxx/0x801Dxxxx` overlay tail-jumps kept internal. FP
instructions are CODE; **no inline float pool** (all float constants load from out-of-chunk
`0x801F` rodata). No data islands.

## Outgoing function straddler (chunk 18 → chunk 19)
`func_00130E60` `[0x130E60,0x131000)` — prologue `addiu $sp,-0x18` @`0x130E60`, NO `jr $ra`
in range, continues to `0x131050` in chunk 19. `kind:"straddler-head"`. **Chunk-19 first
action: emit `func_00130E60_chunk19tail` `[0x131000,0x131050)`.**

## Adversarial correction
The code-C verifier disproved one boundary: `func_0012EC6C` `[0x12EC6C,0x12F010)` contained
**two** return sequences — the framed function returns `jr $ra`@`0x12ECD4` + epilogue
`addiu $sp,0x28`@`0x12ECD8`, then a **distinct frameless leaf** begins at `0x12ECDC`
(`lui $v0,0x8019` / `lbu` — writes `$v0`, reads incoming `$a0` directly, no stack frame,
ends `jr $ra`@`0x12F008`). Split into `func_0012EC6C [0x12EC6C,0x12ECDC)` (framed) +
`func_0012ECDC [0x12ECDC,0x12F010)` (frameless-leaf). code-A/B: 0 disproofs.

## Parent-workspace evidence (sweep 2026-06-23)
Chunk 18 is **near-new territory** in the parent: ~70 functions carry only heuristic
auto-classifier role tags (char_data / text_vm / class_def / promotion consumer), **no
hand-authored names**. Hot-path PC samples prove it executes in scenario + combat_transition
states; runtime slots resolve to the **0x801D band** (NOT the linear `0x8019/0x801A` image).
LEADS (conservative, names stay `func_*`): `0x126D24`/`0x1284C4`/`0x12B440` = text-VM
consumers; `0x129068` = class-def consumer; `0x12FBB8` = Chaos-Frame stat reader/writer
(global `0x801936A9`); `0x12DE80..0x12FAF0` flagged in the chaos-frame research log as
UI-banner **data** source pointers (verified here as CODE in this window — they are
functions, the "data pointer" label referred to their role, not their bytes).

## Data
**None.** Every byte of chunk 18 is code. No data inventory file (consistent with the
all-code chunks 9–12, 17). The content scan's "float-const runs" were false positives — FP
*instructions* (`swc1`/`lwc1`/`sub.s`), not float data.

## Method & verification
Parent context + content scan (no data regions) → 8 code-slice agents (Workflow) → combine
(`combine_chunk.js`; first = `func_00120FC4_chunk18tail`, last = `func_00130E60`; 0 label
conflicts) → `check_boundaries` PASS (95 parts; 0 fragment/cross/under/leak/straddler/
data-island) + `check_splits` 0 fragments → 3-agent adversarial swarm (1 missed-leaf fix) →
re-gate → `split_original_mips_part --remove-source`. Gates: `check_manifest` ALL PASS (19
chunks / 2,446 parts); `assemble_original_mips` byte-exact (`40D4E787…B409`); `verify_setup`
all green; `audit_code_region` OK.

## Caveats / next frontier
- Conservative code names (overlay RAM/global/callee identity suspect; parent semantic tags
  recorded as leads). Dispatcher target tables in `0x801E…/0x801F…` relocated RAM unresolved.
- **Next frontier `0x00131000` (chunk 19).** FIRST continue the outgoing FUNCTION straddler:
  `func_00130E60` continues to `0x131050` (chunk-19 tail file `func_00130E60_chunk19tail`
  `[0x131000,0x131050)`). Chunk 19 is MIXED (code + a large trailing data region).
