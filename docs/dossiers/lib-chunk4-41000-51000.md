# Chunk 4 — overlay-relocated code, frameless-leaf-dense (`0x00041000..0x00051000`)

Source-ownership pass for the fifth 64 KiB chunk. **CODE-DOMINANT and
OVERLAY-RELOCATED**: every function lives in the `0x8016xxxx..0x8017xxxx` overlay
slot (`0x41098`→RAM `0x8016B198`, `0x50F98`→RAM `0x8017B098`), NOT at the linear
`ROM + 0x8006FC00` address the decode comments show. Because RAM/callee/global
identity is therefore unverifiable, **all 374 code parts are conservative
`func_<addr>`** plus 2 straddler markers = **376 tracked parts** under
`asm/original/rev0/lib/`. Byte-exact rebuild preserved (code SHA
`40D4E787…B409`, ROM SHA `571E8339…CC67A`).

Coverage after this chunk: `0x1000..0x51000` = 327,680 B = **11.50 %** of the
2,849,204-byte evidenced executable extent (chunks 0–3 were 9.20 %). The 10 %
evidenced-executable target `0x000468F8` is **surpassed**.

## Why this chunk is a function-split (not a data pass)

The parent overlay map shows **164 loaded functions** in `0x41000..0x51000`
(first `0x41098`, last `0x50F98`), all in real RAM snapshots — overlay code, not
data. (Contrast chunk 3, which was data-dominant.) An independent scan found
**0 inline pointer-table data islands** (0 runs of ≥4 consecutive `0x80xxxxxx`
pointer words), so the four indirect-`jr` jump tables in this chunk
(`0x41098`, `0x466F4`, `0x46854`, `0x50F98`) read their tables from relocated
runtime RAM, not from inline ROM — no `data_`/`jumptable_` parts were needed.

## The dominant correction: frameless-leaf recovery

The parent DB only detects functions with a standard `addiu $sp,$sp,-N`
prologue, so it **massively under-counts** this chunk: 165 base partition files
(164 parent fns + the straddler tail) expanded to **376 final parts** — about
**211 recovered functions**, almost all **frameless leaf** accessors/helpers the
parent DB hid inside over-merged records or "gaps". Four base-partition "gaps"
were not data at all but dense clusters of frameless leaves (each a
`jr $ra`-terminated run of argument-register index math / table accessors):

- `0x420C8..0x4271C` (folded onto parent `0x41F48`) — ~9 leaves.
- `0x430F4..0x43FD8` (3812 B) — the largest cluster, ~40 leaves of
  `andi $a0,0xFF` / `sll`/`lui+addiu` table-index accessors.
- `0x4555C..0x45AE0` — ~14 leaves.
- `0x45CA8..0x45F30` — ~5 leaves.

Plus per-function un-merges (e.g. trailing frameless leaves split off the
prologue functions at `0x41098`, `0x41674`, `0x41C20`, `0x42B2C`).

## Straddlers (both ends)

- **Head from chunk 3:** `func_00040ff4_chunk4tail` `[0x41000,0x41098)` is the
  tail of `func_00040FF4` whose head is `func_00040ff4_chunk3head` in chunk 3
  (a byte-arg clamp/÷3/÷0x300 helper). It is the first chunk-4 part.
- **Head into chunk 5:** `func_00050f98_chunk4head` `[0x50F98,0x51000)` is the
  head of `func_00050F98` (parent size 1268 → ends `0x5148C`), which **continues
  into chunk 5**. It is the last chunk-4 part; its tail
  (`func_00050f98_chunk5tail` `[0x51000,0x5148C)`) is the first chunk-5 file when
  chunk 5 is processed.

## Method

Same proven pipeline, now run on tracked tools:
`tools/dump_function_context.js` (164 parent records, 49 named, 13 hazards, 18
gaps) → `tools/plan_chunk.js` (165 base files) → `tools/slice_chunk.js`
(12 slices) → 12-agent analysis swarm (boundary refinement, overlay-conservative
naming; validate+retry per slice) → `tools/integrate_chunk.js` (376 parts) →
`tools/check_splits.js` → 8-region adversarial refutation swarm →
`tools/split_original_mips_part.js`. All RAM/callee leads were treated as
unreliable (overlay) so no descriptive names were promoted.

## Verification (boundary correctness, not just byte-exactness)

Deterministic checks over all 376 parts (each overlay-immune where noted):

- **0 fragments** — every code part contains a return or `j`/`jal`
  (`tools/check_splits.js`).
- **0 prologue-after-return under-splits** — no two prologue functions left
  merged (scan for a return followed, after the delay slot + alignment nops, by
  a fresh `addiu $sp,-N`).
- **0 cross-boundary PC-relative branches** across all 376 parts — every
  intra-function branch stays inside its part (the strongest structural
  invariant; overlay-immune since branches are PC-relative). No function is split
  mid-body and none are wrongly merged.
- **0 true delay-slot leaks** — no code part's *last* word is an un-followed
  control-transfer (return/branch/`j`) whose delay slot leaked to the next part.
- **0 inline pointer-table data islands**.

Adversarial swarm: 7/8 regions clean; **1 real error found and fixed** — a
delay-slot mis-cut at `0x484B4`: `func_000484b4` had excluded its `jr $ra` delay
slot `0x484BC` (`sh $a0,0x7B08($at)`), and the next part wrongly started on that
orphaned delay slot instead of the true entry `0x484C0`. Fixed by extending
`func_000484b4` to `0x484C0` and renaming the next part `func_000484bc`→
`func_000484c0`. (My deterministic checks missed it because both halves still
contained a return; the LLM review caught it — and I added the literal-last-word
delay-slot-leak check afterward, which now confirms 0 remaining.)

Gate results: `tools/check_manifest.js` ALL PASS (1186 parts); `node
tools/assemble_original_mips.js` byte-exact; `node tools/verify_setup.js` PASS
(5 composite chunks / 1186 files / 95 fallback); `node tools/audit_code_region.js`
OK; `git diff --check` clean.

## Caveats / next frontier

- Every name is a conservative address label. Upgrading to descriptive names
  requires overlay-aware RAM resolution (real RAM per
  `scripts/ob64_overlay_map.json`: this chunk is the `0x8016B198+` slot) plus
  runtime/mutation evidence. The frameless-leaf accessors (e.g. the `0x43100+`
  `lui 0x8019`/table-index family) are an obvious future naming cluster once the
  overlay's data tables at `0x8018xxxx/0x8019xxxx` are decoded.
- The per-part `note` headers record each boundary's rationale (frameless leaf /
  un-merged from parent / prologue fn / straddler); they are static structural
  notes, not verified semantics.
- **Next frontier `0x00051000` (chunk 5).** FIRST continue the straddler:
  `func_00050f98_chunk4head` continues to `0x5148C` (chunk-5 head file
  `func_00050f98_chunk5tail` `[0x51000,0x5148C)`). Then classify chunk 5's
  code/data mix via the overlay map before choosing the pipeline.
