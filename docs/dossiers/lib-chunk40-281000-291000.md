# lib chunk 40 — `0x00281000..0x00291000`

Classification: **MIXED** (leading DATA → CODE → interior DATA island → CODE → outgoing
function straddler-head). Mission-briefing/combat display-list + UI/results code. No incoming
straddler — chunk starts with data continuing chunk 39's DATA C.

- Split JSON: `build/chunk_00281000-00291000_splits.json`
- Data index: `docs/data-index/rev0/chunk40-data-region-inventory.json`
- Promoted source: `asm/original/rev0/lib/` (159 parts)

## Composition

**159 parts = 142 normal code + 1 straddler-head + 0 straddler-tail + 16 data.**

ROM-ordered region map:

- **DATA L (leading)** `0x00281000..0x00281860` (2,144 B, 6 parts) — continues chunk 39's DATA C:
  640×480 screen-dimension records (`0x028001E0`), a ~314-word RAM-pointer table
  (`0x80227xxx`/`0x80228xxx`), "Yes"/"No" UI strings, and a float64 pool (0.5/0.5/160.0/120.0).
- CODE C1 `0x00281860..0x002866E4` — functions incl. the big dispatcher `func_00284288`
  (parent-tagged "combat/scenario dispatcher", 87 callees; `addiu $sp,-0x78` + `sdc1 $f20`;
  internal beq/slti ladder, overlay tail-jumps); recovered frameless leaves (128 B div-math leaf
  `func_00283D94`, 224 B FP leaf `func_00286444`). Ends `jr $ra`@`0x002866DC`.
- **DATA M (interior)** `0x002866E4..0x00286BD0` (1,260 B, 10 parts) — float32 const pool
  (35.0/1.333/1.0/5000.0/0.15/-120.0/150.0), strings, zero-fill, and a `0x8022Axxx` RAM-pointer
  table.
- CODE C2 `0x00286BD0..0x00291000` — functions + frameless GBI/FP builders (incl. `func_0028F18C`,
  the 1944 B parent "gap", a GBI display-list builder ending `jr $ra`@`0x0028F910`).
- Outgoing FUNCTION straddler-head `func_00290D50` `0x00290D50..0x00291000` (TRUE entry is a
  read-before-write preamble @`0x290D50` (`lui $v0,0x8024`/`lw 0x8023DE38`, read by `beq`@`0x290D64`)
  preceding prologue `addiu $sp,-0x20`@`0x290D58`; builds display lists; NO `jr $ra` before
  `0x00291000`; continues into chunk 41 as `func_00290D50_chunk41tail`).

## Data island inventory (16 parts, 3,404 bytes)

Bytes by class: table 1,552 · data 1,380 · float 220 · rodata 184 · zero_fill 68.
Full per-part index: `docs/data-index/rev0/chunk40-data-region-inventory.json`.

- **DATA L** `0x281000..0x281860` — screen-dim records + the large RAM-pointer table
  (`table_*`, ~314 words, `0x80227xxx`/`0x80228xxx` with zero separators) + "Yes"/"No" rodata +
  float64 doubles. The pointer table is likely a handler/jump table (raw-but-classified).
- **DATA M** `0x2866E4..0x286BD0` — float32 const pool (`float_*`) + rodata strings + zero-fill +
  a `0x8022Axxx` pointer table (`table_*`).

All data byte-owned and classified (`parsed`/`raw-but-classified`); 0 bytes undecoded-and-unclassified.

## Role leads (conservative; names stay `func_*`)

Mission-briefing/combat display-list module continuing chunks 36-39, plus a UI/results component
(the "Yes"/"No" strings + 640/480 records + 0.5/160/120 doubles suggest a confirm-dialog / screen
layout). `func_00284288` is the central combat/scenario dispatcher (parent-evidenced, 87 callees).
Names stay `func_*` pending runtime proof.

## Parent workspace evidence sweep

Searched parent `C:\Users\Joe\Projects\OgreBattlel64` for `0x281000..0x291000`. Validated leads
(documentation only):

- ROM `0x00284288` (`docs/mips-decode.md`, `scripts/ob64_xrefs.json`, `ob64_symbols_v2.json`):
  "combat/scenario dispatcher", 87 callees — recorded as a naming lead for the in-range dispatcher.
- ROM `0x0028CA1C` (`docs/archive/chaos-frame-army-management-overlay-research-log-2026-06-09.md`):
  speculative "loader/decoder cluster" — low confidence, in CODE C2.
- `Cutscene Frames/dma_log_all.txt`: DMA "RAWLOW" loads from `0x281860..0x2877D0` during cutscene
  rendering — confirms active graphics/display-list content in range.

Overlay-mapping caveat (RSR-001): `ram_snapshots/overlay_sources.json` documents combat overlay
sources (combat high-slot `0x278F90`→RAM `0x80224800` ending `0x27CC90`; combat main-engine
`0x2637B0`→RAM `0x8020EC00` ending `0x2697B0`). **Both end BEFORE `0x281000`**, so chunk 40 is NOT
covered by the documented overlay sources — it is unclaimed-overlay/data; runtime overlay mapping
for this range remains unproven.

Rejected / cautioned: the `scripts/ob64_4a_audit.json` "LZSS block inventory" claiming
`0x28123e..0x290d04` as compressed asset data is NOT consistent with the byte-exact disassembly
(this range has real prologues, `jr $ra` returns, and FP code; `audit_code_region` classifies
`0x1000..0x2B89B4` as executable). Treated as a misaligned/false match; chunk 40 is code + the
two data islands documented above, not an LZSS asset blob.

## Mistakes corrected / hazards handled

- Chunk starts with DATA (640/480 records), not code — classified from scratch (no incoming
  straddler), first prologue at `0x281860`.
- `check_boundaries` caught 1 HARD cross-boundary failure: `func_0028A7B0`/`func_0028A7E4` were
  over-split — the `bne`@`0x28A7BC` is an INTERNAL forward branch (the `j 0x8022E8C4`@`0x28A7DC` is
  one exit path; fall-through continues at `0x28A7E4`). Merged into one `func_0028A7B0`
  `[0x28A7B0,0x28A814)`.
- Frameless recoveries in parent gaps: 128 B@`0x283D94`, 224 B@`0x286444`, 1944 B@`0x28F18C` are
  all CODE (not data); the genuine data islands are L and M only.
- Adversarial swarm (4 code + 2 data skeptics): **all 6 CLEAN** (the merge fix confirmed; only 1
  cosmetic note-byte-count nit).

## Verification

`check_boundaries` PASS (0 fragment/cross/under/leak/straddler/data-island); `check_splits`
0 fragments (6 ≤12 B files, all real leaves); `check_manifest` 159 parts; `assemble_original_mips`
byte-exact (code SHA `40D4E787…B409`). See review handoff.
