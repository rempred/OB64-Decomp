# Chunk 10 — all-code: army-mgmt / display-list builders (`0x000A1000..0x000B1000`)

Source-ownership pass for the eleventh 64 KiB chunk. **ALL CODE** (no interior
data), like chunk 9 — but with substantially more parent-DB boundary defects to
correct. **35 tracked parts**: **33 code `func_*` + 2 straddler markers (1 tail + 1
head)**, **0 data**. Byte-exact rebuild preserved (code SHA `40D4E787…B409`, ROM SHA
`571E8339…CC67A`). Coverage `0x1000..0xB1000` = 720,896 B = **25.3016 %** of the
2,849,204-byte evidenced executable extent; code-only classified ≈ 590,108 B
(**20.71 %**).

Parent-DETECTED (27 parent fns, 25 named — same `character::char-data consumer` /
army-mgmt / F3DEX display-list-builder family as chunk 9), but the parent DB
over-merged frameless leaves into several large records and missed two
frameless-leaf gap clusters. A 5-agent adversarial swarm (4 region refuters + 1
data-hunter) tried to disprove every boundary and the all-code claim and found **0
disproofs**; the data-hunter confirmed 16,384/16,384 words decode as MIPS, max
inline pointer-run 1 word.

## Region map (single region — all code)

| Region | Range (z64) | Bytes | Class | Parts |
|---|---|---:|---|---:|
| STRADDLER+CODE | `0xA1000..0x000B1000` | 65,536 | code | 35 (1 straddler-tail + 33 `func_` + 1 straddler-head) |

## Incoming straddler (chunk 9 → chunk 10)
`func_000A0DAC_chunk10tail` `[0xA1000,0xA118C)` — honest tail of `func_000A0DAC`
(true entry `0xA0DAC` in chunk 9, head `func_000A0DAC_chunk9head`). Confirmed: no
prologue at `0xA1000` (opens mid-body); ends `jr $ra`@`0xA1184` + epilogue `addiu
$sp,0x30`@`0xA1188`; next prologue `0xA118C`. Frame `-0x30` matches the chunk-9
parent prologue. `kind:"straddler-tail"`.

## CODE `0xA118C..0xB0BFC` (33 framed + frameless functions)
Parent-detected, but with the run's most boundary corrections. Highlights:
- **3 preamble-orphans** (read-before-write `lui/lw` before the parent-labeled
  prologue): `func_000A4A00` (parent `0xA4A08`; `lui/lw 0x80196AF8`, consumed
  `0xA4A40`); `func_000AB5EC` (parent `0xAB5F4`; `lui/lw 0x80196AF8`, consumed
  `0xAB608`); `func_000AEB8C` (parent `0xAEB94`; `lui/lbu 0x801936E0`, consumed
  `0xAEBB0`).
- **Frameless leaves the parent over-merged or missed (7 recovered):**
  - GAP1 `func_000A71C8` `[0xA71C8,0xA723C)` — a frameless float-compare/clamp
    helper (`lwc1`/`sub.s`/`mtc1`/`c.s`/`bc1t`), after the 10 KB `func_000A4A00`.
  - GAP2 cluster `func_000AE298` `[0xAE298,0xAE30C)`, `func_000AE30C`
    `[0xAE30C,0xAE384)`, `func_000AE384` `[0xAE384,0xAE3F0)` — three frameless leaves
    (parent reported the 344-byte span as a leading gap on `func_000AE3F0`); each
    ends `jr $ra` (0xAE304/0xAE37C/0xAE3E8) and re-loads the global base at entry.
  - `func_000AB040` `[0xAB040,0xAB0FC)` — frameless leaf un-merged from parent
    `func_000AAF74` (adversarially confirmed: parent returns `jr $ra`@`0xAB038`;
    `0xAB040` is a fresh frameless entry, not reached from the parent).
  - `func_000AB6D8` `[0xAB6D8,0xAD1F8)` (6,944 B) — un-merged from the parent's
    7,172-byte `func_000AB5F4` record. Adversarially confirmed: the framed
    `func_000AB5EC` returns `jr $ra`@`0xAB6D0`; `0xAB6D8` is a frameless entry
    reading 5 params; the body is ONE contiguous switch (1 `jr $ra`@`0xAD1F0`, 0
    prologues, 25 `j 0x801xxxxx` overlay tail-jumps as case exits). **The parent's
    single-function view of `[0xAB5F4,0xAD1F8)` was wrong.**
  - `func_000AD6A0` `[0xAD6A0,0xAD6E0)` — frameless init leaf un-merged from parent
    `func_000AD1F8` (returns `jr $ra`@`0xAD698`).
- **5 internal jump-table dispatchers (NOT boundaries):** `jr $v0` at `0xA5768`
  (in `func_000A4A00`), `0xA84D0` (`func_000A8370`), `0xAB3D8` (`func_000AB330`),
  `0xB04E0` (`func_000AEB8C`), `0xB0C50` (`func_000B0BFC_chunk10head`) — all
  `sltiu/sll2/lui 0x801F/addu/lw/jr`, **target tables in `0x801EF998 / 0x801EF9F8 /
  0x801EFA10 / 0x801EFA30 / 0x801EFA70` relocated overlay RAM, not inline ROM.**
- **1 multi-entry function:** `func_000ADA74` (secondary `0xADB74` reached only by
  fallthrough — kept whole).
- Largest functions (all single, validated whole): `func_000A4A00` 10,184 B,
  `func_000AEB8C` 8,304 B, `func_000A8370` 7,292 B, `func_000AB6D8` 6,944 B.

## Outgoing straddler (chunk 10 → chunk 11)
`func_000B0BFC_chunk10head` `[0xB0BFC,0xB1000)` — true entry `0xB0BFC` (prologue
`addiu $sp,-0x40`), body continues **past `0xB1000` to `0x000B1F00`** in chunk 11
(return `jr $ra`@`0xB1EF8`). Confirmed: NO `jr $ra` in `[0xB0BFC,0xB1000)`; contains
an internal `jr $v0` dispatch @`0xB0C50` (not a boundary). `kind:"straddler-head"`.
**Chunk-11 first action: emit `func_000B0BFC_chunk11tail` `[0xB1000,0x000B1F00)`.**

## Method & verification
`promote_original_mips` (seed) → `dump_function_context` (27 parent fns) +
content-scan (`build/scan_chunk.js`, gitignored) → `plan_chunk` (`--tail-end
0xA118C`) → `slice_chunk --disasm` (7 slices) → **7-agent analysis swarm** (frameless
recovery, over-merge un-merge, 3 preamble-orphan folds, conservative naming) →
`integrate_chunk` → `check_boundaries` + `check_splits` → **5-agent adversarial
swarm** (4 region refuters + 1 data-hunter) → `split_original_mips_part
--remove-source`. No tool changes.

Deterministic invariants over all 35 code parts (`check_boundaries`): 0 fragments,
0 cross-boundary branches, 0 under-splits, 0 delay-slot leaks, 0 straddler-position
errors, 0 inline data islands. Adversarial data-hunt (whole chunk): max pointer-run
1 word, max zero-run 2 words, max printable run 1 word, 0 undecodable words; all 5
dispatcher tables in relocated RAM. Gates: `check_manifest` ALL PASS (11 chunks /
1,611 parts); `check_boundaries` PASS; `check_splits` 0 fragments;
`assemble_original_mips` byte-exact; `verify_setup` PASS (11 / 1,611 / 89);
`audit_code_region` OK; 0 data files with function wording; `git diff --check` clean.

## Caveats / next frontier
- Conservative overlay address names (RAM/global/callee identity suspect; linear
  decode-comment RAM column is wrong). Display-list-builder / army-mgmt role is a
  structural + parent-DB inference, not runtime-proven.
- Jump-table dispatcher targets read from `0x801EF…` relocated RAM — not resolved.
- `func_000AB6D8` is conservatively a single `func_*` part: it is one contiguous
  single-return switch-body, but it is plausibly the case-body of a logical
  dispatcher whose entry is elsewhere (semantics not runtime-proven).
- **Next frontier `0x000B1000` (chunk 11).** FIRST continue the straddler:
  `func_000B0BFC_chunk10head` continues to `0x000B1F00` (chunk-11 tail file
  `func_000B0BFC_chunk11tail` `[0xB1000,0x000B1F00)`, true entry 0xB0BFC). Chunk 11
  has 113 parent function labels and 654 nops — content-scan for data FIRST (it may
  differ in shape from chunks 9–10).
