# lib chunk 41 — `0x00291000..0x002A1000`

Classification: **MIXED** (incoming function straddler-tail → CODE → interior DATA island → CODE
→ outgoing function straddler-head). FP-heavy mission-briefing/combat + UI code with one data
island.

- Split JSON: `build/chunk_00291000-002A1000_splits.json`
- Data index: `docs/data-index/rev0/chunk41-data-region-inventory.json`
- Promoted source: `asm/original/rev0/lib/` (160 parts)

## Composition

**160 parts = 134 normal code + 1 straddler-tail + 1 straddler-head + 24 data.**

ROM-ordered region map:

- Incoming FUNCTION straddler-tail `func_00290D50_chunk41tail` `0x00291000..0x002910DC`
  (continues `func_00290D50` from chunk 40; returns `jr $ra`@`0x002910D4` + delay
  `addiu $sp,0x20`@`0x002910D8`).
- CODE C1 `0x002910DC..0x00299D44` — FP-heavy functions + UI/display-list builders; recovered
  frameless leaves + preamble-orphan folds. Ends `jr $ra`@`0x00299D3C`.
- **DATA D (interior)** `0x00299D44..0x0029A4C0` (1,916 B, 24 parts) — leading zero-fill +
  RAM-pointer tables (`0x8022Exxx`/`0x8023Exxx`/`0x8023BC..C5xx`/`0x8023D8..DBxx` incl. trailing
  `0x8023DB30`/`0x8023DB3C`) + ASCII string pools ('Palatinean Year', '0123456789',
  'Saldian'/'Viragore' and element/faction/format pools) + a float32 30.0 constant.
- CODE C2 `0x0029A4C0..0x002A1000` — FP/display-list functions; recovered frameless leaves
  (incl. the `func_0029BBD8` recovery — the parent's apparent `func_0029BC18` prologue is the
  delay slot of `beq`@`0x29BC14`, NOT a separate function).
- Outgoing FUNCTION straddler-head `func_002A0EF0` `0x002A0EF0..0x002A1000` (prologue
  `addiu $sp,-0x30` + `sdc1 $f20`; NO `jr $ra` before `0x002A1000`; continues into chunk 42 as
  `func_002A0EF0_chunk42tail`).

## Data island inventory (24 parts, 1,916 bytes)

Bytes by class: rodata 648 · table 632 · data 520 · zero_fill 84 · float 32.
Full per-part index: `docs/data-index/rev0/chunk41-data-region-inventory.json`.

DATA D `0x299D44..0x29A4C0` — 15-word leading zero-fill, then interleaved RAM-pointer tables,
ASCII string pools (calendar/faction/element/menu text), a 30.0f constant, and a trailing
`0x8023DBxx` pointer run. All byte-owned and classified (`parsed`/`raw-but-classified`); 0 bytes
undecoded-and-unclassified.

## Role leads (conservative; names stay `func_*`)

FP-heavy mission-briefing/combat + UI/menu module continuing chunks 36-40. DATA D's string pools
('Palatinean Year' = the OB64 in-game calendar; 'Saldian'/'Viragore' faction/region names)
suggest a status/calendar/faction UI component. Names stay `func_*` pending runtime proof.

## Parent workspace evidence sweep

Covered by the run-wide `0x281000..0x2B1000` sweep (see chunk-40 dossier / review). No
chunk-41-specific parent function leads beyond the callgraph; the combat overlay sources in
`ram_snapshots/overlay_sources.json` end before `0x281000`, so chunk 41 is unclaimed-overlay/data
for runtime-mapping purposes (RSR-001).

## Mistakes corrected / hazards handled

- One interior data island D carved via `build/carve_chunk.js`.
- The analysis swarm mislabeled 7 mid-chunk functions (ending in overlay tail-jumps) as
  `straddler-head`; reclassified to `code` (only `func_002A0EF0` is the real outgoing straddler).
- Fixed 9 preamble-orphan label conflicts (label set to the prologue addr → corrected to the part
  start / name).
- `check_boundaries` caught a 4-byte DATA D coverage gap (a string-terminator zero word @`0x299FA0`
  left uncovered) — folded into `rodata_00299f80`.
- `check_boundaries` caught a slice-seam preamble fragment (`func_0029FF74`, an 8-byte
  read-before-write preamble) — merged forward into `func_0029FF7C`.
- Adversarial swarm (4 code + 1 data skeptics): **all 5 CLEAN** (1 LOW note-count typo fixed).

## Verification

`check_boundaries` PASS (0 fragment/cross/under/leak/straddler/data-island); `check_splits`
0 fragments; `check_manifest` 160 parts; `assemble_original_mips` byte-exact (code SHA
`40D4E787…B409`). See review handoff.
