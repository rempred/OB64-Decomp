# lib chunk 36 — `0x00241000..0x00251000`

Classification: **MIXED** (code → DATA island A → code → DATA island C → code →
outgoing function straddler-head). Mission-briefing / combat display-list module.

- Split JSON: `build/chunk_00241000-00251000_splits.json`
- Data index: `docs/data-index/rev0/chunk36-data-region-inventory.json`
- Promoted source: `asm/original/rev0/lib/` (164 parts)

## Composition

**164 parts = 134 normal code + 1 straddler-tail + 1 straddler-head + 28 data.**

Region map:

- Incoming FUNCTION straddler-tail `func_00240FF0_chunk36tail` `0x00241000..0x002410A0`
  (continues `func_00240FF0`, prologue `addiu $sp,-0x20` in chunk 35 @`0x00240FF0`;
  returns `jr $ra`@`0x00241098` + delay `addiu $sp,0x20`@`0x0024109C`).
- CODE region 1 `0x002410A0..0x00243F14` — GBI/RDP display-list builders + promotion/
  class/character consumers.
- Interior DATA island A `0x00243F14..0x002447A0` (2,188 bytes).
- CODE region 2 `0x002447A0..0x0024B410` — incl. recovered frameless GBI builders
  `func_0024A37C`/`func_0024A638` and dispatcher `func_0024AF04`.
- Interior DATA island C `0x0024B410..0x0024BCA0` (2,192 bytes).
- CODE region 3 `0x0024BCA0..0x00250F9C` — frameless range-check leaf `func_0024BCA0`,
  a cluster of frameless divide/scale helpers (`func_0024BD60`..`func_0024BDEC`), and more
  display/class code.
- Outgoing FUNCTION straddler-head `func_00250F9C` `0x00250F9C..0x00251000`
  (prologue `addiu $sp,-0x18`; a beq-cascade jump-table dispatcher; NO `jr $ra` before the
  boundary; continues into chunk 37 as `func_00250F9C_chunk37tail`, returns
  `jr $ra`@`0x002510D8`).

## Data island inventory (28 parts, 4,380 bytes)

Bytes by class: data 1,804 · rodata 1,188 · table 876 · float 456 · zero_fill 56.
Full per-part index: `docs/data-index/rev0/chunk36-data-region-inventory.json`.

- **Island A** `0x243F14..0x2447A0` — 0x801E-band display-list pointer tables, float pools,
  packed/16-bit parameter tables, a single-pointer column, and rodata.
- **Island C** `0x24B410..0x24BCA0` — packed 16-bit record data, 0x801x-band pointer
  tables, a float32 array, and rodata strings. Ends at `0x24BCA0` (a frameless CODE leaf
  begins there, owned separately).

## Role leads (conservative; names stay `func_*`)

From the parent evidence sweep (`build/context`, `docs/overlay-system.md`,
`docs/promotion-system.md`) — leads only, validated against byte-exact disassembly:

- This is a **mission-briefing / combat display-list satellite module**: ~14 functions are
  parent-tagged active in `mission_briefing`+`combat`. They build F3DEX/GBI display lists
  (control globals `0x801F0000..0x801F007E`, framebuffer/DL pointer `0x800F9BA0`/`0x800A9E70`)
  from class/promotion/character data (`0x801C`/`0x801D` globals).
- `func_002447A0` is a high-fan-in resource alloc/free helper (10 cross-chunk callers).
- The large leaves `func_00242E30` (F3DEX task builder) and `func_00243A24`
  (display-list fragment builder) are GBI-immediate-heavy CODE.

## Mistakes corrected / hazards handled

- **Two interior data islands** carved via `build/carve_chunk.js` (multi-region); the
  parent-gap "data leads" at `0x24A37C` (frameless GBI builders) and `0x24BD60` (frameless
  divide/scale helpers) were proven CODE, not data, and recovered as frameless leaves.
- Region C's data ends at `0x24BCA0`; the trailing frameless range-check leaf
  `func_0024BCA0` (parent-undetected) was carved into CODE region 3 (synthetic leading entry).
- **Delay-slot leak fix (adversarial/boundary):** the divide-helper cluster
  (`func_0024BD60`..`func_0024BDEC`) was initially split ON the `jr $ra` word; corrected so
  each helper owns its delay slot.
- Many read-before-write preamble-orphans folded FORWARD, named for the PART START
  (not the prologue address) to avoid the duplicate-label assemble hazard.

## Verification

`check_boundaries` PASS (0 fragment/cross/under/leak/straddler/data-island);
`check_splits` 0 fragments (1 informational TINY stub `func_0024B3BC`, has `jr $ra`).
Adversarial swarm (5 code + 1 data + straddler skeptics). Byte-exact rebuild preserved
(see review handoff).
