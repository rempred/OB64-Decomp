# Chunk 19 — MIXED: encounter/dispatcher code + trailing scenario data (`0x00131000..0x00141000`)

Source-ownership pass for the twentieth 64 KiB chunk. **MIXED**: an incoming FUNCTION
straddler-tail, a large CODE region (encounter/dispatcher code, including the
`neutralEncounterDispatcher`), then a large trailing DATA region ending in an outgoing
DATA straddler. **80 tracked parts**: **64 code (incl. 1 incoming straddler-tail) + 16
data**. Byte-exact rebuild preserved (code SHA `40D4E787…B409`, ROM SHA `571E8339…CC67A`).
Coverage `0x1000..0x141000` = 1,310,720 B = **46.0030 %** of the 2,849,204-byte evidenced
executable extent; code-only classified ≈ 1,103,712 B (**38.7376 %**).

Pipeline: parent context + content scan (code/data boundary `0x13C49C`) → 7 code-slice +
1 data-region analysis agents (Workflow) → combine → `check_boundaries`/`check_splits` →
4-agent adversarial swarm (3 code verifiers + 1 data hunter). **0 disproofs.** No tool
changes.

## Region map (3 ROM-ordered regions)

| Region | Range (z64) | Bytes | Class | Parts |
|---|---|---:|---|---:|
| incoming straddler-tail | `0x131000..0x131050` | 80 | code | 1 |
| CODE (encounter/dispatcher) | `0x131050..0x13C49C` | 46,156 | code | 63 |
| DATA (trailing) | `0x13C49C..0x141000` | 19,300 | data | 16 |

## Incoming function straddler (chunk 18 → chunk 19)
`func_00130E60_chunk19tail` `[0x131000,0x131050)` — tail of `func_00130E60` whose true
entry `0x130E60` is in chunk 18. Returns `jr $ra`@`0x131048` + delay@`0x13104C`; the next
function starts exactly at `0x131050` (parent end). `kind:"straddler-tail"`.

## CODE `0x131050..0x13C49C` (63 parts)
Overlay-relocated (decode RAM column `0x8019/0x801Axxxx` is the WRONG linear map; real
runtime slots `0x801E` band, scenario state). 18 framed prologues + 9 preamble-orphans
(read-before-write folds; each named `func_<startaddr>` with `label`=name) + 9 recovered
frameless leaves + ~27 plain code. **Dispatcher-heavy**: many internal `jr $reg`
jump-table dispatchers (`0x13173C`, `0x137750`, `0x139584`, `0x13A0E8`, … → relocated
`0x801E/0x801F` tables) and `j 0x801Dxxxx/0x801Exxxx` overlay tail-jumps, all kept
internal. The parent over-merged `func_00131388` into one record — actually 5 functions
(split into the framed parent + 4 frameless leaves). **The last code function
`func_0013C060` `[0x13C060,0x13C49C)`** (preamble-orphan, 8-byte `lui/lbu 0x8019…`
read-before-write before the parent's `0x13C068` prologue) is the parent-documented
**`neutralEncounterDispatcher`** (see parent evidence) — name kept conservative; ends
`jr $ra`@`0x13C494` + epilogue@`0x13C498`, exactly at the data boundary `0x13C49C`.

## DATA `0x13C49C..0x141000` (16 parts) + outgoing straddler
- 2-word `zero_fill_` → a **bit-pattern LUT** `[0x13C4A4,0x13C524)` (single-bit-set
  pattern `0x80004000/0x20001000/0x08000400/…` repeating ~4×) → three **RAM-pointer
  tables** (`0x801E7Dxx`/`0x801E90xx` bands, stride `0x20`) → a large **fixed-stride
  record/script table** `[0x13C550,0x13D824)` (regular 16-byte rows, monotonic index field
  `0x0C..0x10`; fields not decoded) → **packed-fill blocks** (`0x00010001`, `0xFFFFFFFF`
  runs) and a **descending-high-byte ramp** → a **packed-byte tail**.
- **Outgoing DATA straddler `data_00140EA0_chunk19head`** `[0x140EA0,0x141000)` — a
  packed-byte structure (values ≤`0x0C`: `0x09090909`/`0x05000709`/`0x0C0A0709`) beginning
  at `0x140EA0` and continuing past `0x141000` into chunk 20 with no terminator. **Not** a
  clean boundary. `kind:"data"`.
- All `kind:"data"`; no field semantics invented.

## Parent contradiction resolved (linear-map fallacy)
The parent sweep flagged "live combat code" at ROM `0x13DD74`/`0x13EE90`/`0x140180` in the
`0x13C498..0x141ED0` "gap" (from a wiki battle-turn-queue trace mapping RAM `0x801AFD80` →
ROM `0x140180` by the linear `ROM = RAM − 0x8006FC00`). **DISPROVEN** by byte-exact Rev 0:
the entire region `[0x13C49C,0x141000)` has **0 `addiu $sp,-N` prologues, 0 `jr $ra`
(0x03E00008), 0 `sw $ra`** — no function is possible. **Independently refuted by the
adversarial data-hunter** (statistical profile: 646× `0xFFFFFFFF`, 581× `0x00000000`, 352×
`0x00010001`). The bytes at those ROM offsets are packed DATA (`0x13DD74=0x5B875B09`
descending bytes; `0x140180=0x294B2109` paired-byte tile). The real overlay source for that
RAM lives at a different ROM offset; this is the discredited linear-map fallacy.

## Parent-workspace evidence (sweep 2026-06-23)
- **`0x0013C068` = `neutralEncounterDispatcher`** (HIGH confidence, docs-verified:
  overlay-system.md, neutral-encounters.md, neutral-encounter-mips.md). 219 instr / 104-B
  frame; terrain-compat early-exit, 8 flag gates, base-rate roll (÷72000), 30-unit loop,
  Decoy-Cap doubling, spawn via `0x801B8D44` marker 46. **Patch sites `0x13C1FC`
  (`li $s0,50` normal) / `0x13C200` (`li $s0,10` alt)** — Rev-0 offsets; `editor/parsers.js`
  encodes a Rev0(`0x13C1xx`)/Rev1.1(`0x13C208/21C/220/248`) split. The whole function is
  source-owned as **`func_0013C060`** (conservative; dispatcher identity recorded as a
  strong lead). `symbols_v2` mis-tags it "char-data consumer" — the docs are authoritative.
- LEADS (conservative): `0x134830`/`0x1349BC`/`0x13511C`/`0x1361FC` = encounter UI /
  neutral-Talk state cluster; `0x132B80` = reward-item writer (`sh` to `0x801F0B50`);
  `0x136C40` = largest fn (9,400 B). The `neutral_encounter_table` (`0x141ED0`) and
  `creature_drop_table` (`0x142258`) sit ABOVE `0x141000` (chunk 20+) — this window holds
  their reader code, not the tables.

## Data inventory (machine-readable: `docs/data-index/rev0/chunk19-data-region-inventory.json`)
**Total data: 19,300 B in 16 parts.** By class: packed-highbyte 12,108 B · fixed-stride
record/script table 4,848 B · packed-fill 1,592 B · outgoing straddler-head 352 B ·
packed-markers 156 B · bit-pattern LUT 128 B · ram-pointer table 108 B · zero-fill 8 B.
State: parsed (zero-fill, pointer tables); raw-but-classified (record table, packed blocks,
bit-LUT — type clear, fields not decoded). No invented field names. No ASCII/text → no
human-readable string export needed.

## Method & verification
Region map (content scan + parent-gap analysis; code/data boundary `0x13C49C` pinned at the
last fn's `jr $ra`@`0x13C494`) → 7 code-slice + 1 data agent (Workflow) → combine
(`combine_chunk.js`; data names → `<kind>_<addr>`; kinds normalized) → `check_boundaries`
PASS (80 parts; 0 fragment/cross/under/leak/straddler/data-island) + `check_splits` 0
fragments → 4-agent adversarial swarm (**0 disproofs**; data-hunter refuted the parent
code-in-gap claim) → `split_original_mips_part --remove-source`. Gates: `check_manifest` ALL
PASS (20 chunks / 2,526 parts); `assemble_original_mips` byte-exact (`40D4E787…B409`);
`verify_setup` all green; `audit_code_region` OK; data-index JSON valid; 0 data files with
function wording.

## Caveats / next frontier
- Conservative code names (overlay RAM/global/callee identity suspect; the
  `neutralEncounterDispatcher` identity recorded as a lead, name stays `func_0013C060`).
  The record/script table fields, the bit-LUT meaning, and the pointer-table wiring are NOT
  decoded (follow-up). Dispatcher target tables in `0x801E…/0x801F…` relocated RAM unresolved.
- **Next frontier `0x00141000` (chunk 20).** FIRST continue the outgoing DATA straddler:
  `data_00140EA0_chunk19head` `[0x140EA0,0x141000)` (packed-byte structure) continues into
  chunk 20 — emit `data_00141000_chunk20tail` first. Note the documented `neutral_encounter_table`
  (`0x141ED0`, 40×20 B) and `creature_drop_table` (`0x142258`, 36×8 B) are early in chunk 20.
