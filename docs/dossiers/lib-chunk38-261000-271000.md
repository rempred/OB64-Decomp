# lib chunk 38 — `0x00261000..0x00271000`

Classification: **ALL CODE** (incoming function straddler-tail → dense FP/GBI display-list +
mission-briefing/combat dispatcher code, frameless-leaf dense → outgoing function straddler-head).
No data island (deterministic content scan found no pointer-word runs, no ASCII pools, no
zero-fill islands; the two large parent "gaps" are frameless code).

- Split JSON: `build/chunk_00261000-00271000_splits.json`
- Promoted source: `asm/original/rev0/lib/` (232 parts)
- No data index (all code).

## Composition

**232 parts = 230 normal code + 1 straddler-tail + 1 straddler-head + 0 data.**

Region map:

- Incoming FUNCTION straddler-tail `func_00260F30_chunk38tail` `0x00261000..0x0026109C`
  (continues `func_00260F30`, prologue `addiu $sp,-0x20` in chunk 37 @`0x00260F30`; interior
  `j 0x8020C4xx` are overlay tail-jumps; returns `jr $ra`@`0x00261094` + delay
  `addiu $sp,0x20`@`0x00261098`).
- CODE `0x0026109C..0x00270FF0` — ~230 functions: FP/GBI display-list builders (DE00/E200/E700/
  D9FF/FA00 GBI command immediates emitted through the `0x800F9BA0` display-list cursor),
  mission-briefing/combat record builders (high-fanin helpers calling `0x260B0C`/`0x260B44`/
  `0x260E00`), and 16-bit-selector dispatchers (`jr $v0`/`jalr $v0` jump tables + `j 0x8020/0x8021`
  overlay tail-jumps, all internal). Frameless-leaf dense; many tiny accessors recovered.
- Outgoing FUNCTION straddler-head `func_00270FF0` `0x00270FF0..0x00271000`
  (prologue `addiu $sp,-0x28`@`0x00270FF0`; NO `jr $ra` before `0x00271000`; continues into
  chunk 39 as `func_00270FF0_chunk39tail`, returns there `jr $ra`@`0x00271068` + delay
  `addiu $sp,0x28`@`0x0027106C`).

## Parent-gap frameless recoveries (the central nuance this chunk)

The parent DB left several "gaps" that are NOT data — all proven frameless CODE:

- 288 B gap `0x002639D8..0x00263AF8`: alignment nops + a frameless GBI display-list builder
  leaf `func_002639E0` (DE00/E200/E700 packets via `0x800F9BA0`, `jr $ra`@`0x00263AE8`) + a
  2-word read-before-write preamble `func_00263AF0` folding into prologue `func_00263AF8`.
- 96 B gap `0x00264FB4..0x00265014`: alignment + frameless leaf `func_00264FC0`.
- 796 B gap `0x002664A4..0x002667C0`: alignment + frameless switch/dispatch leaf
  `func_002664B0` (reads `0x80197178`; `beq $a3` case dispatch; `j 0x80211998`/`0x802119A0`
  overlay tail-jumps) + a further frameless leaf `func_00266550`.

## Role leads (conservative; names stay `func_*`)

Mission-briefing/combat module (parent active states `mission_briefing`+`combat`). This is the
display-list/dispatcher continuation of chunks 36-37: FP geometry/vector helpers + GBI command
builders + command/message dispatchers. Overlay-relocated; the decode-comment RAM column is the
wrong linear map. Names stay `func_*` pending runtime proof (RSR-001/RSR-014).

## Mistakes corrected / hazards handled

- Deterministic content scan (zero-control-flow + pointer/zero/ASCII density) confirmed
  ALL CODE — the two big parent "gaps" (288 B@`0x2639D8`, 796 B@`0x2664A4`) are frameless code,
  not data.
- Adversarial swarm (6 code skeptics) caught 3 HIGH issues, all fixed and re-gated:
  - `func_00261C90` over-ran a read-before-write preamble; split `func_00261D00` folded forward
    (preamble `lui $a0,0x8022`/`lw 0xD58` consumed live by `jal 0x800712C4`@`0x261D10`).
  - `func_00263050` was a 4-way over-merge; un-merged into `func_00263050`/`func_00263074`/
    `func_002630CC`/`func_00263120` (0x8022.0DA0/0DA8 slot-table accessor family).
  - `func_00267038`/`func_00267108` boundary misplaced; moved the 4-word preamble
    `[0x267108,0x267118)` (read-before-write `$v1`) to `func_00267108`.
- The other big-band verifiers (`0x267800..0x26A800`, `0x26A800..0x26D800`) were clean.

## Parent workspace evidence sweep

Searched parent `C:\Users\Joe\Projects\OgreBattlel64` for `0x261000..0x271000` (z64 + RAM
forms + overlay tail-jump targets). **No specific parent doc leads** for this range — consistent
with overlay display-list/dispatcher code. `scripts/ob64_callgraph.json` lists function offsets
in range but with no semantic labels. No patch-workbench hook sites in range.

## Verification

`check_boundaries` PASS (0 fragment/cross/under/leak/straddler/data-island); `check_splits`
0 fragments; `check_manifest` 232 parts; `assemble_original_mips` byte-exact (code SHA
`40D4E787…B409`). Adversarial swarm 6 verifiers (3 HIGH fixed, 2 clean, 1 straddler/end clean).
