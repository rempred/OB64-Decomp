# Chunk 24 — MIXED: FP/menu/display code wrapping a large ~26.7 KB interior data region (`0x00181000..0x00191000`)

Source-ownership pass for the twenty-fifth 64 KiB chunk. **MIXED, 3 ROM-ordered regions**:
CODE1 → a large interior DATA region → CODE2, with an incoming FUNCTION straddler-tail and an
outgoing FUNCTION straddler-head. **63 tracked parts**: **40 code + 23 data**. Byte-exact rebuild
preserved (code SHA `40D4E787…B409`, ROM SHA `571E8339…CC67A`). Coverage `0x1000..0x191000` =
1,638,400 B = **57.5038 %** of the 2,849,204-byte evidenced executable extent; code-only
classified = 1,325,868 B (**46.5347 %**).

Pipeline: parent-evidence sweep + byte-exact proret scan (which **refuted the parent DB** — see
below) → 8 code-slice + 6 data-region analysis agents (Workflow) → combine →
`check_boundaries`/`check_splits` → 6-agent adversarial swarm. **All 6 verdicts CLEAN** (3
cross-slice preamble folds resolved before the adversarial pass; 1 cosmetic data-subdivision
note, no re-tiling needed).

## Region map (3 ROM-ordered regions)

| Region | Range (z64) | Bytes | Class | Parts |
|---|---|---:|---|---:|
| CODE 1 (FP/menu/display) | `0x181000..0x1822E4` | 4,836 | code | 13 |
| DATA (interior; bitmaps + record tables + pointer tables + float pool) | `0x1822E4..0x188B60` | 26,748 | data | 22 |
| CODE 2 (char/display) + inline data island + outgoing straddler | `0x188B60..0x191000` | 33,952 | code+1 island | 28 |

## Parent-DB correction (headline)
The parent functions DB labels code across the whole chunk and **missed the ~26.7 KB interior
DATA region `0x1822E4..0x188B60`**. A byte-exact proret scan plus **three adversarial data
verifiers** (each scanning every word) confirm **0 `addiu $sp,-N` prologues and 0 `jr $ra`
(0x03E00008)** across the entire region — same failure mode as chunk 23's two data islands. The
parent's "9028 B orphan" at `0x189778` is, by contrast, genuine CODE (one prologue + one `jr$ra`
+ trailing frameless leaves). Boundaries pinned from byte-exact returns/prologues, never the DB.

## Incoming FUNCTION straddler (chunk 23 → chunk 24)
`func_0017FF4C_chunk24tail` `[0x181000,0x181118)` — tail of `func_0017FF4C` whose `addiu $sp,-0x88`
prologue is in chunk 23 at `0x17FF54`. Ends `jr $ra`@`0x181110` + delay `addiu $sp,0x88`@`0x181114`
at exactly `0x181118` (parent combat_transition lead). `kind:"straddler-tail"`.

## CODE 1 `0x181000..0x1822E4` (13 parts)
Overlay-relocated (runtime `0x8021/0x8022` band). FP/menu/display functions. 5 preamble-orphans,
all adversarially confirmed read-before-write: `func_00181648` (2-word), **`func_0018197C`** (a
**~15-word hoisted-const preamble** — flagged for review, adversarially confirmed to be ONE
function with a single prologue@`0x1819BC` + single `jr$ra`@`0x181B4C`, not a separate part),
`func_00181B54` (4-word, folded across the c1s1/c1s2 slice seam), `func_00181F5C`,
`func_001820B0`. Tiny frameless-leaf accessor `func_001820A4` [0x1820A4,0x1820B0) (`lui $v0,0x8022`
/ `jr$ra` / delay `lbu`). Ends `jr$ra`@`0x1822DC` + delay@`0x1822E0`.

## DATA `0x1822E4..0x188B60` (22 parts) — the interior data region
`zero_fill_001822E4` (3 nops) → packed/high-entropy blocks (`data_001822F0` row-marker stride
0x20; `data_001824E4` sparse `0x00010001` bitmask) → **font/tile bitmap stream** `data_001825F0`
(~6.7 KB, `0x02`-fill + `0x78`-band glyph markers, entropy ~4.36 = bitmap not compression) →
a zero-interspersed small-byte table cluster (`0x185178..0x185950`) → **fixed-stride record
tables**: `data_00185950` (**363 rows × 0x10**, invariant cols `0xFFFB0000`@+4 / `0xFFFFFFFF`@+0xC)
and `data_00187000` (**177 rows × 0x10**, same structure) → **`table_00187B10`** (16 monotonic
`0x8021`-band RAM pointers) → `data_00188000` (packed records: `0x05` quads + `0x01` headers
paired with `0x8021B/C` pointers) → **`table_001888D8`** (140 monotonic `0x8021D`-band pointers,
incl. the `0x8021DB68/0x8021DB90` run) → `zero_fill_00188B08` (2 nops) → **`float_00188B10`**
(10 IEEE-754 doubles). All adversarially confirmed 0 prologues/returns. Record-table field
semantics and the bitmap pixel layout are **undecoded** (raw-but-classified).

## CODE 2 `0x188B60..0x191000` (28 parts) + inline data island + outgoing straddler
Starts with a **frameless DL-builder leaf** `func_00188B60` (no prologue, builds a GBI/RDP
display list, `jr$ra`@`0x188CD4`). 10 preamble-orphans (incl. `func_00188CDC`, `func_0018BAB4`
and `func_0018DC84` folded across slice seams) — all adversarially confirmed read-before-write.
The huge **`func_00189778`** (8788 B) is one prologue + one `jr$ra` (the parent's "orphan").
- **Inline DATA island `data_0018F044`** `[0x18F044,0x18F100)`: UI labels `"Soldier"`@`0x18F06C`,
  `"Remove"`@`0x18F090` + an `0x8021`-band pointer mini-table; 0 prologues/returns; code resumes
  at `func_0018F100`. Decoded export: `chunk24-ui-labels-18F044`.
- **Outgoing FUNCTION straddler-head `func_0018FB30`** `[0x18FB30,0x191000)` — `addiu $sp,-0x78`
  prologue@`0x18FB30`; **NO `jr$ra` in `[0x18FB30,0x191000)`** (adversarially confirmed);
  continues into chunk 25 (`func_0018FB30_chunk25tail`). `kind:"straddler-head"`.

## Adversarial pass (6 verifiers, all CLEAN)
3 code + 3 data skeptics. **0 boundary disproofs.** Verifiers confirmed: the ~15-word
`func_0018197C` preamble is a single function (not over-split); all 15 preamble-orphans are
genuine read-before-write folds; `func_00189778` has exactly one prologue/return; the data
region has 0 code; the `0x1822E4` and `0x188B60` seams are byte-exact; pointer tables are
`0x8021`-band; the float pool is IEEE-754; the inline `data_0018F044` is data. The only finding
was 1 LOW cosmetic note (the bitmap stream is subdivided at non-structural fill boundaries — all
parts correctly `kind:data`, no re-tiling needed). The 3 cross-slice preamble-orphan folds
(`func_00181B54`, `func_0018BAB4`, `func_0018DC84`) were applied before the gates.

## Data inventory (machine-readable: `docs/data-index/rev0/chunk24-data-region-inventory.json`)
**Total data: 26,936 B in 23 parts.** By class: packed/record/unknown 24,980 B · RAM-pointer
tables 1,824 B · IEEE-754 const 80 B · zero-fill 52 B. **Parsed:** the two fixed-stride record
tables (row count + stride + invariant columns), the two `0x8021`-band pointer tables, the
float64 pool, and the UI-label rodata (`chunk24-ui-labels-18F044` export). **Raw-but-classified:**
the font/tile bitmap stream + packed/high-entropy blocks (possibly compressed/graphics,
HYPOTHESIS). **Undecoded:** record-table field semantics; bitmap pixel layout; packed-blob
contents.

## Method & verification
Parent-evidence sweep + proret scan (derived the 3-region map, correcting the parent DB) →
slice_extract (own byte-exact slices, no parent-DB seeding) → 8 code + 6 data agents (Workflow) →
combine (`combine_chunk.js`) → 3 cross-slice preamble folds → `check_boundaries` PASS (63 parts;
0 fragment/cross/under/leak/straddler/data-island) + `check_splits` 0 fragments → 6-agent
adversarial swarm (all CLEAN) → `split_original_mips_part --remove-source` (2 folded parts then
renamed to match their names + manifest fixed). Gates: `check_manifest` ALL PASS (25 chunks /
3,035 parts); `rebuild_from_source_manifest` byte-exact (ROM `571E8339…CC67A`);
`assemble_original_mips` byte-exact (code `40D4E787…B409`); `verify_setup` all green;
`audit_code_region` `no-credible-code-edge-into-tail`; `git diff --check` clean; data-index JSON
valid; 0 data files with function wording.

## Caveats / next frontier
- Conservative code names (overlay-relocated, runtime `0x8021/0x8022`). The big interior data
  region is undecoded (bitmaps + record tables with unknown field semantics + packed blobs).
- **Next frontier `0x00191000` (chunk 25).** FIRST continue the outgoing **FUNCTION** straddler:
  `func_0018FB30` `[0x18FB30,0x191000)` continues into chunk 25 — emit
  `func_0018FB30_chunk25tail` first (its `jr$ra` is at `0x19116C`). Chunk 25 is code-dominant with
  a dialogue string-pool data island and ends in another outgoing function straddler.
