# Chunk 8 — straddler tail + code + game-data + code (`0x00081000..0x00091000`)

Source-ownership pass for the ninth 64 KiB chunk. **MIXED, three ROM-ordered
regions**: an incoming function-straddler tail + parent-detected code, then a
game-data block, then more code. **87 tracked parts**: **61 code `func_*` + 2
straddler markers + 24 data**. Byte-exact rebuild preserved (code SHA
`40D4E787…B409`, ROM SHA `571E8339…CC67A`). Coverage `0x1000..0x91000` = 589,824 B
= **20.70 %** of the 2,849,204-byte evidenced executable extent; code-only
classified ≈ 459,036 B (**16.11 %**).

Chunk 8 is largely **PARENT-DETECTED** (43 parent fns + 43 overlay fns), the first
mostly-detected chunk since chunk 5 — but the parent DB still over-merges and
orphans 2-word read-before-write preambles, and one ~3.6 KB code subregion is
parent-undetected frameless leaves.

## Region map

| Region | Range (z64) | Bytes | Class | Parts |
|---|---|---:|---|---:|
| STRADDLER+CODE1 | `0x81000..0x85818` | 18,456 | code | 20 (1 straddler-tail + 19 `func_`) |
| DATA | `0x85818..0x87200` | 6,632 | data | 24 |
| CODE2 | `0x87200..0x91000` | 40,448 | code | 43 (42 `func_` + 1 straddler-head) |

Boundary evidence: code→data `0x85818` (fn `0x85684` returns `jr $ra` 0x85810,
then 2 nops + packed blob); data→code `0x87200` (frameless display-list builder
`move $t4,$a1; lui 0x800F; … E700` begins; pointer table ends at `0x871fc`).

## Incoming straddler (chunk 7 → chunk 8)
`func_0007ffac_chunk8tail` `[0x81000,0x810DC)` is the honest tail of `func_0007FFAC`
(true entry `0x7FFAC`; head `func_0007ffac_chunk7head` in chunk 7). Confirmed from
disasm: `jr $ra` at `0x810D4` + epilogue `addiu $sp,0x50` at `0x810D8`; the next
function starts at `0x810DC`. **NOT an independent function** — kept as
`kind:"straddler-tail"`.

## CODE1 `0x81000..0x85818` (19 `func_*` + tail)
Parent-detected. Includes the 5,492-byte `func_00083C5C` `[0x83C5C,0x851D0)`
(display-list builder; internal indirect jump, single `jr $ra`) — parent
over-merged two trailing frameless leaves (`func_000851D0` float-const setup,
`func_00085450`) into it; un-merged. `func_00083610` is a genuine multi-entry
function (frameless table-setup entry `0x83610` falls through to the `addiu $sp,-0x8`
body at `0x83644`). Several 8-byte `lui/lw $vN,0x7AF8` preamble-orphans folded.

## DATA `0x85818..0x87200` (24 parts) — game data (mission names + options menu)
4 `zero_fill_` + 9 `data_` + 4 `rodata_` + 7 `table_`. All `kind:"data"`; the
decode-comment mnemonics (dsra32/sd/j/regimm) are coincidental — proven data
(code returns at 0x85810, resumes 0x87200; 0 prologues/returns in-region;
pointer/ASCII/zero evidence). Highlights:
- `rodata_00085960` `[0x85960,0x85C60)` — **length-prefixed mission/location-name
  string pool** (1 length byte + ASCII + `0x0F` separator): Castro Canyon, Zenobian
  Border, Crenel Canyon, Volmus Mine, Tenne Plains, … Fort Romulus.
- `rodata_00085c60` `[0x85C60,0x85EA8)` — **NUL-terminated UI/options-menu pool**:
  Message/Cursor/Game speed, Sound settings, Mono/Stereo, Restore defaults, …,
  Prologue/Chapter:One.., No Data, + elemental names Deus/Tierra/Agua/…/Raio.
- `data_00085820`/`data_000858e4` — packed delta/offset/index blobs (0x02-0x27
  values, 0xFF group delimiters).
- `zero_fill_*` at `0x85818` (8 B lead-in nops), `0x858bc` (40 B), `0x86864`,
  `0x86954`.
- `table_000864a0` `[0x864A0,0x86728)` — stride-8 records {small index, RAM pointer
  `0x801A6xxx`}; `table_00086728`/`table_00086828` — RAM-pointer tables
  (`0x801A6Dxx/0x801A6Exx`); plus `data_*` animation/display-bytecode records
  (0xE2/0xE3/0xE7/0xDF lead bytes) and small-value/coordinate record blobs.
- Unknowns: the record/bytecode field semantics and pointer-table→string wiring are
  not decoded. The `0x801Axxxx` pointers are runtime overlay RAM (NOT a linear ROM
  back-map).

## CODE2 `0x87200..0x91000` (42 `func_*` + straddler-head)
Begins with a **parent-undetected** frameless cluster `0x87200..0x88024` — 7+
display-list builders (the many `j 0x8019xxxx` are relocated overlay tail-jumps,
NOT function boundaries; returns at `0x87398/0x87544/0x876dc/0x87900/…`). Then
parent-detected fns with several over-merge un-merges (parent files 1/2/6/8 hid
trailing frameless leaves) and preamble-orphan folds. All conservative `func_*`.

### Straddler-out (chunk 8 → chunk 9)
`func_00090e54_chunk8head` `[0x90E54,0x91000)` — true entry `0x90E54` (clean
`addiu $sp,-0x30` prologue), continues to `0x912F4`. Chunk-9 tail is
`func_00090e54_chunk9tail` `[0x91000,0x912F4)`.

## Method & verification
`dump_function_context`+`plan_chunk` (CODE1, CODE2) → `slice_chunk --disasm` → two
code analysis swarms (4+6 slices) + a data-classification swarm (2 sub-regions) →
combine → `check_boundaries` → 6-region adversarial swarm (**6/6 clean, 0 issues**)
→ `split_original_mips_part`. Deterministic invariants over all code parts: 0
fragments, 0 cross-boundary branches, 0 under-splits, 0 delay-slot leaks, 0 inline
data islands. Gates: `check_manifest` ALL PASS; `check_boundaries` PASS;
`assemble_original_mips` byte-exact; `verify_setup` PASS; `audit_code_region` OK.
No tool changes this chunk.

## Caveats / next frontier
- Code names are conservative overlay address labels (RAM/global identity suspect;
  the linear decode-comment RAM column is wrong for overlay code). Data record/
  bytecode field semantics not decoded.
- **Next frontier `0x00091000` (chunk 9).** FIRST continue the straddler:
  `func_00090e54_chunk8head` continues to `0x912F4` (chunk-9 tail file
  `func_00090e54_chunk9tail` `[0x91000,0x912F4)`). Chunk 9 should remain largely
  parent-detected — but content-scan for interior data regions first (chunks 5–8
  each had data).
