# Chunk 20 — MIXED: scenario data tables + encounter/dispatcher code (`0x00141000..0x00151000`)

Source-ownership pass for the twenty-first 64 KiB chunk. **MIXED, data-heavy at the
start**: a large leading DATA region (incoming packed-byte straddler + graphics/float
pools + two documented scenario tables + RAM-pointer tables + a 125-string game-text
pool) then a CODE region (encounter/dispatcher readers, continuing chunk 19) ending in an
outgoing FUNCTION straddler; one inline data island sits inside the code. **175 tracked
parts**: **89 code (incl. 1 outgoing straddler-head) + 86 data**. Byte-exact rebuild
preserved (code SHA `40D4E787…B409`, ROM SHA `571E8339…CC67A`). Coverage `0x1000..0x151000`
= 1,376,256 B = **48.3032 %** of the 2,849,204-byte evidenced executable extent; code-only
classified ≈ 1,148,920 B (**40.3242 %**).

Pipeline: content-scan region map → data-classification swarm (3 agents) + code-analysis
swarm (7 slice agents) → combine → `check_boundaries`/`check_splits` → 5-agent adversarial
swarm (3 code + 2 data). **1 disproof** (hidden code at `0x145210` mis-classified as data;
fixed). Added durable `tools/decode_ob64_tables.js`; decoded both named tables + the game
string pool.

## Region map (ROM-ordered)

| Region | Range (z64) | Bytes | Class | Parts |
|---|---|---:|---|---:|
| DATA (straddler + gfx/float + tables + ptr tables + strings) | `0x141000..0x145210` | 16,912 | data | 84 |
| CODE (encounter/dispatcher) | `0x145210..0x14DE88` | — | code | (incl. 2 recovered leaves) |
| DATA island (ptr tables + F3DEX + float64 pool) | `0x14DE88..0x14EBE0` | 3,416 | data | 1 |
| CODE (cont.) + outgoing straddler-head | `0x14EBE0..0x151000` | — | code | — |

(Code region [0x145210, 0x151000) = 89 parts total incl. the data island; the leading data
region is 84 parts + the island = 86 data parts.)

## Incoming DATA straddler (chunk 19 → chunk 20)
`data_00141000_chunk20tail` `[0x141000,0x1416A0)` — continuation of the chunk-19 packed-byte
structure (`data_00140EA0_chunk19head`; all bytes ≤`0x0D`). Ends `0x1416A0` at a
`0xDF000000` display-list transition (adversarially confirmed). `kind:"data"`.

## Leading DATA `0x141000..0x145210` (84 parts)
Packed-byte straddler → RSP/RDP display-list/graphics blocks → a float32 const pool
(`~0x141E14`) and the doc'd terrain-rate/slot const region → **two documented tables (see
below)** → large `0x801A/0x801B` RAM-pointer tables (≈5.5 KB) interleaved with IEEE
float/double const pools and **ASCII rodata debug/symbol strings** ("MakeRouteList",
"Magnetic", "spMakeTable", "vtx_cnt") → the **125-string game-text pool** `rodata_001432E4`
(see below) → a trailing pointer/string region ending exactly at the code start `0x145210`.

### neutral_encounter_table `table_00141ED0` `[0x141ED0,0x142200)` — 40 rows × 20 B (816 B)
HIGH confidence (docs/neutral-encounters.md + editor/parsers.js + table_map.json size 816;
byte-verified). 4-byte lead pad; rows at `+4..+804` (`ROM = 0x141ED0 + 4 + (row-1)*20`);
12 trailing bytes incl. a `0x4C504144` "LPAD" marker. Each row = 10 terrain-indexed 2-byte
slots; slot = `[u8 classA, u8 classB]` (A==B single / A≠B 50-50 / `00 00` empty). Decoded:
`docs/data-index/rev0/chunk20-neutral-encounter-table.json` +
`data/decoded/rev0/tables/chunk20-neutral-encounter-table.md`. Runtime RAM copy `0x801ED790`.

### creature_drop_table `table_00142258` `[0x142258,0x142378)` — 36 rows × 8 B (288 B)
HIGH confidence (docs/drop-table.md + editor/parsers.js + table_map.json size 288;
byte-verified: Hawkman class `0x27` → `80 31 80 9B 80 E8`). Record (8 B): `[0]`=u8 pad;
`[1]`=u8 class_id; `[2-3]/[4-5]/[6-7]`=u16BE drop slots (bit15=equipment flag, bits0-14=item
id into item_stat_table @ ROM `0x62310`); record 35 = all-zero sentinel (35 real records).
Decoded: `docs/data-index/rev0/chunk20-creature-drop-table.json` +
`data/decoded/rev0/tables/chunk20-creature-drop-table.md`. Runtime RAM copy `0x801EDB18`.
(Inter-table gap `[0x142200,0x142258)` = 88 B of small ASCII-ish values, not decoded.)

### Game-text string pool `rodata_001432E4` `[0x1432E4,0x1449F0)` — 5,900 B, 125 strings
Decoded via `tools/decode_rodata_strings.js` →
`docs/data-index/rev0/chunk20-string-pool-1432E4.json` +
`data/decoded/rev0/strings/chunk20-string-pool-1432E4.md` (round-trip byte-exact). Real
game UI/battle messages ("Legion led by the commander of…", "Winning condition: Entering
enemy headquarters.", "The legion has disbanded…petrification…"). `0x81xx` two-byte control
codes (newline/variable markers) preserved verbatim as `\x81\x97`/`\x81\x94` escapes; their
exact semantics are UNRESOLVED.

## CODE `0x145210..0x151000` (89 parts incl. the data island)
Overlay-relocated (runtime `0x801F4xxx` band, scenario state). 31 framed prologues + 14
preamble-orphans (read-before-write folds; each named `func_<startaddr>` with `label`=name)
+ 17 frameless leaves (incl. several tiny `jr $ra`-stub leaves the parent over-merged) +
plain code. Internal `jr $v0` jump-table dispatchers (`0x145334`/`0x1456B4`/`0x145A54`/
`0x1470B4`/`0x147544`/`0x149DEC`) and `j 0x801Fxxxx` overlay tail-jumps kept internal.
**Inline DATA island `data_0014DE88` `[0x14DE88,0x14EBE0)`** (3,416 B): nop padding +
`0x06xx` byte/jump tables + `0x801F` pointer tables + F3DEX display-list words + an IEEE-754
double float pool — 0 prologues/returns (adversarially confirmed non-executable). **Outgoing
straddler-head `func_00150550`** `[0x150550,0x151000)` — prologue `addiu $sp,-0x40`, NO
`jr $ra` in range, continues to `0x15105C` in chunk 21.

## Adversarial correction
The data-B hunter found **hidden code at `0x145210`** mis-classified as `data_00145210`: two
real frameless leaves — `func_00145210` (bounded `0x8020`-relative table-scan loop, `jr $ra`
@`0x145278`) and `func_00145280` (3-instr predicate `xori/jr/sltiu`). The data→code boundary
was moved `0x145290` → `0x145210`; the leaves were promoted to code and dataC trimmed.
code-A/B/C and data-A: 0 disproofs (incl. independent confirmation that the `0x14DE88` data
island and both named tables hold no code, and the straddler-head is correct).

## Parent-workspace evidence (sweep 2026-06-23)
Both named tables independently documented + shipping in the editor + snapshot-verified
(see above, HIGH confidence). Code is overlay-relocated to runtime `0x801F4xxx` (scenario);
the symbols_v2 `ram` (0x801B4xxx) column is the WRONG linear back-map — trust
`runtime_ram_primary`. Heuristic classifier tags only for the code (NO hand-authored names);
names stay conservative `func_*`. The chunk-19 linear-map fallacy does NOT recur here.

## Data inventory (machine-readable: `docs/data-index/rev0/chunk20-data-region-inventory.json`)
**Total data: 20,328 B in 86 parts.** By class: rodata-string 6,344 B · ram-pointer-table
5,492 B · display-list/graphics 3,976 B · incoming straddler-tail 1,696 B · neutral table
816 B · zero-fill 776 B · float-const-pool 664 B · creature-drop table 288 B · packed/record
276 B. **Parsed:** the two named tables (1,104 B, full field decode) + the 125-string pool
(5,900 B) + zero-fills + pointer tables. **Raw-but-classified:** display-list/graphics
blocks, packed records, float pools (type clear, fields not all decoded). **Undecoded:** the
88-byte inter-table gap and the 0x81xx string control-code semantics.
Index files added: `chunk20-data-region-inventory.json`, `chunk20-neutral-encounter-table.json`,
`chunk20-creature-drop-table.json`, `chunk20-string-pool-1432E4.json`. Human-readable exports:
`data/decoded/rev0/tables/chunk20-{neutral-encounter,creature-drop}-table.md`,
`data/decoded/rev0/strings/chunk20-string-pool-1432E4.md`.

## Method & verification
Region map → 3 data agents + 7 code-slice agents (Workflow) → combine (data names →
`<kind>_<addr>`; neutral-table boundary aligned to documented 816 B; kinds normalized) →
`check_boundaries` PASS (175 parts; 0 fragment/cross/under/leak/straddler/data-island) +
`check_splits` 0 true fragments → 5-agent adversarial swarm (1 hidden-code fix) → re-gate →
`split_original_mips_part --remove-source`. Gates: `check_manifest` ALL PASS (20 chunks /
2,701 parts); `assemble_original_mips` byte-exact (`40D4E787…B409`); `verify_setup` green;
`audit_code_region` OK; all data-index JSON valid; 0 data files with function wording.

## Caveats / next frontier
- Conservative code names (overlay-relocated; runtime `0x801F4xxx`; parent classifier tags
  not adopted). Pointer-table wiring, display-list format, and the 0x81xx string control-code
  semantics are NOT decoded (follow-up). The 88-byte inter-table gap `[0x142200,0x142258)` is
  raw-but-classified.
- **Next frontier `0x00151000` (chunk 21).** FIRST continue the outgoing FUNCTION straddler:
  `func_00150550` continues to `0x15105C` (chunk-21 tail file `func_00150550_chunk21tail`
  `[0x151000,0x15105C)`). Chunk 21 is MIXED (code + a trailing high-entropy data region with
  an outgoing data straddler into chunk 22).
