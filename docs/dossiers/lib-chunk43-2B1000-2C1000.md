# lib chunk 43 — `0x002B1000..0x002C1000`

Classification: **MIXED — source-owned as code/data parts** (incoming function straddler-tail →
one contiguous CODE region → the evidenced code→data transition at `0x002B89B8` → a non-code
DATA tail). This is the chunk that crosses the audited executable-MIPS extent end `0x002B89B4`.
It is **not** "fully split into functions": it is code parts up to `0x002B89B8` and honest data
parts after.

- Split JSON: `build/chunk_002B1000-002C1000_splits.json`
- Data index: `docs/data-index/rev0/chunk43-data-region-inventory.json`
- Promoted source: `asm/original/rev0/lib/` (89 parts)

## Composition

**89 parts = 80 normal code + 1 incoming straddler-tail + 8 data.**

ROM-ordered region map:

- Incoming FUNCTION straddler-tail `func_002B0E8C_chunk43tail` `0x002B1000..0x002B1014`
  (continues `func_002B0E8C` from chunk 42, prologue `addiu $sp,-0x140`@`0x2B0E94`; epilogue
  `lw $s2/$s1/$s0` restores, returns `jr $ra`@`0x002B100C` + delay `addiu $sp,0x140`@`0x002B1010`).
- **CODE region** `0x002B1014..0x002B89B8` — the mission-briefing / scenario-overview overlay
  (runtime RAM ~`0x8023Cxxx`/`0x8024xxxx`; see parent sweep). 80 functions: parent-DB framed
  functions + recovered frameless leaves + forward-folded read-before-write preambles. Heavy
  FP/double geometry + F3DEX2 display-list builders + DMA/resource loaders. Accesses the
  `0x800F0000` graphics-global table and `0x8022A974`/`0x80230xxx` overlay state. The LAST function
  `func_002B88C8` `0x002B88C8..0x002B89B8` ends `jr $ra`@`0x002B89B0` + delay `addiu $sp,0x40`@`0x002B89B4`.
- **CODE→DATA TRANSITION at `0x002B89B8`** (evidence below).
- **DATA tail** `0x002B89B8..0x002C1000` (34,376 B, 8 parts) — the overlay's graphics payload:
  F3DEX2 display lists + IEEE float/double constant pools + an embedded `N64 PtrTablesV2` signature
  + a trailing high-entropy texture-tile region. No outgoing function straddler (chunk ends in data;
  the data continues into chunk 44).

## Code/data boundary at `0x002B89B8` (evidence)

- The last executable function `func_002B88C8` returns `jr $ra`@`0x002B89B0`; its delay slot
  `addiu $sp,0x40`@`0x002B89B4` is the last executable word, so code ends EXACTLY at `0x002B89B8`.
  `audit_code_region.js` reports the executable extent ending at `0x002B89B4` (its last detected
  `jr $ra`); the +delay-slot end is `0x002B89B8`.
- `0x002B89B8`/`0x002B89BC` are `0x00000000` (alignment); `0x002B89C0` begins an F3DEX2 display
  list (`E7 00 00 00` G_RDPHALF_2/pipesync → `E2` G_SETOTHERMODE_L → `FC` G_SETCOMBINE → `D9`
  G_GEOMETRYMODE → `DF` G_ENDDL), then a π/90.0/60.0f constant pool.
- `[0x002B89B8,0x002C1000)` contains **0 valid parent functions, 0 `jr $ra` epilogues, 0
  `addiu $sp,-N` prologues** — no missed code in the tail. Adversarial boundary skeptic confirmed
  (see Verification). Per repo policy the global non-code tail past `0x002B89B4` is NOT reclassified
  here; only this chunk's bytes are owned exactly.

## Data tail inventory (8 parts, 34,376 bytes)

Full per-part index: `docs/data-index/rev0/chunk43-data-region-inventory.json`. classes:
**zero_fill 172 B (parsed) · data 34,204 B (raw-but-classified)**. No `0x80xxxxxx` pointer words
anywhere → no `table_`/`jumptable_` parts.

- `zero_fill_002B89B8` (8 B) — 2 nops at the code→data boundary.
- `data_002B89C0` (832 B) — display-list fragment + float/double pools (π `0x400921FB54442D18`,
  90.0, 60.0f, 0.5) + embedded ASCII `N64 PtrTablesV2\0`@`0x2B8BA0` + a small offset/size word table.
- `zero_fill_002B8D00` (32 B); `data_002B8D20` (2,960 B, display-list + graphics/constants);
  `zero_fill_002B98B0` (96 B); `data_002B9910` (22,500 B, the bulk F3DEX2 display-list/vertex blob,
  GBI ~33-40%); `zero_fill_002BF0F4` (36 B); `data_002BF118` (7,912 B, trailing high-entropy
  texture-tile pixel data, GBI density coincidental, continues into chunk 44).

All data byte-owned and classified; 0 bytes undecoded-and-unclassified (raw-but-classified packed
display-list/texture blobs are owned and classified, not field-decoded).

## Role leads (conservative; names stay `func_*`)

Parent overlay map (`scripts/ob64_overlay_map.json`) shows all 62 functions loading TOGETHER to
runtime RAM ~`0x8023Cxxx`/`0x8024xxxx` in the `mission_briefing → scenario_overview` snapshots
(08/09) and essentially absent from in-scenario gameplay — i.e. this is the pre-mission
briefing/scenario-overview screen module. The parent function DB assigns **no symbolic names** to
this range (purely structural), so names stay `func_*`. The data tail is the briefing screen's
display-list/graphics/texture assets.

## Parent workspace evidence sweep (`0x2B1000..0x2D1000`)

Searched parent `C:\Users\Joe\Projects\OgreBattlel64` (z64 + RAM forms). Validated:

- `scripts/ob64_functions.json`: 62 valid functions `0x2B1030..0x2B89B4`, then a ~3 MB gap to the
  next DB entry — the DB itself stops at the boundary (structural/extent lead; no names).
- `scripts/ob64_overlay_map.json`: the definitive runtime-RAM evidence — RAM ~`0x8023Cxxx` in
  mission-briefing snapshots (NOT the linear `0x80320Cxx` decode-comment map).
- `scripts/ob64_xrefs.json`: in-range code accesses the `0x800F0000` graphics-global table + GBI
  immediates (display-list builders).

Rejected / corrected:

- `scripts/ob64_4a_audit.json` "sprite blocks" `block_2943_0x2b13ee..block_2961_0x2b860e`
  (RGBA32/CI4 etc.) sit INSIDE byte-exact MIPS code (`0x2B1030..0x2B89B4`, 62 valid functions /
  `jr $ra`-dense) — scanner false positives, same family rejected for chunks 40-42 (the old
  `0x28123E`/`0x290D04` offsets recur in the same list). First plausibly-real graphics block is
  `block_2962_0x2b8a14`, just past the boundary.
- Linear-boot RAM form `0x80320C00..` for this range: rejected (overlay-relocated; 0 xref hits there).
- `scripts/ob64_nuclear_results.txt` `0x8023Cxxx` hits: rejected (runtime stat-byte slot reuse, not ROM code).
- `func_00594A9C..0x598A9C` parent DB false positive: outside range; confirms no fn start ≥`0x2B89B8` in range.

Overlay caveat (RSR-001): the combat overlay sources in `ram_snapshots/overlay_sources.json` END
before `0x281000`, so chunk 43 is NOT covered by documented combat overlay sources;
mission-briefing overlay runtime mapping (~`0x8023Cxxx`) is from `ob64_overlay_map.json` snapshots,
not a linear back-map.

## Mistakes corrected / hazards handled

- 3 slice-seam read-before-write preamble fragments (`func_002B1F78`, `func_002B529C`,
  `func_002B616C` — each `lui 0x8023 / lw -0x568C` after a `jr$ra`+delay) folded FORWARD into the
  next slice's function (2 had been mislabeled `straddler-tail`).
- 1 intra-slice preamble fragment `func_002B1028` (the `0x2B1028` preamble before `func_002B1030`)
  merged forward — `check_boundaries` caught it as a 2-word fragment.
- Label normalization: 5 redundant parent-boundary header labels removed (avoid duplicate glabel);
  31 frameless-leaf/preamble labels set to part-start name; matches `integrate_chunk` convention.
- All 4 parent-DB code-region gaps are FRAMELESS CODE leaves, not data: `func_002B6F40` (FP-distance
  leaf, `jr$ra`@`0x2B6FDC`), `func_002B730C`, `func_002B7B40`, `func_002B82E4` (+3 alignment nops
  @`0x2B6F34` attach to `func_002B6EDC`). No interior data islands in the code region.

## Verification

`check_boundaries` PASS (0 fragment/cross/under/leak/straddler/data-island); `check_splits`
0 fragments / 0 tiny; `check_manifest` 89 parts; `assemble_original_mips` byte-exact (code SHA
unchanged); `audit_code_region` executable extent unchanged (no code edge into the tail).
Adversarial swarm (6 skeptics: boundary, straddler+head, data-tail, code-s5, code-s7, code-s2s3):
see review handoff. Runtime states: none used. Patch-workbench: none encountered (FP/display-list
code + display-list/texture data; no hook sites).

## Next-run first action (chunk 44, `0x002C1000`)

Chunk 43 ends in DATA (no outgoing function straddler). The trailing `data_002BF118` high-entropy
texture region continues across `0x002C1000` into chunk 44 — chunk 44's first action is to own that
data continuation. Chunk 44 `[0x002C1000,0x002D1000)` is past the executable extent: expect pure
data territory (verified: 0 `jr $ra`, 0 pointers, uniform high-entropy graphics/texture data).
