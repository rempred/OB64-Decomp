# Rev 0 Lib Chunk 33 Source Ownership

Range: `0x00211000..0x00221000`

Status: source-owned as tracked original MIPS. Chunk 33 is **MIXED** (incoming
function straddler-tail → code → interior font/glyph + table DATA region → code →
outgoing function straddler-head): a battle/attack-related subsystem with a large
jump-table state machine at the tail.

Part count: **109 total** = 82 normal code + 25 data + 2 function straddlers.

## Region Map

| Range | Owner class | Files | Evidence |
| --- | --- | ---: | --- |
| `0x00211000..0x00211028` | incoming function straddler tail | 1 | Tail of `func_00210C30` from chunk 32; returns `jr $ra`@`0x00211020`, delay `addiu $sp,0xD8`@`0x00211024`. |
| `0x00211028..0x00211D14` | code (R1) | 9 | Frameless `func_00211028` + framed functions; ends `jr $ra`@`0x00211D0C`+delay@`0x00211D10`. |
| `0x00211D14..0x00213B10` | interior DATA territory | 25 | Font/glyph asset block + 0x801A/0x801B/0x801C pointer-jump tables + float64/float32 pools. 0 prologues / 0 returns (verified). |
| `0x00213B10..0x0021EBBC` | code (R2) | 72 | FP/display-list + battle code (frameless accessor `func_00213B10`; dispatcher-heavy; includes the High-Attack hook functions). |
| `0x0021EBBC..0x00221000` | outgoing function straddler head | 1 | `func_0021EBBC`: jump-table state machine, prologue `addiu $sp,-0x2E8`@`0x0021EBBC`; no `jr $ra` before chunk end; continues into chunk 34 (returns `jr $ra`@`0x002213D4`, frame size 0x2E8 matches). |

## Code recoveries / boundary corrections

- Slice-seam **preamble-orphan merges** (the warned hazard — agents had marked
  these as false straddler-heads or left them as dead tails): `func_0021181C`
  (A2/A3 seam; caught by adversarial), `func_00217BA8`, `func_00219A14`,
  `func_0021CBC4` (B-region seams). Each is a genuine read-before-write
  `lui 0x801D / lw -0x174x` preamble merged forward into the following function.
- Over-merge un-splits (e.g. frameless leaf `func_002183D8`) + recovered frameless
  leaves (`func_00213B24`, `func_0021B3E8`); many read-before-write preamble folds.
- jr$v0 jump-table dispatchers (`0x213C00`/`0x215338`/`0x2194AC`/…) and
  `j 0x801Dxxxx` overlay tail-jumps kept INTERNAL.
- The big functions `func_00215CF0` and `func_00216A38` verified single.
- The tail `0x21EBBC..0x221000` (~21 KB, return-less) is **one** jump-table state
  machine `func_0021EBBC` whose case handlers exit via shared `j 0x801DDxxx`
  overlay tail-jumps; it is the outgoing straddler.

The split tool emits no duplicate label for parts whose body already carries the
parent `function boundary candidate` glabel (1 such label dropped).

## Data Inventory

Machine-readable index: `docs/data-index/rev0/chunk33-data-region-inventory.json`.

- Data bytes source-owned: **7,676** (25 files).
- Parsed bytes: **876** (zero-fill, rodata strings, float pools).
- Raw-but-classified bytes: **6,800** (glyph remap map, 16-bit LUT, packed
  graphics blob, the three 0x801x pointer tables).
- Undecoded bytes: **0**.

Region contents (ROM order, `0x211D14..0x213B10`):

- `zero_fill_00211D14` (3 align nops) → `rodata_00211D20` ("%s has changed class.",
  "Supportive Attack" + nul pad) → `float_00211D5C` (float32 consts) →
  `data_00211D7C` (packed parameter/record block).
- glyph/charset codepoint **remap** map (`~0x2122A4`) + per-glyph width table.
- dense **16-bit remap LUT** `table_002123A0` (1456 u16, band 0x0000..0x02CE).
- packed/compressed **graphics blob** (`~0x212F00..0x213120`).
- **0x801A** pointer table (`0x213180`, ~44 ptrs) + pi float64 + 0x801AF2 ptr run.
- **0x801B** pointer table (`0x213290..0x213740`) interleaved with float64/float32
  consts (1.1, 0.5, 0.01, 180.0).
- float64 record pools + int consts (1000/-1000) + **0x801C** pointer table
  (`0x213840..0x213A40`) + float64 record/const pools (tail: pi, 360.0, 1.3,
  320.0, 2.0, 1.5).

Negative data evidence: two adversarial data verifiers scanned every word and
found **0 stack prologues / 0 `jr $ra`** across `0x211D14..0x213B10`; both
code/data seams (`0x211D14`, `0x213B10`) are byte-exact; all 25 parts are
4-aligned (the data-classification pass initially split a string/pad at a
non-aligned offset — corrected by merging into one 4-aligned rodata part).

## Patch Workbench (harvested in-range)

Two High-Attack streamsplit hook sites (from the parent
`tools/build_high_attack_stream_shift_rom.py` / `docs/combat-attack-buffer.md` /
`scripts/ob64_patch_hook_audit.js`), recorded static-only in
`docs/patch-workbench/rev0/patch-workbench-chunks32-33-2026-06-23.json`:

- **Primary insert hook** z64 `0x0021CD48` in `func_0021CBC4` — original words
  `lui $v0,0x801D` / `lw $v0,-0x1740($v0)` (global fetch after a x3
  scale-accumulate into `$s3`). Status **candidate / needs-runtime**.
- **Slot0 zero-return/source hook** z64 `0x0021BF84` in `func_0021B894` — original
  word `beq $s3,$zero` (+ delay `move $v0,$s3`); a PC-relative branch guard.
  Status **candidate / needs-runtime**.

Rev 1.1 near-equivalents `0x21CD18` / `0x21BF54` were checked against byte-exact
Rev 0 disassembly and **rejected** for Rev 0 (different instructions). Nothing
upgraded to `proven`.

## Parent Workspace Evidence

- `tools/build_high_attack_stream_shift_rom.py`, `docs/combat-attack-buffer.md`,
  `scripts/ob64_patch_hook_audit.js` → the two hook sites above (reconciled to
  Rev 0 byte-exact addresses 0x21CD48 / 0x21BF84).
- `scripts/ob64_symbols_v2.json` / `ob64_functions.json` → role-tag leads only;
  names stay `func_*`. The parent DB over-merged/missed many functions; corrected
  from disassembly.

Rejected: `0x8025/0x8026/0x8028/0x8029` linear-map values; Rev 1.1 hook offsets.

## Verification

```powershell
node tools/check_manifest.js                       # ALL CHECKS PASS (chunk 33 = 109 parts)
node tools/check_boundaries.js --splits build/chunk_00211000-00221000_splits.json --disasm build/original-mips/rev0/code_00211000_00221000.s   # BOUNDARY CHECK PASS
node tools/check_splits.js     --splits build/chunk_00211000-00221000_splits.json --disasm build/original-mips/rev0/code_00211000_00221000.s   # 0 fragments
node tools/assemble_original_mips.js               # Exact code-region match: PASS
node tools/verify_setup.js                         # PASS
```

Code-region SHA256 `40D4E7875BA50F005788611C63CF9C42D9154339B36793556BF045C25B64B409`
(unchanged); full ROM SHA256
`571E83396BC81E70DA4C0A20313D82DBD7DFE685F2C37418C8E27F927E2CC67A`.

Adversarial pass: 4 code + 2 data + 1 straddler skeptics. 1 high (the
`func_0021181C` A2/A3 seam preamble) fixed; LOW data-note corrections applied
(glyph remap description, LUT u16 count, float-pool contents); all data verifiers
confirmed 0 code in the data territory and the straddler has no `jr $ra`.

## Next Action

Next frontier is `0x00221000` (chunk 34). Continue the outgoing function
straddler from chunk 33: `func_0021EBBC` begins in chunk 33 at `0x0021EBBC`
(prologue `addiu $sp,-0x2E8`, no preamble), has no `jr $ra` before the boundary,
and must be emitted first in chunk 34 as `func_0021EBBC_chunk34tail` starting at
`0x00221000` (returns `jr $ra`@`0x002213D4`).
