# lib chunk 44 — `0x002C1000..0x002D1000`

Classification: **DATA TERRITORY — source-owned as data parts** (no code). The entire 64 KiB chunk
is non-code data, lying PAST the evidenced executable-MIPS extent end `0x002B89B4`. It continues the
chunk-43 data tail (the `data_002BF118` high-entropy texture region) across the `0x2C1000` boundary.
This chunk is **not** code: it is owned as honest data parts.

- Split JSON: `build/chunk_002C1000-002D1000_splits.json`
- Data index: `docs/data-index/rev0/chunk44-data-region-inventory.json`
- Promoted source: `asm/original/rev0/lib/` (17 parts)

## Composition

**17 parts = 9 data + 8 zero_fill (0 code).** No incoming function straddler (chunk 43 ended in
data; this is a data continuation). No code anywhere.

ROM-ordered region map: one contiguous high-entropy graphics/texture asset region, segmented by 8
small (16–28 B) zero-fill runs that separate texture/graphics objects:

- `data_002C1000` `0x2C1000..0x2C3364` (9,060 B) — **continuation of chunk-43 `data_002BF118`** texture tail.
- `zero_fill_002C3364` (20 B) · `data_002C3378` (1,812 B) · `zero_fill_002C3A8C` (28 B) ·
  `data_002C3AA8` (1,920 B) · `zero_fill_002C4228` (16 B) · `data_002C4238` (1,532 B) ·
  `zero_fill_002C4834` (20 B) · `data_002C4848` (1,616 B) · `zero_fill_002C4E98` (16 B) ·
  `data_002C4EA8` (6,432 B) · `zero_fill_002C67C8` (16 B) · `data_002C67D8` (6,280 B) ·
  `zero_fill_002C8060` (24 B) · `data_002C8078` (14,792 B) · `zero_fill_002CBA40` (24 B) ·
  `data_002CBA58` `0x2CBA58..0x2D1000` (21,928 B) — runs to the chunk end.

## Data accounting (Data Territory Mode)

- Total: **65,536 B** — `data` 65,372 B (raw-but-classified) · `zero_fill` 164 B (parsed, all-zero).
- **parsed**: the 8 zero_fill spans (all-zero alignment between texture objects). **raw-but-classified**:
  the 9 data spans (high-entropy graphics/texture/asset pixel data; not field-decoded; possibly
  compressed). **undecoded-and-unclassified: 0 bytes.**
- Every byte is byte-owned and classified. No byte remains fallback-only.
- **Hypothesis (not decoded):** several `data_` blocks begin with a small `0x00xxxxxx` word
  (e.g. `data_002C1000` `0x00431DCD`-region start, others `0x0063A410`/`0x0033EAFB`/`0x0041FCEE`/
  `0x0060004F`) which is plausibly a length/size prefix of a compressed block. Block entropy is
  ~6.7–7.2 bits/byte. This is a hypothesis only — the blocks are owned as `raw-but-classified`
  high-entropy data, not decoded; do not treat the leading word as a proven field.

## Proof of non-code (why this is honestly data, not forced data)

- **0 `jr $ra`** (`0x03E00008`) across all 16,384 words — real MIPS cannot span 64 KiB with no return.
- **0 stack prologues** (`0x27BD8xxx..0x27BDFxxx`) anywhere — no function entry exists, so no
  prologue+return pair can exist.
- **0 `0x80xxxxxx` RAM-pointer words** — no pointer/jump tables (so no `table_`/`jumptable_` parts).
- **No real ASCII strings** — 1,880 coincidental printable bytes, none forming words/sentences.
- **No archive/compression magic** at `0x2C1000` (no LZSS/MIO0/Yaz0 header); it is a raw continuation
  of the chunk-43 texture tail.
- `audit_code_region.js` classifies `0x2B89B4..0x0063676C` as the data-evidenced non-code tail;
  chunk 44 is wholly inside it. This run owns ONLY chunk 44's bytes — the global tail is NOT reclassified.

## Parent workspace evidence sweep

Covered by the run-wide `0x2B1000..0x2D1000` sweep (see chunk-43 dossier / review). The parent
`scripts/ob64_4a_audit.json` flags RGBA32/CI texture-tile blocks beginning just past `0x2B89B4`
(e.g. `block_2962_0x2b8a14`), consistent with chunk 44 being briefing-screen texture/graphics
assets. No combat overlay source covers this range (overlay sources end before `0x281000`, RSR-001).
No symbolic names, no hook sites. The parent DB has 0 functions in this range.

## Mistakes corrected / hazards handled

- None structural — the chunk is uniformly non-code. Segmented at the 8 genuine zero-fill runs
  (16–28 B) rather than forcing one 64 KiB blob, for discoverability; the split points are real
  zero spans, not invented boundaries. No data was forced into `func_*` files.

## Verification

`check_boundaries` PASS (17 parts, 0 code → 0 fragment/cross/under/leak/straddler, 0 data-island
warnings); `check_manifest` 17 parts; `assemble_original_mips` byte-exact (code SHA unchanged);
`audit_code_region` executable extent unchanged. Adversarial swarm (4 skeptics: 3 region hidden-code
+ structure scans, 1 tiling/classification check): see review handoff. Runtime states: none used.
Patch-workbench: none (pure texture/asset data; no hook sites; static-only tail is not proven patch
space).

## Next-run first action (chunk 45, `0x002D1000`)

Chunk 44 ends in DATA. The `data_002CBA58` high-entropy texture region continues across `0x2D1000`
into chunk 45. Chunk 45 `[0x002D1000,0x002E1000)` is deeper in the non-code tail — expect more data
territory (texture/graphics/asset data). The global non-code tail `0x002B89B4..0x0063676C` remains a
separate, larger reclassification design (do not force it in a single chunk run).
