# Chunk 27 — CODE-DOMINANT MIXED: FP-heavy class/char/encounter/resource code + 2 data islands (`0x001B1000..0x001C1000`)

Source-ownership pass for the twenty-eighth 64 KiB chunk. **CODE-DOMINANT MIXED** (even more
code-dense than chunk 26): an incoming FUNCTION straddler-tail, FP-heavy class/char/encounter/
resource code, two notable inline DATA islands + two small zero-fill islands, and an outgoing
FUNCTION straddler-head. **142 tracked parts**: **128 code + 14 data**. Byte-exact rebuild preserved
(code SHA `40D4E787…B409`, ROM SHA `571E8339…CC67A`). Coverage `0x1000..0x1C1000` = 1,835,008 B =
**64.4042 %** of the 2,849,204-byte evidenced executable extent; code-only classified = 1,514,224 B
(**53.1455 %**).

Pipeline: parent-evidence sweep + byte-exact proret scan → 13 code-slice + 1 data analysis agents
(Workflow) → reconcile (kind-normalize + 3 cross-slice preamble folds + incoming-tail rename) →
`check_boundaries`/`check_splits` PASS → 7-agent adversarial swarm. **Adversarial all CLEAN — 0
boundary moves** (only LOW naming/note nits accepted).

## Region map (ROM-ordered)
| Region | Range (z64) | Class | Notable |
|---|---|---|---|
| Incoming straddler-tail | `0x1B1000..0x1B1070` | code | `func_001B0F78_chunk27tail` |
| CODE 1 | `0x1B1070..0x1B226C` | code | preamble-orphans `func_001B1070`/`func_001B1518` |
| D5 string/table island | `0x1B226C..0x1B2670` | data | status/menu strings + tables + double pools |
| CODE 2 | `0x1B2670..0x1B9920` | code | `func_001B6538` (3.7KB dispatcher); resource loader `func_001B902C` |
| display-list island | `0x1B9920..0x1BA050` | data | DL/float-ramp/color-LUT |
| CODE 3 | `0x1BA050..0x1C0FC8` | code | `func_001BC020` (4.4KB), `func_001BDF2C` (4KB), loaders `func_001BDD70`/`func_001BDE70` |
| Outgoing straddler-head | `0x1C0FC8..0x1C1000` | code | `func_001C0FC8` |

Most scanner-flagged "float runs" are **FP CODE** — this chunk is FP-heavy. The genuine data is the
two islands + two small zero-fill pads.

## Incoming FUNCTION straddler (chunk 26 → chunk 27)
`func_001B0F78_chunk27tail` `[0x1B1000,0x1B1070)` — tail of `func_001B0F78` whose entry is a
read-before-write preamble (`lui/lbu $v0`) @`0x1B0F78` + `addiu $sp,-0x18` prologue @`0x1B0F80` in
chunk 26. Ends `jr$ra`@`0x1B1068` + delay `addiu $sp,0x18`@`0x1B106C`. `kind:"straddler-tail"`.

## Data islands (14 parts, 2,888 B — `docs/data-index/rev0/chunk27-data-region-inventory.json`)
- **D5** `[0x1B226C,0x1B2670)` (1,028 B, 10 parts): `rodata_001B226C` status/menu string pool
  (18 strings: "Empty", "Not Applicable", "Target:", "Cannot be used", "HP recovery",
  "Petrification recovery", "Resurrection", "Level up", STR/VIT/INT/MEN/AGI/DEX up, "ALI rise",
  "ALI fall", "Luck change", with inline `\x0e`/`\x10`/`\x0f` markers) + byte-triplet/param tables
  + IEEE-754 double pools (pi/180, 2*pi, 175.0) + a 0x80218/0x80219-band RAM-pointer table.
  Adversarially confirmed 0 prologues/0 `jr$ra`.
- **Display-list island** `[0x1B9920,0x1BA050)` (1,840 B, 3 parts; sits after `func_001B924C`):
  `data_001B9920` RDP/RSP display-list + texture/palette command rows + `float_001B9E40` (16
  consecutive monotonic IEEE-754 floats 0.08→0.60) + `data_001B9E80` byte/color LUTs
  (`0xFFFFFFFF`/`0x646400FF`/`0x787800FF`) + 0x8021-band pointer tables. Adversary scanned all
  462 words: 0 prologues/0 `jr$ra`.
- **Zero-fill islands**: `zero_fill_001B1504` (3 nops) + `zero_fill_001BBFA8` (2 nops).

Decoded ASCII export: `data/decoded/rev0/strings/chunk27-status-menu-1B226C.md` (+ JSON index).
**parsed** (ASCII, float/double pools, pointer tables) vs **raw-but-classified** (display-list
command rows, byte/param/color tables — field semantics undecoded).

## Notable code (names stay conservative `func_*`)
- **`func_001B6538`** `[0x1B6538,0x1B7414)` — 3.7KB dispatcher (preamble-orphan; internal `jr $v0`
  jump-table @`0x1B657C` + `j 0x80219xxx` tail-jumps, all internal); one `jr$ra`@`0x1B740C`.
- **`func_001B924C`** — RDP/RSP display-list builder (one prologue + one `jr$ra`@`0x1B9918`,
  before the display-list island).
- **`func_001B902C`/`func_001BDD70`/`func_001BDE70`** — parent leads = dma/resource loaders.
- **`func_001BC020`** (~4.4KB, fold), **`func_001BDF2C`** (~4KB) — adversarially confirmed single.
- **`func_001B3ED8`** — cross-slice fold: a 13-word arg/const read-before-write setup (loads
  `$a1-$a3` float constants + globals before `addiu $sp,-0x170`@`0x1B3F0C`).

## Outgoing FUNCTION straddler (chunk 27 → chunk 28)
`func_001C0FC8` `[0x1C0FC8,0x1C1000)` — prologue `addiu $sp,-0x18`@`0x1C0FC8` (the prior function
returns `jr$ra`@`0x1C0FC0`; **no** entry preamble, unlike chunk 26's outgoing head). **NO `jr$ra`
in `[0x1C0FC8,0x1C1000)`**; continues into chunk 28. **Next frontier `0x001C1000`**; chunk 28's
first action is `func_001C0FC8_chunk28tail`. `kind:"straddler-head"`.

## Adversarial pass (7 verifiers: 5 code + 2 data) — all CLEAN
0 boundary moves. `func_001B924C` confirmed single; both data islands confirmed 0 prologues/0
`jr$ra`; all 3 cross-slice folds (`func_001B3ED8`/`func_001BA37C`/`func_001BC020`) confirmed genuine
read-before-write; outgoing straddler confirmed 0 `jr$ra`. LOW (accepted, not boundary changes):
preamble-orphan parts named by the body-prologue address (e.g. `func_001B2DCC` at part start
`0x1B2DC4`) — documented intentional convention; a descriptive imprecision in `data_001B9E80`.

## Method & verification
slice_extract (13 code slices, D5 carved) → 13 code + 1 data agents (Workflow) → reconcile (3 folds
+ incoming-tail rename `func_001B0F80`→`func_001B0F78`) → `check_boundaries` PASS (142 parts; 0 of
each) + `check_splits` 0 fragments → 7-agent adversarial swarm (0 boundary moves) →
`split_original_mips_part --remove-source`. Gates: `check_manifest` ALL PASS (28 chunks);
`assemble_original_mips` byte-exact (code `40D4E787…B409`); 0 data files with function/true-entry/
boundary wording; data-index JSON valid.

## Caveats / next frontier
- Conservative code names (overlay-relocated; true runtime band `0x801Bxxxx-0x801Cxxxx`, NOT the
  decode-column `0x8022/0x8023`). Resource-loader identities are parent leads; names stay `func_*`.
- Undecoded data: the display-list command-stream layout, byte/param/color table fields, and the
  overlay-relocated pointer-table targets.
- **Next frontier `0x001C1000` (chunk 28).** FIRST continue the outgoing **FUNCTION** straddler:
  `func_001C0FC8` continues into chunk 28 — emit `func_001C0FC8_chunk28tail` first and confirm its
  `jr$ra` (no entry preamble; the tail begins at `0x1C1000`).
