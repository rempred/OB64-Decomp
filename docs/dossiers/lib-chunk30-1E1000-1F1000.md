# Rev 0 Lib Chunk 30 Source Ownership

Range: `0x001E1000..0x001F1000`

Status: source-owned as tracked original MIPS. Chunk 30 is **MIXED** (code →
interior data → code, with function straddlers at both ends): FP/RDP
display-list-heavy world-map / char-data / resource code, one large interior
**Sound-Test ("Ogre Battle 64 BGM Selection") + staff-credits** data territory,
then more code.

Part count: **122 total** = 89 normal code + 2 function straddlers + 31 data.

## Region Map

| Range | Owner class | Files | Evidence |
| --- | --- | ---: | --- |
| `0x001E1000..0x001E120C` | incoming function straddler tail | 1 | Tail of `func_001E0FC8` from chunk 29; returns `jr $ra`@`0x001E1204`, delay slot `0x001E1208`. |
| `0x001E120C..0x001EE574` | code (R1) | 87 | FP/RDP display-list builders + world-map state machines + char-data consumers + resource loaders. Recovered frameless leaves and preamble-orphan folds (below). |
| `0x001EE574..0x001F0A30` | interior DATA territory | 31 | Sound-Test/BGM screen graphics + scene-name pool + pointer tables + record table, then alphabet + format strings + staff-credits roll + handler/float tables. 0 prologues / 0 `jr $ra` (proven). |
| `0x001F0A30..0x001F0F90` | code (R2) | 12 | Display-init helpers (`func_001F0A30`+, call `0x801C5938`) + attack/queue push helpers; 2 tiny frameless leaves at `0x001F0F5C`/`0x001F0F6C`. |
| `0x001F0F90..0x001F1000` | outgoing function straddler head | 1 | `func_001F0F9C`: read-before-write preamble at `0x001F0F90` (lui $v1,0x801D / lw -0x1744 / lw 0x56C0) precedes parent prologue `0x001F0F9C`; no `jr $ra` before chunk end; continues into chunk 31 (returns at `0x001F1024`). |

## Code recoveries / boundary corrections

Recovered parent-undetected frameless leaves and folds (all confirmed from
byte-exact disassembly; many caught by the analysis swarm, two by the
adversarial pass):

- `func_001E175C` — frameless RDP display-list leaf (reads `$a0/$a1/$a2`).
- `func_001E9D84` — frameless float/int comparator leaf (`c.s`/`slt`, ptr-diff
  index), `jr $ra`@`0x001E9DF4`.
- `func_001EB4D8` — frameless function (read-before-write `0x801C` global,
  `div`/`break` guards), `jr $ra`@`0x001EB600`.
- **Adversarial split #1:** former `func_001EA134` held FOUR functions — split
  into `func_001EA134` (preamble-orphan wrapper, calls `resource_free`),
  `func_001EA158`, `func_001EA1FC`, `func_001EA2F4` (frameless leaves).
- **Adversarial split #2:** former `func_001EAE50` held TWO functions — split
  into `func_001EAE50` (preamble-orphan) + `func_001EB030` (frameless RDP
  display-list builder).
- `func_001ED888` — frameless leaf (reads `$a1/$a0`), `jr $ra`@`0x001ED8CC`.
- `func_001F0F5C` / `func_001F0F6C` — tiny frameless attack/queue helpers in R2.
- Numerous preamble-orphan folds (2-word read-before-write `lui 0x801C` + `lw`
  loads consumed by the following framed prologue): `func_001E3104`,
  `func_001E3440`, `func_001E361C`, `func_001E4C74`, `func_001E809C`,
  `func_001EA134`, `func_001EAE50`, `func_001EB608`, `func_001EBD5C`,
  `func_001EC124`/`2E8`/`480`/`618`/`7B0`/`948`/`func_001ECC28`, `func_001ED348`,
  and the outgoing straddler `func_001F0F9C`.

The split tool emits no duplicate label for parts whose disassembly body already
carries the parent `function boundary candidate` glabel (header label dropped
when it equals a body glabel — matches `integrate_chunk` behaviour).

## Data Inventory

Machine-readable index:
`docs/data-index/rev0/chunk30-data-region-inventory.json`
(includes the full decoded 46-string scene-name pool and 126-string credits
pool, the alphabet, the screen format strings, all pointer tables, the record
table hypothesis, and the float constants).

- Data bytes source-owned: **9,404** (31 files).
- Parsed bytes: **2,888** (zero-fill, all string pools, float pools).
- Raw-but-classified bytes: **6,516** (packed graphics/GBI display-list,
  pointer/handler tables, the fixed-stride record table).
- Undecoded bytes: **0** (every span is at least classified).

Region contents (ROM order):

- `0x001EE574..0x001EE590` `zero_fill_001ee574` — leading alignment.
- `0x001EE590..0x001EEB10` packed graphics / GBI-F3DEX2 display-list blob
  (`data_001ee590`, `table_001ee914` [7 ptrs 0x800E band], `data_001ee930`).
- `0x001EEB10..0x001EEB30` `rodata_001eeb10` — title "Ogre Battle 64 BGM
  Selection" (parent `docs/rom-layout.md` cites z64 `0x1EEB1F`).
- `0x001EEB30..0x001EECF0` `data_001eeb30` — packed sound-test control/graphics
  block (SJIS half-width bytes present).
- `0x001EECF0..0x001EEF44` `rodata_001eecf0` — **46-string scene/location name
  pool** (Castro Canyon … Prologue, Ch. 1–4, No Data).
- `0x001EEF44..0x001EF034` `table_001eef44` (+`rodata_001ef030`) — 59-word
  0x801B-band RAM-pointer table + trailing count word `0x14`.
- `0x001EF034..0x001EF8F0` — scalar pool + display-list block + **fixed-stride
  record table** `table_001ef1f4` (~12-byte records, descending 0x801B pointer
  column; stride/fields a hypothesis).
- `0x001EF8F0..0x001EF928` `rodata_001ef8f0` — glyph-order alphabet
  "ABC…xyz.3&,".
- `0x001EF928..0x001F0150` — glyph-width/display-list data + 0x801A-band pointer
  tables + a pi/-90.0 double pool (`float_001f0018`) + the screen format strings
  `rodata_001f0088` ("%s Scene %d", "999:59:59", "%d:%02d:%02d").
- `0x001F0150..0x001F0934` `rodata_001f0150` — **126-string staff-credits /
  ending-roll pool** (Presented … Atlus U.S.A., Inc., Localization Staff).
- `0x001F0934..0x001F0A30` — zero pads + 0x801A/0x801B-band handler-pointer
  tables + a 1.1 double (`float_001f0970`) + a pi/-90.0 pool (`float_001f09d0`).

Negative data evidence: an independent byte-exact scan and two adversarial data
verifiers found **0 stack prologues and 0 `jr $ra`** across the whole
`0x001EE574..0x001F0A30` territory (no hidden code); both code/data seams
(`0x001EE574`, `0x001F0A30`) are byte-exact.

## Parent Workspace Evidence

- `docs/rom-layout.md` / `docs/ROM_STATUS.md` (parent) document the
  "Ogre Battle 64 BGM Selection" Sound-Test string at z64 `~0x1EEB1F` and the
  sound-test selection logic in the MIPS region — confirms the chunk-30 data
  classification. (The 0x8AE044 sound-test format table is a *different* table in
  the archive cluster, out of range.)
- `scripts/ob64_symbols_v2.json` / `ob64_functions.json` assign ROLE TAGS only
  ("character::char-data consumer", "dma/resource::resource loader",
  "dispatch::dispatcher/state-machine") — used as leads; names stay `func_*`.
- The pervasive `0x801CE8BC` owner global (z64 `lui 0x801D` / `lw -0x1744`)
  referenced across chunk 30/31 identifies this region as the battle/attack-queue
  module (see chunk 31 for the High-Attack streamsplit guard at `0x1F36F0`).

Rejected: `0x8025/0x8026` linear-map values and out-of-range `0x01Exxxxx`
archive offsets were not treated as in-range evidence; `0x801x` RAM values are
overlay leads only.

No patch-workbench metadata was harvested for chunk 30 (the High-Attack hook
site is in chunk 31).

## Verification

```powershell
node tools/check_manifest.js                       # ALL CHECKS PASS (chunk 30 = 122 parts)
node tools/check_boundaries.js --splits build/chunk_001E1000-001F1000_splits.json --disasm build/original-mips/rev0/code_001E1000_001F1000.s   # BOUNDARY CHECK PASS (0 fragment/cross/under/leak/straddler)
node tools/check_splits.js     --splits build/chunk_001E1000-001F1000_splits.json --disasm build/original-mips/rev0/code_001E1000_001F1000.s   # 0 fragments
node tools/assemble_original_mips.js               # Exact code-region match: PASS
```

Code-region SHA256 `40D4E7875BA50F005788611C63CF9C42D9154339B36793556BF045C25B64B409`
(unchanged); full ROM SHA256
`571E83396BC81E70DA4C0A20313D82DBD7DFE685F2C37418C8E27F927E2CC67A`.

Adversarial pass: 5 code skeptics + 2 data skeptics. 2 high-severity missed
frameless leaves found and fixed (the two splits above); 2 LOW data-typing note
nuances corrected; all data verifiers confirmed 0 code in the data territory.

## Next Action

Next frontier is `0x001F1000` (chunk 31, ALL CODE). Start by continuing the
outgoing function straddler from chunk 30: `func_001F0F9C` begins in chunk 30 at
`0x001F0F90` (preamble) / parent prologue `0x001F0F9C`, has no `jr $ra` before
the boundary, and must be emitted first in chunk 31 as
`func_001F0F9C_chunk31tail` `0x001F1000..0x001F102C` (returns `jr $ra`@`0x001F1024`).
