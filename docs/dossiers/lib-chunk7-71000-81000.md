# Chunk 7 — data blob + 2 code regions + Controller-Pak menu data (`0x00071000..0x00081000`)

Source-ownership pass for the eighth 64 KiB chunk. **MIXED, FOUR-region**:
data-blob continuation → parent-undetected code → game-data block → parent-detected
code. **103 tracked parts**: **80 code `func_*` + 1 straddler-head + 22 data**.
Byte-exact rebuild preserved (code SHA `40D4E787…B409`, ROM SHA `571E8339…CC67A`).
Coverage `0x1000..0x81000` = 524,288 B = **18.40 %** of the 2,849,204-byte
executable extent; code-only classified ≈ 400,132 B (**14.04 %**).

## Four-region map (recon + adversarial-confirmed)

| Region | ROM range | Bytes | Kind | Parent DB |
|---|---|---:|---|---|
| DATA1 | `0x71000..0x71280` | 640 | data (2 parts) | undetected |
| CODE1 | `0x71280..0x783A0` | 28,960 | code (45 parts) | **undetected** (`scan_functions`) |
| DATA2 | `0x783A0..0x79730` | 5,008 | data (20 parts) | undetected |
| CODE2 | `0x79730..0x81000` | 30,928 | code (36 parts) | detected (27 fns) |

Boundary evidence: blob→code `0x71280` (frameless string-search fn starts);
code→data `0x783A0` (fn returns `jr $ra` 0x78394, data `001F1C1F` at 0x783A0);
data→code `0x79730` (first parent fn). The parent DB resumes at `0x79730` after
its long gap (chunk 5 `0x5C1A8` → chunk 7 `0x79730`), so CODE1 is parent-undetected
and CODE2 is parent-detected.

## DATA1 `0x71000..0x71280` (2 parts)
- `data_00071000` `[0x71000,0x71258)` — tail of chunk-6 `data_00070e70` packed
  record/offset blob (0x8x/0x9x/0xF1/0xF8 lead-byte words decoding as load/scd with
  garbage offsets; 0 returns/prologues). **Straddler-in continuation.**
- `table_00071258` `[0x71258,0x71280)` — 10-word RAM-pointer table (`0x80197Cxx`).

## CODE1 `0x71280..0x783A0` (45 `func_*`)
Parent-undetected overlay code seeded with `scan_functions` (33 framed fns → 45
after frameless-leaf recovery). Includes two large internal jump-table dispatchers
(`func_00071e00`, `func_0007315c` — the `jr $v0` sites are computed switch jumps,
NOT returns), queue/dequeue accessor leaf pairs, and many 2-word global-load
preamble-orphans folded into the functions they feed. All conservative `func_*`.

## DATA2 `0x783A0..0x79730` (20 parts) — Controller-Pak / save-data menu data
Identifiable game data (see `build/wf_data.js` output for the full per-part map):
- **String pools (`rodata_*`):** "Reset Control Deck."; chapter labels
  (Prologue/Chapter One…Final Chapter); a glyph/charset table + debug names
  (Magnus/Mario/Legion/Pika, DEL_DATA/MUSIC_ON); month abbreviations (Jan.…Dec.,
  8-byte stride); an element/zodiac name pool (Deus/Tierra/Agua/…); UI/menu strings
  (Controller Pak Menu, OgreBattle Game Notes, Erase/Load/Save/Exit, "OGREBATTLE64
  %d", format tokens `%s{X60}%2d`/`{C0}`…); the `NOBE`/`EB` save magic.
- **Pointer/index tables (`table_*`):** several RAM-pointer tables (18/55/128/424/
  12/15-word) of `0x801Axxx/0x8019xxx/0x8017xxx` pointers — parallel string-pointer
  arrays with default-fill slots.
- **Small byte/index tables + layout records (`data_*`):** glyph-width/progression
  index arrays, a UI rectangle/layout record block.
All `kind:"data"`; every `jr/jal/branch` here is a coincidental decode of pointer/
ASCII/packed-byte words (0 returns, 0 prologues, 0 overlay functions).

## CODE2 `0x79730..0x81000` (36 parts: 35 `func_*` + 1 straddler-head)
Parent-detected overlay code (27 parent fns → 36 after preamble-orphan folds +
frameless-leaf recovery). The 11,144-byte `func_00079E7C` `[0x79E7C,0x7CA04)` is
confirmed ONE switch-dispatcher function (four internal `jr $v0` jump-table
dispatches, single `jr $ra`). Deterministic invariants over all 81 code parts: 0
fragments, 0 cross-boundary branches, 0 under-splits, 0 delay-slot leaks.

### Straddler-out (function)
`func_0007ffac_chunk7head` `[0x7FFAC,0x81000)` — true entry `0x7FFAC` (a 2-word
`lui/lw` preamble; parent labeled `0x7FFB4`). The function continues to `0x810DC`;
its chunk-8 tail is `func_0007ffac_chunk8tail` `[0x81000,0x810DC)`.

## Method & verification
`scan_functions` (CODE1) + `plan_chunk` (CODE2) → `slice_chunk --disasm` → two code
analysis swarms (5+5 slices) + a data-classification swarm (DATA1+DATA2) → combine
→ `check_boundaries` → 6-region adversarial swarm → fixes → `split_original_mips_part`.
Boundary fixes: 1 delay-slot leak, 3 slice-seam preamble straddler-head mislabels
folded, 1 adversarial preamble-orphan fold (`0x7E148`). Tool fix:
`split_original_mips_part.js` now sanitizes `*/`/`/*` in note/label text (a
charset-string note `+-*/=` had broken the C-comment and the assembler). Gates:
`check_manifest` ALL PASS; `check_boundaries` PASS; `assemble_original_mips`
byte-exact; `verify_setup` PASS; `audit_code_region` OK.

## Caveats / next frontier
- Code names are conservative overlay address labels. The Controller-Pak menu data
  is identified but its pointer-table→string wiring is not decoded.
- **Next frontier `0x00081000` (chunk 8).** FIRST continue the straddler:
  `func_0007ffac_chunk7head` continues to `0x810DC` (chunk-8 head file
  `func_0007ffac_chunk8tail` `[0x81000,0x810DC)`). Chunk 8 is the first **fully
  parent-detected** chunk in a while (the parent DB's `0x79730`+ run continues into
  it), so `plan_chunk` should work for most of it — but content-scan for data
  regions first (chunk 7 had two).
