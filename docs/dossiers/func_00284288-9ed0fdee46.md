# func_00284288: Preserved Matching-Workbench Candidate

## Status

This source is a research candidate. The original assembly remains the accepted owner.

- Candidate: `9ED0FDEE460C920DC9A3906DE125591A33055CC4F0175249790959EFBB8FFD16`
- Target: `func_00284288` at ROM `0x284288`
- Source: `docs/archive/matching-c-candidates/2026-08-31-func_00284288-9ed0fdee46.c`
- Source SHA-256: `958986E6E7A4E933D10B8A41B8F4020C798282DEFC3B3E8A1D38C322FA279062`
- Latest scratch class: `cfg-mismatch`
- Latest scratch score: `25.7`

## Preservation reason

Successor structured PURE_C research skeleton: central parser state and cursor
roles are named from retail/callee evidence, 15 declarations are reconciled
against the prototype ledger, and all 153 commands have a case-aware report. It
deliberately retains canonical assembly ownership; retail CFG remains 583/145
versus candidate 578/143 because the pinned compiler cross-jumps the
source-distinct command 0x5D and 0x80000006 call tails.

## Case-CFG comparison contract

The schema-2 report serializes this candidate's exact candidate ID, the actual
compile-run ID, and both sides of the comparison contract. Retail uses dispatch
`+0x0080`, first command body `+0x0894`, and
`post-command=+0x1EC4`. The actual candidate inputs are dispatch `+0x0080`, body
`+0x08A0`, and `post-command=+0x1EC8`. Required or changed inputs fail closed.

The run-independent result digest is
`164228EC18C985EBD8C9E03ACD53F6E1B515A00D12C26DF0418EA5A54EF256B7`.
It covers 153 mapped commands, with 70 exact structural comparisons, 125
block-count matches, 152 call-list matches, and 153 successor and shared-tail
matches. It is a structural scratch comparison, not semantic or matching proof.

Reproduce it from a clean checkout without a pre-existing workbench database:

```powershell
node tools/reproduce_func_00284288_case_cfg.js --actual-dispatch 0x80 --actual-body 0x8A0 --actual-tail post-command=0x1EC8
```

The isolated compilation run ID changes between executions; the candidate ID
and result digest above must not. Full prerequisites, generated paths, expected
outputs, and evidence-regeneration commands are in
`docs/audit/2026-08-31-func-00284288-preparatory-reconstruction.md`.

## Frozen source identity

The archived source was not trimmed to address its blank-at-EOL diagnostics.
Its SHA-256 and candidate ID therefore remain unchanged. An exact-path
`.gitattributes` rule suppresses only `blank-at-eol` for this frozen file; the
repository's global whitespace checks remain active. Any source edit requires a
new successor identity and a rerun of this comparison.

## Evidence boundary

Scratch object comparison does not prove canonical linker ownership, relocation resolution, target bytes, or full-ROM identity. Resume through the normal diff and verification workflow.

## First recorded difference

```json
{
  "actual": "sw $s0, 0x48($sp)",
  "actualWord": "0xAFB00048",
  "expected": "sw $s1, 0x4C($sp)",
  "expectedWord": "0xAFB1004C",
  "index": 8,
  "offset": 32,
  "pc": 2149745368
}
```
