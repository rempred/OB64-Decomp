# lib chunk 42 — `0x002A1000..0x002B1000`

Classification: **MIXED** (incoming function straddler-tail → CODE → big interior DATA island →
CODE → float64 DATA pool → CODE → outgoing function straddler-head). FP-heavy mission-briefing/
combat + UI code with two data islands. This is the final chunk of the chunks 40-42 run; it
approaches but stays within the evidenced executable extent end `0x002B89B4`.

- Split JSON: `build/chunk_002A1000-002B1000_splits.json`
- Data index: `docs/data-index/rev0/chunk42-data-region-inventory.json`
- Promoted source: `asm/original/rev0/lib/` (171 parts)

## Composition

**171 parts = 157 normal code + 1 straddler-tail + 1 straddler-head + 12 data.**

ROM-ordered region map:

- Incoming FUNCTION straddler-tail `func_002A0EF0_chunk42tail` `0x002A1000..0x002A135C`
  (continues `func_002A0EF0` from chunk 41; returns `jr $ra`@`0x002A1354` + delay
  `addiu $sp,0x30`@`0x002A1358`).
- CODE C1 `0x002A135C..0x002A82B4` — FP-heavy functions + display-list builders; recovered
  frameless leaves (incl. 156 B FP matrix-transform leaf `func_002A3310`). Ends `jr $ra`@`0x002A82AC`.
- **DATA A (big interior)** `0x002A82B4..0x002A8D20` (2,668 B, 11 parts) — leading zero-fill +
  RAM-pointer tables (`0x80xxxxxx`) + ASCII strings + a trailing dispatch/jump pointer run of
  repeated `0x802376E4` (with `0x80237770`/`0x802376F4` variants) `0x2A8D00..0x2A8D1C`.
- CODE C2 `0x002A8D20..0x002AE338` — FP functions; recovered frameless FP-preamble `func_002A90EC`
  (lwc1 $f0..$f12 from `0x801DE8xx`) + 520 B frameless leaf `func_002ADF9C`-region. Ends
  `jr $ra`@`0x002AE330`.
- **DATA B (float64 pool)** `0x002AE338..0x002AE3C0` (136 B, 1 part) — IEEE double constants:
  π (`0x400921FB54442D18`, repeated), 180.0, 90.0, 160.0, 120.0, 0.5, 1.0, 0.005 — angle/scale math.
- CODE C3 `0x002AE3C0..0x002B1000` — FP functions; recovered 204 B frameless leaf
  `func_002AF5F4`-region.
- Outgoing FUNCTION straddler-head `func_002B0E8C` `0x002B0E8C..0x002B1000` (TRUE entry is a
  2-word read-before-write preamble @`0x2B0E8C` (`lui $v1,0x8023`/`lw 0x8023A974`) preceding
  prologue `addiu $sp,-0x140`@`0x2B0E94`; epilogue lw-restores run to `0x2B0FFC`, NO `jr $ra`
  before `0x002B1000`; continues into chunk 43 as `func_002B0E8C_chunk43tail`).

## Data island inventory (12 parts, 2,804 bytes)

Full per-part index: `docs/data-index/rev0/chunk42-data-region-inventory.json`.

- **DATA A** `0x2A82B4..0x2A8D20` (2,668 B, 11 parts) — leading zero-fill, RAM-pointer/index
  tables, ASCII string pool(s), and a trailing repeated-handler dispatch-pointer run
  (`jumptable_`/`table_` `0x802376E4`...). Raw-but-classified where packed.
- **DATA B** `0x2AE338..0x2AE3C0` (136 B, 1 part) — `float_` IEEE float64 constant pool
  (π/180/90/160/120/0.5/1.0/0.005) with leading/trailing zero words folded in.

All data byte-owned and classified; 0 bytes undecoded-and-unclassified.

## Role leads (conservative; names stay `func_*`)

FP/geometry + display-list module continuing chunks 36-41. DATA A's repeated `0x802376E4`
dispatch-pointer run is a handler/jump table; DATA B is an angle/scale math double pool (π, deg↔rad
180.0, 90.0). Parent docs flagged a "loader/decoder cluster" near `0x28CA1C` (chunk 40) but no
specific chunk-42 function leads beyond the callgraph. Names stay `func_*` pending runtime proof.

## Parent workspace evidence sweep

Covered by the run-wide `0x281000..0x2B1000` sweep (see chunk-40 dossier / review). The combat
overlay sources in `ram_snapshots/overlay_sources.json` end before `0x281000`, so chunk 42 is
unclaimed-overlay/data for runtime-mapping purposes (RSR-001). No chunk-42-specific hook sites.

## Mistakes corrected / hazards handled

- Two interior data islands (A big, B float64 pool) carved via `build/carve_chunk.js`.
- The analysis swarm mislabeled 4 mid-chunk functions as `straddler-head`; reclassified to `code`
  (only `func_002B0E8C` is the real outgoing straddler).
- Fixed 44 preamble-orphan label conflicts (label set to prologue addr → corrected to part start).
- `check_boundaries` caught an 8-byte overlap at the outgoing straddler: `func_002B0D30` had been
  extended to `0x2B0E94`, swallowing the straddler's 2-word preamble; corrected its end to
  `0x2B0E8C` (it returns jr$ra@`0x2B0E84`).
- `check_boundaries` caught a slice-seam preamble fragment (`func_002ADF30`, an 8-byte
  read-before-write preamble) — merged forward into `func_002ADF38`.
- Frameless recoveries (156 B@`0x2A3308`, 56 B@`0x2A90EC` FP preamble, 520 B@`0x2ADF9C`,
  204 B@`0x2AF5F4`) proven CODE; the genuine data islands are A and B only.
- Adversarial swarm (5 code + 2 data skeptics): results recorded in the review handoff.

## Verification

`check_boundaries` PASS (0 fragment/cross/under/leak/straddler/data-island); `check_splits`
0 fragments; `check_manifest` 171 parts; `assemble_original_mips` byte-exact (code SHA
`40D4E787…B409`). See review handoff.

## Next-run first action (chunk 43, `0x002B1000`)

Chunk 42 ends mid-function: the outgoing straddler `func_002B0E8C` `[0x002B0E8C,0x002B1000)`
continues into chunk 43. First action for chunk 43: emit `func_002B0E8C_chunk43tail` starting at
`0x002B1000` and confirm its return (its epilogue `jr $ra` is just past `0x002B1000`). Note: chunk
43 (`0x002B1000..0x002C1000`) crosses the evidenced executable-extent end `0x002B89B4` — expect a
code→data transition partway through; classify honestly (do not assume code past `0x2B89B4`).
