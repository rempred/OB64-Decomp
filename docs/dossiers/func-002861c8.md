# `func_002861C8`: director query comparison and scanner

Date: 2026-08-30

## Status

The complete accepted owner is exact C with a two-register compiler-allocation escape hatch. Its
source class is therefore `HYBRID_C`, not Matching C.

- ROM range: `0x002861C8..0x00286444`
- Live range: `0x8022A1F8..0x8022A474`
- Size: 636 bytes
- Accepted build symbol: `func_002861C8`
- Supported alias: `director_query_compare_and_scan` (`SUPPORTED_ALIAS`)
- Source: `src/lib/func_002861C8.c`
- Original assembly reference: `asm/original/rev0/lib/func_002861C8.s`
- Structural audit: `docs/audit/2026-08-30-func-002861c8-owner-structure.md`

Only the address-based build symbol is exported. Static evidence supports the alias, but no runtime
trace, controlled mutation, or recognized library identity promotes it to `CANONICAL`.

## Structural result

The accepted range remains one owner. Untouched compiler output partitions it gaplessly into the
global entry at owner offset `+0x000`, an internal-call-only local scanner at `+0x134`, and a local
label finder at `+0x230` that has one fixed-address external call. These compiler-local symbols do
not create additional accepted owners or public aliases.

The source reconstructs every path in the owner: all six comparison modes, both failure-scanner
modes, the backward depth-matching path, and the label finder. It does not omit the scanner tails.
The compiler-generated six-word switch table occupies the accepted `func_002861C8` fragment of the
same auxiliary row already used by `func_00283E14`. The row boundary, placement, and eight-byte
assembly tail remain unchanged. The verifier needed a shared-row contract because the former
single-fragment contract could not describe two C contributors; no shared executable tail or new
secondary-entry mechanism was required.

## Pure-C retry and concrete blocker

The archived lift was used only as a lead. The complete owner and its caller/entry relationships
were independently reconstructed from the accepted assembly, canonical ROM, parser caller, and
auxiliary-row evidence. Manual variants went beyond the archived lift and tested declaration and
assignment order, ordinary block-scoped temporaries, distinct cursors, pointer forms, direct and
derived constants, reordered conditions, loop/goto shapes, and redundant references.

The closest ordinary `PURE_C` source reproduced the exact three-function partition, switch table,
relocations, control flow, and every instruction except six register-allocation words. KMC assigned
the two long-lived scanner constants to `$t3` and `$t2`, respectively, while retail assigns them to
`$t2` and `$t3`:

| Owner offset | Candidate word | Retail word |
|---:|---:|---:|
| `+0x1B0` | `0x3C0B8000` | `0x3C0A8000` |
| `+0x1B4` | `0x356B0002` | `0x354A0002` |
| `+0x1B8` | `0x3C0A0FFF` | `0x3C0B0FFF` |
| `+0x1BC` | `0x354AFFFE` | `0x356BFFFE` |
| `+0x1E8` | `0x544B0005` | `0x544A0005` |
| `+0x218` | `0x0143102A` | `0x0163102A` |

That is six differing bytes across six instruction words. The accepted fallback binds only those
two C locals to `$10` and `$11`; it injects no instructions and implements no semantic logic in
assembly. The binding changes compiler allocation only, but it is still an assembler escape hatch
and is correctly classified as `HYBRID_C`.

## Supported-alias evidence

The alias attempt independently checked the complete body, its parser caller, its internal call,
the fixed-address caller of the local label finder, constants, and data accesses:

- The sole external call to the global entry, at ROM `0x0028616C`, passes the parser's local nesting
  counter, current word cursor, stream base, and query result, then replaces the local cursor with
  the return value.
- The primary body reads the following comparison selector and target, implements exactly six
  equality/inequality and signed relational modes, and advances or invokes its local scanner.
- The scanner recognizes bridge `0x80000000` and barrier `0x80000002` words, supporting both direct
  forward resynchronization and backward depth matching.
- The local finder is called at ROM `0x002854D0`; it searches for label word `0x80000005` plus a
  caller-supplied ID, and its result replaces the Director cursor.
- The owner references no strings and provides no evidence for a narrower gameplay-object meaning.

This evidence supports `director_query_compare_and_scan` as a cautious `SUPPORTED_ALIAS`. It does
not authorize a canonical symbol rename.

## Acceptance gates

The final activated configuration passed:

- `node tools/diff.js func_002861C8`: exact decoded rows, zero differing bytes or instruction
  words, exact linked target SHA-256, and matching relocations;
- `node tools/verify.js --target func_002861C8`: sole C ownership, exact placement, relocations,
  target bytes, and complete ROM;
- `node tests/active_targets.js`, `node tests/compiler_text_functions.js`, and
  `node tests/func_002861C8_structure.js`;
- the 451-target `tests/phase8_matching_c.js` artifact suite, including negative shared-row,
  preserved-tail, ownership, and relocation mutations; and
- the heavyweight structural audit with the parent research root configured.

`node tools/verify.js --target func_002861C8 --require-pure` completed the same exact ownership,
placement, relocation, target-byte, and complete-ROM checks, then correctly exited nonzero with
`RESULT: NOT MATCHING C (HYBRID_C)`. The final exact baseline contains 387 `PURE_C` functions /
18,992 bytes and 64 `HYBRID_C` functions / 35,228 bytes.
