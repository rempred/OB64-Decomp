# func_00215CF0: PURE_C Research and Exact HYBRID_C Activation

## Status

`func_00215CF0` is now an exact `HYBRID_C` replacement. It is not matching C
and does not contribute to the `PURE_C` count.

- Target: `func_00215CF0` at ROM `0x215CF0`
- Active source: `src/battle/func_00215CF0.c`
- Active source SHA-256: `69C4F92273BC8469F007EE75A4F9239F6AF1D537E7951E66CF0A13E7A207EF30`
- Hybrid workbench candidate: `AC2A8F63FA4B911D9B07E2ABF1B46CBB7A332592242506047E7072F8C06507D3`
- Source class: `HYBRID_C`
- Function extent: `3400` bytes / `850` instructions
- External calls at retail offsets: `57` / `57`
- Reviewed load-relevant relocations: `295`
- Linked target SHA-256: `8E0531C287F3635893AAEB1282F89ADFC257682F8ED69D1311B0C23B21B21C9E`
- Expected target SHA-256: `8E0531C287F3635893AAEB1282F89ADFC257682F8ED69D1311B0C23B21B21C9E`
- Differing linked bytes / instruction words: `0` / `0`
- Compiler assembly rewritten: no
- Separate assembler object: none

The only hybrid mechanisms are two empty extended-assembly dependency
constraints. Their templates are empty, they have no clobbers or raw words,
they bind no fixed registers, and they emit zero instructions and zero bytes.

## Preserved PURE_C result

The strongest pure-C attempt remains archived for future compiler research:

- Candidate: `20B217BD5262A7190B1502B61DCD99B52633B709B9055D6E15450560F21B8510`
- Source: `docs/archive/matching-c-candidates/2026-09-04-func_00215CF0-20b217bd52.c`
- Source SHA-256: `5CCD6A73B29CBC0B5DA58BEEA2A4B3477E3F35D77DD9279CB83BBDFB695324A9`
- Workbench score: `71.65`
- Exact extent: `3400` / `3400` bytes (`850` / `850` instructions)
- Structural instruction LCS: `848` / `850`
- Generic aligned differences: `297`
- Generic differing bytes: `687`
- Scratch CFG blocks: `169` / `177`
- Recognized scratch relocations: `295`
- External calls at retail offsets: `57` / `57`
- Scratch artifact: `build/matching/runs/720790BD143440EDA43C29AE73BD88BAE8F26072147C10EE28848FFFE239B87C`

The generic mismatch counts are dominated by unresolved relocation fields
because the target was still ASM-owned when that candidate was measured. The
direct object comparison isolated only two structural instruction-order
differences.

## Reconstruction gain

The continuation reduced the original archived candidate from `691` aligned
instruction differences to `297` while preserving exact extent and the
ten-argument ABI at each `func_00054e24` predecessor. The material source-shape
gains were:

- restoring the retail state-3 dialog/search local lifetimes;
- splitting only the state-7 cursor increment so KMC merges the retail common
  tail without shifting calls;
- correcting the state-7 one-argument `func_00055234(0x10C)` call;
- placing each resource-clear store in the following copy call's delay slot;
- duplicating each flag assignment so KMC merges only the common store; and
- reusing the state-2 byte result local so retail `$v0` is selected.

All 57 external calls then occurred at their retail offsets. The two remaining
pure-C structural differences were the same allocator schedule in two copies:

```text
PURE_C   0x4AC / 0xC4C: move $s0,$v0
retail   0x4AC / 0xC4C: lui  $a0,%hi(D_801CE8BC)
retail   0x4B0 / 0xC50: lw   $a0,%lo(D_801CE8BC)($a0)
retail   0x4B4 / 0xC54: move $s0,$v0
```

Direct nesting, separate allocator-result and owner pseudos, reordered pure-C
assignments, and bounded volatile-read variants did not produce the retail
order. The exact neighboring functions `func_002158E4`, `func_002159D0`, and
`func_00217BA8` use the same empty register-dependency technique for the same
allocator/copy schedule. This established a concrete compiler-scheduling blocker after
the sustained pure-C attempt.

## Authorized compiler-influence resolution

The final assignment explicitly authorized testing the neighboring zero-byte
fallback after pure-C exhaustion. At each of the two allocator/copy sites, the
source now:

1. stores the allocator result in a temporary;
2. loads `D_801CE8BC` into the owner operand;
3. transfers the allocator result into the long-lived snapshot operand through
   an empty `"=r"` output tied to input `"0"`, with the owner as a separate
   `"r"` dependency; and
4. performs the remaining resource copy in C.

The first tested neighboring constraint form fixed both schedules. The
workbench reported `relocationMaskedExact: true`, zero opcode differences, and
zero register differences. The canonical linked diff then proved exact decoded
rows and exact raw linked bytes. After recording the compiler object's complete
`295`-entry relocation contract, the focused diff reported `MATCH` for the
contract and zero differing bytes.

## Accepted verification

The single consolidated `node tools/verify.js` run passed baserom identity,
toolchain identity, source policy, sole C linker ownership, target placement,
all relocations, target bytes, and full-ROM identity. The complete rebuilt ROM
is byte-identical to the canonical Rev 0 baserom.

The subsequent single `node tools/status.js` run reported:

- exact `PURE_C`: `460` functions / `32416` bytes;
- exact `HYBRID_C`: `66` functions / `39192` bytes;
- assembly owners remaining: `5658` / `6438964` bytes;
- other/data owners: `1058` / `35432596` bytes; and
- unknown source classifications: `0`.

This result must always be reported as exact `HYBRID_C`, never matching C.
