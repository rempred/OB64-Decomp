# func_00215CF0: Preserved Matching-Workbench Candidate

## Status

This source is a research candidate. The original assembly remains the accepted owner.

- Candidate: `20B217BD5262A7190B1502B61DCD99B52633B709B9055D6E15450560F21B8510`
- Target: `func_00215CF0` at ROM `0x215CF0`
- Source: `docs/archive/matching-c-candidates/2026-09-04-func_00215CF0-20b217bd52.c`
- Source SHA-256: `5CCD6A73B29CBC0B5DA58BEEA2A4B3477E3F35D77DD9279CB83BBDFB695324A9`
- Latest scratch class: `cfg-mismatch`
- Latest scratch score: `71.65`
- Scratch extent: `3400` / `3400` bytes (`850` / `850` instructions)
- Scratch CFG blocks: `169` / `177`
- Recognized scratch relocations: `295`
- Aligned instruction differences: `297`
- Differing bytes: `687`
- Structural instruction LCS: `848` / `850`
- External calls at retail offsets: `57` / `57`
- Scratch artifact: `build/matching/runs/720790BD143440EDA43C29AE73BD88BAE8F26072147C10EE28848FFFE239B87C`

## Preservation reason

Wave 4 sustained PURE_C reconstruction: exact 3400-byte extent and all 57 call offsets; structural LCS 848/850. Remaining two duplicated scheduler differences are the allocator-result move preceding the D_801CE8BC load; the identical retail sequence in func_002158E4 currently requires a HYBRID_C empty register-constraint bridge, which is outside this target's assigned scope.

## Continuation gain

This continuation reduced the archived baseline from `691` aligned instruction
differences to `297` while preserving exact extent and the ten-argument ABI at
each `func_00054e24` predecessor. The generic workbench count remains dominated
by unresolved relocation fields because this function is still ASM-owned. A
direct structural comparison of opcodes and register fields reaches `848` of
`850` instructions.

The material source-shape gains were:

- restoring the retail state-3 dialog/search local lifetimes;
- splitting only the state-7 cursor increment so KMC merges the retail common
  tail without shifting calls;
- correcting the state-7 one-argument `func_00055234(0x10C)` call;
- placing each resource-clear store in the following copy call's delay slot;
- duplicating each flag assignment so KMC merges only the common store; and
- reusing the state-2 byte result local so retail `$v0` is selected.

All 57 external calls now occur at their retail offsets. The two remaining
in-symbol structural differences are the same schedule in two copies:

```text
candidate 0x4AC / 0xC4C: move $s0,$v0
retail    0x4AC / 0xC4C: lui  $a0,%hi(D_801CE8BC)
retail    0x4B0 / 0xC50: lw   $a0,%lo(D_801CE8BC)($a0)
retail    0x4B4 / 0xC54: move $s0,$v0
```

Direct nesting, separate allocator-result and owner pseudos, reordered pure-C
assignments, and bounded volatile-read experiments did not produce the retail
order. The adjacent exact implementation in `src/battle/func_002158E4.c`
contains the identical allocator/copy sequence and uses an empty
register-constraint inline-assembly bridge for this ordering. That is concrete
evidence of the current KMC scheduling blocker. Because a hybrid fallback is
outside this assignment, this candidate remains `PURE_C` research only and the
original assembly remains the accepted owner.

## Evidence boundary

Scratch object comparison does not prove canonical linker ownership, relocation resolution, target bytes, or full-ROM identity. Resume through the normal diff and verification workflow.

## Final baseline verification

After preservation, the single consolidated `node tools/verify.js` run passed
baserom identity, toolchain identity, source policy, C ownership, placement,
relocations, linked target bytes, and full-ROM identity. It reported the
accepted baseline as `460` exact `PURE_C` functions / `32416` bytes and `65`
exact `HYBRID_C` functions / `35792` bytes. This proves the inactive research
preservation did not disturb the accepted baseline; it does not promote or
prove this nonexact candidate.

## First recorded difference

```json
{
  "actual": "lui $a2, 0x0000",
  "actualWord": "0x3C060000",
  "expected": "lui $a2, 0x801D",
  "expectedWord": "0x3C06801D",
  "index": 0,
  "offset": 0,
  "pc": 2149394976
}
```
