# Chunk 16 — MIXED: leading scenario data + neutral-encounter code (`0x00101000..0x00111000`)

Source-ownership pass for the seventeenth 64 KiB chunk. **MIXED**: a leading DATA region
(record-table tail + 0x801A RAM-pointer/jump tables + a float64 const pool) then a large
CODE region ending in an outgoing FUNCTION straddler. **95 tracked parts**: **72 code
(incl. 1 outgoing straddler-head) + 23 data**. Byte-exact rebuild preserved (code SHA
`40D4E787…B409`, ROM SHA `571E8339…CC67A`). Coverage `0x1000..0x111000` = 1,114,112 B =
**39.1026 %** of the 2,849,204-byte evidenced executable extent; code-only classified ≈
926,404 B (**32.5145 %**).

Pipeline: content-scan region map → data-classification swarm (2 agents) + code-analysis
swarm (8 slice agents) → combine → `check_boundaries`/`check_splits` → **5-agent
adversarial swarm** (3 code verifiers + 2 data hunters). **0 structural disproofs** — the
only adversarial finding was a mis-cited evidence word in one data note (fixed; the
boundary was already correct). No tool changes (data string export uses the durable
`tools/decode_rodata_strings.js` added this run).

## Region map (3 ROM-ordered regions)

| Region | Range (z64) | Bytes | Class | Parts |
|---|---|---:|---|---:|
| DATA (record tail + packed/pointer) | `0x101000..0x101A40` | 2,624 | data | 11 |
| DATA (0x801A ptr tables + f64 pool) | `0x101A40..0x101CE0` | 672 | data | 12 |
| CODE (neutral-encounter path) | `0x101CE0..0x111000` | 62,240 | code | 72 |

## Incoming DATA straddler (chunk 15 → chunk 16)
`data_00101000_chunk16tail` `[0x101000,0x101024)` — the 9 words that COMPLETE a truncated
fixed-stride 0x50-byte (20-word) float/param record begun at `0x100FD4` in chunk 15
(word[11] @`0x101000` = `0x42340000` = 45.0). The **same 0x50-stride record table then
continues** at `0x101024` for 4 more records (`0x101024/074/0C4/114`; header word[0]=
`0x03`/`0x04`, word[1]=`0x64`=100) and **ends at `0x101164`** (the word there is
`0x00000019`, an index — not a record header; adversarially confirmed). `kind:"data"`.

## DATA `0x101000..0x101CE0` (23 parts)
- Fixed-stride record table (above) → an **index+float ramp** `[0x101164,0x101204)` →
  packed small-int blocks and small RAM-pointer sub-tables (`0x801B` band) → a packed
  tail with trailing zero alignment.
- **Three RAM-pointer tables** in the `0x801A`/`0x801B` band `[0x101A40,0x101C94)`,
  including a **~100-entry `0x801AEAEC` default-fill jump/dispatch table** punctuated by
  real targets — per parent evidence these are scenario-state jump tables feeding the
  `0x102FA8` dispatcher (LEAD; wiring not decoded). Two embedded ASCII format strings
  `"{P%03d,%03d}"` and `"{C12}"` carved out of the tables.
- **IEEE-754 float64 constant pool** `[0x101C98,0x101CD4)`: 1.0, 180.0, pi
  (`0x400921FB54442D18`), pi/2, 2.5 (+ small-int pairs), then zero padding to the code
  start `0x101CE0`. All `kind:"data"`; no field semantics invented.

## CODE `0x101CE0..0x111000` (72 parts)
Overlay-relocated (decode RAM column `0x8017xxxx` is the WRONG linear map; real runtime
slots `0x801A..0x801C`, scenario/combat_transition states). **Starts with a
parent-missed FRAMELESS leaf `func_00101CE0`** `[0x101CE0,0x101DDC)` (divide-by-3 magic
`0xAAAAAAAB` loop) before the parent's first framed fn. 39 framed prologues + 23
preamble-orphans (recurring 2-word `lui/lw 0x801F…` read-before-write folds, each named
`func_<startaddr>` with `label`=name) + 9 recovered frameless leaves. Internal `jr $v0`
jump-table dispatchers (`0x104EA4`, `0x1052D8`, `0x1103DC`, …, tables in relocated
`0x801F`) and `j 0x801Bxxxx` overlay tail-jumps kept internal. **Outgoing straddler-head
`func_00110160`** `[0x110160,0x111000)` — prologue `addiu $sp,-0x88` @`0x110160`, NO
`jr $ra` in range, continues to `0x111464` in chunk 17.

## Parent-workspace evidence (sweep 2026-06-23)
The parent **already documents this chunk's CODE** as the **neutral-encounter path**:
- `0x00102FA8` = scenario event-state dispatcher (88 callees; reads `event_state`
  `@0x801F0DE0`, bounds <100, dispatches via jump table `@0x801EDC40`). rt `0x801AE868`.
- `0x00105CC8` contains **text_renderer** at +~`0x53A4` → ROM `0x0010706C`. rt `0x801B292C`.
- `0x0010D484` = spawn/materialize helper (marker 46); `0x0010DDBC` = follow-up spawn /
  event state machine (recovers class_id, writes presentation ID 441).
- `0x0010F300-0x0010F334` = class-data consumer (`class_id*72`, reads `0x80187C14`).
These are **LEADS only** — names here stay conservative `func_*` (overlay-relocated,
runtime identity unproven from bytes alone). The 0x50-byte record table and the 0x801A
pointer/jump tables are NOT documented in the parent (verified live against parent ROM).

## Data inventory (machine-readable: `docs/data-index/rev0/chunk16-data-region-inventory.json`)
**Total data: 3,296 B in 23 parts.** By class: fixed-stride record table 2,140 B ·
ram-pointer table 760 B · indexed-float ramp 160 B · packed/record 116 B · float64 const
pool 60 B · incoming straddler-tail 36 B · zero-fill 24 B. State: parsed where literally
readable (zero/ascii/float64); raw-but-classified for the record table / pointer tables /
packed blocks (type clear, fields not decoded). No invented field names.

## Method & verification
Region map (content scan + parent-gap analysis: data→code boundary `0x101CE0`) → 2 data
agents + 8 code-slice agents (Workflow) → combine (decimal→hex norm; data names →
`<kind>_<addr>`; preamble-orphan label = own name) → `check_boundaries` PASS (95 parts; 0
fragment/cross/under/leak/straddler/data-island) + `check_splits` 0 fragments → 5-agent
adversarial swarm (0 structural disproofs; 1 data-note evidence fix) →
`split_original_mips_part --remove-source`. Gates: `check_manifest` ALL PASS (17 chunks /
2,285 parts); `assemble_original_mips` byte-exact (`40D4E787…B409`); `verify_setup` green;
`audit_code_region` OK; data-index JSON valid; 0 data files with function wording.

## Caveats / next frontier
- Conservative code names (overlay RAM/global/callee identity suspect; parent symbol-DB
  semantic tags recorded as leads, not adopted). The 0x801A scenario jump tables, the
  record-table fields, and the packed blocks are NOT decoded (follow-up).
- **Next frontier `0x00111000` (chunk 17).** FIRST continue the outgoing FUNCTION
  straddler: `func_00110160` continues to `0x111464` (chunk-17 tail file
  `func_00110160_chunk17tail` `[0x111000,0x111464)`). Chunk 17 is ALL CODE.
