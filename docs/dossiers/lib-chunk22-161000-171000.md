# Chunk 22 — MIXED: UI/text + type-name/terrain resource data wrapping FP-heavy menu code (`0x00161000..0x00171000`)

Source-ownership pass for the twenty-third 64 KiB chunk. **MIXED**: a large leading DATA
region (incoming DATA straddler-tail, packed/bitmap blobs, `0x801F/0x8021` pointer & float
pools, and decoded ASCII string pools), a CODE region in the middle (FP-heavy menu/item/legion
routines), then a trailing DATA region (packed-halfword data, UI strings, GBI/RDP display-list
data) ending in an outgoing DATA straddler. **99 tracked parts**: **35 code + 64 data**.
Byte-exact rebuild preserved (code SHA `40D4E787…B409`, ROM SHA `571E8339…CC67A`). Coverage
`0x1000..0x171000` = 1,507,328 B = **52.9035 %** of the 2,849,204-byte evidenced executable
extent; code-only classified = 1,249,188 B (**43.8434 %**).

Pipeline: parent-evidence sweep + content scan (data both ends, code middle) → 6 code-slice +
3 data-region analysis agents (Workflow) → combine → `check_boundaries`/`check_splits` →
6-agent adversarial swarm (3 code + 3 data). **All 6 verdicts CLEAN** (1 kind correction + a
rodata-seam merge + 4 note-accuracy nits applied). No tool changes.

## Region map (3 ROM-ordered regions)

| Region | Range (z64) | Bytes | Class | Parts |
|---|---|---:|---|---:|
| DATA leading (incl. incoming straddler-tail) | `0x161000..0x165FC0` | 20,416 | data | 54 |
| CODE (FP-heavy menu/item/legion routines) | `0x165FC0..0x16FB90` | 39,888 | code | 35 |
| DATA trailing (incl. outgoing straddler-head) | `0x16FB90..0x171000` | 5,232 | data | 10 |

Note: unlike most chunks, chunk 22 **begins and ends in DATA** — the incoming straddler is a
DATA continuation (not a function), and the outgoing straddler is also DATA.

## Incoming DATA straddler (chunk 21 → chunk 22)
`data_00161000_chunk22tail` `[0x161000,0x161388)` — tail of chunk-21's
`data_0015FDF8_chunk21head` packed small-byte stream (alphabet `0x0F/0x13/0x2B`). Ends
byte-exact at `0x161388`: the last packed word `0x13131313`@`0x161384` gives way to
`0x00110000`@`0x161388` (alphabet break, adversarially confirmed). `kind:"data"`.

## DATA leading `0x161000..0x165FC0` (54 parts)
Interleaved resource data. **VERIFIED no hidden code** (0 `addiu $sp,-N` prologues, 0
`jr $ra` — first prologue is `0x165FC8`, first `jr $ra` is `0x1677A0`, both far above). Notable
parsed sub-blocks:
- **Weapon/armor TYPE-name table** `rodata_00163FC0` `[0x163FC0,0x1641C4)` — `Sword`,
  `Greatsword`, `Thrusting Sword`, `␎1-Handed␐cAxe/Hammer␏`, `2-Handed Axe/Hammer`,
  `Short Spear`, `Spear`, `Whip`, `Claw`, `Bow`, `Mace`, `Staff`, `Doll`, `Small Shield`,
  `Shield`, `Light Armor`, `Armor`, `Full Body Armor`, `Robe`, `Garment`. Embedded
  format-control bytes `\x0E/\x10/\x0F` (category/icon markers). **Merged across the
  `0x164000` swarm region seam** so the `"2-Handed Axe/Hammer"` string is not split.
- **Terrain-name + battle/legion/item UI message pool** `rodata_001650A0` `[0x1650A0,0x165BD4)`
  (2,868 B) — `Highway/Bridge/Plain/Barrens/Forest/Marsh/Highlands/River`, `Snowy …`
  variants, `Stronghold`, `Castle Wall/Gate`, `Within Castle Wall`, `Malefic Woods`,
  `Generator`, `Building`, `Before the Castle Gate(Air)`, then messages
  (`No usable items are in the inventory.`, legion/battalion relocation prompts,
  `You are about to use %s.`, `Proceed?`).
- **`{Cn}` color/format tokens** `rodata_00165F8C` (`{C6}{C7}{C0}{C1}`).
- **RAM-pointer tables** `table_001641C4` (20× `0x8016xxxx`), `table_00164214` (`0x8021xxxx`),
  `table_00165BE0` (string-pointer table, `0x8021xxxx`), `table_00165E50` (`0x8020xxxx`).
- **IEEE-754 float pools** `float_00165E00` (doubles 8.0833/7.5833/6.9167),
  `float_00165E30` (singles 1.05/1.0/0.95), `float_00165FB0` (single 3.0703125).
- Packed/bitmap/high-entropy blobs (`0xEE/0xDD/0xCC/0xFE` bands) — raw-but-classified,
  possibly compressed/graphics (HYPOTHESIS); no pointer/stride structure decoded.

## CODE `0x165FC0..0x16FB90` (35 parts)
Overlay-relocated (decode RAM column is the WRONG linear map; real runtime slots `0x8021xxxx`
band, scenario-state overlay). **17 framed prologues + 10 preamble-orphans + 7 frameless
leaves + 1 plain code.** FP-heavy (sdc1/ldc1/mtc1/cvt/mul.d/c.lt.d/bc1f) interpolation/menu
bodies are CODE. Internal `jr $reg` jump-table dispatchers and `j 0x8021xxxx` overlay
tail-jumps kept internal. Two boundary entries of note:
- **`func_00165FC0`** (preamble-orphan, ~6 KB) — the **DATA→CODE boundary**. True entry is the
  read-before-write pair `lui $v1,0x801F`@`0x165FC0` / `lw $v1,0x3658($v1)`@`0x165FC4`; `$v1`
  is first consumed by `beq $v1,$v0`@`0x166010` before any write across the `addiu $sp,-0xD0`
  prologue@`0x165FC8`. Adversarially confirmed genuine.
- **`func_0016A56C`** (preamble-orphan, ~3.4 KB) — a **lower-confidence** fold. The
  `lui $a0,0x8019`/`lw $a0,-0xA74($a0)` pair@`0x16A56C` is wedged between the prior fn's
  `jr $ra`@`0x16A564`+delay and the `addiu $sp,-0xA8` prologue@`0x16A574`. The body
  **redundantly reloads** the identical global into `$a0`@`0x16A600` (so `$a0` is not strictly
  read-before-write) — recorded honestly. Folded forward as the entry because the 2 words are
  code (not a RAM pointer), are unreachable by fall-through from the returning prior fn, and a
  standalone 8-byte part would orphan them. Adversary endorsed the fold as the most defensible.
- Parent LEAD: a growth-table consumer near `func_00167DE0` (heuristic, RRP `0x80216da0`);
  name stays conservative `func_*`.

## Adversarial pass (6 verifiers, all CLEAN)
3 code-boundary + 3 data-classification skeptics, each prompted to refute. **0 boundary
disproofs.** Findings applied: (1) `func_0016C8B8` was mislabeled `frameless-leaf` but is
framed (`addiu $sp,-0x18`, saves/restores `$ra`) → kind→`prologue`. (2) merged
`rodata_00163FC0`+`rodata_00164000` so the weapon-type table is one coherent part. (3) 4
note-accuracy nits: `data_001614B8` first word is `0xE000EE0E` (not `0xEEEEEEEE`);
`data_00161388` was one trailing-zero word short; `table_001641C4` is 20 (not 21) pointers;
`float_00165E00` first double is 8.0833 (not 8.16667, recomputed and verified). The verifiers
also independently confirmed: `func_00165FC0`/`func_00167DE0` are single large functions (one
`jr $ra` each), the `0x165FC0` and `0x16FB90` data/code seams are byte-exact, and both data
regions contain zero prologues/returns.

## DATA trailing `0x16FB90..0x171000` (10 parts) + outgoing straddler
- The last code function `func_0016FA90` ends `jr $ra`@`0x16FB88` + delay
  `addiu $sp,0x20`@`0x16FB8C`; data begins exactly at `0x16FB90` (`0x856B856B`).
- Packed-halfword blobs (`0x856B/0x6D27` bands), small UI string pools (`rodata_0016FE40` =
  `Is this acceptable?` / `…canceled. Proceed?`; `rodata_0016FEB0` = stat-label abbreviations
  `Dir./Hit./Eva./Gua./Int./Ret.` + `Cannot change the option while…`), and a GBI/RDP
  display-list block `data_0016FF34` (E7/DF/D7/D9 command bytes + `0x8021/0x8018` pointers —
  data, not code).
- **Outgoing DATA straddler `data_001708C8_chunk22head`** `[0x1708C8,0x171000)` — a contiguous
  `0xF83E`-family packed-halfword structure beginning byte-exact at `0x1708C8` (after zero pad
  `0x1708B4-0x1708C4`), running with no terminator to the last word `0xF83EF83E`@`0x170FFC` →
  continues into chunk 23 (`data_00171000_chunk23tail`, which starts `0xF83EF83E`). `kind:"data"`.

## Parent-workspace evidence (sweep 2026-06-23)
- **Address-space correction:** symbols_v2 `ram=` values for this range (`0x801d5xxx`) are the
  WRONG linear back-map; real runtime slots are the `0x8021xxxx` band (runtime_ram_primary /
  overlay snapshot), scenario-state overlay.
- **ASCII content correction:** the parent guessed a "weapon-name table" at `0x1650A0`; the
  decode proves that pool is **terrain names + battle/legion/item UI messages**. The actual
  weapon/armor **type**-name table is the smaller block at `0x163FC0..0x1641C4`.
- No LHA archive entries, item/class-stat tables, or sprite/anim catalogs fall in range
  (parent artifacts). All functions carry only heuristic tags; names stay `func_*`.

## Data inventory (machine-readable: `docs/data-index/rev0/chunk22-data-region-inventory.json`)
**Total data: 25,648 B in 64 parts.** By class: packed/unknown 20,540 B · ASCII strings
3,620 B · RAM-pointer tables 792 B · zero-fill 600 B · IEEE-754 const 96 B. Decoded ASCII
exports (byte-exact, control codes preserved): `chunk22-equipment-type-name-table`,
`chunk22-ui-string-pool`, `chunk22-trailing-ui-strings` (JSON in `docs/data-index/rev0/`, MD in
`data/decoded/rev0/strings/`). **Undecoded:** the packed/bitmap/high-entropy blobs (possibly
compressed; no fixed structure or pointer/stride evident).

## Method & verification
Parent-evidence sweep + content scan (data/code/data boundaries pinned at `0x165FC0` and
`0x16FB90`) → 6 code-slice + 3 data agents (Workflow) → combine (`combine_chunk.js`; data names
→ `<kind>_<addr>`; kinds normalized) → `check_boundaries` PASS (99 parts; 0
fragment/cross/under/leak/straddler/data-island) + `check_splits` 0 fragments → 6-agent
adversarial swarm (all CLEAN; 1 kind + 1 merge + 4 note fixes) → re-gate →
`split_original_mips_part --remove-source`. Gates: `check_manifest` ALL PASS (23 chunks /
2,899 parts); `rebuild_from_source_manifest` byte-exact (ROM `571E8339…CC67A`);
`assemble_original_mips` byte-exact (code `40D4E787…B409`); `verify_setup` all green
(23 real-asm / 77 fallback); `audit_code_region` `no-credible-code-edge-into-tail`;
`git diff --check` clean; data-index JSON valid; 0 data files with function wording.

## Caveats / next frontier
- Conservative code names (overlay-relocated, runtime `0x8021xxxx`). `func_0016A56C` is a
  lower-confidence preamble fold (redundant `$a0` reload, recorded honestly). The packed/
  high-entropy data blobs are undecoded (possibly compressed; no structure evident).
- **Next frontier `0x00171000` (chunk 23).** FIRST continue the outgoing DATA straddler:
  `data_001708C8_chunk22head` `[0x1708C8,0x171000)` (packed `0xF83E`-family halfword stream, no
  terminator) continues into chunk 23 — emit `data_00171000_chunk23tail` first and prove its
  end. Chunk 23 is also MIXED (6 regions: data + code1 + data island1 + code2 + data island2 +
  code3) and ends in the **function** straddler-head `func_0017FF4C` → chunk 24.
