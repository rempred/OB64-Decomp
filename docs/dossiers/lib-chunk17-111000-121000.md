# Chunk 17 — ALL CODE: char-data / encounter code (`0x00111000..0x00121000`)

Source-ownership pass for the eighteenth 64 KiB chunk. **ALL CODE** — an incoming
FUNCTION straddler-tail, ~64 functions, and an outgoing FUNCTION straddler-head; **0
data**. **66 tracked parts**: 1 incoming straddler-tail + 37 framed prologues + 23
preamble-orphans + 4 frameless leaves + 1 outgoing straddler-head. Byte-exact rebuild
preserved (code SHA `40D4E787…B409`, ROM SHA `571E8339…CC67A`). Coverage
`0x1000..0x121000` = 1,179,648 B = **41.4027 %** of the 2,849,204-byte evidenced
executable extent; code-only classified ≈ 991,940 B (**34.8146 %**).

Pipeline: parent-context + content scan (61 parent functions; no pointer-table or
record-table data signature) → 8 code-slice analysis agents (Workflow) → combine →
`check_boundaries`/`check_splits` → 3-agent adversarial swarm. **0 disproofs** across all
verifiers. No tool changes.

## Region map (ALL CODE)

| Region | Range (z64) | Bytes | Class | Parts |
|---|---|---:|---|---:|
| incoming straddler-tail | `0x111000..0x111464` | 1,124 | code | 1 |
| CODE | `0x111464..0x120FC4` | 64,352 | code | 64 |
| outgoing straddler-head | `0x120FC4..0x121000` | 60 | code | 1 |

## Incoming function straddler (chunk 16 → chunk 17)
`func_00110160_chunk17tail` `[0x111000,0x111464)` — tail of `func_00110160` whose true
entry `0x110160` is in chunk 16 (`func_00110160`, prologue `addiu $sp,-0x88`). Returns
`jr $ra` @`0x11145C` + delay `addiu $sp,0x88` @`0x111460`; the next function starts
exactly at `0x111464` (parent end). `kind:"straddler-tail"`.

## CODE `0x111464..0x120FC4` (64 parts)
Overlay-relocated (decode RAM column `0x8018xxxx` is the WRONG linear map; real runtime
slots `0x801B..0x801C`, scenario/combat_transition states). 37 framed prologues + 23
preamble-orphans + 4 recovered frameless leaves. The preamble-orphans are the recurring
read-before-write folds (1–8 word `lui/lw/lbu/mtc1` preambles loading `0x801F…` globals
or float regs the following `addiu $sp,-N` body reads before writing); each is named
`func_<startaddr>` with `label`=name (the inner parent-DB prologue label stays embedded).
Recovered frameless leaves: `func_00113A30`, `func_00113A90`, `func_00114C40`,
`func_00119120`. Internal `jr $v0` jump-table dispatchers (`0x11C954` → relocated
`0x801FE438`; `0x11EAD8` → `0x801EE478`), `jalr $v0` (`0x11D354`), and the many
`j 0x801Cxxxx`/`j 0x801C9xxx` overlay tail-jumps to shared epilogues are kept internal.
In-stream `lui+mtc1` FP constants (e.g. 3.0) are CODE, not data. No inline data islands.

## Outgoing function straddler (chunk 17 → chunk 18)
`func_00120FC4` `[0x120FC4,0x121000)` — prologue `addiu $sp,-0x50` @`0x120FC4`, NO
`jr $ra` in range (last word `sdc1 $f20,0x20($sp)` @`0x120FFC` is still prologue body),
continues to `0x1211F8` in chunk 18. `kind:"straddler-head"`. **Chunk-18 first action:
emit `func_00120FC4_chunk18tail` `[0x121000,0x1211F8)`.**

## Parent-workspace evidence (sweep 2026-06-23)
Chunk 17 is **largely new territory** in the parent: only `0x00115440` carries a concrete
role lead — a large function (parent end `0x1172A0`) whose interior labels
`0x80186210`/`0x80186190` are "simple bitfield helpers, not encounter-specific"
(`docs/neutral-encounter-mips.md:409`; rt `0x801C0D00`). All other chunk-17 functions
carry only heuristic auto-classifier role tags (char-data / promotion / text-vm consumer),
NOT semantic names. A parent RAM `0x118xxx` cutscene/text-context reference was REJECTED
as a false match (different address space — a CPU-built table, not ROM `0x118xxx`). Names
here stay conservative `func_*`; runtime slots `0x801B..0x801C` (scenario/combat_transition).

## Data
**None.** Every byte of chunk 17 is code (incoming straddler-tail + functions + outgoing
straddler-head). No data inventory file is needed (consistent with the all-code chunks
9–12). The content scan found 0 pointer-table runs and 0 record-table signatures; the
small float-const runs are all in-stream FP constants (CODE).

## Method & verification
Parent context + content scan (no data regions) → 8 code-slice agents (Workflow) →
combine (`combine_chunk.js`; first = `func_00110160_chunk17tail`, last = `func_00120FC4`;
0 label conflicts) → `check_boundaries` PASS (66 parts; 0 fragment/cross/under/leak/
straddler/data-island) + `check_splits` 0 fragments → 3-agent adversarial swarm (**0
disproofs**) → `split_original_mips_part --remove-source`. Gates: `check_manifest` ALL
PASS (18 chunks / 2,351 parts); `assemble_original_mips` byte-exact (`40D4E787…B409`);
`verify_setup` all green; `audit_code_region` OK.

## Caveats / next frontier
- Conservative code names (overlay RAM/global/callee identity suspect; parent symbol-DB
  semantic tags recorded as leads, not adopted). Dispatcher target tables in `0x801E…/
  0x801F…` relocated overlay RAM unresolved.
- **Next frontier `0x00121000` (chunk 18).** FIRST continue the outgoing FUNCTION
  straddler: `func_00120FC4` continues to `0x1211F8` (chunk-18 tail file
  `func_00120FC4_chunk18tail` `[0x121000,0x1211F8)`). Chunk 18 starts in code.
