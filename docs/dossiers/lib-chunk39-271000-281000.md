# lib chunk 39 — `0x00271000..0x00281000`

Classification: **MIXED** (the most interleaved chunk so far — 3 code regions + 3 interior
data islands). Incoming function straddler-tail → code → DATA territory → code → GBI-blob DATA
→ code → tail DATA (chunk ends in data; **no outgoing straddler**).

- Split JSON: `build/chunk_00271000-00281000_splits.json`
- Data index: `docs/data-index/rev0/chunk39-data-region-inventory.json`
- Promoted source: `asm/original/rev0/lib/` (155 parts)

## Composition

**155 parts = 135 normal code + 1 straddler-tail + 0 straddler-head + 19 data.**

ROM-ordered region map:

- Incoming FUNCTION straddler-tail `func_00270FF0_chunk39tail` `0x00271000..0x00271070`
  (continues `func_00270FF0`, prologue `addiu $sp,-0x28` in chunk 38 @`0x00270FF0`; returns
  `jr $ra`@`0x00271068` + delay `addiu $sp,0x28`@`0x0027106C`).
- CODE region 1 `0x00271070..0x00273FFC` — parent functions (FP/display-list + message
  dispatchers); ends `jr $ra`@`0x00273FF4`.
- **DATA island A** `0x00273FFC..0x00275850` (6,228 B, 12 parts) — large data territory.
- CODE region 2 `0x00275850..0x00279DA8` — frameless code band `0x275850..0x275C9C` (recovered:
  `func_00275850`, `func_00275AF8` GBI builder, preamble-fold `func_00275C98`→`func_00275C9C`)
  + parent functions/dispatchers; ends `jr $ra`@`0x00279DA0`.
- **DATA island B** `0x00279DA8..0x0027A020` (632 B, 1 part) — static GBI/F3DEX2 display-list asset.
- CODE region 3 `0x0027A020..0x00280D48` — frameless start `func_0027A020` + parent functions +
  recovered frameless GBI/FP builders (`~0x27D050`, `~0x2804D4`, `~0x280970`); ends
  `jr $ra`@`0x00280D40`.
- **DATA island C** `0x00280D48..0x00281000` (696 B, 6 parts) — tail data; **chunk ends in data**,
  frontier `0x00281000` is clean (no continuation).

## Data island inventory (19 parts, 7,556 bytes)

Bytes by class: data 4,888 · table 2,072 · jumptable 244 · float 64 · zero_fill 288.
Full per-part index: `docs/data-index/rev0/chunk39-data-region-inventory.json`.

- **DATA A** `0x273FFC..0x275850` — packed header (`0x274000`), a 136-word RAM-pointer table
  (`table_0027401c`, almost all `0x8021F460`), a packed 16-bit-pair array (`data_0027423c`), a
  272-word RAM-pointer table (`table_00274650`, `0x8021F6xx..0x8022xxxx`), a large packed
  display-list/binary blob (`data_00274a90`), ID/index arrays + float32 consts (`data_00275644`),
  a 5-entry overlay handler pointer table (`table_002756f0`, `0x8020E6A4..0x8020E770`), a float64
  const pool (`float_00275710`: a pi-like double, 3.0, 180.0 — angle/scale math), and a jump table
  (`jumptable_00275750`: 8 rows, seven 7-pointer rows + a final 5-pointer row, `0x80214xxx..0x8021Exxx`),
  with small zero-fill spans.
- **DATA B** `0x279DA8..0x27A020` — `data_00279da8`, a static GBI/F3DEX2 RDP display-list asset
  (E7 pipesync / E3 setothermode / D9 geometrymode / E2 / D7 texture / DF enddl command words +
  operands) with packed RGBA color (`0x00FF00FF`/`0x00FF0064`), a double 0.5, and coord ints.
- **DATA C** `0x280D48..0x281000` — two small-int LUT blocks (`table_00280d5c` 97 words,
  `table_00280f2c` 8 words; per-entry width/count values 0x01-0x2F), interior + trailing
  zero-fill, and a final 4-record packed block (`data_00280ff0`: `0x0280`/`0x01E0` = 640/480
  screen-dimension pairs).

All data is byte-owned and classified (`parsed`/`raw-but-classified`); the packed display-list
blobs (`data_00274a90`, `data_00279da8`) are raw-but-classified (not fully field-decoded). 0 bytes
undecoded-and-unclassified.

## Role leads (conservative; names stay `func_*`)

Mission-briefing/combat display-list module (parent active states `mission_briefing`+`combat`),
continuing chunks 36-38. Region 2/3 are FP/GBI display-list builders + 16-bit-selector message
dispatchers (`andi $a0,0xFFFF` + `beq`/`slti` + `j 0x8020/0x8022` overlay tail-jumps). DATA A's
jump table + float pool feed the dispatchers. Names stay `func_*` pending runtime proof.

## Parent workspace evidence sweep

Searched parent `C:\Users\Joe\Projects\OgreBattlel64` for `0x271000..0x281000` (z64 + RAM forms).
Validated leads (documentation only; names stay `func_*`):

- **ROM `0x0027ADD4`** (`docs/neutral-encounter-mips.md`): a UI/message record builder with a
  duplicate ROM copy at `0x000EC838` (chunk 14), allocating into the `0x800E8350` record pool.
  Owner in this chunk is a CODE region-3 function — recorded as a naming lead only.
- **ROM `0x00278F90`** (`ram_snapshots/overlay_sources.json`): a "combat high-slot" overlay source
  → RAM `0x80224800` (delta `0x7FFAB870`, size `0x3C00`). Concrete overlay-mapping data point for
  the C3 code here; applies only to this span (overlay relocation is NOT linear), still needs
  runtime proof (RSR-001).
- `scripts/ob64_callgraph.json`: 60+ function offsets in range, no semantic labels.

Rejected / false match:

- `0x026261xx` "bounds records" (enemy-system.md) — actual address `0x02626191` (~39.9 MB, ROM
  data tail), NOT `0x0026_2xxx`; out of range, rejected.

## Mistakes corrected / hazards handled

- This chunk's "gaps" interleave DATA and frameless CODE within the same parent gap. Confirmed by
  byte-exact scan: parent gap `0x273FFC..0x275C9C` is DATA `0x273FFC..0x275850` then frameless
  CODE; parent gap `0x279DA8..0x27A06C` is DATA `0x279DA8..0x27A020` then frameless CODE
  (`func_0027A020`). The small parent gaps at `0x27B1B4` (frameless `j 0x80226B54`), `0x27D050`
  (frameless GBI builder), `0x2804D4` (frameless FP) are CODE, not data; the 164 B gap at
  `0x275DC4` is a frameless-leaf cluster.
- 3 interior data islands carved via `build/carve_chunk.js` (multi-region) + 3 code regions sliced.
- Adversarial swarm (4 code + 3 data skeptics): **all 7 structurally CLEAN** (no boundary/tiling
  changes). Fixed only 4 LOW note-text accuracy nits (jumptable row description, float-pool
  "pi-like" labeling, a small-int-LUT byte-count, a stale "Slice end." note).
- check_splits flagged 6 TINY parts (8-12 B) — all verified real frameless leaves (2-3 word
  getter/setter accessors ending `jr $ra`), 0 fragments.

## Verification

`check_boundaries` PASS (0 fragment/cross/under/leak/straddler/data-island); `check_splits`
0 fragments (9 ≤12 B files, all real leaves). Adversarial 7 verifiers all clean. Byte-exact
rebuild preserved (code SHA `40D4E787…B409`; see review handoff).
