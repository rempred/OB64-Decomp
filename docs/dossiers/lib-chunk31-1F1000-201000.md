# Rev 0 Lib Chunk 31 Source Ownership

Range: `0x001F1000..0x00201000`

Status: source-owned as tracked original MIPS. Chunk 31 is **ALL CODE** (no data
territory): FP / GBI display-list-builder-heavy code that is part of the
battle/attack-queue module (the same `0x801CE8BC` owner global threaded through
chunk 30). Both ends are function straddlers.

Part count: **86 total** = 84 normal code + 2 function straddlers + 0 data.

## Region Map

| Range | Owner class | Files | Evidence |
| --- | --- | ---: | --- |
| `0x001F1000..0x001F102C` | incoming function straddler tail | 1 | Tail of `func_001F0F9C` from chunk 30; returns `jr $ra`@`0x001F1024`, delay slot nop `0x001F1028`. |
| `0x001F102C..0x002006E8` | code | 84 | FP/GBI display-list builders, world-map/promotion/char-data consumers, attack/queue helpers, resource loaders. Frameless-leaf and preamble-orphan recoveries (below). Includes the High-Attack cleanup function `func_001F3540`. |
| `0x002006E8..0x00201000` | outgoing function straddler head | 1 | `func_002006E8`: prologue `addiu $sp,-0x58`@`0x002006E8` (no preamble; prior fn delay slot @`0x002006E4`); display-list builder; no `jr $ra` before chunk end; continues into chunk 32 (parent end `0x00201108`). |

## Code recoveries / boundary corrections

- **Parent over-merge un-splits:**
  - At `0x1F102C` (the parent over-merged `func_001F0F9C`'s tail region): split
    into `func_001F102C` (frameless push), `func_001F1050` (preamble-orphan),
    `func_001F10F0` (frameless FP leaf), `func_001F114C`.
  - At `0x1FF7EC`: split into `func_001FF7EC` (framed leaf), `func_001FF830`
    (frameless setter), `func_001FF868` (frameless setter), `func_001FF8E0`
    (preamble-orphan).
- **Frameless flag-accessor cluster** `0x1F8D70..0x1F8DDC`: six tiny
  `lui/(addiu)/lw|sltiu + jr $ra` get/set leaves
  (`func_001F8D70/8D88/8D94/8DAC/8DC4/8DD0`).
- **Adversarial split (HIGH):** former `func_001F8A54` held two functions — split
  into `func_001F8A54` (framed display-list builder, ends `jr $ra`@`0x1F8BD4`) +
  `func_001F8BDC` (frameless display-list builder leaf).
- Other recovered frameless leaves: `func_001F3AE8` (FP leaf).
- Preamble-orphan folds (read-before-write verified): `func_001F1050`,
  `func_001F3540`, `func_001FB1E4`, `func_001FDEC8`, `func_001FF8E0`,
  `func_001FFE80`.
- **Adversarial note correction (MED):** `func_001F309C` keeps its boundary but
  its header note was corrected — the 2-word leading load at `0x1F309C` is a
  DEAD/redundant load, not a read-before-write preamble (the body never consumes
  `$v0` before redefining it; re-anchoring to the prologue `0x1F30A4` would orphan
  the two words as a fragment, so the conservative entry stays at `0x1F309C`).
- Internal-only (not boundaries): the `jr $v0` jump-table dispatchers
  (`0x1F134C`/`0x1F1A98`/`0x1F2718`/`0x1F71D0`-band/`0x1FAEB8`/`0x1FFC5C`/etc.) and
  the `j 0x801Bxxxx`/`0x801Cxxxx` overlay tail-jumps.

The split tool emits no duplicate label for parts whose disassembly body already
carries the parent `function boundary candidate` glabel.

## Data Inventory

None. Chunk 31 has **0 data parts**. The content scan found no RAM-pointer-word
runs, no zero-fill runs, and no genuine ASCII pools; the only "ascii"/"float"
scan hits are FP instructions and in-stream GBI display-list command immediates
(all CODE). Six adversarial verifiers independently confirmed every part has
exactly one terminal `jr $ra` (or is the outgoing straddler), with no hidden data
island.

## Patch Workbench (harvested in-range)

The **Rev 0 High-Attack streamsplit `resource_free` owner-guard** hook site at
z64 `0x001F36F0` falls inside `func_001F3540` (`0x1F3540..0x1F3714`). At
`0x1F36F0` the code calls `resource_free` (`0x800712C4`) with delay-slot
`lw $a0,-0x1744($a0)` (`$a0` = the owner pointer at `0x801CE8BC`), then clears
that owner global. The High-Attack fix hooks this to skip the free for
streamsplit-owned buffers.

Recorded **static-only / `candidate` / needs-runtime** in
`docs/patch-workbench/rev0/patch-workbench-chunks30-31-2026-06-23.json`
(original words, displaced instructions, resume address, delay-slot/register
hazards, overlay/RAM caveats, and the runtime proof still required). NOT proven
from source ownership. Parent evidence: `docs/combat-attack-buffer.md`,
`scripts/ob64_patch_hook_audit.js` (parent workspace).

## Parent Workspace Evidence

- `scripts/ob64_symbols_v2.json` / `ob64_functions.json` assign ROLE TAGS only
  ("character::char-data consumer", "promotion::promotion consumer",
  "dma/resource::resource loader") — used as leads; names stay `func_*`.
- `0x801CE8BC` owner global (z64 `lui 0x801D` / `lw -0x1744`) ties chunk 31 to the
  attack/queue module shared with chunk 30.

Rejected: `0x8025/0x8026` linear-map values; the `0x801F36D8` RAM token table
(→ROM `~0x103ED8`, chunk 16, out of range); `0x801x` RAM values are overlay leads
only.

## Verification

```powershell
node tools/check_manifest.js                       # ALL CHECKS PASS (chunk 31 = 86 parts)
node tools/check_boundaries.js --splits build/chunk_001F1000-00201000_splits.json --disasm build/original-mips/rev0/code_001F1000_00201000.s   # BOUNDARY CHECK PASS
node tools/check_splits.js     --splits build/chunk_001F1000-00201000_splits.json --disasm build/original-mips/rev0/code_001F1000_00201000.s   # 0 fragments
node tools/assemble_original_mips.js               # Exact code-region match: PASS
node tools/verify_setup.js                         # PASS
```

Code-region SHA256 `40D4E7875BA50F005788611C63CF9C42D9154339B36793556BF045C25B64B409`
(unchanged); full ROM SHA256
`571E83396BC81E70DA4C0A20313D82DBD7DFE685F2C37418C8E27F927E2CC67A`.

Adversarial pass: 6 code skeptics. 1 high-severity missed frameless leaf found
and fixed (`func_001F8A54` → `func_001F8BDC`); 1 med note correction
(`func_001F309C`); 4 verifiers clean. All confirmed each part has exactly one
return and no hidden data.

## Next Action

Next frontier is `0x00201000` (chunk 32). Start by continuing the outgoing
function straddler from chunk 31: `func_002006E8` begins in chunk 31 at
`0x002006E8` (clean prologue, no preamble), has no `jr $ra` before the boundary,
and must be emitted first in chunk 32 as `func_002006E8_chunk32tail` starting at
`0x00201000`.
