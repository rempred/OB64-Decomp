# Rev 0 Lib Chunk 32 Source Ownership

Range: `0x00201000..0x00211000`

Status: source-owned as tracked original MIPS. Chunk 32 is **ALL CODE** (no data
territory): **frameless-leaf-dense** FP / display-list + class-def / char-data
code (the parent function DB heavily over-merged the many tiny accessor leaves).
Both ends are function straddlers.

Part count: **198 total** = 196 normal code + 2 function straddlers + 0 data.

## Region Map

| Range | Owner class | Files | Evidence |
| --- | --- | ---: | --- |
| `0x00201000..0x00201108` | incoming function straddler tail | 1 | Tail of `func_002006E8` from chunk 31; returns `jr $ra`@`0x00201100`, delay `addiu $sp,0x58`@`0x00201104`. |
| `0x00201108..0x00210C30` | code | 196 | FP/display-list builders, class-def / char-data consumers, resource loaders; ~50+ frameless leaf accessors recovered from parent over-merges; preamble-orphan folds; jr$v0 jump-table dispatchers + j 0x801Cxxxx overlay tail-jumps internal. |
| `0x00210C30..0x00211000` | outgoing function straddler head | 1 | `func_00210C30`: prologue `addiu $sp,-0xD8`@`0x00210C30` (no preamble); no `jr $ra` before chunk end; continues into chunk 33 (returns `jr $ra`@`0x00211020` + delay `addiu $sp,0xD8`@`0x00211024`). |

## Code recoveries / boundary corrections

- Incoming straddler-tail `func_002006E8_chunk32tail` confirmed (returns @`0x201100`).
- Preamble-orphan `func_00201108` (lui 0x8019/lbu + lui 0x801D/lw read-before-write
  preceding prologue `addiu $sp,-0x30`@`0x201118`).
- Two large parent "gaps" `[0x201424,0x20193C)` and `[0x2023A4,0x202CE8)` were
  parent-undetected frameless code — recovered as real functions.
- The parent DB heavily over-merged tiny frameless leaf accessors (byteswap,
  half-swap, set/clear-bits, table getters); these were un-merged across the chunk
  (e.g. slice [0x20BFA4,0x20CA68) alone held ~34 recovered leaves).
- Many read-before-write preamble-orphan folds (2-word `lui 0x801D / lw`
  global-pointer loads consumed by the following framed prologue).
- jr$v0 jump-table dispatchers (`0x205CE4`/`0x2063A8`/`0x206C48`/`0x2076CC`/…/`0x20AA28`)
  and jalr$v0 vtable dispatchers kept INTERNAL.
- **Adversarial fixes:** 1 missed frameless leaf — former `func_0020156C` was
  over-extended to `0x201598` with a fabricated return note; corrected to end at
  `0x201584` (its real `jr $ra`@`0x20157C`) and split off the frameless leaf
  `func_00201584` `[0x201584,0x2015C8)`. 2 preamble-orphan boundary corrections —
  `func_002091F4` and `func_0020934C` were re-anchored to start at their
  read-before-write preamble (not the prologue 8 bytes later), with the previous
  function's end trimmed accordingly.

The split tool emits no duplicate label for parts whose disassembly body already
carries the parent `function boundary candidate` glabel (4 such header labels
dropped).

## Data Inventory

None. Chunk 32 has **0 data parts**. The content scan found no RAM-pointer-word
runs and no ASCII pools; the only zero runs are the two 3-word alignment gaps
(`0x201424`, `0x20BCD4`) attached to their returning functions. Six adversarial
verifiers independently confirmed every part has exactly one terminal `jr $ra`
(or is the outgoing straddler) and found no hidden data island.

## Parent Workspace Evidence

- `scripts/ob64_symbols_v2.json` / `ob64_functions.json` assign ROLE TAGS only
  (char-data consumer, resource loader, class-def consumer) — used as leads; names
  stay `func_*`. The parent DB's over-merges/missed-leaves were corrected from
  byte-exact disassembly.
- No patch-workbench metadata was harvested for chunk 32 (the High-Attack hook
  sites are in chunk 33). No in-range editor/patch hook sites.

Rejected: `0x8025/0x8026` linear-map values; `0x801x` RAM values are overlay
leads only.

## Verification

```powershell
node tools/check_manifest.js                       # ALL CHECKS PASS (chunk 32 = 198 parts)
node tools/check_boundaries.js --splits build/chunk_00201000-00211000_splits.json --disasm build/original-mips/rev0/code_00201000_00211000.s   # BOUNDARY CHECK PASS
node tools/check_splits.js     --splits build/chunk_00201000-00211000_splits.json --disasm build/original-mips/rev0/code_00201000_00211000.s   # 0 fragments
node tools/assemble_original_mips.js               # Exact code-region match: PASS
```

Code-region SHA256 `40D4E7875BA50F005788611C63CF9C42D9154339B36793556BF045C25B64B409`
(unchanged); full ROM SHA256
`571E83396BC81E70DA4C0A20313D82DBD7DFE685F2C37418C8E27F927E2CC67A`.

Adversarial pass: 6 code skeptics — 5 clean, 1 high (the `func_0020156C` leaf
split above). The two earlier preamble-orphan corrections were applied before the
adversarial pass.

## Next Action

Next frontier is `0x00211000` (chunk 33, MIXED). Continue the outgoing function
straddler from chunk 32: `func_00210C30` begins in chunk 32 at `0x00210C30`
(prologue `addiu $sp,-0xD8`, no preamble), has no `jr $ra` before the boundary,
and must be emitted first in chunk 33 as `func_00210C30_chunk33tail`
`0x00211000..0x00211028` (returns `jr $ra`@`0x00211020`).
