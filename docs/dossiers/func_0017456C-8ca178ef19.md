# func_0017456C: Preserved Matching-Workbench Candidate

## Status

This source is a research candidate. The original assembly remains the accepted owner.

- Candidate: `8CA178EF19BC95076BB9D0FDD3F3559CE6F3B9849B89BEF224933CD37634C89C`
- Target: `func_0017456C` at ROM `0x17456C`
- Source: `docs/archive/matching-c-candidates/2026-08-25-func_0017456C-8ca178ef19.c`
- Source SHA-256: `8C5F9C14031FE83832E9104EB62B2E9C912738848668503B37D80A675C9AAE2C`
- Latest scratch class: `length-mismatch`
- Latest scratch score: `27.88`

## Preservation reason

Resolver-corrected class-change UI state machine; best type-correct pure-C reconstruction after mapping nine false external jumps to accepted internal labels. Exact frame/save set, 688/701 instructions, 577/701 opcode LCS; remaining differences are distributed CFG, addressing, branch-likely, register-coloring, and scheduling gaps, not a localized hybrid candidate.

## Evidence boundary

Scratch object comparison does not prove canonical linker ownership, relocation resolution, target bytes, or full-ROM identity. Resume through the normal diff and verification workflow.

## First recorded difference

```json
{
  "actual": "beq $v0, $zero, 0x00175018",
  "actualWord": "0x104002A4",
  "expected": "beq $v0, $zero, 0x0017504C",
  "expectedWord": "0x104002B1",
  "index": 6,
  "offset": 24,
  "pc": 1525124
}
```

## Accepted target and generation coverage

- Accepted owner: ROM `0x0017456C..0x00175060`, 2,804 bytes / 701 instructions.
- Retail shape: 96-byte frame, 132 basic blocks, 33 direct calls, and one indirect jump.
- The complete configured eight-ruleset ensemble was attempted. All eight initial generations stopped at the unresolved 103-entry table. After placement repair, structured, goto-preserving, and stack-struct m2c shapes were regenerated. The five specialized structured transform families were evaluated against the repaired source and were mechanically inapplicable, so they add no distinct source for this target.
- Repaired baseline results: structured and stack each scored `20.92` at 2,664 bytes / 666 instructions with a 72-byte frame and 131 blocks; gotos scored `19.81` at 2,656 bytes / 664 instructions with a 72-byte frame and 129 blocks.

## Placement and control-flow correction

Total Resolver places ROM `0x0017456C` at live `0x8021764C` in the exact direct-contiguous ROM/DMA slab, a delta of `+0x800A30E0`. This maps runtime table `0x8021AC30` to ROM `0x00177B50`, where the accepted 103-entry table has 15 distinct destinations and every destination lies within this function.

The same placement proves nine disassembler symbols were false external-function identities and are actually internal branches:

| Live target | Accepted ROM label |
| --- | --- |
| `0x802179E4` | `0x00174904` |
| `0x80217CA8` | `0x00174BC8` |
| `0x80217D14` | `0x00174C34` |
| `0x80217D88` | `0x00174CA8` |
| `0x80217DFC` | `0x00174D1C` |
| `0x80217E70` | `0x00174D90` |
| `0x80217ECC` | `0x00174DEC` |
| `0x80218124` | `0x00175044` |
| `0x8021812C` | `0x0017504C` |

Those corrections were made only in ignored analysis assembly. Accepted assembly, boundaries, and ownership were not changed.

## Best recovered C

Recovering the two apparent stack-local triples as fields in one 40-byte address-escaped scratch object naturally reproduces retail's 96-byte frame and exact `$s0` / `$s1` / `$ra` save set. Correcting byte, signed-word, and pointer access widths then yields this preserved candidate:

- 2,752 bytes / 688 instructions;
- exact 96-byte frame and saved-register set;
- 133 blocks versus retail's 132;
- opcode-sequence LCS 577/701 (`82.31%`);
- first six instructions structurally and register-wise equal, with the first recorded word difference being only the epilogue branch displacement caused by the shorter body.

An earlier broad-word-typed draft scored slightly higher (`27.95`, candidate `CFDF6459722ABED3ECA9C574CE41EB23F5644FF66F781F52A64B6523A21322C1`) and emitted 691 instructions, but its incorrect word accesses reduced opcode LCS to 551/701 and changed required byte semantics. It is not the preferred source hypothesis.

## Semantics and remaining blocker

The recovered function is the class-change UI/menu state coordinator behind the 103-entry handler table. It advances a state word, coordinates transition helpers, moves two word and three byte presentation values toward target values, manages fade/timing bytes, and initializes or clears UI/resource state. It is not the promotion eligibility formula. This agrees with the parent research classification of ROM table `0x00177B50` as class-change UI handlers rather than eligibility checks.

The remaining differences are spread across almost every active case: absolute-global address reuse, state-update join placement, branch-likely lowering, signed clamp expression lowering, register coloring, delay-slot scheduling, and case fallthrough topology. A hybrid would need to replace broad portions of the 701-instruction dispatcher, not a small residual fragment. That fails the localized-hybrid gate, so the accepted assembly remains in place.

No linked diff, verifier, ownership change, or full-ROM build was run for this nonexact target.
