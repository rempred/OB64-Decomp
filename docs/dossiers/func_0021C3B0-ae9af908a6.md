# func_0021C3B0: Matching-C Acceptance

## Status

The preserved research candidate was completed and promoted as the canonical C owner.

- Target: `func_0021C3B0` at ROM `0x21C3B0`
- Active source: `src/battle/func_0021C3B0.c`
- Active source SHA-256: `BE8E96C64511E82F3674FAC8A94BC4C23F6B82C7217E7F130F81F4099D90732E`
- Source class: `PURE_C`
- Extent: `512` bytes / `128` instructions
- Relocations: `27`, contract matched
- Target and linked SHA-256: `DC90E3EE150A784F0E79C93D488526E631F0D4BDE8CEDEC97E888850AE2C9194`

## Resolution

The preserved candidate already reproduced the five-register loop, table split, field
writes, and helper-call order. Three source-shape corrections closed the remaining gap:

- expressing the 20-entry traversal as a `for` loop let KMC form the retail
  branch-likely exits and restored the missing loop-increment instruction;
- retaining the side selector as `s32` produced the retail signed `slti`; and
- staging the indexed table address through the existing byte pointer preserved the
  retail operand order in the otherwise commutative `addu`.

The initial canonical link after the loop and signedness corrections had decoded
instruction equality and only one raw word mismatch at offset `0x184`. The staged
pointer update changed `addu $v0,$v0,$s2` to retail's
`addu $v0,$s2,$v0`, producing exact raw bytes.

## Acceptance evidence

- `node tools/diff.js func_0021C3B0`: decoded rows exact, raw linked bytes exact,
  target SHA exact, and relocation contract matched.
- `node tools/verify.js --target func_0021C3B0 --require-pure`: baserom,
  toolchain, source policy, sole C ownership, placement, relocations, target bytes,
  and full ROM all passed; result `MATCHING C`.
- Generated verifier totals at acceptance: `460` `PURE_C` functions / `32416`
  bytes and `63` `HYBRID_C` functions / `33960` bytes.

## Preserved provenance

The prior workbench source remains archived at
`docs/archive/matching-c-candidates/2026-09-03-func_0021C3B0-ae9af908a6.c` with
candidate id
`AE9AF908A6D51866F66626965DD0A675581B2FA10A3A87A84FAA871F793743E0`.
Its source SHA-256 is
`D79EF0B5A6CB317CD263C4486FB8AECFA28E790E342DED794B1C91E344F739DE`.
