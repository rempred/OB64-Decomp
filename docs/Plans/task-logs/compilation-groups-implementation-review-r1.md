# Compilation-group implementation review

**Verdict: Accepted.** Completed. The frozen implementation satisfies the accepted native compilation-producer design and its corrected raw `.reginfo` contract within the declared first-mode boundaries. No admissible material finding remains. The Director may propagate this tooling result; production source activation remains a separate complete-wave action because the reviewed registry is intentionally empty.

Required Director action: record the Accepted verdict and preserve this review record. The Director may then route a separately authorized complete compilation-group source wave through the normal matching workflow and its one final changed-input verifier.

## Frozen subject and eligibility

This review covers canonical commit `45904b577d67de81acad661a0c8286e01c43ee4c`, compared with launch baseline `5c0507e44035c2d9bfbd86e6bd5be67ffdce8a77`. The subject commit is single-parented at `c210cf2`. The accepted design is `5dad709` plus correction `b87e908`, with Accepted design review `0840b42`.

Assignment `compilation-groups-implementation-review`, revision 1, launch `COMPILATION-GROUPS-IMPLEMENTATION-REVIEW-20260906-01`. Reviewer `/root/compilation_groups_implementation_review`; Director `/root`, native task `01a07262-aeca-7341-ad10-2dba705ff988`, host `local`. The complete claim was created atomically at `docs/Plans/task-logs/compilation-groups-implementation-review-r1.claim.json` before any other review write.

The worker reported completed, supplied the required evidence, and released all writes. I did not produce the implementation. The exact implementation-owned production paths had no diff from the frozen subject during review; later main commits were disjoint program or research documentation. Production source, active-target/linkage records, owner boundaries, and the frozen worker evidence were not modified.

## Claims reviewed

I reviewed whether the implementation:

- admits one authenticated native compiler producer only for a strict ordered census of contiguous accepted function owners;
- preserves bytes, public functions, the group-base section anchor, encoded addends, relocation identity, native tail, and derived owner alignment through projection;
- proves raw, projected, stripped, linked, map, manifest, fallback-removal, and complete-ROM evidence without weakening existing structural checks;
- classifies and compiles a group once while retaining one accepted result per member;
- invalidates CURRENT and diff-cache reuse for changed group, source, dependency, tool, configuration, or implementation identities;
- rejects unsupported workbench, auxiliary, multi-owner, multi-function, mixed-slice, partial-member, stale-report, and malformed metadata shapes;
- preserves the art-native, legacy multi-owner, ordinary target, source-policy, cache, status, and accounting contracts; and
- leaves the production group registry empty and activates no source.

## Review method

I traced the accepted design requirements through the frozen diff and directly inspected the complete new producer module and the affected active-target, source-policy, text projection, Phase 8 build/verification, CURRENT, cache, diagnostic, manifest, linker, status, and test paths. I checked supported object-path consumers for stale standalone assumptions and inspected the raw/projected/stripped ELF checks separately from final linked-byte checks.

I made a reviewer-owned snapshot from `git archive` of the subject, SHA-256 `93107CF34D88DC5F5EC43DB4856C495B675ADC2BAA90161A4E7B480CCCEB428D`, then supplied the existing baserom, local tool configuration, and authenticated toolchain under the review root. Git archive stores canonical LF blobs while the Windows checkout materializes the reviewed registry and module with CRLF. I overlaid those two unchanged operational copies only after confirming a normalized subject comparison, so the focused replay used the same 53-byte registry identity as the frozen audit. A temporary `R:` path alias kept the pinned historical compiler below its Windows path-length limit; the alias was removed after each run.

I independently reconciled the preserved changed-input audit package, all 564 build/fresh/verification companion records, the fingerprint-specific object manifest, and the worker's exact focused and legacy evidence paths. I also added a synthetic two-member binding probe with an interior 12-byte boundary to test derived alignment, owner-relative/group-relative relocation evidence, and direct workflow rejection boundaries not dependent on the worker fixture.

## Findings

No admissible material findings.

The group registry schema is closed and binds every configured member to one accepted, unambiguous owner in a common placement context. The normalized producer covers the full ROM/RAM extent, derives alignments as 16 and 4 in the independent nontrivial-boundary probe, and assigns each relocation both owner-relative and group-relative places. Raw, projected, and stripped validation preserves the complete function and relocation census, anchors the original section symbol to the first owner, keeps text bytes and `.reginfo` payload stable across projection, and requires `.reginfo` removal before link.

Compilation, classification, caching, manifest collapse/expansion, linker selection, source-object proof, map ownership, linked placement, and status accounting all resolve through the unique producer while retaining complete member records. The canonical workbench rejects a grouped member with the required complete-group diagnostic. The changed group-registry identity changes the recomputed CURRENT fingerprint, and a recorded build with a stale identity rejects before acceptance.

## Reused frozen evidence

The Director-preserved copies under `build/sequential-main-intake/compilation-groups-implementation-r1/` match their assigned hashes:

| Evidence | SHA-256 |
|---|---|
| `audit-report.json` | `C84FC8504ACB9E0C3FBD9495BA86A82AD380B2DDEE6F1E5B789D12A46CC4CD70` |
| `current-build-report.json` | `61319F3F1667D1B80F8D9F70DAC4B20653929893D5C61C333689D093ABC05998` |
| `current-state.json` | `B7038AE56578E0D164BEDF358BEC1950A07FDD018463691D47C44200C8EF8F18` |
| `current-verification.json` | `2A837B56D02452A333704C31DBB70334831F7F7C6BD181A0F58FF1F4F64A6FAF` |
| `fresh-compilation.json` | `3C70064F8B11167109705110D991E360DF75453D81BB435FA488FB1E583B1A6D` |
| `setup-report.json` | `D1C16E156BC6201E3C523953C2A39D6BEEFA4FC0D376F48AF57203E26FBA50DC` |

The companion state authenticates CURRENT fingerprint `6CD705C5F9B66EC2FDE7C2D3504D79B04318B4B907B8D5006E8AAD13197C1A70`. I recomputed that value from the frozen production inputs and all 564 preserved source-policy records. The build report authenticates `config/matching-c-compilation-groups.json` as 53 bytes with SHA-256 `C1DD4D66A62ED91ED83616E310ADE37F2D22FF11C55225D7B8BF3A6F23F04834`; its `groups` array is empty. The audit, fresh compilation, build, and deterministic verification agree on 564 targets, 499 `PURE_C`, 65 `HYBRID_C`, zero `ASM`/`UNKNOWN`, and an exact 41,943,040-byte ROM with SHA-256 `571E83396BC81E70DA4C0A20313D82DBD7DFE685F2C37418C8E27F927E2CC67A`. Every build target's text/object evidence equals its fresh companion, every text/object/link record equals its verification companion, every linked owner is exact, and the fingerprint-specific production manifest contains zero group records.

The worker focused report remains at `build/compilation-groups-implementation-r1/focused-7sG31m/report.json`, SHA-256 `00E0CAA0A7E228501090B0A7C785E2E7BE653CA81AB507FF8D7F5A823760B9E9`. The preserved mixed Phase 8 output contains exactly one group manifest record, `objects/c/groups/pose_fixture.o`, with all five ordered pose members and one producer object. Its complete ROM independently hashes to the canonical value above. The preserved art-native regression output also hashes to the canonical ROM and contains no group manifest record.

## Tests and results

| Check | Result |
|---|---|
| Reviewer `review-probes.js` | PASS. Reconciled six preserved proof hashes; checked 564 build/fresh/verification companions; recomputed the exact CURRENT fingerprint; proved registry-identity sensitivity; checked zero production group records; exercised 11 direct rejection controls. Result: `build/compilation-groups-implementation-review-r1/review-results.json`, SHA-256 `9D504BEC61B1A545DD53472AF7D171E4D6C89C0E8368FB2C4AA3F4A63C00B8E4`. |
| Frozen reviewer copy, `node tests/compilation_groups.js` | PASS. Two PURE_C groups: 80 bytes with two functions, four relocations and eight tail bytes; 16 bytes with two functions, no relocations and zero tail. Split/unsplit link checks and 68 negative controls passed. Log: `focused-test-r3.log`. |
| Frozen reviewer copy, `node tests/active_targets.js` | PASS, 564 targets. |
| Frozen reviewer copy, `node tests/source_policy.js` | PASS. PURE_C/HYBRID/ASM/UNKNOWN, macro-hidden assembly, dependency invalidation, external dependency, path, duplicate dependency, and executable-identity controls passed. |
| Frozen reviewer copy, `node tests/diff_object_cache.js` | PASS. Cold/warm reuse, 14 invalid-entry rebuilds, requested-target freshness, sibling isolation, configuration sealing, and source-object-prefix evidence passed. |
| Frozen reviewer copy, `node tests/multi_owner_text.js` | PASS. Legacy split/unsplit behavior and eight malformed-evidence controls passed; the intended cross-section branch rejected. |
| Worker `node tools/test.js` | Reused exact log: PASS, 16/16 suites in 35.3 seconds. |
| Worker mixed Phase 8 group fixture | Reused and independently inspected: PASS, six PURE_C target results, five grouped members, one producer, exact complete ROM. |
| Worker art-native regression | Reused and independently inspected: PASS, exact complete ROM and 22 retained malformed-evidence/raw/link/cache mutations. |
| Worker final changed-input audit | Reused exact preserved package: PASS with the authenticated compilation-group registry, fresh source-to-object proof, structural gates, and exact complete ROM. Per assignment, I did not rerun this unchanged full audit. |

The first focused replay attempt stopped before the fixture because a Git archive omits ignored `.toolchains`; the second reached the compiler and stopped on the review snapshot's long absolute path. Both observation-environment logs are preserved. Supplying the authenticated ignored toolchain and shortening only the path through the temporary alias produced the passing third run. Neither early stop exercised or contradicted the reviewed implementation.

## Evidence limits

The production registry is empty, so the full audit proves unchanged production behavior and authenticates the new registry/tool inputs but does not itself exercise an active group. General group behavior is established within the declared initial shape by the two independent pinned-compiler fixtures, the exact mixed pose fixture, direct synthetic binding falsifiers, strict artifact replay, and legacy regressions. This verdict does not claim an original retail compilation-unit boundary, semantic behavior beyond exact linked bytes, support for auxiliary/local/multi-owner combinations, or acceptance of any future source wave.

No emulator or live runtime was used. No full audit was repeated. No implementation repair, source/configuration edit, branch, worktree, stage, commit, or push was made.

## Documentation consequences and route

The implementation's updates to `docs/WORKFLOW.md`, `docs/SOURCE_POLICY.md`, and `docs/AUDIT.md` accurately describe the accepted initial producer contract. No further canonical documentation change is required by this review. The empty production registry and the distinction between tooling acceptance and source-wave acceptance must remain explicit.

Exact next route: return this Accepted verdict to Director `/root`. The Director may commit and propagate the released review record and reviewed tooling. Any first production group activation must be routed as a separate complete matching wave, preserve the reviewed structural boundaries, and pass the normal final verifier for its changed integrated inputs.

All reviewer-owned report and ignored evidence writes are released to the Director.
