# A9C complete-owner preparation r1

Completed read-only preparation. This preparation covers the complete 3,372-byte owner, including its 1,992-byte continuation.
Director /root must bind preceding accepted action-mode results before any future source activation.

Task combat-resolution-a9c-preparation, revision 1; launch COMBAT-RESOLUTION-A9C-PREPARATION-20260907-01.
Receiver /root/sequential_scope; Director native 01a07262-aeca-7341-ad10-2dba705ff988; local; Astra Medium.
Ready dd41986a6560806cfa0993678ca4b001df5c089a; starting HEAD/activation 3c4b5ad657f4071cd39eacc97309b9afcf2126e6.
Coordination e9ba46d2e746be0d31c06400bfa566ec727025fd; accepted source W7 469a1416918592749d61dc34e3e079796f7b673c.

Fresh claim created atomically and read back before other writes. Report and ignored root were absent.
Current W8 source/configuration, disjoint allocation research and held Resolver files remain untouched.
Required guides and research-aide routing are reused from prior preparation; current assignment and seven-member draft were read.
Completed: authenticated relevant corrected inputs and complete ROM words, then inspected storage, transfers, helper contracts and data references.
Main hypothesis: the accepted two-part owner is reconstructible as one complete function with real escaped local objects.
Alternative: head-only interpretation or invented scalar storage would omit continuation state and output objects.
All word, owner or placement mismatches stop this preparation. Static evidence does not establish runtime behavior or matching acceptance.

## Completed result

Completed read-only preparation. All 843 instructions match the canonical ROM across both physical parts.
The complete owner needs an 80-byte mutable state and an evaluator output region with an unresolved path-specific count bound.
Its float lookup consumes words from two existing data rows. Director must bind current preceding-wave contracts before future implementation.
No source, compiler, build, runtime, database, shared-tool or Git mutation occurred. No new structural, semantic or matching acceptance is claimed.

## Scope and identity

| Neutral name | Meaning in this note | Address | Address space | Evidence role |
|---|---|---|---|---|
| Complete A9C owner | Assigned function, including continuation | `[0x00230A9C,0x002317C8)` | z64 ROM | All 3,372 bytes / 843 words compared. |
| Head | First physical source part | `[0x00230A9C,0x00231000)` | z64 ROM | 1,380 bytes / 345 words. |
| Continuation | Second physical source part | `[0x00231000,0x002317C8)` | z64 ROM | 1,992 bytes / 498 words. |
| Accepted placement | Same logical owner in resource-loader-0022a280 | `[0x801ED7CC,0x801EE4F8)` | RAM virtual address | Accepted equal-length projection, delta `0x7FFBCD30`. |
| Float lookup | Locally bounded numeric factor reads | `[0x00239BA0,0x00239BC8)` | z64 ROM | Ten words; no indirect dispatch. |
| Float lookup placement | Accepted slab projection | `[0x801F68D0,0x801F68F8)` | RAM virtual address | Literal indexed load at ROM `0x231444`. |

Corrected package SHA256 is `0EFA3D1990C347FBCCFCA32666FE6430BFDD0491E780B43D4F91CC68787BC1B1`.
Its correction report SHA256 is `0CF8D2C24D4894C79A1109A9C71D5F47A8AE1A662C75053684008804D6E453AB`, frozen at `740732e18b1f3e5808b167572662473918d35c2d`.
Seven positive per-call alias records are reused. Same-address aliases establish no new callee, execution context, ownership or meaning.
The original retrieval's empty-alias claims remain superseded; no lookup investigation was repeated.

Original W7 source parts are `asm/original/rev0/lib/func_00230A9C.s` and `func_00230A9C_chunk35tail.s` in the same directory.
Their SHA256 values are `3967B2372FC919C9F6F4BD63F7B8945B8D1CC3D81F6457B5440A882476B81471` and `8D5747B8A1B6F85B9161790EF65409D501D5259EEBD59D3F83FA1D12FB35385B`.
Canonical normalized ROM is 41,943,040 bytes, SHA256 `571E83396BC81E70DA4C0A20313D82DBD7DFE685F2C37418C8E27F927E2CC67A`.
Complete owner bytes have SHA256 `417B196DAF15E25FC61AEB38EE4B408B7BF5583B56349AE8BB942D1FAF33C1E6`.

`evidence.json` binds 19 relevant original-package Git inputs, 20 supporting W7 inputs and two current preparation inputs.
The package's older draft is retrieval provenance; the controlling draft is separately authenticated at coordination `e9ba46d2e746be0d31c06400bfa566ec727025fd`.
Frozen F580 report at `87a8e9ebb3bc3932cb7dff0a8e85e4a40ef0fd1c` retains SHA256 `8BF67422AE0FFD1DA0A02F2EFE6B8EB6C9811D6ABE9BF35C2B994A4B7E806EB7`.
It supplies directly shared interfaces only. Its count and combined-table limits are unchanged.

## ABI and storage

Incoming `a0` points to three word-sized object-pointer slots. Incoming `a1` points to a mutable word and is retained at adjusted `sp+0xBC`.
No incoming `a2/a3` or incoming stack argument is consumed as a public argument.
The frame is `0x160` bytes. Saved `s0..s8` occupy `0x138..0x158`; `ra` occupies `0x15C`.
The function returns explicit zero or one in `v0`. Rejection can follow object `+0x68` writes or helper side effects.
An early helper-completion path returns one. The normal path writes the caller word before returning one.
The common restore starts at ROM `0x231798`; `jr ra` at `0x2317C0` restores the frame in its delay slot.

All offsets below use adjusted `sp`. These are observed footprints, not invented declarations covering gaps.

| Storage | Observed footprint and obligation |
|---|---|
| `0x20/24/28` | Three raw result words. Each starts at `-1`; qualifying objects supply a helper result also stored at object `+0x68`. Excluded pair members retain/reset their sentinel. |
| `0x30/34/38` | Compacted selected object pointers. Successful grouping supplies exactly two or three entries; only that selected count is traversed. |
| `0x40+4i` | Three possible word values used for per-selected scheduling arithmetic. Preserve signed and unsigned comparisons separately. |
| `0x50+4i` | Three possible helper result words, or explicit zeros on the alternate path. Later publications and bookkeeping reuse them. |
| `0x60..0x68` | Exactly eight bytes cleared at entry. Passed as third argument to `func_001F197C` only at ROM `0x231120`. Caller reads its first word. |
| `0x68..0xB8` | Exactly 80 copied bytes, five 16-byte iterations from nested record `+0x44..+0x94`. Passed as mutable state to `001F0E04`, `001F0E64` and `001F197C`. Four copies refresh this region for different paths; preserve each copy. |
| `0xBC`, `0xC4`, `0xCC`, `0xD4` | Caller-word pointer, evaluator result and retained calculation words. They survive calls and final bookkeeping. |
| `0xDF`, `0xE7` | Separate bytes captured from selected target `+0x57/+0x5B`. Later passed as zero-extended ABI words. |
| `0x10..0x1C` | Outgoing fifth through eighth arguments. They are not independently escaped local objects. |

Accepted `func_001F197C(object,state,int*out)` writes a variable word sequence and a `-1` terminator when `out` is non-null.
It guarantees an initial zero and, with no emitted entries, a zero followed by `-1`.
Its general implementation does not bound count to one. An eight-byte clear cannot establish that bound.
Here, a second emitted entry would place the terminator at `sp+0x68`, where the copied state begins.
The future writer must establish the selected-path output bound or faithfully account for actual aliasing before choosing declarations.
This note neither declares an overflow nor fabricates a larger buffer. Other evaluator calls pass null output explicitly.
When `0020BFE4` returns zero after evaluation, this owner overrides evaluator result with 15 and the first output word with 10.

`001F0E04(state,raw_selector)` writes state halfwords/bytes and initializes embedded arrays.
`001F0E64(state,raw_selector)` also reads state byte `+0x48`; the full 80-byte copy preserves that byte beyond the evaluator's named fields.
Its raw-zero path can call `rand` internally. Literal caller call counts therefore do not represent total generator execution counts.
Neither helper has an active named C target in the bounded registry read; original ASM contracts control these interfaces.

## Complete control and continuation

The following consecutive ROM intervals cover all owner instructions. Detailed transfers and delay slots are in the evidence maps.

| ROM interval, exclusive end | Obligations |
|---|---|
| `230A9C..230BA4` | Initialize output and three sentinels. Scan three input slots using `20C104`, `20C2C0`, `20C32C`, `20C2F4`, `20C310`. Require object `+0x6C` equals global context word and signed `+0x6C < +0x70`. Store `233ED4` result in local slot and object `+0x68`. |
| `230BA4..230D1C` | Select three equal non-sentinel results, or one of the three equal pairs. Compact corresponding pointers and resolve target with `20C478`. For two selected objects, process non-null excluded object through F2BC, then query `20F8DC`. Preserve early zero/one returns and later `20D814/20C2C0` rejection. Capture target bytes `+0x57/+0x5B`. |
| `230D1C..230EB0` | Apply EF50 to selected objects. Compute initial unsigned maximum from target `+0x94` and caller word. Conditional `22222C` results produce per-selected differences; alternate path supplies zeros. Then signed deficit/max calculations synchronize times. Increment global context byte `+0x818`, publish `0x1C`, increment object `+0x6C`, and retain updated `+0x94`. |
| `230EB0..231000` | Narrow `20C448` result to halfword before `000454E0`, then narrow its result to byte for `0x3E` publications. Re-query `20BFE4`; conditional `0x03` publications precede 80-byte copy, selector 6 reset and null-output evaluation. Publish `0x05` and add evaluator result to selected local values. |
| `231000..231144` | Query `2361EC`, `2363FC`, `2365BC`, then preserve three ordered `rand` calls. Build unsigned modulo-100 comparison. Refresh state copy. Derive selector raw mode 0,1,2 through conditional `201584`, call `201798`, narrow byte and reset state. Evaluate with non-null output; preserve override result 15/output 10. |
| `231144..231368` | Per-selected `0x06/0x07` publications depend on comparison and repeated `20BFE4` queries. Derive retained start/end words. Conditional seven-argument `0x17` uses subtype `0x5D` or `0x5F/0x60`, target helper result, byte value and selected object `+0x68`. Preserve signed endpoint comparisons and raw timing choices 5/25 or 5/15. |
| `231368..2314F8` | On initial comparison success, reset count and scan three nested pointer slots per selected object. For each non-null slot, repeat threshold helpers and call distinct generator `00022E60`; signed remainder/threshold result increments count. Load numeric float factor and preserve FP conversion chain. Call `23697C` with six arguments, then retained constant-branch arm and eight-argument ADFC. |
| `2314F8..231654` | Alternate path queries `201584`, copies target state, selects raw `0x17/0x15` through `1F0E04`, evaluates with null output and publishes `0x0E`. Refresh state, reset with `0x18/0x16`, evaluate again and publish `0x0F`. Preserve repeated use of the first evaluator result when calculating final target time; do not algebraically remove it. |
| `231654..231798` | For each selected object, conditionally call six-argument `2223E0`, then publish `0x08/0x09`. Store fields `+0x84/+0x88/+0x8C`; clamp negative `+0x8C` to zero using branch-likely behavior. Update `+0x94`. Write caller word from first selected base plus fields `+0x84/+0x88`; return one. |
| `231798..2317C8` | Restore all saved registers and return, including final delay slot. |

Seven explicit head-to-continuation branches are recorded: five reach the common epilogue; two reach continuation start.
Their sites are ROM `230C84`, `230C90`, `230CD4`, `230CEC`, `230CFC`, `230F00`, `230FB4`.
Normal loop completion also falls through from delay instruction `230FFC` to `231000`.
Selected count `s7`, target pointer `s5`, narrowed value `s4`, local pointer/value arrays and caller pointer remain live across that seam.
There is no continuation entry prologue or new public argument contract. No direct transfer returns from continuation to head.

At ROM `231490`, `t2` is explicitly zeroed; the next branch always selects the arm at `2314AC` under the recorded instruction stream.
The intervening literal call at `23149C` remains part of the accepted owner bytes and the 69-site map.
Preserve this matching obligation without claiming the call executes, omitting its bytes, inventing a volatile condition, or proposing a compiler escape hatch.
No compiler-shape conclusion was tested.

## Randomness, numeric lookup and ownership

Literal `rand` sites are ROM `23102C/231034/23103C`.
Combine results as `((r0<<18)&0x0C000000)|(r1<<15)|r2`, followed by unsigned remainder 100 and unsigned threshold comparison.
The nested non-null-slot loop instead calls `00022E60` at ROM `2313D0` and uses signed remainder 100 and signed comparison.
Preserve generator distinction, state effects, call order and helper re-queries. No probability or gameplay meaning is inferred.

The loop resets its count to zero, visits at most three selected objects and three nested slots each, and adds comparison results 0 or 1.
Thus the numeric lookup index is locally bounded to `0..9`; zero remains possible after initial success.
The ten IEEE-754 words are `00000000,3F19999A,3F4CCCCD,3F800000,3FA66666,3FCCCCCD,40000000,40133333,40266666,40400000`.
`float-map.md` records exact words and decoded values. Consumed 40 bytes have SHA256 `FF14CDC354EF9E643165F42DF22C131772040BCC34DD7A8D343CC9B221A3C184`.
There is no indirect dispatcher: the only indirect transfer is final `jr ra`.

The owner converts helper result from signed integer to single precision, multiplies by the factor, truncates to integer, then converts back to single precision.
It passes those float bits in `a2` to `23697C`; the callee confirms `mtc1 a2,f24`.
Do not replace this conversion chain with an integer product, double-precision calculation, or direct untruncated float argument.
Fourth, fifth and sixth arguments are zero. No incoming floating argument or saved FP register is present in A9C itself.

| Existing data owner | Accepted extent, ROM | Read obligation |
|---|---|---|
| Row 4244, `primary:5a1e8093830fed1ea023`, `zero_fill_00239b94.s` | `[239B94,239BA4)`, 16 bytes | Index zero reads its last four bytes at `239BA0`. Full-row ROM SHA256 `374708FFF7719DD5979EC875D56CD2286F6D3CF7EC317A3B25632AAB28EC37BB`. |
| Row 4245, `primary:aeadab66e54ef6f468eb`, `float_00239ba4.s` | `[239BA4,239BD4)`, 48 bytes | Indices 1..9 read first 36 bytes. Remaining 12 bytes stay outside this locally consumed footprint. Full-row ROM SHA256 `27EA1BFE7D43158826D61A85B4582696192C46A1EA17736394474914D5E799D7`. |

W7 has no A9C active C/linkage/multi-owner contract and no auxiliary contract overlapping these data rows.
Bounded current registry snapshots also contain no A9C target records; their hashes are evidence snapshots, not a new accepted source baseline.
The future writer must bind current preceding-wave ownership and account for actual emitted numeric data while preserving both complete existing rows.
The consumed footprint does not authorize a new partition, row merge, padding definition, overlap or boundary change.
Route any demonstrated representation requirement separately under existing structural/tooling rules.
F580's distinct table_00239ec0 prerequisites remain unchanged and are not re-investigated here.

## Helper interfaces and resumption

`helper-map.md` lists all 38 literal target addresses, 69 call sites, accepted placement projections and corrected aliases.
`evidence.json` retains eight preceding and three following instructions around each call, all 158 direct transfers, 293 memory references and nine FP instructions.
`owner.txt` contains every instruction in both parts. Original branch-comment addresses remain legacy decoding aids; ROM/VMA columns and decoded transfer targets use accepted placement.

Accepted command wrappers forward three (`D200`), four (`D230`), five (`D25C`), six (`D28C`) or seven (`D2C0`) ABI words.
Preserve each outgoing count and narrowing; do not infer a variadic shared prototype.
ADFC consumes eight arguments: target, zero, calculated time, result, zero, byte-zero, one, and zero on the recorded constant branch.
Its original prologue reads the sixth argument as a byte from the big-endian stack word.
`22222C` consumes selected object, target and selected fields `+0x5C/+0x64`; `2223E0` additionally consumes captured target bytes before those stack fields.
`201798` retains its accepted four-argument selector interface; the returned byte is masked before reset.
The zero helper takes `(destination,size)`, not a three-argument standard memset.
Shared evaluator, selector and publisher interfaces were reused from F580 without repeating its owner analysis.

The future sole writer must first bind the complete preceding seventeen-member action-mode wave and newest accepted baseline.
Next, settle the output count/aliasing contract and real local declarations, then reconstruct all control regions across the physical seam.
Preserve numeric table words, generator calls, constant-branch bytes and complete final bookkeeping before compiler-shape iteration.
Keep both original ASM parts available until replacement handling is proven.
The seven-member final wave remains `0022F580,00230A9C,002317C8,00231CC0,00232280,00232458,00232D7C`.
Use focused linked diffs only after future source activation, then one combined final verifier after all seven are ready.
All original family and High Attack obligations remain intact. W8 remains the sole production wave during this preparation.

## Verification, limits and handoff

The bounded Python extractor ran successfully against exact Git inputs and canonical ROM; no compiler or acceptance verifier was invoked.
Instruction coverage, all non-call direct destinations, physical-part conservation, float entries and output hashes were checked.
Two guessed helper C paths were absent; exact registry/original ASM resolved the inputs. This was a retrieval correction, not a game finding.
Git status reported inaccessible global-ignore/cache warnings; scoped output ownership and ignored-root checks remain available.
No broader source scan, runtime evidence, historical Resolver observation or scratch compiler metric supports these conclusions.

Observations have exact static byte support. Reconstruction interpretations and remaining count/ownership prerequisites are preparation findings, not new accepted semantics.
Any changed output count, byte mismatch or different current ownership contract would require revisiting the corresponding preparation assumption.
No canonical-document edit is proposed. Director performs applicable intake; assigned writes are released at terminal handoff.

## Output hashes

All six files are under ignored "build/combat-resolution-a9c-preparation-r1/". These artifacts are preparation evidence, not matching proofs.

| Artifact | SHA256 |
|---|---|
| evidence.json | BD540D7ADA7AD1293459E9AA4E5584D4DA4A8DE8E794365F706622949A51B011 |
| float-map.md | C65F363BBD75414BF9AEFAE01CA8372EA1493BF38914652DDBBB858FEEFCD964 |
| helper-map.md | 3BC3AD225A5BDBEDDB23D9FA179BC4BAA74A8B3F967A7F2CAD00D1B3C12375A6 |
| owner.txt | FD3B9E8C3A747DE4AF1648E0F36B06B15E5123AD68A1B39D5D8115177C7C7401 |
| prepare.py | A0E58BAE2A05A68D394250C8094520C6EB506890F8A08981D2DCB190E0D7E013 |
| transfer-map.md | EF59DC0B711BD9550AE12BCB7310121F6B0D08C08F1563B0C2B1B7F629A896B4 |
