# Combat selector observer review r1

Verdict: **Accepted**. Review is completed. The frozen preparation and adapter pass Critical offline review for one later bounded run under exact prerequisites.

This verdict does not authorize capture or resolve selector consumer provenance. Director `/root` may freeze this review and prepare one concrete run.

The Director must obtain Joe's approval for that exact run before capture starts. The later runtime owner must preserve every limit in this report.

## Frozen subject and eligibility

The adapter subject is canonical commit `8949896b6cc68479ed9f36f85f985726a979d8a6`. The adapter report SHA-256 is `8E1B8BAFAD389B72AC92DB3BB7616C876F403AF3CE946EC0B84C97F7DAD20B2C`.

The ignored adapter package is `build/total-resolver/combat-selector-observer-adapter-r1/`. Its `adapter.py` SHA-256 is `4338EFA0B4115F712A40C0D49F400A12A950DB3057043FC96F3A80B36DC6959E`.

Its artifact-manifest SHA-256 is `753E414641EFB3D3054E416D981B2481D9E7092F7AFCC76A6D88C01B41326815`. Its API-manifest SHA-256 is `ED2C278C69A6D42291BF3A179BE9ACABBA4033A31B1FC39BF30FB8AD73C9A722`.

The preparation subject is canonical commit `8f58be163fec58268d8c33fa64ac1e7db2f6ffe6`. Its report SHA-256 is `3AAEF0A9122246803DFBEC736F2B1AD091E223C888EADAF6804EEDA95F5802B5`.

The frozen current-knowledge retrieval is commit `6b89bf345d2399c98191573f3774c1d164f46601`. Its `retrieval.json` SHA-256 is `59C104ECCE02494E1F0F47DE15394269F688B361F9AC2705B5AFC41CA9408C13`.

Accepted selector research is commit `1f68aa001c612e2a7ce160e0cdf1d83a0c8b2048`. Its independent accepted review is commit `024a971`.

Both workers reported completion and released their writes. I produced neither result and found no unfinished worker correction.

The assignment, revision, launch, receiving task, Director task, and host matched activation. The assigned claim and report paths were absent.

I created the permanent claim before other review writes. Its creation time is `2026-09-07T00:35:18.077Z`.

The claim SHA-256 is `82D95DD6E2C49C8DDE264EFD93167EAB2CB011A51B66F19D47E7D4D9CBA2A44A`. It identifies `/root/combat_selector_observer_review` on host `local`.

Activation HEAD was `f329659cd9fd376954786d0adc6c230e2627191c`. Main advanced through descendant commits during the review.

The final inspected HEAD was `bdf03d8aac7583f9ded491ac3e74b03cdcae41f3`. Concurrent matching and readability paths stayed outside the assigned review files and ignored root.

The final unrelated paths were three `src/lib/` C files and the readability worker's claim and report. They did not affect any hashed review input.

The review remained eligible under the `NORMAL` inventory profile. No other writer owned the claim, report, or ignored review root.

## Claims reviewed

I reviewed these material claims:

- The selected knowledge miss is limited to database `023E881A-314D-4398-9E22-1E05952A3537`, ledger 17, and its 17 accepted sessions.
- Existing recorder APIs can express one exact focused entry watch and one bounded generic execution watch without a production profile change.
- The adapter authenticates its target, ROM, selected knowledge, frontier, production APIs, runtime inventory, configuration, approval binding, and review binding.
- The adapter uses the accepted observation-only facade and recorder lifecycle without global watch clearing, automatic ingestion, or game-state mutation.
- A successful output remains `live-unreviewed` and distinguishes entry detection from caller, target-origin, and behavioral evidence.
- The exact adapter is suitable for a later separately approved bounded run only after all missing inputs and ownership values become concrete.

The strongest competing interpretation is that the selected corpus missed a reachable path. Unplayed content, historical suppression, and an unused routine remain unresolved alternatives.

A generic event at the reused live range could belong to unrelated overlay content. The adapter refuses to treat that event as a qualified entry.

## Review method

I inspected the complete adapter, tests, documentation, preparation, retrieval, accepted research, accepted review, original ASM, and production API call paths.

I recomputed all 11 adapter artifact hashes and all 48 API-manifest hashes. I also recomputed six preparation artifacts and eleven preparation input hashes.

Every hash matched. The frozen adapter and preparation reports also matched their canonical commits byte-for-byte.

I pair-normalized the read-only master ROM independently. Its raw and normalized SHA-256 values matched the accepted identities.

The eight ASM words reproduced the exact 32-byte signature. The normalized ROM contained that signature at z64 `[0x00201778,0x00201798)`.

I hashed the selected 1,541,058,560-byte knowledge database. Its SHA-256 matched `B5CFB5DF59D837FDBEEC620E289AC8378823844EE15B910B829E7ECF1F44E005`.

`knowledge verify` passed all 26 checks. A fresh read-only function query reproduced two placements and zero instructions, edges, sessions, or calls.

The placement resolver independently returned IDs `1057` and `7834`. Both map the owner to physical `[0x001BE2E8,0x001BE308)`.

I ran the worker tests from a reviewer-owned copy. All 14 tests passed.

I added a separate full `execute()` test on a disposable adapter copy. It used synthetic ROM and knowledge fixtures plus the repository fake bridge.

The synthetic test used real `CaptureStore`, `Pj64CaptureRecorder`, metadata, manifest, and session verification code. It made no socket connection.

The path closed continuously on its budget. Session verification passed, ingestion stayed false, both watches were recorder-owned, and no global clear occurred.

The test confirmed that the client override placed `frontier.trf` inside the disposable run root. The temporary synthetic authority record was deleted with the fixture.

I ran 26 affected recorder, focused-capture, lifecycle, and client tests. All passed.

The production bridge harness passed protocol `0.17.0`, frontier format 6, observation-only capture, and two focused-context events.

Offline `doctor` passed the active binary, native source set, deployed bridge, and runtime-bridge identities. The optional historical database remained `SKIP`.

## Adversarial-test admissibility

The full execute-path test was an **Acceptance test**.

1. Assigned claim: the exact adapter can drive the accepted lifecycle and produce a closed, deferred, verified staging package.
2. Producer: a separately approved adapter configuration using accepted recorder APIs and a pristine protocol-0.17 bridge.
3. Ordinary sequence: validate bindings, verify identities, preflight, create a fresh run, install watches, observe, stop, drain, close, and verify.
4. Material consequence: an orchestration failure would make the adapter unsuitable for the proposed later run.
5. Smallest falsifier: one disposable end-to-end execution using production lifecycle code and the repository fake bridge.
6. Evidence grade: this test proves offline implementation behavior only. It supplies no game observation or real-runtime result.
7. Threat model: every mutation stayed in the reviewer root. No live bridge, existing database, ROM, worker package, or production source changed.

The entry-classification mutations were also **Acceptance tests**.

1. Assigned claim: only a signature-qualified focused entry can stop the observation loop as `entry_seen`.
2. Producer: the authenticated bridge's focused event packet or its ordinary generic and return packets.
3. Ordinary sequence: the recorder stores a packet, then the adapter reads it during the next stop check.
4. Material consequence: a false positive would mislabel unrelated overlay or pre-return context as a selector entry.
5. Smallest falsifier: change one packet field for generic kind, return role, function, signature flag, signature bytes, or live start.
6. Evidence grade: these checks stay within the documented packet and offline classifier contract.
7. Threat model: the changes model reachable ordinary packet classes. They do not alter a trusted schema or verifier.

## Findings

There are no blocking findings. No worker correction or proportional re-review is required.

The first reviewer checker run failed after its synthetic lifecycle completed. The checker used `removed_sequence_id` instead of the accepted `removed_sequence` column.

The disposable fixture was removed automatically. The corrected reviewer query passed in a new clean fixture.

The first bridge harness command omitted its required bridge path. The corrected command named the authenticated parent bridge source and passed.

One PowerShell manifest command also lacked required token spacing. PowerShell rejected it before execution, and the corrected read-only command passed.

These failures belonged to reviewer-owned tooling and command use. They did not expose an adapter defect or change frozen evidence.

## Review result by claim

### Preparation and knowledge boundary

The retrieval identity and contents are **Verified static retrieval**. The current database again reports `placed-not-executed` for this owner.

That result means only that no qualified invocation exists in the selected 17-session corpus. It does not establish dead code or uncaptured absence.

The preparation preserves reachable, unplayed, suppressed, and unused alternatives. Its proposed finite miss remains window-bounded.

### Existing API contract

The API compatibility is **Verified offline**. `RecorderSettings` accepts explicit `focused_watches` and generic `watches` without CLI profile registration.

The focused watch uses function ID 3197, the complete signature, sample mode `all`, no pointer recipes, and zero stack words.

The generic execution watch spans only the same eight instructions. The normal recorder coverage and ten standard safety ranges remain active.

The observation-only facade exposes no global clear, input injection, state control, stepping, pause, resume, or RAM-write method.

### Identity and authority contract

The configuration schema rejects changed target, ROM, port, capture scope, ingestion policy, invalid budget, incomplete identity, and output escape.

The approval binding covers the canonical parsed configuration and run ID. The review binding covers the exact adapter and API manifest.

These JSON records prevent accidental stale reuse. They cannot authenticate Joe's statement, reviewer identity, or runtime ownership by themselves.

No valid approval record or accepted review record exists in the worker package. This report supplies a verdict, not capture approval.

The Director may later create the review record from this frozen verdict. It must bind the exact adapter and API-manifest hashes stated above.

### Lifecycle and evidence contract

The adapter performs source and database checks before runtime connection. It creates a run root only after the first pristine-runtime preflight.

Recorder startup repeats preflight and checks session metadata. Shutdown removes only recorder-owned watches, drains ordered evidence, closes staging, and verifies it.

The adapter never ingests. A failed verification, interruption, loss, epoch change, or cleanup failure remains visible in the run package.

The configured budget starts before recorder startup. Individual bridge requests, final draining, and verification can extend total process time.

The real-run suitability is **Supported**, conditional on exact preparation. No live run was performed, so runtime timing and trigger reachability remain unverified.

## Evidence meanings and limits

A qualified entry proves a signature-gated entry packet at the accepted placement. It does not prove a connected caller or target-pointer origin.

A caller observation requires an actual callsite, executed delay slot, actual target, or an exact incoming edge. Tail jumps require their incoming edge.

Target origin requires a connected definition or load-to-transfer chain with the necessary register or memory values. `ra` alone is insufficient.

The focused return packet occurs before `jr ra` executes its delay slot. Its `v0` value is not this accessor's returned byte.

Addressed-byte, post-delay `v0`, return destination, and first-use evidence support a stronger behavioral claim. They remain outside the initial caller-origin claim.

Generic events at this live address remain unqualified without exact signature and connected placement evidence. Interior entries remain raw leads.

A finite miss proves only no qualified hit during the approved route and window. It cannot prove the routine unused.

The route, trigger, runtime owner, bridge epoch, current port availability, input, budget, output, and Joe's approval remain missing.

If a future route uses a savestate, the runtime owner must add the parent-required load readback. The adapter authenticates the file but not the loaded state.

The preferred current candidate is the verified vanilla ROM with a named manual route. The adapter predicts no selector hit.

No capture, session action, GUI use, watch installation, emulator control, existing database mutation, production edit, ROM build, staging, commit, or push occurred.

## Reused frozen evidence

This review reused these frozen inputs without changing them:

- accepted selector research at `1f68aa0` and review at `024a971`;
- frozen retrieval commit `6b89bf3` and its twelve embedded artifact identities;
- preparation commit `8f58be1`, its report, and exact ignored package;
- adapter commit `8949896`, its report, and exact ignored package;
- accepted original ASM, overlay configuration, Total Resolver sources, and authenticated Project64 bridge source; and
- the selected knowledge database and master ROM through read-only access.

Reviewer evidence is under `build/total-resolver/combat-selector-observer-review-r1/`.

The independent evidence file is [review-evidence.json](../../../build/total-resolver/combat-selector-observer-review-r1/review-evidence.json). Its SHA-256 is `B4F9D5AC1959BA9FC1DDEACEE3151E5C423BBCADE07BA20B4322FE40B5B4CD37`.

The checker is [review_check.py](../../../build/total-resolver/combat-selector-observer-review-r1/review_check.py). Its SHA-256 is `AC6242128245F650165EE2F6F6990D7963C6EDF374C89CAA785761BB5F5E94D1`.

The package manifest is [artifact-manifest.json](../../../build/total-resolver/combat-selector-observer-review-r1/artifact-manifest.json). Its SHA-256 is `55C2073DAE9DD0EBD3F7F0023183CC9F8E7B5A6583445D7EF84699DA24634131`.

## Tests and results

| Check | Result |
|---|---|
| Frozen identity | Passed: both reports, 12 retrieval artifacts, six preparation artifacts, eleven preparation inputs, 11 adapter artifacts, and 48 API inputs matched. |
| Real target identity | Passed: raw and normalized ROM hashes, eight ASM words, ROM signature, database hash, metadata, frontier, and placements matched. |
| Current knowledge | Passed: all 26 verifier checks; two placements; zero target instructions, edges, sessions, callers, or callees. |
| Adapter tests | Passed: 14 tests from a reviewer-owned copy. |
| Full synthetic execute path | Passed: closed continuous staging, verifier PASS, deferred ingestion, owned cleanup, local frontier path, and no global clear. |
| Affected production tests | Passed: 26 recorder, focused-capture, lifecycle, and client tests. |
| Bridge harness | Passed after command correction: protocol 0.17, frontier 6, observation-only capture, and focused packets. |
| Runtime inventory | Passed offline: active binary, native sources, deployed bridge, and runtime-bridge identity authenticated. |

## Documentation consequence

The Director may mark the preparation and adapter review accepted at their offline evidence boundary. The selector's consumer-provenance gate stays open.

No semantic name, family completion, runtime fact, source class, matching status, or ROM acceptance changes from this review.

The Director may record the existing-API capability and missing run prerequisites beside the selector gate. Preserve every evidence limit above.

## Exact next route

Director `/root` on host `local` must:

1. Freeze this review report and ignored evidence package.
2. Keep `func_00201778` consumer provenance, target origin, argument domain, behavior, and matching obligations open.
3. Assign one runtime owner and exact port, bridge epoch, selected database, frontier, vanilla input, route, budget, and fresh output directory.
4. Add the required load-readback evidence if the selected route uses a savestate or save.
5. Create a review record bound to adapter SHA-256 `4338EFA0B4115F712A40C0D49F400A12A950DB3057043FC96F3A80B36DC6959E`.
6. Bind that record to API-manifest SHA-256 `ED2C278C69A6D42291BF3A179BE9ACABBA4033A31B1FC39BF30FB8AD73C9A722` and this frozen report.
7. Present the complete configuration and its canonical hash to Joe for one explicit capture approval.
8. Start no capture unless Joe approves that exact run and the runtime owner repeats all required preflight checks.
9. Keep the resulting staging package un-ingested and `live-unreviewed` until its evidence receives the required later review.
10. Treat a qualified entry, connected caller-origin chain, and stronger behavioral package as three distinct outcomes.
11. Treat a miss as bounded to its approved route and window.

The reviewer releases the claim, report, and ignored evidence writes after the terminal collaboration handoff.
