# Capture startup correction R2

Blocked. Automatic approval review rejected the first bridge ownership patch before application. R2 changed no production source or tests. The Director must resolve approval for the concrete proposal before implementation can resume. Critical review and deployment remain pending.

## Identity and scope

- Task: combat-capture-startup-correction, revision 2.
- Launch: COMBAT-CAPTURE-STARTUP-CORRECTION-20260907-02.
- Worker: /root/compilation_groups_design, Astra Medium.
- Director: /root; native task 01a07262-aeca-7341-ad10-2dba705ff988; local.
- Ready scope commit: d655e032813588c8de4eec89946f970fc1563e11.
- Activation HEAD: 0284e7dd402a4f35dda5f4c6ebf599a0404d2580.
- Canonical coordination baseline: bcfc6f2ef08bdb1b3ba28999d8436b66246de058.
- Accepted matching baseline: 469a1416918592749d61dc34e3e079796f7b673c.
- Parent baseline: f940a2825ac97453746d3e9cef9c5cabc60a8dd2.

The claim was created atomically before other writes. The assigned provisional inputs matched all prompt hashes. The writer check found no overlapping production owner. Previously read unchanged guides remained applicable. Disjoint preparation reports appearing later were preserved.

## Concrete proposal for approval

The proposal targets only the research source bridge at C:/Users/Joe/Projects/OgreBattlel64/tools/project64/ob64_pj64_bridge.js. It does not target the deployed script.

The proposed bridge change introduces one session identity for capture instrumentation and queued evidence. A matching token and epoch would be required before operations could change that session. Failed startup would stop its instrumentation while retaining ownership of its residue.

The proposed transfer commands separate reading evidence from removing it. A bounded pending batch would remain available until the host confirms durable preservation. This addresses lost responses and failed writes without inventing evidence.

The proposed compatibility change advances the source protocol from 0.17.0 to 0.18.0. Unguarded legacy capture commands and destructive drain would fail explicitly. Recorder/client integration and historical protocol-read support would require corresponding changes. No configuration or deployment identity was changed.

The 4,096-event pending-transfer bound is a transport batch limit. It is not a capture duration, first-hit or total-event limit. Manual capture duration and manual Stop must remain unchanged.

This patch is an unfinished implementation proposal. It has not been applied, executed or tested. Its separate pending-prefix protection was queued after the rejected call and never attempted. Python ownership, durable sidecars, lifecycle handling, closed residual export and adversarial tests remain unimplemented.

Approval would permit continuation of the bounded offline source implementation and tests. It would not establish correctness, authorize deployment, permit live recovery or approve a capture.

## Automatic approval review rejection

The exact rejected action was the first tools.apply_patch call targeting the parent bridge source. The automatic review reason was:

> This alters the external bridge’s global protocol, command authorization, ownership state, and event-queue semantics, creating broad persistent runtime and compatibility impact beyond the explicitly authorized offline correction.

R2 prompt lines 28, 34 and 38 explicitly name the bounded ownership/transfer protocol, compatibility checks and parent bridge path. Line 42 prohibits deployment and runtime activity. This conflict was reported to the Director.

The Director instructed the worker not to retry or apply an equivalent mutation indirectly. The Director requested inert proposal preservation and this blocked handoff. No retry, workaround or dependent canonical production change occurred.

## Exact inert artifacts

All paths below are under build/total-resolver/combat-capture-startup-correction-r2/.

| Artifact | Purpose | SHA256 |
|---|---|---|
| rejected-bridge-patch.txt | Exact rejected patch text; inert | 083848BB93DB4631C6855A64E03898E5C314A6159A8776068D7F496119F9F4BB |
| unattempted-pending-prefix-patch.txt | Queued follow-up patch; never attempted | D9E115B9B310354304FCFBD775D53815B28DF01F8AB3AF2A0CC83063829671BD |
| automatic-review-rejection.txt | Exact reason and execution boundary | 724E29BB3B372BF60D5720D24E624028A8F633723057EFA6F14F744C3B0E6CD7 |
| design-under-approval.md | Ownership, durability, lifecycle and test requirements | F153569981B62B032F4A6F085A43DB002427021CE610CBBA70BAEBF470D44450 |

bridge-r2-input.js preserves the exact provisional bridge input. unchanged-input-check.json records all nineteen R1 artifact comparisons. evidence-index.json binds the R2 report, claim and generated artifacts.

## Verification and unchanged production inputs

A post-rejection SHA256 check confirmed that the bridge remained unchanged. A final comparison reauthenticated all nineteen artifacts in the frozen R1 index. Every artifact matched, including the bridge, provisional tests, recorder, sessions, client and CLI sources.

| Provisional source input | Unchanged SHA256 |
|---|---|
| Parent tools/project64/ob64_pj64_bridge.js | BF798A2E941D0FD25116633220AA84AFEC421597730308E9B824B6D131EDD771 |
| Canonical tools/total_resolver/tests/bridge_110_harness.js | 6DA2D3F7E9F44E1AC0C378E16B9EBE008112376A845B20C6D6B7FF439572E787 |
| Canonical tools/total_resolver/tests/test_startup_wire.py | E7680C094BBED6C27E3C60613648230D5778C2B126FB1339FF00A2BD9C2A7901 |

R1's two passing parser/harness tests remain frozen evidence. R2 did not rerun unchanged parser tests or claim lifecycle verification. No new protocol behavior is established.

R1 retains the exact parent baseline, pre-existing bridge input, parser candidate and three lineage deltas. Their identities remain unchanged. The pre-existing bridge authorship and full acceptance remain unknown. Fresh Critical review must cover the complete parent HEAD-to-final-result delta after coherent implementation.

## Limits, next action and release

Only the new R2 claim, report and ignored evidence root were written. No production code, tests, accepted configuration, matching C/ASM, compiler, linker, source-policy or structural input changed under R2. No Git mutation occurred.

No deployed script, native executable, Project64, GUI, live queue, real database, capture, ingestion or runtime operation occurred. No C++ or ROM build ran. activeBridge.sha256 and activeNativeRuntime.bridgeScriptSha256 remain unchanged.

The Director will present the inert proposal for concrete approval. Resolver source remains held. A resumed implementation must complete the protocol and offline tests before independent review. Deployment, identity binding, readiness checks and live residual recovery require a separate assignment.

Historical failed records without proven ownership must remain unavailable to the new export operation. Neither a future token nor current RAM can reconstruct their missing provenance or baseline bytes.

All assigned R2 production, test, report and evidence writes are released at this terminal handoff. R1 records and evidence remain frozen. No further source mutation is authorized by this report.
