**Verdict: Accepted.**

**Material result:** The frozen report's bounded historical conclusion, source/operation distinctions, pass-identity comparisons, boot-stage observations, transfer limits, faithful control, and missing-evidence boundary withstand independent static review.

**Consequence:** The precise owner-conditioned allocation-to-snapshot transfer remains unresolved historical research. The preserved artifacts neither demonstrate that exact experiment was run nor prove it was historically untried. Boot R2 does not prove that the actor transfer will survive allocation or disappear at the same pass.

**Required Director action:** `/root` on host `local` may propagate this reviewed diagnostic result. Keep the proposal untested, retain the complete five-member actor wave and its prior context prerequisite, and require a separately authorized diagnostic plus applicable review before using the proposal as a candidate or repair recipe.

# Combat actor branch diagnostics review R1

Review task `combat-actor-branch-diagnostics-review`, revision 1; launch `COMBAT-ACTOR-BRANCH-DIAGNOSTICS-REVIEW-20260907-01`. Reviewer `/root/combat_shared_dispatch_review`; Director `/root`, native `01a07262-aeca-7341-ad10-2dba705ff988`; host `local`. The fresh claim was created atomically and read back before review writes. W7's production source/build surface remained read-only and excluded.

## Frozen subject and eligibility

- Frozen subject commit: `6849bcef27a88ea0597e7cf605e9cc84c27d7953`.
- Frozen worker report: `docs/Plans/task-logs/combat-actor-branch-diagnostics-r1.md`, SHA-256 `AE816EFCF1246A0BA3812FDF6CD2F28AC94CF768DAAFFE847BB9E0CFEF8E3665`.
- Frozen evidence index: `build/combat-actor-branch-diagnostics-r1/artifact-identities.json`, SHA-256 `310680553FD10344DD55FB820F89602722FB7B3AABE47D0A403745022669FE1C`.
- Accepted source baseline: `0e1191013aeebed2929c9caff7c1f139169ad7f3`, confirmed as an ancestor of the frozen subject.
- The worker assignment is completed, its claim matches revision 1 and launch `COMBAT-ACTOR-BRANCH-DIAGNOSTICS-20260907-01`, and the frozen commit changes only its prompt status, claim, and report. No unfinished worker correction or assigned-surface collision was present.

The reviewer did not produce the subject. The exact review claim still identifies this receiving task, Director/native task, launch, revision, and host.

## Claims reviewed

1. The exact owner-conditioned duplicated local transfer is not demonstrated in the bounded retained artifacts, while the dossier's unbound summary prevents a stronger claim that it was never tried.
2. The retained do-while, one-shot loop, inline-helper, sequential-carrier, and historical probe sources are operationally different from the proposed owner-valued condition.
3. The do-while and one-shot runs have different raw pass files, alpha-canonical convergence from combine through schedule2, divergence at initial RTL and delay slots, and equal non-retail final emitted state.
4. Boot R2 preserves its condition and duplicate global stores through schedule2 and has only the merged store by the jump2 snapshot. Those two snapshots establish an observed transition boundary, not the internal rule that caused it and not transferability to the actor's local return-value lifetime.
5. The complete archived 236-byte `func_002158E4` source is the smallest faithful control, and an exact historical source identity plus compiler/input identity and pass evidence bracketing disappearance are the minimum evidence needed to promote a historical-stage claim.
6. The report preserves the five-member actor wave, prior context prerequisite, source-class and canonical acceptance gates, and makes no source, matching, structural, or semantic acceptance claim.

The strongest competing interpretation was that an overlooked retained branch source, raw dump equality mistaken for normalized equality, or the accepted boot workaround could turn the preparation's proposal into an already-falsified or already-proven recipe. The independent checks found none of those conditions.

## Independent method and admissibility

I used only read-only Git/file inspection and a reviewer-owned offline checker. The checker authenticated every indexed artifact, scanned the bounded historical target sources, parsed the preserved experiment records, compared pass identities, inspected the boot source and exact UIDs in schedule2/jump2, and checked the faithful-control and preparation identities.

These are acceptance tests within the assigned static evidence grade. Their ordinary producer paths are the retained matching candidate/probe outputs, the named allocator study runs, the source-probe output, and accepted boot diagnostic dumps. A contradictory source or pass record in those roots would directly invalidate the report's bounded conclusion. Reading and comparing them is the smallest useful falsifier and does not introduce a new runtime, threat model, source mutation, or acceptance standard.

No candidate source, compiler, linked diff, build, verifier, source-policy generator, runtime, bridge, database, recovery, or ingestion operation was used.

## Tests and results

| Test | Independent result |
|---|---|
| Frozen identities | Subject report and evidence index exactly match the assigned hashes. |
| Evidence index | 84 unique paths; all 84 exist and match recorded lengths and SHA-256 values. |
| Retained actor source falsifier | 24 retained candidates; the complete target root contains 33 C files when eight preparation sources and the historical probe are included. None contains an `if` or ternary expression after comments and literals are removed. |
| Manual result records | Nine records, `manual` through `manual9`; every `candidateId` resolves to one of the 24 retained candidate sources. They do not supply the missing owner-conditioned source or pass lineage. |
| Historical probe/trial/dossier | Probe D4DD932B has direct allocation-to-snapshot assignment before the owner load and no condition. Trial assembly has only a generic source path and no bound source digest. The dossier names a family of duplicated branches/goto lifetimes without predicate, source identity, or pass history. |
| Focused source distinction | Runs 6296C9F3, 8480CB0C, and 9F48236B have the recorded source hashes; none expresses the proposed owner-valued duplicated assignment. The first two are do-while/one-shot carrier shapes; the third is an inline helper. |
| Raw versus canonical pass comparison | Do-while and one-shot raw file/content hashes differ at all seven retained stages. Canonical RTL differs at `rtl`, agrees at `combine`, `schedule1`, local allocation, global allocation, and `schedule2`, then differs at `delay-slots`. |
| Final-emission boundary | The two loop runs share emitted-state SHA-256 `30D1EAA445D0F491313CE4621EA9F0F34774348DA9FBAD01F85885B36CBA79EA`, 236 text bytes, and 27 relocations. Both are non-retail and retain residual regions of 12 bytes at `0x18` plus 8 bytes at `0x2C`. |
| Missing pass evidence | The two loop run directories retain RTL, combine, schedule1, local/global allocation, schedule2, and delay-slot dumps; neither retains a CSE or jump snapshot. |
| Sequential source probe | Allocation, owner read, and unconditional snapshot transfer occur in that order. Its record retains causal signature `23809A8B11ABE9537D02B33CA8FD71B66243BA921A5A8D1E2F613026F51C6167`, the known 11 unequal bytes/3 instructions, and an explicit non-acceptance boundary. |
| Boot correspondence | `conditional.c` loads `output` before testing `context` and has two identical global-output stores. Schedule2 retains UIDs 123, 126, branch 129, and stores 131/137; jump2 retains 123, 126, and store 137 while branch 129 and store 131 are absent. |
| Boot transfer limit | The accepted boot report records exact `PURE_C`, complete-ROM success, and explicitly disclaims a general compiler recipe. The actor proposal remains a local transfer after an allocator return copy, not boot's duplicated global store. |
| Faithful control and wave limits | Archived control SHA-256 is `C59E098C0633E9DE88F4ABB550A6CB124BC315584D254776C4ACE4AEDAFB91A1`; the preparation SHA-256 is `39D46ADD050168C55072925DAF03E4384FFC725A2975106F7BCF8A43137EA7B7`; the subject retains all five future members and every named canonical gate. |

Final checker: `build/combat-actor-branch-diagnostics-review-r1/check.mjs`, SHA-256 `9A38A732EF54C2B43B4C118BA6BC557EF917BD5A9F2F759E9661C04416FD768C`. Clean attempt 3 passed 30/30 checks with no failures: `build/combat-actor-branch-diagnostics-review-r1/attempt3/independent-results.json`, SHA-256 `AD6C6334B237BB726EF8FE7E3556A34AF5CD868C4117BFCBAC56AE4E08C7B7DF`.

The first checker invocation had a syntax-only extra delimiter and produced no evidence. After that repair, attempt 1 executed but its exact single-line text predicate did not tolerate a Markdown line wrap in the source-probe document. The failed output and corresponding checker are preserved. The assertion was narrowed to accept whitespace at that one wrap; evidence inputs and the substantive condition were unchanged. A clean attempt passed, and attempt 3 added byte/hash identities for supplemental manual and target-source inputs before repeating the same 30 checks.

## Findings

No admissible findings.

The nine manual JSON records, trial assembly, and preparation sources are supplemental search inputs outside the worker's 84-entry evidence index. The reviewer-owned attempt 3 binds their current bytes and hashes. This is not a material omission from the frozen result: none supplies positive source/pass lineage, and the report uses them only inside an explicitly bounded, unresolved historical conclusion. It does not generalize their absence to all historical objects or private workspaces.

## Evidence limits and documentation consequence

This review does not establish whether the exact branch experiment occurred historically. It does not identify the compiler's internal elimination rule from the two boot snapshots, predict actor pass survival, validate an actor candidate, review ordinary matching source, or confer any matching/semantic/structural acceptance.

No canonical domain-document update, source change, family exclusion, or matching-status change follows. The accepted review permits only propagation of the worker's bounded research conclusion. A future authorized diagnostic should first reuse an exact recovered branch source if one appears; otherwise it may test one precisely bound owner-conditioned local transfer against the faithful control, with the pass/result distinctions and complete-wave limits already stated in the subject.

## Exact route and release

Director `/root`, host `local`: record `Accepted` for frozen subject `6849bcef27a88ea0597e7cf605e9cc84c27d7953` and may propagate its static diagnostic conclusion. Do not treat the proposal as executed, causal, matching, or transferable from boot. Preserve all four actor hybrids, accepted `func_0021C3B0`, the prior context prerequisite, and the later five-target `PURE_C`/ownership/placement/relocation/target-byte/full-ROM gates. If the diagnostic is pursued, route it as a separate authorized worker result with applicable proportional review.

All review commands are complete. Only the assigned claim, this report, and ignored `build/combat-actor-branch-diagnostics-review-r1/` evidence were written. All reviewer-owned writes are released to `/root`.
