# Combat draw W8 R5

Unresolved handback. Completed26 new inputs through supported intermediate forms; no new production best was selected. Receiver `/root/combat_draw_continuation`, Astra Medium, owns sole production source/build writes. Director `/root`; release40174bbe; launchCOMBAT-DRAW-WAVE8-20260908-05. Fresh claim was created atomically and read back with host/time. All14 sources and6 shared inputs authenticate against R4 index6A1D35FE and are separately copied under `starting-inputs/`.

Joe's clarification governs selection: an unselected worse input does not eliminate its source structure. Existing reports remain frozen. All14 targets and one final complete-wave verifier remain required; no full verifier has run.

Sequence A begins from R3 whole-row endpoint state, which placed companion/strip in retail registers but introduced accessed storage and displaced row/u1. The first coordinated change shares the real final vertex pair and its narrowed branch-produced coordinates. Subsequent narrowing/lifetime changes will be decided from actual input-specific evidence. The selected D037 baseline remains separate from exploratory inputs.

## Sequence A observations

Six coordinated3C00 controls are preserved. No current-best source changed. Each new input has its own PURE_C result, pinned object/relocations and separate pinned/diagnostic agreement. All processes completed synchronously; no live session handle remains.

| Step | Bytes | Frame | Native words | companion/u1/strip/row |
|---|---:|---:|---:|---|
| Prior whole-row endpoint |6740|512|434|s6/s5/s8/s7|
| Common final pair |6704|512|508|s7/s5/s8/s6|
| Separate vertex/packet endpoints |6704|512|508|s7/s5/s8/s6|
| Four common vertex calls |6608|512|484|s7/s6/s8/s5|
| Packet expressions at packed consumers |6616|504|469|s8/s7/s6/s5|
| Branch first pair, common y/final pair |6688|504|485|see sequence-a-roles.json|
| Previous step plus raw packet endpoint |6680|512|502|s7/s6/s8/s5|

The phase split reduces raw endpoint home accesses from8 to6 but does not remove its cross-call lifetime. The final raw packet endpoint715 has12 references across388 instructions and17 calls. It occupies an accessed home at1C4. Replacing that carried raw value with each actual packed consumer removes the home and recovers frame504, while changing companion/strip assignments back. These are input-specific interactions, not a claim that the broader row-end hypothesis is false.

The four-common-call form changes the actual first-pair branch layout, not only register allocation. Restoring the two first-pair paths with common y retains a distinct intermediate for later work. None is selected: the current inputs still differ from the complete target. The sequence is paused pending a more specific way to retain a useful endpoint history without its accessed spill or loss of retail call layout. Score alone did not close it.

One generated phase-split draft placed a declaration after statements, rejected by the pinned compiler. Its source/error log are preserved with c90-placement-error names. The corrected declaration retains the intended assignment point. No failed compile is counted as an allocation observation.

## Sequence B question

Revisit DB10's guarded natural-array aliases as an intermediate, despite its576-byte frame. The real variant result must survive the key call. Move the actual insertion-only pointer definitions after that call and restrict their uses to insertion, keeping dedup direct-array addressing. Determine whether this removes the additional accessed storage while retaining any useful preheader order. This is a new lifetime boundary on the previously retained alias input, not a rerun of the whole-loop aliases. Calls, guards, natural starts and six array operations remain fixed.

## Sequence B observations

Five new DB10 inputs follow the older guarded whole-loop aliases. Insertion-only alias assignments after the key call produce5572/frame552/native513. Sharing sourceList across dedup/insertion gives5588/frame560/native524. The extra home1F4 is actual source-base pseudo989, initialized from fp+24 and loaded for dedup/shift/insertion. It is not a fourth non-emitting home.

A local aggregate containing exactly the six existing18-element arrays retains5588/frame560/native524. This grouping does not merge the duplicate address histories in this input. A source-index shift loop then gives5592/frame560/native523: decrement shift before copying destination[shift+1] from source[shift]. The existing unsigned index<shift guard ensures no decrement underflow. Final shift and insertion index remain unchanged. No pointer before an array or extra runtime test is introduced.

Restoring outer context/resource aliases in that source-index/grouped context gives5616/frame576/native259. It adds accessed base homes again. Three comparison-only homes remain throughout all five inputs. These inputs demonstrate that grouping, source-index shift and alias lifetime interact with actual address storage, but do not yet supply the retail allocation or full preheader order. The alias structure remains an open hypothesis, not disproved by these scores. The next missing distinction is how to merge actual repeated source-base representations while retaining useful hoisting and all other accesses. Best5BF6 remains restored.

Sequence A now branches to a narrower real row-end representation. Vertex consumers require signed16, and packed endpoint consumers require only low bits after multiplication/masking. Preserve the fallback helper's full row-end argument separately. Test signed16 row-end state across only the consumers that permit it, then inspect whether its allocation differs from the previous full-int endpoint spill. This is a value-domain change, not storage added for frame size.

## Sequence A representation branch

The full-row endpoint was then narrowed only for signed16 vertex consumers and low12-bit packed consumers. The fallback helper kept its full integer coordinate. That control gives6720/frame512/native496 and an accessed HI endpoint home at1C6. Separating short vertex state from a shared packed endpoint gives6672/frame512/native493, with an accessed SI endpoint home. Restoring distinct packet-end definitions gives6732/frame504/native475. Its complete native text and actual relocations equal R3's vertex-only endpoint control, as recorded in `convergences.json`. This is an observed convergence of these inputs, not a rejection of all endpoint representations.

Equal aggregate scores do not imply equal code. The phase-separated/common-final pair, grouped/ungrouped DB10 aliases, and two short common6098 corner forms have equal selected metrics but different native text. Their exact objects and relocation records remain separate.

## Sequence C:6098 texture and geometry lifetimes

R4's shared-final-vertex-narrowing input remained a starting intermediate despite its worse result. Sharing full y and bottom-y computation gives4112/frame408/native391. This makes y483 global across4 calls and assigns it s4. It does not preserve the original branch-local allocation.

The Director's read-only helper supplied a distinct coordinated question: connect the actual top and bottom texture-row consumers through one mutable vertexRow. The helper did not write sources or run experiments. Its bounded retained-control check and retail offsets were delivered through collaboration; no additional source-review gate applies.

Retail retains the top texture value through each first pair and replaces it after the second call. Relevant owner offsets are97C–984/9B0, A08–A14/A40, then9E0–9EC andA70–A78. These are instruction/value correspondences, not original variable names.

| New6098 step | Bytes | Frame | Native words |
|---|---:|---:|---:|
| Common full y from R4 |4112|408|391|
| Shared mutable int texture row |4120|408|364|
| Branch-local mutable int texture row |4112|408|363|
| Shared int row delta update |4124|408|365|
| Branch-local int delta update |4116|408|364|
| Explicit arithmetic stages |4108|408|364|
| Branch-local short delta state |4208|408|380|
| Shared short delta state |4136|408|391|
| Both common corner outputs short |4136|408|391|
| Shared short texture row plus evolving full y |4120|408|391|
| Branch-local short row with direct update |4148|408|49|
| Previous step plus short source-x |4168|408|440|

The shared mutable int variable connects real consumers, but pseudo484 has36 references,4 deaths and3 crossed calls. It remains globally allocated s2. The flipped copy still occurs before the second call, after the outgoing argument store. A common C variable alone does not constrain that scheduling.

The next step derives the bottom texture coordinate from the live top coordinate. Ordinary mode adds strip-1; flipped mode subtracts strip-1. Final signed16 conversion preserves the original coordinate modulo65536. Header/stride evidence bounds strip to0..128 on defined division paths, so these operations on the preceding signed16 coordinate do not overflow int. Persistent row and all call arguments retain their original values. These are real coordinate updates, not dummy dependencies.

The combined delta form moves the flipped update after the second call. Branch-local delta variables still have two deaths. Initial arithmetic consumes the old variable through a temporary, and the final outgoing argument store records its later death. `REG_DEAD` at UID1263 versus1293 for pseudo487, and1380 versus1410 for513, binds that distinction in the branch-local delta flow dump.

Explicit arithmetic stages change the emitted placement again: the flipped subtract/add can move before the call. Two deaths remain. This observation concerns these exact source expressions, not a general scheduling rule.

Short texture state restores local promoted texture consumers, but the branch-local delta input retains an extra raw flipped endpoint and emits seven vertex call sites. Its ordinary branch has s0 texture/s1 full-y/s2 narrowed-y; the flipped branch differs. The shared short-delta input has s0 texture consumers in both paths and the common final pair, but full-y/narrowed-y are s2/s1. Typing bottomY short leaves those roles and metrics unchanged; native text is not byte-identical.

Connecting full y through the common tail makes it global in s3 rather than restoring the desired local classes. Returning to branch-local short texture state with direct bottom-coordinate assignment removes the extra raw flipped lifetime. It restores the full4148-byte extent, the expected first-pair s0 texture/s1 full-y/s2 narrowed-y arrangement and the shared call tail. This input remains a viable alternative source form, with native49 versus best46. It is not selected; complete target differences remain.

The final type step gives source-x its declared decoded-field signed16 domain. It adds an accessed HI home plus a separately extended value, moves some other homes, and gives4168/frame408/native440. This input does not explain unused retail positions. Its worse score alone does not reject the broader type hypothesis.

All twelve6098 controls retain23 accessed homes and zero COMBINE_USE events. Every frame is408 versus retail456. The common-tail/texture-state sequence changes actual lifetimes, register assignments and call merging, but supplies no source history for the two groups of three unused retail positions. Those allocation questions remain distinct from corrected vertex roles.

A generated branch-local draft missed its vertexRow declaration. Its exact source/error log are preserved as `branch-local-mutable-vertex-row.declaration-error.*`; the corrected input was compiled separately. Neither compile error is counted among the26 successful inputs.

## Current result and unresolved handback

This pass completed26 new PURE_C compiler inputs through three coordinated sequences. Every successful input has a pinned object, actual relocation record and separate diagnostic-compiler agreement. `results.json` binds each authored/expanded identity and result. Current bests were restored after every production compile. No process remains running; no session handle needs resuming.

No selected production source or configuration changed. All20 current source/shared identities still equal R4's starting index. The exact restored source set is in `terminal-current-inputs.json`.

The supported questions remain open. For3C00, the raw endpoint state changes useful allocation roles but receives accessed storage; removing that state changes those roles back. ForDB10, aliases change real address storage, but grouping and source-index shifts do not merge the observed duplicate source-base history or create another non-emitting home. For6098, connected signed16 texture state gives a useful alternative full-size form, while local geometry classes and unused frame positions remain separate unresolved decisions. The recorded inputs do not prove any broader source structure impossible.

The current nonexact bests remain3C00 D03797FD04EEE60592E0644A837E09308568D3E82448883997A10A85DA7F91F5;6098 D76444B2C4AF7AD44E40DA2F68456E894355AA9F1B6BF4D44F4B792DC10097E1;DB10 5BF6ACF3CEF8F4A229AA143CCEB76376AC333B455BB1EDA18E9296476D791FD3. Focused-exact7ADC remains F3F90BD828DE9141A6E48D4BBBF4691F3A5B6C60C828CE9B2F8289408457B518.

Inherited focused results remain3C00 770 bytes/261 words different,6098 56 bytes/46 words different, and DB10 frame552 versus560 with its separate preheader order. These were not remeasured by new linked builds. New native counts exclude relocation words and must not substitute for full target equality.

`build/combat-draw-wave8-r2/terminal-proof-locations.json`, SHA256 E89B601527125A09406971C120DC6EAB17F323B6F391FBBBD6686E2F2E3D450F, locates preserved focused/reference proof. It retains each input's qualifications, including the older DB10 comparison; it is not a new current-wave acceptance report. Exact7ADC's bound proof remains `func_001F7ADC.exact-endpoint-lifetimes.proof.json`, SHA256 E233BD5B433E71B6E24C0EF8190E340CB7A7AED244249F44A91F823F473105DB.

No focused linked build or full verifier ran because no final candidate was selected. All fourteen targets, including ten focused-exact new candidates and accepted BFF8, remain one combined assignment. They still require the single final complete-wave verifier after the three residual targets are solved. No wave acceptance or scope reduction follows from this handback.

The packet tool's acceptance was received during this pass. Existing unchanged packets were reused; no redundant decompiler run or shared implementation change occurred. Held Resolver files, accepted W7, source-policy/compiler identities, ownership rules and mappings remain untouched.

R5 is terminal at this handback. All production source/build authority and R5 claim/report/evidence writes are explicitly released to Director `/root`. The Director is the next actor for continuation routing; no other production writer was active during this pass.
