# Combat draw W8 R5

In progress. Receiver `/root/combat_draw_continuation`, Astra Medium, owns sole production source/build writes. Director `/root`; release40174bbe; launchCOMBAT-DRAW-WAVE8-20260908-05. Fresh claim was created atomically and read back with host/time. All14 sources and6 shared inputs authenticate against R4 index6A1D35FE and are separately copied under `starting-inputs/`.

Joe's clarification governs selection: an unselected worse input does not eliminate its source structure. Existing reports remain frozen. All14 targets and one final complete-wave verifier remain required; no full verifier has run.

Sequence A begins from R3 whole-row endpoint state, which placed companion/strip in retail registers but introduced accessed storage and displaced row/u1. The first coordinated change shares the real final vertex pair and its narrowed branch-produced coordinates. Subsequent narrowing/lifetime changes will be decided from actual input-specific evidence. The selected D037 baseline remains separate from exploratory inputs.

## Sequence A observations

Six coordinated3C00 controls are preserved. No current-best source changed. Each new input has its own PURE_C result, pinned object/relocations and separate pinned/diagnostic agreement. All processes completed synchronously; no live session handle remains.

| Step | Bytes | Frame | Native words | companion/u1/strip/row |
|---|---:|---:|---:|---|
| Prior whole-row endpoint |6740|512|434|s6/s5/s8/s7|
| Common final pair |6704|512|508|s7/s5/s8/s6|
| Separate vertex/packet endpoints |6704|512|508|s7/s5/s8/s6|
| Four common vertex calls |6608|512|484|s7/s6/s8/s5|
| Packet expressions at packed consumers |6616|504|469|s8/s7/s6/s5|
| Branch first pair, common y/final pair |6688|504|485|see sequence-a-roles.json|
| Previous step plus raw packet endpoint |6680|512|502|s7/s6/s8/s5|

The phase split reduces raw endpoint home accesses from8 to6 but does not remove its cross-call lifetime. The final raw packet endpoint715 has12 references across388 instructions and17 calls. It occupies an accessed home at1C4. Replacing that carried raw value with each actual packed consumer removes the home and recovers frame504, while changing companion/strip assignments back. These are input-specific interactions, not a claim that the broader row-end hypothesis is false.

The four-common-call form changes the actual first-pair branch layout, not only register allocation. Restoring the two first-pair paths with common y retains a distinct intermediate for later work. None is selected: the current inputs still differ from the complete target. The sequence is paused pending a more specific way to retain a useful endpoint history without its accessed spill or loss of retail call layout. Score alone did not close it.

One generated phase-split draft placed a declaration after statements, rejected by the pinned compiler. Its source/error log are preserved with c90-placement-error names. The corrected declaration retains the intended assignment point. No failed compile is counted as an allocation observation.

## Sequence B question

Revisit DB10's guarded natural-array aliases as an intermediate, despite its576-byte frame. The real variant result must survive the key call. Move the actual insertion-only pointer definitions after that call and restrict their uses to insertion, keeping dedup direct-array addressing. Determine whether this removes the additional accessed storage while retaining any useful preheader order. This is a new lifetime boundary on the previously retained alias input, not a rerun of the whole-loop aliases. Calls, guards, natural starts and six array operations remain fixed.

## Sequence B observations

Five new DB10 inputs follow the older guarded whole-loop aliases. Insertion-only alias assignments after the key call produce5572/frame552/native513. Sharing sourceList across dedup/insertion gives5588/frame560/native524. The extra home1F4 is actual source-base pseudo989, initialized from fp+24 and loaded for dedup/shift/insertion. It is not a fourth non-emitting home.

A local aggregate containing exactly the six existing18-element arrays retains5588/frame560/native524. This grouping does not merge the duplicate address histories in this input. A source-index shift loop then gives5592/frame560/native523: decrement shift before copying destination[shift+1] from source[shift]. The existing unsigned index<shift guard ensures no decrement underflow. Final shift and insertion index remain unchanged. No pointer before an array or extra runtime test is introduced.

Restoring outer context/resource aliases in that source-index/grouped context gives5616/frame576/native259. It adds accessed base homes again. Three comparison-only homes remain throughout all five inputs. These inputs demonstrate that grouping, source-index shift and alias lifetime interact with actual address storage, but do not yet supply the retail allocation or full preheader order. The alias structure remains an open hypothesis, not disproved by these scores. The next missing distinction is how to merge actual repeated source-base representations while retaining useful hoisting and all other accesses. Best5BF6 remains restored.

Sequence A now branches to a narrower real row-end representation. Vertex consumers require signed16, and packed endpoint consumers require only low bits after multiplication/masking. Preserve the fallback helper's full row-end argument separately. Test signed16 row-end state across only the consumers that permit it, then inspect whether its allocation differs from the previous full-int endpoint spill. This is a value-domain change, not storage added for frame size.
