# F580 complete-owner preparation r1

Status: complete read-only preparation. This bounded preparation preserves complete `func_0022F580` and all seven future resolution-wave members.
Director `/root` must bind preceding accepted action-mode results and sole production ownership before source activation.

Task `combat-resolution-f580-preparation`, revision 1; launch `COMBAT-RESOLUTION-F580-PREPARATION-20260907-01`.
Receiver `/root/sequential_scope`; Director native `01a07262-aeca-7341-ad10-2dba705ff988`; host `local`; Astra Medium.
Ready `caa154f815b5d006142587bb9d0d6e28d9d00ac8`; activation/starting HEAD `d37ff1046e0451b65fe45f685b092eceff1ee2a1`.
Coordination `287e0e02020e11547b3dcde0295be99637b04b39`; accepted source W7 `469a1416918592749d61dc34e3e079796f7b673c`.

The complete fresh claim was created atomically and read back before other writes. Assigned report and ignored root were absent.
Existing W8 source/configuration work and held Resolver test changes remain untouched.
Required guides are unchanged from the preceding preparation. Current assignment, future draft, sequential route, and corrected package control this work.
The accepted research-aide index routes byte questions to original assembly. No database or runtime aide is needed.

Completed: authenticated relevant corrected-package inputs, compared all 1,351 instructions with ROM, and mapped complete calls, transfers, table entries, and stack footprints.
Leading interpretation: neutral argument records and local temporary objects support a complete reconstruction under accepted ownership.
Alternative: guessed scalar declarations or same-address aliases obscure escaped objects and context-dependent interfaces.
Direct argument registers, stores, helper contracts, and accepted placements distinguish these cases.
Any ROM identity, byte, extent, or controlling placement mismatch stops this preparation.
The result supports source preparation only; no new semantic, structural, or compiler conclusion becomes accepted.

## Completed evidence and limits

Assigned writes are released at terminal handoff. No source implementation, compiler experiment, build, runtime/database operation, Git mutation, or new acceptance was performed.

The extractor authenticated 18 relevant corrected-package inputs and 17 supporting W7 inputs, retaining exact Git object/path/byte-count/SHA256 records in `build/combat-resolution-f580-preparation-r1/evidence.json`. The controlling corrected package is SHA256 `0EFA3D1990C347FBCCFCA32666FE6430BFDD0491E780B43D4F91CC68787BC1B1`; correction report SHA256 `0CF8D2C24D4894C79A1109A9C71D5F47A8AE1A662C75053684008804D6E453AB`, frozen at `740732e18b1f3e5808b167572662473918d35c2d`. Original retrieval's empty-alias claims are superseded; this work reused corrected per-site arrays without repeating alias research.

Canonical normalized ROM is 41,943,040 bytes, SHA256 `571E83396BC81E70DA4C0A20313D82DBD7DFE685F2C37418C8E27F927E2CC67A`. All 1,351 original instruction words equal ROM `[0x0022F580,0x00230A9C)`, owner SHA256 `67D12B7C85632621E2ADC9F77021475871881FEC5145430B7D74D278C9228B7F`; original ASM SHA256 `3B9E75EAAD4DF53BA4135F890006EE4C04C200A72234419C5DA9930159B188AA`. Accepted slab `resource-loader-0022a280` places this owner at `[0x801EC2B0,0x801ED7CC)`, delta `0x7FFBCD30`.

The bounded maps cover all 90 literal calls / 47 target addresses, 243 direct transfers (including calls), 463 memory references, all ten dispatcher entries and both indirect transfers (dispatcher and return). Non-call direct destinations stay within the owner. `owner.txt` retains original instruction comments, whose legacy target addresses are decoding aids only; its ROM/VMA columns and decoded JSON transfer targets use accepted placement. No floating-point instructions or indirect calls occur. Following `func_00230A9C` remains a separate complete 3,372-byte owner, including its continuation.

## ABI and concrete storage

Incoming `a0` points to three 32-bit object-pointer slots; the owner traverses four bytes per slot. Incoming `a1` is a mutable word pointer, retained at `sp+0x1EC`. Its value has both signed and unsigned uses; preserve operation-specific casts. Incoming `a2/a3` and incoming stack arguments are not consumed as public arguments. Frame size is `0x2C0`; saved `s0..s8` occupy `0x298..0x2B8`, `ra` is at `0x2BC`. No floating-point save area exists. `v0` returns explicit zero or one, but zero does not imply absence of side effects. Restore begins at ROM `0x230A6C`; `jr ra` at `0x230A94` has stack restoration in its delay slot at `0x230A98`.

Offsets below are relative to adjusted `sp`. They describe observed footprints, not proposed C declarations or inferred objects filling unused gaps.

| Storage | Complete reconstruction obligation |
|---|---|
| `0x28`; `0x30/34/38` | Per-selected byte values and three object pointers, respectively. Pointer list is initialized with three literal zero words, populated and compacted; preserve adjacent distinct data. |
| `0x40` | Up to three five-byte records. Caller initializes record byte zero; `func_002397D4` supplies remaining bytes and can reorder/update records. Do not fabricate missing initializers. |
| `0x148` | One-byte output from that helper's third argument; callee's byte store establishes footprint. Return is narrowed to byte at `0x207`; `0xFF` rejects. |
| `0x50..0xD0` | Exactly `0x80` bytes cleared for descriptor. Bytes `0x50..58`, count word `0x5C`, IDs `0x60+4i`, halfwords `0x88+2i`, bytes `0x9C+i` and `0xA6+i`, halfwords `0xB0+2i`, flags `0xC4+i`. Deletion shifts synchronized arrays and retries the current index. Preserve full clear and runtime count; geometry alone is not a proven universal count bound. |
| `0xD0..0x120` | 80-byte mutable subrecord copied in five 16-byte iterations from the first selected object's nested record, offsets `0x44..0x94`. |
| `0x120..0x138` | Explicitly cleared 24-byte work region passed to accepted `func_001F197C`. Helper writes a variable sequence of words and `-1` terminator; owner reads first word and retains returned value at `0x20C`. Clear extent is not proof of a universal helper output bound. Special override writes `0x20C=10`, first word `5`. |
| `0x138+4i` | Up to three retained selected-record words. |
| `0x150..0x15C` | Three-word temporary pointer list, initialized from same zero literal; source-first copy `(0x150,0x30,12)` at ROM `0x22F9B4`. |
| `0x160/164`, `0x168/16C` | Separate four-byte outputs of `func_00237D5C(raw_id,out0,out1)`; invalid domain writes `-1` to both. |
| `0x170` | Mutable single byte passed to `func_0022B06C` at five sites. Callee uses byte load/store; caller consumes byte, not returned register. |
| `0x178` | Selected-ID word list followed by `-1`: at most three IDs plus sentinel. |
| `0x188`, `0x1B8` | Parallel final word lists, optional additional first record plus descriptor count, each terminated by `-1`. Required written footprint is `4*(output_count+1)`; 48-byte spacing alone does not prove capacity. Future declarations must bind the descriptor producer's count bound. |
| `0x10..0x20` | Outgoing fifth through ninth arguments, including pointer arguments. Do not misclassify these as independent escaped locals. |

Other retained words (`0x1F4`, `0x1FC`, `0x20C`, `0x214`) and narrowed bytes (`0x207`, `0x21F`, `0x227`, `0x22F`) must retain actual uses. Pointer spills such as `0x244` do not establish new semantic fields. No array capacity is invented from frame spacing.

## Full control-flow obligations

These consecutive regions cover the complete body; instruction-level branches and delay slots are in the maps. Field numbers and raw mode values are deliberately neutral.

| ROM interval (exclusive end) | Obligations |
|---|---|
| `22F580..22F678` | Initialize lists/work storage. First-object helper selects global byte `801976DC` or `801976E8`; build stride-25 record at `801971F0`. Distinct random helper `00022E60` and signed remainder-100 comparison against record byte `+0x0C` can return zero. |
| `22F678..22F8BC` | Scan exactly three input slots; retain helper filters `20C2C0/20C32C/20C2F4/20C310`, object `+0x6C` context comparison and signed `<0x70`, EF50 side effects, fields `+0x78/+0x7C`, selected exclusion flag, computed `+0x68`. Require at least two selected; preserve two/three-record equality grouping, five-byte record construction and helper byte-`FF` gate. |
| `22F8BC..22FA1C` | Conditional two-of-three/three-of-three routing through F2BC and `20F8DC`; some helper paths return one early. Compact both pointer list and associated raw selection values. |
| `22FA1C..22FC9C` | Clear/populate descriptor using literal bytes and `233F94/2340EC/23887C/237E2C`. Zero count still updates global `+0x818`, selected `+0x6C/+0x94` and publishes `0x1C` records before returning zero. Otherwise `239338(descriptor,p0,p1,p2)`, byte result from `25FD00`, copied subrecord, selector `201798`, reset `1F0E64`, evaluator `1F197C`, and special override. |
| `22FC9C..22FF1C` | Signed maximum of selected `+0x94`, global updates, narrowing, per-selected `0x1C`, seven-argument `0x0B`, then `0x3E` publication. Increment global `+0x823` byte; retain flag-dependent target distinction. |
| `22FF1C..2302D4` | Iterate descriptor; remove invalid items with coordinated shifts and count decrement. Preserve `2363FC/2365BC`, random triples, raw byte `+0x53` cases 9/8, byte `+0x51` qualification, `+0x9C` flags, guarded dispatcher and `+0xC4` result stores. |
| `2302D4..23085C` | Raw descriptor byte `+0x51` groups 1,2,8,A,4 and default. Group 1 calculation/`22A7B8`, optional `237AF8`, nine-argument `0x1F`, signed half rounding, clamp 1..9999. Group 2 adds `20D4C8` result with halfword narrowing at object `+0x20`, optional B06C, subtype 2. Groups 8/A/4 conditionally set object `+0x40` bits 8/10/4 and publish subtypes 3/7/4 versus 6. Other values converge to bookkeeping. Preserve all five byte-modifier calls, repeated reads and signed maximum at `0x214`. |
| `23085C..230A6C` | Build selected-ID and optional extra-target parallel lists with `-1` sentinels. Seven-argument `0x16` carries three list pointers. Update object `+0x84/+0x88/+0x8C/+0x94`, caller word maximum then `+0x200`, global context `+0x828`, final four-argument `0x20` and return one. |
| `230A6C..230A9C` | Common complete restore/return, including delay slot. |

Twelve actual `rand` calls form four ordered triples at ROM `230030/38/40`, `2300C0/C8/D0`, `2301A0/A8/B0`, `230240/48/50`. Combination is `((r0<<18)&0x0C000000)|(r1<<15)|r2`, then unsigned modulo 100 using multiplier `0x51EB851F`. Preserve call order, conditional execution, unsigned comparisons, and signed half/quarter-related rounding corrections. Do not replace triples with one call or infer probabilities. Accepted `rand` owner `0002CBCC` at VMA `8009C7CC` returns `0..0x7FFF`; existing C is HYBRID, not a newly accepted PURE_C target. Early `00022E60` at `80092A60` is a distinct generator updating `800AA740`.

## Dispatcher and current table prerequisite

Indirect jump is ROM `0x230190` / VMA `0x801ECEC0`. Gate is raw descriptor byte `+0x54 == 8` and unsigned `(byte[+0x57]-2)<10`. Ten words at ROM `[0x239F50,0x239F78)`, VMA `[0x801F6C80,0x801F6CA8)`, SHA256 `F296C518868C06D24C3668A2300A30BF05429C77CB217A49272304BBF079F363`, lead to six unique internal destinations:

| Raw selector | ROM destination | Local effect |
|---|---|---|
| 2 | `230230` | Set flag one. |
| 3,7,8,9,10 | `2302B0` | Default flag remains zero. |
| 4 | `230198` | Primary flag gates triple-random comparison with halved threshold. |
| 5 | `230210` | Copy primary flag. |
| 6 | `230218` | Primary flag and signed comparison against object's unsigned halfword `+0x20` can set one. |
| 11 | `230238` | Primary flag gates triple-random comparison with quarter threshold. |

Out-of-range also reaches `2302B0`; outer byte gate bypasses at `2302C8`. Retain delay-slot byte clearing, optional `2379AC`, and eventual descriptor flag store. Literal table words, guarded destinations and row coverage do not establish new semantics or permit a structural partition.

Existing row 4248, `primary:bb297c4c5ff7ddb23a65`, owns `table_00239ec0.s`, ROM `[0x239EC0,0x23A3A0)`, 1,248 bytes, SHA256 `DB6B37FC1619F6541AC8FD469202CCE1A11A3B0686BAC43C0CF2AD7B1EFF10FF`. F580 also reads all three zero words at `[0x239F44,0x239F50)` / VMA `0x801F6C74`, SHA256 `15EC7BF0B50732B49F8228E07D24365338F9E3AB994B00AF08E5A3BFFE55FD8B`, for two pointer-list initializations. W7 has no F580 C/auxiliary ownership contract; the bounded working snapshots likewise have no F580 target/linkage/multi-owner/group entry.

Accepted `combat-shared-table-preparation-r1.md` and its review describe preceding B06C/EF50 compatibility: B06C `[239EC0,239EE8)`, retained D14C `[239EE8,239F28)`, EF50 entries plus padding `[239F28,239F48)`, retained ASM tail `[239F48,23A3A0)`. F580's first initializer word lies in that proposed EF50 padding and its next two words in the tail; its dispatcher table lies wholly in that tail. Preserve the actual initializer reads. The accepted preparation is not combined production acceptance. Future implementation must bind the actual preceding action-mode contracts and reconcile full row coverage before assigning actual emitted F580 rodata; keeping the complete tail while overlapping it with new C ownership is not valid. This note proposes no partition, padding definition, row merge, or extra source section. Any demonstrated representation change follows separate existing structural/tooling rules.

## Helpers and practical resumption

`helper-map.md` supplies every address/site count, accepted placement candidates, and corrected aliases; JSON preserves argument context before and after every call. Corrected aliases are positive at 18 F580 call records. They are navigation evidence only. `D_801F0E1C` must not be rejected solely for its prefix or promoted to a semantic name. At `801F4B5C`, same-slab `func_00237E2C` and overlay-8 `func_00145D3C` are competing address projections; preserve accepted caller context rather than assuming one universal identity.

Accepted interfaces directly constrain reconstruction: `201798(sourceArt,ownerContext,actionId,rawMode)` returns a byte (raw mode zero here); `1F197C(object,state,int*out)` returns a word and writes terminated output; `1F0E64` receives the masked selector. Existing `D2C0` wrapper forwards seven ABI words and `D334` nine; retain these contracts, including pointer-valued words and ninth argument at outgoing `sp+0x20`. The `0x16` call at `230974` passes kind, base-plus-first-time, zero, raw byte `0x207`, then lists `0x178/0x188/0x1B8`. `0x1F` publications split values into signed high shift and low byte, retaining further byte/global arguments. Original copy is source-first `(source,destination,size)`; original zero helper is `(destination,size)`, not a three-argument standard memset. `237D5C` writes two scalar words; B06C mutates one byte. Existing accepted C and original helper input hashes are indexed in the evidence.

For the future sole writer: first bind accepted complete preceding 17-member action-mode results, newest baseline and actual row contracts. Then settle concrete local declarations and producer count bounds, reconstruct all control regions and escaped-object calls, preserve the complete table/default path and all final list publication/return behavior, and only then iterate focused linked diffs. This preparation offers no compiler-shape workaround. Preserve the original ASM fallback and whole 5,404-byte target. Finish all seven original resolution members (`0022F580`, `00230A9C`, `002317C8`, `00231CC0`, `00232280`, `00232458`, `00232D7C`) before the combined final verifier. Original family and High Attack wave obligations remain unchanged; source activation has not been granted here.

## Output binding

All paths below are under ignored `build/combat-resolution-f580-preparation-r1/`. These are bounded preparation artifacts, not matching proof reports. Exact input hashes and accepted-versus-working snapshot distinctions are in `evidence.json`.

| Artifact | SHA256 |
|---|---|
| `prepare.py` | `F50E2A554B6DC2ACF03E8F281235E8D435AA61E60EF9006723792DD9FCD79772` |
| `evidence.json` | `26950953679541DE11BFAA132315CDC5804B44FAA9E3AFAF661FFE8655F2CE25` |
| `owner.txt` | `8E4F6E2C5EA21F66CB2AFCAE7C1CADF2AD182BE84994C8F3377C74C47852CAF2` |
| `helper-map.md` | `B2EDE011702845077B9084F74BFA24D8C4A1D84F0FAFD837E8CAC53E086C0112` |
| `dispatch-map.md` | `35D350FE62E97B619BEA60DF6B83E13B3CF5CF94AB73BFAA38D967B336D2741F` |
| `transfer-map.md` | `3311DE13B355304F6C1F14E85341DEE7D8F5AC31042A26F9DEF1C9EE68475799` |

No historical Resolver observation or scratch compiler metric is used as accepted evidence. Outstanding producer-capacity and combined table-ownership requirements are explicit source-preparation constraints, not claims that existing ownership is wrong. Director receives this bounded result for applicable review; no semantic, structural, or matching status is advanced.
