# Sequential Director resumption

In progress. Joe transferred the stalled Director task and restored the complete sequential program goal.
The new Director resumes Combat Wave 8 with preserved sources and the same acceptance gates.
An isolated compiler-allocation investigation precedes guided source experiments. Squad and High Attack still wait for Combat closure.

## History recovered

The retrieval worker parsed all 25,098 records in task `01a07262-aeca-7341-ad10-2dba705ff988` and all 18,089 records in its source worker.
The source worker was `/root/boot_materializer_reasoning`, task `01a0797e-a579-78f0-b60a-639cc6a8ce2a`.
The complete raw rollouts and extracted retrieval records remain local, outside tracked source history.
The bounded recovery is under ignored `build/task-recovery-20260907/`.

The parent recorded a remote compaction transport failure at 2026-09-07 20:09:55 UTC.
The message reports a disconnected stream and a response-body decoding error after 2,885,113 milliseconds.
Later turns were interrupted. Both the parent and its source worker were confirmed idle through the task API.
No technical decompilation failure follows from that transport error.

The current Director is `/root` in local task `01a07dad-52c1-7cb0-9913-d9f7afa91281`.
Joe's current goal preserves one family on main, internal workers, clean-room derivation, readable C and complete-wave verification.
The three families remain Combat, Squad and High Attack, in that order.
Ordinary matching needs no independent source review. Structural, tooling and applicable research changes retain review.
No new branch, worktree or push is authorized. Coherent local commits remain authorized.

## Preserved completed work

The sequential program remains the current acceptance index.
Accepted results include selective frozen-family intake, compilation-group support and its review, readability, and all three boot hybrid conversions.
Combat Waves 5, 6 and 7 remain accepted at `d70fd85`, `0e11910` and `469a1416` respectively.
There is no reason to repeat their unchanged full-ROM verification during takeover.

Wave 8 has nine new focused exact candidates and the previously accepted `func_0020BFF8`.
These nine candidates remain provisional. No complete Wave 8 verifier has run.
All fourteen original members remain required.

## Recovered interruption state

| Target | Recovered state | Consequence |
|---|---|---|
| `func_001F3C00` | Best preserved source; 6752-byte candidate versus 6740-byte owner | Original ASM remains its temporary development fallback. C must be reactivated before final verification. |
| `func_001F6098` | Best preserved source; 4148 bytes, 408-byte frame, 46 native word differences | Continue source matching. |
| `func_001F7ADC` | Worktree contains the last failed 3804-byte shared-full-render-words control | Preserve that failed trial, then restore the 3800-byte reused-command-plain-line best candidate. |
| `func_0020DB10` | Guarded-dedup-for candidate; 5548 bytes, 552-byte frame, 28 native word differences | Continue from this newer candidate; its earlier linked proof belongs to a different source. |

The renderer's interrupted source was not restored before the old task stopped.
This differs from the earlier progress note and is explicitly bound in the R2 continuation assignment.
Native diagnostic differences do not establish linked-byte or matching acceptance.
The preserved best renderer still has 48 differing linked bytes and 29 differing words.
DB10's known failed separate-latch control has a 560-byte frame but incorrect latch accesses.

## Current routing and preserved holds

Routing commit `22fba854` activates the isolated DB10 allocation-trace investigation.
Routing commit `e585a9e8` assigns the complete W8 R2 continuation to `/root/combat_draw_continuation` for initial recovery.
Production writes wait for the diagnostic worker's release. Reusable new diagnostic claims wait for independent review.
The diagnostic compiler is isolated; production compiler identity, flags and acceptance rules remain unchanged.
Guided source searches follow only where evidence supplies meaningful alternatives.
Reduction remains optional until a precise allocation property can be preserved and retested in full DB10.

The held Resolver proposal, provisional test changes, runtime controls and capture state remain untouched.
The earlier automatic review rejection is preserved; this matching continuation neither retries nor depends on those protocol changes.
Selector observation remains a separate unresolved discovery gate. Static evidence does not establish selector non-use.
All later shared, supplemental, structural, semantic and family-completion obligations remain in the sequential program.
