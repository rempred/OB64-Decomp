# Combat draw W8 R3

Unresolved handback. Receiver `/root/db10_allocation_trace`, Astra Medium, held sole production source/build ownership after R2's explicit release and now releases all writes.
Director `/root`; activation HEAD `2936bfc37725b011139b894c91d43cf00de113d5`; launch `COMBAT-DRAW-WAVE8-20260908-03`.
The fresh claim was created atomically and read back. All fourteen sources and six header/configuration hashes match R2's terminal index.
Starting sources and exact input index are preserved under ignored `build/combat-draw-wave8-r3/`.

All fourteen targets remain required in one final full-wave verifier. Ten provisional focused-exact candidates and accepted BFF8 stay preserved.
Three targets remain nonexact: 3C00, 6098 and DB10. No full verifier has run.
No process remains running. Held Resolver edits and the Director's untracked/disjoint task records are untouched.

This pass tested 3C00's real row-loop state and remaining companion/strip allocation, keeping D037's proven pointer and endpoint distinctions.
Prior failed null timing, copy/scope, shared size-cursor and endpoint controls remain frozen.
Source changes must retain the real operations and full-target evidence; native scores are development diagnostics, not acceptance.

## First real row-end state controls

`row-end-coordinate-state` retains the unpacked row-end coordinate across its actual vertex and packet consumers.
It keeps extent 6740 but reaches frame 512/native 434, versus D037's frame 504/native 209.
Fresh pinned/diagnostic agreement shows companion100 in s6 and strip115 in s8, matching those retail roles.
However row116 and u1 move to s7/s5, and a real accessed home is added. This remains a failed control.
The row-end pseudo715 has 16 references across 384 instructions and 25 calls; strip references fall from 40 to 28.

Separating the real consumer groups gives vertex-only and packet-only controls.
Both shrink to 6732 bytes, so neither can replace the accepted 6740-byte owner.
Their native diagnostics are 475 and 433 respectively. Complete source/input/compiler/object/policy packages are preserved.
The vertex-only trace retains the original companion/strip register assignment and frame 504; no new source exception follows.
D037 is restored after every control. Sessions25184,88087 and60897 have completed; no full or focused linked verifier ran.

`branch-complete-companion-cursors` duplicates only the current companion tail into its two format branches, keeping independent cursor loads, WIDTH order, 8/C stores and the common primary body. This differs from the older broad complete-packet duplication. It produces6768 bytes/frame504/native343 and is rejected. Session65164 completed and D037 was restored. Its unchanged diagnostic compiler agrees with pinned output.

## 6098 range-update control

`unsigned-component-range-guards` expresses each existing out-of-range component update as `(u32)next >= 256u`, followed by the actual signed low/high choice. In-range values retain the preceding component. The float calculations and conversion remain unchanged. It produces4132 bytes/frame408/native496, versus D764's4148/frame408/native46. Session94076 completed; D764 is restored.

Pinned-agreed tracing still records23 accessed scalar homes and zero COMBINE_USE events. sourceY remains at164 and the decoded pointer at16C. This control does not explain the two groups of three unaccessed retail positions. The disjoint neighbor comparison likewise finds the existing5654 vertex/packet forms already present in D764; it supplies no new missing-home recipe.

## Real overlap-step control

`shared-overlap-step` completed under session70991. It shares the real strip-minus-one step among accumulation, normal/flipped row and remaining updates, without lengthening vertex/packet state. This tests a different source value from the retained row-end controls. It produces6724 bytes/frame504/native266, so it is rejected against the6740-byte owner. Pinned-agreed tracing gives strip115=s8 and row116=s5, but companion100=s7 and u1-114=s6 remain wrong; tile922 also uses s6. These partial role correspondences do not repair the missing16 bytes or establish a retail compiler history. D037 is restored.

## Complete control matrix and evidence

| Target/control | Bytes | Frame | Native differences | Selected |
| --- | ---: | ---: | ---: | --- |
| 3C00 starting D037 | 6740 | 504 | 209 | preserved |
| whole-row endpoint | 6740 | 512 | 434 | no |
| vertex-only endpoint | 6732 | 504 | 475 | no |
| packet-only endpoint | 6732 | 512 | 433 | no |
| branch-complete companion cursors | 6768 | 504 | 343 | no |
| shared overlap step | 6724 | 504 | 266 | no |
| 6098 starting D764 | 4148 | 408 | 46 | preserved |
| unsigned component range guards | 4132 | 408 | 496 | no |

Native comparisons exclude actual relocation words and are development diagnostics; shifted instruction layouts can inflate their scores. No result above is a linked acceptance result. Each new control retains authored and expanded source, mechanical source-policy result, pinned compiler assembly, assembled object, actual relocation data, native comparison and a complete pinned-agreed diagnostic pass set. The unchanged tracer was reused without instrumentation changes. `results.json` binds every new authored/expanded input hash and diagnostic agreement.

All fourteen sources and six header/configuration files were rehashed at handback and exactly equal the starting inputs. No source candidate or configuration was selected. The ten focused-exact candidates and previously accepted BFF8 remain preserved. 3C00 remains D037 (focused770 bytes/261 words different), 6098 remains D764 (focused56 bytes/46 words different, frame408 versus456), and DB10 remains5BF6 (frame552 versus560 and its separate initialization-order residual). These are the inherited authenticated residuals, not new linked measurements. No focused linked build or full-wave verifier ran in R3.

The DB10 predicate-origin reader found no new comparison-only-home lead: its four inspected sort/run predicates stay emitted, unlike the three established entry-zero-folded homes. The 6098 neighbor reader found the relevant5654 vertex and primary-packet expressions already present. Their frozen reports remain separate evidence; R3 does not repeat their negative inventories or claim that all source forms are exhausted. No justified new operation/lifetime discriminator remains from this bounded pass. Further work still needs one; these failures do not establish pure-C impossibility.

The ignored evidence root is `build/combat-draw-wave8-r3/`. Terminal input index SHA256 is `F42C3D2C13B5CD1ED6F75C9BEDF0110A043589FC94D790BAA2FFA87931613E30`; result matrix SHA256 is `B2EE42BBC29F430DB2B60685E0894A99BC91F9DEEABA070D316FE8C68A690CA2`; manifest SHA256 is `67330515115AD35A16C5D7AA953806DFED1136A27B0B19CDA65ABE265CE248A5`, binding262 files. `finish.js` authenticates the unchanged20 inputs and all six diagnostic source/input agreements. All compiler sessions listed above ended normally; no handles need resuming.

R3 is terminal and its claim/report/evidence are frozen. Production source/build ownership and all R3 writes are released to the Director. W8 remains incomplete: all fourteen targets still require one combined final verifier after the three residual targets are solved. No compiler, tool, source-policy, ownership, structural, runtime, Resolver or Git change was made, and no new review gate is introduced.
