# DB10 preserved allocation survey R1

Completed. None of the93 authenticated distinct compiler inputs has more than three non-emitting scalar homes. Larger frames add accessed storage. The Director can return this bounded negative result and the per-input role census to the source worker. It does not explain retail's missing home or establish pure-C impossibility.

## Scope and frozen corpus

Receiver `/root/db10_allocation_trace`; Director `/root`, local task `01a07dad-52c1-7cb0-9913-d9f7afa91281`. Assignment revision1, launch `COMBAT-DB10-PRESERVED-ALLOCATION-SURVEY-20260907-01`. The permanent claim was created atomically and read back before other writes.

Starting main HEAD was `efdd228eab332b947ba9f99f905b6397c28ffcd4`; preceding prompt coordination was `9a7251d1`. The governing guides and accepted unchanged diagnostic report/review were reused. Current R2 survey and combination records were read. The source worker retained production ownership while returning to3C00.

The exact corpus is the96 direct files matching `build/combat-draw-wave8-r1/func_0020DB10.*.c`. No recursive diagnostic directory or other target was added. `corpus.json` preserves every absolute source path, byte size, source hash, copied filename, source kind, retained assembly identity, duplicate disposition, and complete input hash.

| Corpus stage | Count / disposition |
|---|---|
| Filename matches | 96 |
| Authored snapshots | 94 |
| Expanded-input copies | 2 |
| Distinct source-byte files | 95 |
| Distinct complete compiler inputs | 93 |
| Exact-input analyses reused | 13: ten R2 packages and three accepted DB10 packages |
| Previously unexamined complete inputs | 80 |
| New compiler invocations | 160: one pinned and one unchanged tracer per new input |
| Unsupported, failed, or unexamined distinct inputs | 0 |

`shared-final-counter.c` duplicates `pre-count-induction.c` byte-for-byte. Their compiler input was handled once.

`best-linked.input.c` is the expanded input for `null-first-peer-flags.c`, hash `E9280D970F8CFFE1CAB7AD40438903C8F7297FFEFDD53CA25E4112C597879268`. `input.c` is the expanded input for `guarded-dedup-for.c`, hash `08561F8CD28616666538499F3F86CD3BB4A58D38B608DB0872742DFF5DCC375D`. They add no authored candidates or distinct invocations.

Both expanded copies lack a separately named `.s` sibling under their literal prefix. Exact-input reuse resolves that evidence distinction. Every authored snapshot has retained assembly. All94 authored assemblies agree with the corresponding pinned output after only `.file` metadata is excluded.

## Authentication and reuse

The accepted tracer remains SHA-256 `457429BD4F33D7E841961AA1E76F238BB58117B8D0AEB7B1CA14908D2B7113A6`. The pinned compiler remains `F3F1C99A322F5B3D8C108C2A44AF1D6D084DD27575C5D60BF0F0D33FFF34B1C6`. Both were copied into this task's ignored root and authenticated before invocation. No executable, instrumentation, compiler flag, or build contract changed.

The93 distinct authored files were mechanically preprocessed/classified as PURE_C using the existing API; the duplicate inherits that same source identity. This is classification, not matching acceptance. Exact expanded-input copies were identified against already-authenticated inputs.

The two R2 indexes supply37 prior diagnostic packages; the accepted original DB10 package supplies three more. `reuse-catalog.json` records all40 candidate input identities and the authenticated files used to decide reuse. Thirteen inputs intersect this corpus. Their compiler invocations were not repeated.

For reused packages, input bytes, command status, executable identities, pinned/diagnostic agreement, and supplied analysis hashes were checked. Accepted package files were also checked against its reviewed manifest. Fresh corpus preprocessing provides exact-input equality, rather than a source filename guess.

Every new complete input received fresh pinned and unchanged-tracer invocations. Assembly agrees exactly after removing the sole `-da` option-comment insertion. All source, input, binary, command, assembly, and trace identities remain available per input. No diagnostic conclusion precedes this agreement check.

## Allocation results

The following table counts distinct complete inputs. Initial pseudo homes are `alter_reg` HOME events. The separate column includes scalar allocation outside those events.

| Frame bytes | Initial pseudo homes | Non-emitting comparison homes | Other accessed scalar allocation | Inputs |
|---|---|---|---|---|
| 536 | 5 | 2 | 0 | 17 |
| 536 | 5 | 3 | 0 | 1 |
| 544 | 6 | 2 | 0 | 10 |
| 552 | 7 | 3 | 0 | 62 |
| 560 | 8 | 3 | 0 | 2 |
| 576 | 9 | 3 | 1 | 1 |

All inputs allocate432 fixed-local bytes. All609 initial pseudo homes use `reload-initial`, `from_reg=-1`, SI mode, four requested bytes, eight-byte effective alignment, and eight rounded bytes. The big-endian correction is four.

Across these candidate compilations,252 homes are non-emitting. Each retains one standalone USE with REG_DEAD in local-allocation output and no emitted stack access/address formation. Every directly retained originating definition is an unsigned less-than comparison between register values. None adds a fourth non-emitting home.

Every other initial home has actual emitted accesses. The survey additionally checked every STACK event, rather than assuming every scalar allocation has a HOME event. The one additional scalar allocation is accessed, as described below.

These totals describe this corpus's compiler observations. They are not runtime event counts, independent function counts, or accepted matching progress.

## Selector, latch, and pointer distinctions

`role-census.json` records the actual accesses and selected source/pass origins for each input separately. All stack offsets below are hexadecimal bytes relative to that candidate's function stack pointer.

Every input has selector pseudo72 at1CC, directly initialized from a0. Every input has the shared/first-latch pseudo73 at1D4, initialized to zero. The split-latch control retains the first latch there and adds the second separately.

The contexts-base-minus-four pointer has a home in91 inputs. Its initial virtual-local origin is+68 decimal, corresponding to the real address `sp+5C`. The resources counterpart has a home in75 inputs: virtual-local+140, corresponding to `sp+A4`.

Their pseudo numbers and home offsets vary by input and are recorded individually. Missing home events do not mean missing pointers or missing array operations. Different source forms can retain addresses in registers or use other real base representations. No synthetic missing-home entry was added.

Three larger-frame controls deserve explicit distinction:

- `distinct-second-latch.c`, frame560: additional real pseudo459 at1DC has three emitted accesses. Pointer861/870 homes are1E4/1EC. This is the known wrong second-latch location, not a new non-emitting home.
- `early-queue-counter.c`, frame560: additional real count pseudo74 at1DC has six emitted accesses. Pointer832/841 homes are1E4/1EC. Source and initial RTL identify the early count lifetime; the control remains failed.
- `named-sort-bases.c`, frame576: accessed bases794/795 occupy1DC/1E4; resource-minus-four870 occupies1EC, and another shifted-array address887 occupies1F4. Its three USE-only homes are1FC/204/20C.

The named-base control also requests four SI bytes after initial reload allocation, with alignment argument0 and effective alignment4. Local extent grows504→508; final frame alignment yields512 local bytes plus24 outgoing and40 saved-register bytes.

That later allocation returns `sp+210` (528 decimal). The emitted store/reload preserves t4 across `func_002015C8`; global-pass UIDs3685/3686 bracket call2819. It is therefore accessed storage. The unchanged tracer does not identify its allocator call path or pseudo, so neither is invented here.

`stack-census.json` preserves that extra allocation separately from the nine initial pseudo homes. It does not change the maximum of three non-emitting homes.

## Corpus limits and consequence

This is a complete survey of the frozen96-path R1-root corpus, deduplicated to93 complete inputs. Other R1 subdirectories, later R2 source experiments outside exact reuse, and hypothetical new forms are outside its negative boundary.

The survey found no additional non-emitting pattern to route as a source lead. Frames560 and576 do not establish matching success. The actual selector/latch/pointer roles and accessed spills must remain separate from total frame size.

No retail compiler input or allocation trace is available. The result cannot identify retail's unused storage, prove that another pure-C form cannot reproduce it, or justify padding, artificial references, compiler exceptions, or structural changes.

All fourteen W8 targets and their single final complete-wave verifier remain required. This application of the reviewed tracer adds no ordinary matching review gate. No source recipe or new reusable-method or semantic claim is proposed.

## Evidence and release

All evidence is under `build/combat-db10-preserved-allocation-survey-r1/`. `summary.json` contains the corpus and pattern totals. `units.json` records exact-input deduplication/reuse. Each input directory contains the copied complete input, assembly, raw trace, selected retained passes, and detailed `analysis.json`.

`corpus.json` and copied source files preserve all96 original identities. `role-census.json` maps every input back to all corpus aliases. `reuse-catalog.json` preserves the exact reused source paths and file hashes. Original R1/R2 and accepted diagnostic packages remain read-only.

The scripts are task-local orchestration and parsing only. `prepare.js` freezes the corpus and authenticates reuse; `run.js` performs only unexamined invocations. Existing results prevent an accidental repeated compiler survey. Use the following commands to reanalyze preserved outputs without compilation:

```powershell
node build/combat-db10-preserved-allocation-survey-r1/run.js read
python build/combat-db10-preserved-allocation-survey-r1/summarize.py
```

No compiler failure occurred. The initial origin parser included inline-helper RTL sections; it was narrowed to `func_0020DB10` before final role attribution. Only parsing was repeated. The all-STACK check detected the named-base allocation outside HOME logging and recorded its real accesses. No instrumentation extension was needed.

Only the assigned claim/report/ignored root were written. No source trial, production/configuration/tooling change, canonical diff/build/verifier, runtime/GUI/bridge/database action, agent launch, or Git mutation occurred. No canonical-document change is proposed.

`output-manifest.json` binds final artifacts, this report, and the permanent claim. Final checks reauthenticate the corpus and reused inputs. All commands finished, and all assigned writes are released at collaboration handoff.
