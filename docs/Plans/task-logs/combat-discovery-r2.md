# Combat discovery r2 log

Status: completed bounded research; review pending. Writes released to Director /root at terminal handoff.

Activation: combat-discovery r2, COMBAT-DISCOVERY-20260906-02, /root/combat_discovery, local. Canonical main baseline 5bdac865c53718f7c5352e41619b0e00555f0333. All owned paths absent; claim created exclusively before any other writes. Concurrent tooling and propagation changes are disjoint and preserved.

Plan: independently derive the fixed-image transformation from original instructions and inspect its output. Trace its caller inputs and final drawing consumer. Test whether the decoded key occurrence belongs to a typed reference structure or image data. For the selector, use a stronger bounded entry/dataflow analysis and retain any irreducible runtime question. No raw/decoded literal rescan is planned.

Completed: authenticated frozen retrieval and source/parser/ROM identities; independently decoded the fixed-image key; checked exact instruction words in thirteen original ASM files; reconstructed and cross-checked every visible pixel of all 101 strips. Visually inspected the final legible index-grayscale sheet. The labels and full-image texture-rectangle draw chain support a review-pending attack-label service exclusion. Both immediate callers use the complete checked 160-byte mapper whose values lie in 0..100.

The retrieved key occurrence has no typed loader-reference provenance. A whole-object RGBA32 hypothesis visualization is coherent but is explicitly not a proven format or replacement for the SPRITE_CI8 retrieval label. It does not support an additional resource consumer.

Selector: checked raw resource header publication and the twelve bytes between offsets 08A0 and 08AC. Four width-three groups are conditional data views, not proved argument domains. Enumerated all encoded J/JAL/relative branches in accepted descriptor-10 text against every selector instruction; zero incoming direct edges and no ordinary predecessor fallthrough. Preserved all 338 indirect transfers as unresolved, including jr-ra returns. No dead-code or exhaustive-runtime conclusion. Report specifies the smallest qualified positive invocation/provenance observation.

Verification command: python -B build/combat-discovery-r2/selector_check.py; passed all assertions and generated image-evidence.json / selector-evidence.json. No C/build/runtime test applies to this report-only research. Generated evidence and scripts remain ignored. Artifact manifest freezes their SHA-256 values; tracked report and log have no trailing whitespace and the complete claim retains its assigned identity.

Rejected experiment: trying the fixed-image boot-LZ decoder on raw resource 00315736 failed with a back-reference-before-output error. The actual loader/copy chain and accepted raw-resource evidence require no codec; authenticated raw payload parsing then succeeded. This failed hypothesis was not hidden or bypassed. A broad text search supplied a literal wildcard filename that rg rejected, and one guessed original source filename was absent; subsequent evidence uses the existing accepted raw-copy account and exact initializer instructions. Neither lookup issue affects the passing bounded checkers.

Director was informed that r2 performed no current Total Resolver status/verify or knowledge query. Director separately assigned Sol High combat-selector-runtime-retrieval-r1; later evidence requires a fresh interpreted addendum and must not silently modify this terminal r2 record. The report retains the current-query and frozen placement-only limits.

Only assigned report/log/claim and ignored output were written. Concurrent tooling/compilation-groups edits were preserved; prior research, accepted inventory/plans, production source/config/tooling, historical worktrees, and runtime databases were not changed. No subagents, branches/worktrees, builds, compilation, emulator/capture, external source access, staging, commit or push. Independent Material review remains required before using the new scope/semantic proposals. All original matching and completion gates remain.

