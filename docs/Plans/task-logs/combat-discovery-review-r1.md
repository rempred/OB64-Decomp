# Combat discovery r1 independent review

Verdict: **Accepted with corrections**. The frozen Combat discovery result
passed the assigned material review. Seven boundary candidates and five
additional shared integrations have supported family relationships. The
ten-site, 156-slot dispatch census; selector-root publication chain;
conditional group-2 renderer owner; registration identity and scalar argument
contract; and twenty-record allocation/traversal measurement follow from the
allowed static and saved-placement evidence. One parent-document line citation
drifted because unrelated uncommitted insertions shifted an already committed
passage. The Director may propagate the accepted dispositions after pinning
that citation to the recorded parent commit and lines 787–799. The Director
must retain the `func_001F0C24` fixed-image gate, the `func_00201778` consumer
gate, and every stated placement, execution, semantic, and matching limit.

## Frozen subject and eligibility

The frozen subject is canonical commit
`150c9fca5d371fc7ad9245fcf6f94268cdf25311`. The reviewed result is
`docs/Plans/combat-discovery-r1.md` with SHA-256
`EA6A2F9DB52A559B1241DF239E7D92A51AA9A25718D996438E768EAD2F28F684`,
plus `docs/Plans/task-logs/combat-discovery-r1.md` and the identified ignored
evidence under `build/combat-discovery-r1/`. A direct Git comparison confirmed
that both reviewed Markdown files remain identical to the frozen commit.

The worker had completed the assignment and released its writes before review.
The review claim was absent and was created atomically before any other review
write for launch `COMBAT-DISCOVERY-REVIEW-20260906-01`. The claim identifies
reviewer task `/root/combat_discovery_review` on host `local`. The assigned
report and ignored evidence root were also absent. Existing canonical and
parent changes were attributable and outside the assigned surfaces. The later
production tooling writer does not own these review paths or the frozen
research inputs. The review remained eligible under the `NORMAL` inventory
profile.

## Claims reviewed

The material questions were:

- whether each of the eight boundary candidates has the proposed family
  disposition;
- whether all ten jump-table sites and 156 entries remain inside their accepted
  owners;
- whether the nine selector entries have an adequate bounded reference account,
  especially the data-only account for `func_00201778`;
- whether accepted group-2 configuration, ABI, and saved placements support
  `func_0025FD90` as the renderer interface at live `0x8020B1E0`;
- whether live `0x800EA604` maps to `func_0003fb94` and receives a scalar rather
  than an executable callback from `func_00208DC8`;
- whether the proposed shared integrations preserve all 67 original shared
  owners and the existing generic-service boundaries; and
- whether the allocation wording should state twenty measured records while
  leaving occupancy, capacity, and safety questions open.

## Admissible findings

There are no blocking findings.

### Boundary candidates

| Candidate | Review judgment |
|---|---|
| `func_001F0C24` | **Candidate; gate retained.** Static words prove construction and publication of a fixed 90-by-15 image using resource key `0x00391270`. Its callers pass the preceding resource result and then the returned image to live `0x801B5CAC`. This does not identify the image as a selected body/pose-table owner or prove its final draw role. |
| `func_001F0E64` | **Supported inclusion.** It writes selector `+0x04`, resets adjacent program state, is called with constructed-object state `+0x44`, and joins the selector reads in `func_001F1218`. The accepted parent runtime passage independently corroborates the write/read path. |
| `func_002071F4` | **Supported inclusion.** Its root, `0xB0` stride, pointer fields, and expiry counters join the established materializer/renderer pool. |
| `func_002073CC` | **Supported inclusion.** It searches the same pool and publishes resource-backed arrays and associated metadata used by the existing W7/W8 path. No visual name for key 5 follows. |
| `func_00207A70` | **Supported inclusion.** It frees and clears the pool's decoded and fixed cached arrays while preserving the containing records. |
| `func_00207C08` | **Supported inclusion.** It releases the same record resources and clears complete `0xB0` records. Unqualified later-overlay encoded callers remain contextual leads. |
| `func_0020CBDC` | **Supported inclusion.** It consumes actor selection inputs `+0x48/+0x4C`, calls the selector, publishes `+0x50`, constructs resource-backed records, and initializes body-program state on the W8 path. |
| `func_0020D590` | **Supported inclusion.** Its inputs and direct calls join actor selection state, pose metadata, and the existing bound-query path. The stronger axis or visual meaning remains unproved. |

These are family-inventory judgments at **Supported** grade. They do not confer
`PURE_C`, linked ownership, target-byte equality, or complete-ROM acceptance.

### Dispatch and selector accounts

Independent decoding found ten `jr $v0` dispatches in the eight assigned
owners. `func_00207658` contains three; each other owner contains one. The two
opcode interpreters use 22-entry bounds and the other eight sites use 14-entry
bounds. All 156 ROM table values map through accepted descriptor 10 to an
address inside the exact containing semantic row of the dispatch owner. This
accepts the immutable destination census at **Verified static** grade. It makes
no case-execution or immutable-runtime-storage claim.

The independent encoded-target counts for the nine selectors were
`4, 18, 18, 5, 12, 7, 0, 12, 8`. No aligned literal word or raw four-byte entry
pointer occurred anywhere in the normalized ROM for those nine live entries.
For `func_00201778`, static dataflow independently confirms
`root[3*a0+a1]` at RAM root `0x801D0688`; `func_00201BAC` adds resource-relative
halfword `+8` to its resource base and publishes the resulting pointer to that
same root. This supports family-table membership. It does not resolve the entry
consumer or prove dead code. The open gate remains a decoded-data provenance
chain or a qualified execution observation at live `0x801BE2E8` with caller,
target origin, placement, and arguments preserved.

### Renderer and registration interfaces

Both renderer call sites encode live `0x8020B1E0`. Accepted configuration maps
that address to descriptor 9 interior ROM `0x0015C350` or descriptor 12 entry
ROM `0x0025FD90`. Group 2 contains descriptor 12 and excludes descriptor 9.
The accepted atlas has 226 shared input IDs for the call owner
`func_001F3C00` and `func_0025FD90`, and zero for `func_001F3C00` and
`func_00159FD4` in this query. The callee ABI also agrees with the signed
halfword interpolation/copy function. This supports `func_0025FD90` as a
shared numeric interface for the accepted group-2 path. Saved placement is not
execution. A different group or a universal owner claim still requires
call-time residency and target-byte evidence.

`func_00208DC8` directly calls live `0x800EA604` on its qualified null-object
branch. The accepted atlas maps that address to `func_0003fb94`; all 244 saved
inputs containing the caller also contain that placement. Static argument flow
shows the caller forces `a0` to zero and forwards `a1` as scalar state.
`func_0003fb94` uses its low halfword as `a2` for live `0x800EA924`; it does not
call through `a1`. The shared state/dispatch interface is supported. Downstream
`0x800EA924` behavior and exhaustive asynchronous semantics remain open.

### Shared integrations and pool wording

The report conserves all 67 unique owners in the original shared table.
`func_0020C908` directly produces the actor selection inputs.
`func_0020FC7C` sends object state `+0x44` to the body-program initializer.
`func_0021088C`, `func_0021062C`, and `func_00210930` form a specialized
loader/accessor/cleanup set whose result controls the W8 flag update and
program-state call. These five additions have supported integration
relationships. The report correctly preserves the generic resource, memory,
matrix, numeric, and PRNG services as interfaces and retains every original
High Attack owner and bounded exclusion.

The allocation and clear size is `0xDC0`; the record stride is `0xB0`, so the
measured allocation covers twenty records. Maintenance and search loops compare
against `0x14`. The materializer's free-slot countdown starts at `0x13` while
testing index zero, and static all-full control flow advances to index 20. The
Director may replace the old “19-record/0xB0 pool allocation” phrase with:
“allocates and clears `0xDC0` bytes, covering twenty `0xB0` records; maintenance
and search traverse twenty entries.” Simultaneous occupancy, semantic capacity,
the all-full precondition, runtime reachability, and safety remain separate
questions.

### Required correction CR-1: parent citation line drift

The `func_001F0E64` candidate row cites parent
`docs/combat-animation-system.md` lines 1071–1081. At the report's recorded
parent commit `61ad769fec4d60e17940c08ab4bb536342764d31`, the accepted passage
is lines 787–799. It contains the same tested command-to-request-to-selector
write/read chain and the same z64 addresses. Unrelated uncommitted insertions
shift that passage to current lines 1071–1083.

This is a non-semantic citation defect. The evidence existed at the frozen
parent commit, and the static assembly independently supports the same
relationship. The Director must cite the recorded commit at lines 787–799 or
use the stable z64 addresses when propagating the result. No worker research
correction or proportional re-review is required for that bounded cleanup.

## Review method and reused frozen evidence

I read the canonical and parent agent guides, the parent reviewer workflow, the
controlling Combat plan and inventory, the frozen report and log, the accepted
overlay configuration, relevant original assembly, and the saved-atlas scope
and schema. I inspected the worker's ignored scripts and JSON for completeness
and limits.

The independent checker did not consume the worker JSON. It pair-swapped the
parent V64 ROM in memory, authenticated the normalized image, parsed every
original assembly word, derived descriptor-10 dispatch tables, scanned the
complete normalized ROM for selector pointer bytes, checked selected
instructions and call edges, parsed group membership, and made read-only SQL
joins against the accepted offline atlas. Manual inspection checked the
candidate functions, shared dataflow, controlling boundary rules, and parent
citation provenance.

Reused frozen evidence was limited to the accepted semantic and overlay config,
original assembly, the authenticated Rev 0 ROM, the accepted saved-placement
atlas, the committed parent animation and overlay findings, and the worker's
frozen artifacts under `build/combat-discovery-r1/`. No external source,
runtime operation, C compilation, build, audit, or production mutation was
used.

Independent evidence:

- [accepted review checks](../../../build/combat-discovery-review-r1/accepted/review-checks.json)
- [review checker](../../../build/combat-discovery-review-r1/review_checks.py)
- [parent citation provenance](../../../build/combat-discovery-review-r1/citation-provenance.md)
- [inventory conservation](../../../build/combat-discovery-review-r1/inventory-conservation.json)
- [preserved first tool failure](../../../build/combat-discovery-review-r1/attempt-1/failure.md)
- [preserved display-command failure](../../../build/combat-discovery-review-r1/attempt-2-display/failure.md)

## Tests and results

| Acceptance check | Result |
|---|---|
| Frozen identity | Report SHA-256 matched `EA6A2F9DB52A559B1241DF239E7D92A51AA9A25718D996438E768EAD2F28F684`; report and worker log had no diff from `150c9fca5d371fc7ad9245fcf6f94268cdf25311`. |
| ROM and source identity | In-memory normalized ROM was 41,943,040 bytes with SHA-256 `571E83396BC81E70DA4C0A20313D82DBD7DFE685F2C37418C8E27F927E2CC67A`; 6,184 assembly files yielded 1,627,611 unique original words. |
| Dispatch falsifier | Passed: ten sites, 156 entries, every entry inside its accepted owner row. |
| Selector falsifier | Passed within the claimed forms: direct counts matched; `func_00201778` had zero encoded J/JAL targets; all nine entries had zero aligned and raw pointer-byte hits. Negative-scan limits remain explicit. |
| Candidate/shared dataflow | Passed selected ROM/source-word and named-call checks, including selector publication, pool stride/bounds, actor-input production, state initialization, W8 integration, fixed-image construction, and registration argument flow. |
| Renderer placement | Passed: group-2 membership matched; saved input intersections were 226 for `func_0025FD90` and zero for the descriptor-9 alternative. |
| Registration placement | Passed: direct target was live `0x800EA604`; caller/target shared-input intersection was 244; atlas locations agreed. |
| Shared-owner conservation | Passed: 67 rows, 67 unique owners, zero missing from the frozen report. |
| Citation provenance | Passed with CR-1: the accepted passage exists in the recorded parent blob at lines 787–799; only its working-copy line position changed. |

The first checker run failed before producing a result because three reviewer
expectations encoded `$v1` where the source and authenticated ROM encode `$a1`,
`$a1`, and `$v0`. Direct inspection identified that reviewer-tool error. The
original tool and failure are preserved. The narrow repair changed those three
expected words and added an explicit clean output root. The repaired checker
then passed in a fresh `accepted/` directory. Checker SHA-256 is
`017FABFA56674DB2D6E5F429C9913A390AFEC5B5F76ADE2A7176A2B2C1B835CB`;
accepted JSON SHA-256 is
`52FB3999EF77BDA84161554D4FDEBEEFD051C48A42DE7695F6B5FCCC8D22B9F6`.
A later read-only artifact-listing command had a PowerShell parser error because
it piped directly from a `foreach` statement. The corrected command first
stored the loop results, then formatted them and confirmed all assigned
artifacts. It did not execute or alter an acceptance check.

## Evidence limits

- Direct words, table contents, byte counts, and fixed configuration are
  **Verified static**. Family membership and interface interpretations are
  **Supported**. The unresolved image role and indirect selector consumer
  remain **Candidate**. No claim is Editor-ready.
- Saved-atlas rows establish stored placement observations. They are not call
  execution counts, universal residency, or proof about an unobserved group.
- The encoded-address and byte scans cover the stated representations. They do
  not exclude decompressed pointers, synthesized values, aliases, interior
  entries, runtime stores, or other indirect mechanisms.
- The fixed image is proved as an image producer only. The registration result
  is a bounded state/dispatch interface only. Broader visual, callback, timing,
  and asynchronous behavior remains unnamed.
- The twenty-record measurement does not establish permitted occupancy,
  business capacity, or a retail bounds defect.
- This review did not run the source-policy tool, linked diff, compiler, full
  build, or canonical verifier. Generated status and the later complete-wave
  verifier remain authoritative for source class, ownership, exact bytes, and
  matching-C acceptance.

## Documentation consequences

The Director may update the canonical Combat plan and inventory with the seven
supported candidate inclusions, five supported shared integrations, the
conditional group-2 renderer interface, the registration state/dispatch
interface, the verified internal dispatch census, and the corrected measured
allocation wording. That propagation must apply CR-1 and preserve the current
generic-service interfaces, High Attack context, bounded exclusions, and
original shared-owner accounting.

The family cannot be called exhaustive or complete while the fixed-image and
selector-consumer gates remain unresolved. Current source classes and future
matching results must come from generated status and the normal complete-wave
acceptance gates.

## Exact next route

Director `/root` on host `local` should:

1. apply or route the citation-only cleanup in CR-1, using parent commit
   `61ad769fec4d60e17940c08ab4bb536342764d31` lines 787–799 or the stable z64
   addresses;
2. propagate the accepted research dispositions into the controlling Combat
   plan/inventory and assign each accepted shared implementation once through
   the sequential production route;
3. route a bounded qualification of resource `0x00391270` and the final draw
   role for `func_001F0C24`; and
4. route decoded-data provenance or a qualified runtime pointer-origin
   observation for `func_00201778` at live `0x801BE2E8`.

No additional group-2 renderer experiment is required for the accepted
conditional claim. Claims for another group need call-time residency and target
bytes. Downstream registration semantics at live `0x800EA924` need separate
consumer evidence only if the Director wants a stronger interpretation.

The reviewer releases the claim, report, and ignored evidence writes after the
terminal collaboration handoff.
