# Independent Material review: guided upstream permuter trial

**Verdict: Accepted.** The frozen trial supports optional bounded permutation searches for independently justified source relations. It proves tool compatibility and faithful enumeration, without proving faster matching or any source match. Director `/root` may propagate this limited research use. Ordinary W8 work must retain its canonical source and complete-wave gates.

Reviewer `/root/db10_trace_review`; Director `/root`; launch `COMBAT-GUIDED-PERMUTER-REVIEW-20260908-01`; review activation release `cc2bef9a39752e9b6ba8c8c37c517f964da06641`; frozen trial `d7e8775367076335fa5cb18d652c904e0a5fc758`.

## Frozen subject

The assigned trial report hashes to `C19367DF9E3E1E91C5E79C63230C5A031A9564A6C415B5013B87E0A414B59F00`. The assigned evidence manifest hashes to `BCAA33A2C468CB6AC0689659B86A74D64C9193D8A8F70FA7E51848B3A8DD2E76`.

I checked every manifest entry independently. All 2,787 entries have the recorded byte count and SHA256. The frozen adapter, inputs, upstream archive, environment, commands, objects, scores, negative controls, and failed setup artifacts remain intact.

The review claim was created atomically before review-root writes. Its task, launch, reviewer, host, and revision values match the activation. The trial report is complete, and the worker released all writes and processes.

## Claims reviewed

The trial claims that actual upstream decomp-permuter can enumerate small source-choice sets through the pinned production compiler and assembler. It claims a private accepted-address link provides diagnostic output and scoring, rather than canonical ownership proof.

It also claims two nonvacuous exact controls, a nonzero negative control, and faithful retained-seed round trips. The four searches claim 32 finite unique candidates across two seeds and two source relations.

The first relation moves an existing signed upper-bound predicate after all three float-to-int conversions. The second narrows each new zero-or-one predicate from `int` to `unsigned char`.

The trial claims neither relation improves the retained seed. It does not reject untested source structures or require each intermediate candidate to improve.

## Independent method and results

I copied only required frozen inputs into `build/combat-guided-permuter-review-r1/`. I changed copied launch paths to target that private root. The frozen trial remained read-only.

The private runs used actual upstream revision `059609d4aec73eb0650726772954e1ad575825f8`. They invoked the authenticated KMC compiler, GNU 2.6 assembler, and GNU 2.6 linker with the recorded flags.

Both exact controls passed independently:

- The 28-byte no-relocation control scored zero and matched all bytes.
- The 164-byte control scored zero, matched all bytes, and retained both relocation records.

The deliberately incorrect shift control scored 5. It preserved the 28-byte extent and differed by exactly one byte and one instruction word.

I reran the complete eight-choice guided search on the retained seed. Upstream made nine compiler calls, including the repeated base choice. The run produced eight unique inputs and zero compiler errors.

All choice codes from `000` through `111` were present. Candidates without eager red scored 1314. Candidates with eager red scored 1339.

Every candidate remained 4,148 bytes with a 408-byte frame and 162 relocations. My independent ELF parser reproduced every relocation record. It also reproduced the four reported eager-red instruction changes exactly.

I independently rescored all 32 frozen guided and representation candidates. Every score, signature, text hash, extent, frame, difference count, and relocation count matched `results.json`.

All 32 frozen generated sources classified as `PURE_C`. They contain no assembler escape. Each search contains exactly the eight intended three-bit combinations.

The representation outputs grow by four bytes for each selected `unsigned char` predicate. Their frame remains 408 bytes, and each still has 162 relocation entries. This agrees with the reported emitted `andi` instruction per narrowed predicate.

## Scoring and competing interpretation

The strongest competing interpretation was that zero scores came from an empty disassembly. That interpretation applies to the rejected historical scorer, but not the final scorer.

The GNU 2.6 objdump produced zero parseable instruction words for the exact control. Upstream still reported score zero, making that result vacuous.

The authenticated GNU 2.39 objdump reconstructed all seven exact-control words. It also reconstructed all words for every independently checked output. The exact control's zero score is therefore supported by complete scoring coverage and the separate full-byte comparison.

The score remains a heuristic instruction comparison. It cannot replace complete byte, relocation, ownership, source-class, or ROM verification.

## Source relations and search meaning

The eager predicate relation preserves the tested integer clamp condition. `!(next < 256)` equals `next >= 256` for the existing integer result. The extra comparison has no source-level side effect.

The narrowed predicate relation also preserves its value. A C comparison produces exactly zero or one, both representable as `unsigned char`.

The emitted results show these relations are compatible with the pinned pipeline. They do not establish original compiler allocation history or semantic equivalence outside the stated conversion domain.

Neither retained seed improved. The trial therefore demonstrates bounded combination testing, without demonstrating fewer matching iterations or a better current candidate.

Worse candidates remain unselected rather than disproven. Selective placement, other representations, different lifetimes, and supported multi-step trajectories remain open questions.

## Isolation and adoption limits

The adapter performs a private one-owner link at the accepted address. It rejects extra allocated input sections. This is adequate for the reviewed single-owner, no-extra-data cases.

The path is task-local and manual. The adapter itself authenticates the production compiler and Binutils tools. The surrounding frozen manifest and scoring-tool record authenticate upstream, launcher, environment, and GNU 2.39 scoring.

Future use must reauthenticate that complete set and confirm nonempty scoring coverage. The adapter alone is not a sealed verifier or a general import path.

Acceptance permits optional manual searches when source evidence already justifies a small relation set. It does not permit arbitrary mutations, automatic source adoption, toolchain changes, or score-based matching claims.

The review used frozen case-local inputs. Later W8 source changes are separate and cannot alter this verdict's input identity. No production source, configuration, shared tool, compiler, runtime, canonical build, verifier, or Git state changed.

## Findings and route

No admissible material finding was found. The Director may retain this path as optional analysis evidence within the reviewed limits. Any generated candidate must return to the ordinary linked-diff and final complete-wave workflow.

All review writes are confined to the claim, this report, and `build/combat-guided-permuter-review-r1/`. All review writes and processes are released to Director `/root`.
