# Verdict: Accepted

**Material result.** Completed. Correction `99a793b7384de997fcaa6369d03c4fa8604da7f7` resolves `CSOR1-F01`. The accepted loader and publisher establish one conditional six-slot table state. Each slot maps to an exact same-slab owner start, and none equals the selector entry. The correction preserves the runtime, mutability, other-origin, downstream, and observation limits.

**Consequence.** The Director may propagate this result as accepted static research. It replaces R1's blanket no-compatible-mapping and no-discriminating-experiment rationale. It does not establish wrapper execution, selector execution, downstream non-reachability, selector non-use, family exclusion, source acceptance, or matching acceptance.

**Required Director action.** Mark `CSOR1-F01` resolved and accept the R2 correction. Preserve the selector observation gate and every conditional limit. Do not route another worker correction. Any runtime capture or further provenance search needs its own authority and evidence boundary.

## Frozen subject and eligibility

- Assignment: `combat-selector-offline-review` revision 2.
- Launch: `COMBAT-SELECTOR-OFFLINE-REVIEW-20260907-02`.
- Frozen correction: `99a793b7384de997fcaa6369d03c4fa8604da7f7`.
- Controlling review: `05d141dc62bced513cb6d3470cb724a526f621d7`.
- Accepted research baseline: `d70fd853fdffacf71290b24763e010a549276a55`.
- Reviewer: `/root/compilation_groups_implementation_review`.

The correction report is 12,304 worktree bytes. Its SHA-256 is `51EE260734DB5C9373785B42AE7D02CDEB8451744143A54129A4A8E1B6CAE08C`.

The subject commit's report blob is 12,303 bytes. Its SHA-256 is `72111A27BAFBB9863225B8B4802BA5B8DCB28534DB23C15D936B998BB62FCAE2`. Newline normalization makes the subject blob and assigned worktree report identical. This identity note is not a finding.

The correction evidence is 72,295 bytes. Its SHA-256 is `342170CD6B1345F0E265E01B40AEB4445A98A25223653DE6F9E1D8B1D6A3EDBA`. The controlling review SHA-256 remains `2259F81AD79A60544E4021E3D09C05827FB9C62C1AC4A38DD3F5DC4FC2420CD4`.

The worker reported completion. The correction commit changes only its R2 prompt, claim, and report. The reviewer did not produce the correction. W7 production files and disjoint review outputs never entered the evidence boundary.

The fresh claim was created atomically before review evidence. All review writes stayed within the assigned claim, report, and ignored review root. No production, runtime, build, database, or Git mutation occurred.

## Correction-review dispositions

| Prior item | Required status | Review result |
|---|---|---|
| Finding `CSOR1-F01` | Run again | Resolved by direct recomputation of the accepted publication chain and all six targets |
| R1 blanket no-compatible-mapping claim | Replace | Replaced by one conditional six-target table result |
| Fourteen-owner and 671-word identity proof | Keep | Frozen report, evidence, and prior 70-check result retain exact identities |
| Descriptor-10 transfer census | Keep | The correction changes no accepted ROM, owner, or discovery input |
| Thirteen immediate-origin observations | Keep | The original evidence hash is exact; only six root-slot cases receive a conditional refinement |
| Signed-return weakening observation | Keep | The selector owner and comparator evidence are unchanged |
| Three root-setter values | Keep | The correction adds accepted placement for the second state without changing the setter words |
| Bounded no-positive-selector result | Keep | The basis is refined; non-use remains unproved |
| Qualified selector observation gate | Keep | The correction repeats the full gate and does not claim observed execution |
| Discovery R2 limits | Keep | The accepted discovery subject and all named input identities remain exact |

The prior accepted checks do not need another full investigation. The correction adds report and evidence records without changing their inputs. The successful R1 reviewer result remains `PASS`, with 70 of 70 checks.

## Claims reviewed

1. The publisher entry belongs to the accepted `resource-loader-00213b10` slab.
2. The loader calls that entry after constructing the accepted load interval.
3. The entry publishes the six-slot table root.
4. The table maps exactly to all six wrapper targets.
5. Each target is an exact owner start within the accepted slab.
6. None of the six direct slots equals the selector entry.
7. The corrected rationale keeps execution, mutation, other origins, downstream reachability, and non-use open.
8. The thirteen immediate-origin observations and accepted discovery limits remain unchanged.
9. The selector observation gate remains open.

## Review method and results

I used the worker package as an index. I did not execute its checker. I reused the prior accepted checks only after confirming their exact frozen identities and causal independence from the correction.

A reviewer-owned Node.js checker performed the affected review. It rehashed all 41 correction input records. It compared the accepted configuration, manifest, overlay data, and relevant original owners with baseline Git blobs.

The checker normalized the verified v64 ROM only in memory. It reproduced normalized z64 SHA-256 `571E83396BC81E70DA4C0A20313D82DBD7DFE685F2C37418C8E27F927E2CC67A`. It then decoded the loader, publisher, table, wrappers, selector, and boundary controls from canonical words.

The checker authenticated 18 relevant original parts against the accepted manifest and ROM. It recorded 8,338 deterministic assertions. That count measures checks, not research coverage.

The successful checker is `build/combat-selector-offline-review-r2/attempt-02/static-review.js`. Its SHA-256 is `1AFA913695E7D27DB6C463CEF3E4BA7EE87B6102BDD65234A803D976892E47B4`.

The result is `build/combat-selector-offline-review-r2/attempt-02/static-review-result.json`. Its SHA-256 is `65D7030B94B35D7E836151AC39340769E262C0306207232553B66A9D287D7BC9`.

## Accepted publication chain

The accepted slab has equal ROM and RAM lengths of 92,016 bytes. Its RAM-minus-ROM delta is `0x7FFBCD30`. The loader instructions construct the exact source start, destination, and source end.

The DMA call's delay slot computes the 92,016-byte length. The later entry call resolves to the loaded slab start. These words establish the static producer path when the load and entry complete.

The publisher constructs the table pointer and stores it at the shared root. Applying the accepted slab delta maps that pointer to the table owner. One preceding word and two trailing words are zero.

| Semantic name | Meaning | Address | Address space | Evidence role |
|---|---|---|---|---|
| Loader source | Accepted slab source start | `0x00213B10` | z64 ROM offset | DMA input |
| Loader source end | Accepted slab source end | `0x0022A280` | z64 ROM offset, end exclusive | DMA length input |
| Loaded slab | Accepted slab destination | `0x801D0840..0x801E6FB0` | RAM virtual range, end exclusive | Published-code placement |
| Publisher entry | Loaded table-root publisher | `0x00213B10` / `0x801D0840` | z64 ROM / RAM virtual | Entry identity |
| Shared root | Location receiving the table pointer | `0x801D0810` | RAM virtual | Wrapper root load |
| Published table | Six direct callback slots | `0x00228D70` / `0x801E5AA0` | z64 ROM / RAM virtual | Conditional target source |
| Selector entry | Unsigned-byte accessor under accepted descriptor 10 | `0x00201778` / `0x801BE2E8` | z64 ROM / RAM virtual | Non-equality target |

## Six conditional targets

Each wrapper loads the shared root and its assigned slot. Each then performs a JALR through the loaded value. This result requires the root and slot contents to retain the published state.

| Slot | Wrapper | JALR z64 ROM offset | RAM target | Mapped z64 ROM offset | Exact owner |
|---:|---|---:|---:|---:|---|
| `+00` | `func_0020BCF4` | `0x0020BD18` | `0x801D85C4` | `0x0021B894` | `func_0021B894` |
| `+04` | `func_0020BD2C` | `0x0020BD48` | `0x801D92E0` | `0x0021C5B0` | `func_0021C5B0` |
| `+08` | `func_0020BD5C` | `0x0020BD78` | `0x801D202C` | `0x002152FC` | `func_002152FC` |
| `+0C` | `func_0020BD8C` | `0x0020BDA8` | `0x801DF9D8` | `0x00222CA8` | `func_00222CA8` |
| `+10` | `func_0020BDCC` | `0x0020BDE8` | `0x801DF404` | `0x002226D4` | `func_002226D4` |
| `+14` | `func_0020BDFC` | `0x0020BE1C` | `0x801D0854` | `0x00213B24` | `func_00213B24` |

All six table values are nonzero. Each maps to one exact manifest owner start inside the accepted slab. None equals selector RAM entry `0x801BE2E8`.

This is a direct-slot equality result for one published table state. It does not prove any wrapper executed in that state. It does not exclude a later call from an owner to the selector.

## Boundary controls and competing interpretations

The first publisher constructs RAM pointer `0x801CFC74`. Accepted descriptor-10 placement maps it to z64 ROM offset `0x00213104`. Its six initial words are zero.

Those initial zeros do not establish later contents. Runtime writers may populate those slots. The first state therefore remains an open provenance route.

The third publisher lies at z64 ROM offset `0x0023B220`. That position is beyond the accepted later slab endpoint at z64 ROM offset `0x0023A3A0`. Its pointer only numerically maps to z64 ROM offset `0x00232F80` under that other slab.

The six numerical words at that location are executable instructions. The third setter lacks compatible accepted placement for that table. This result does not exclude another valid placement or runtime state.

An intervening write to the shared root or any slot would invalidate the conditional six-target application. Other publications and object fields remain possible. Caller arguments, external owners, synthesized targets, relocated code, and interior entry also remain possible.

Non-use remains unproved. The selector's argument domain, table meaning, valid bounds, and behavioral name remain unresolved. The signed comparator paths remain only a weakening observation.

## Observation gate and evidence limits

A qualifying selector record must identify the exact entry or signature. It must record the actual caller or transfer origin. It must retain compatible loaded-code identity, `a0`, `a1`, root identity, addressed byte, and resulting value.

An indirect transfer also needs the target register value and its last definition. Tail or interior entry needs the exact entry and register state. A pre-return snapshot is insufficient because the unsigned byte load runs in the return delay slot.

An existing qualified record can satisfy this gate. Any new capture requires separate authority and existing prerequisites. Inputs must not be filtered to a guessed `0..2` domain.

The correction preserves the thirteen immediate-origin observations. It also preserves the bounded direct-transfer, original-ASM, literal, short-construction, and decoded-corpus discovery limits. Twenty-four other non-return JRs and other unresolved routes remain open.

Static publication does not prove observed loading, wrapper execution, selector invocation, or downstream exclusion. No capture, queue, ingestion, recovery, runtime, GUI, bridge, or database operation occurred. No compiler, linked diff, build, verifier, or source-policy generator ran.

## Findings and reviewer-tool history

No admissible finding remains. `CSOR1-F01` used the `Run again` correction action and is resolved. The replacement interpretation stays within the finding's smallest correction boundary.

Reviewer attempt 00 failed before invoking `apply_patch`. A Markdown backtick terminated the wrapper's JavaScript template literal. It created no checker or result file. The preserved failure record SHA-256 is `26B6A8C3E8DE2E5C8D10120BE193CFF995CCB1B327B9F610FDC020DEA8BB3CE1`.

Attempt 01 checker SHA-256 `43841DBB66BC25D5FDB6F147FC6EE940C16DC29E591C96C37FCF8D264D5DCAEF` stopped on Node's default child-process buffer. The 2,115,224-byte baseline manifest exceeded that buffer. It created no result file.

The attempt 01 failure record SHA-256 is `BDA1A66A51B6A040D3CC963BAFD2A7E6A6D29CB7808DB66ACF3F0FFFE4785D25`. Attempt 02 increased only the Git-read buffer to 16 MiB. All evidence boundaries and substantive assertions stayed unchanged, and attempt 02 passed.

These failures concern reviewer tooling only. They do not affect the frozen correction or reused evidence. No failed game hypothesis occurred.

## Documentation consequences and exact next route

The Director may mark correction `99a793b7384de997fcaa6369d03c4fa8604da7f7` accepted and finding `CSOR1-F01` resolved. The accepted result is one conditional six-target table mapping with no direct selector slot.

The Director may propagate the corrected static rationale to research coordination records. The Director must preserve the frozen R1 report and controlling review as historical evidence. No canonical domain-document edit is required by this correction.

The positive selector-consumer gate remains open. If further work is useful, the Director can route a separately authorized provenance search or qualified observation. A selector-valued publication, caller chain, or qualified invocation would overturn the bounded no-positive result.

This review authorizes no capture, source activation, matching integration, structural change, or family disposition. W7 remains the sole production writer. All Combat, shared, and later family obligations remain intact.
