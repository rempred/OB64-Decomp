# Compilation-group design correction report

Completed. The addendum resolves the unspecified raw `.reginfo` boundary identified by `CGDR-001`. The Director must freeze the correction and route proportional independent re-review. Implementation and production activation remain pending.

## Scope and identity

- Assignment: `compilation-groups-design`, revision 2; internal worker `/root/compilation_groups_design`.
- Launch: `COMPILATION-GROUPS-DESIGN-20260906-02`.
- Director: `/root`, native task `01a07262-aeca-7341-ad10-2dba705ff988`, local.
- Starting main HEAD: `34eeecc0df9fd1056d5fb4655fc6ed1f86b0bb28`.
- Frozen predecessor: `5dad70919ff43bc988cdbff9003be91347153a75`.
- Review: `docs/Plans/task-logs/compilation-groups-design-review-r1.md`.

The fresh assigned files were absent. The complete revision-2 claim was created atomically and read back before any other write. Existing Combat discovery and prerequisite-review files were disjoint and preserved. Git status reported inaccessible global-ignore/cache paths; no complete clean-tree claim is made.

## Result

The correction admits exactly one pinned-shape allocated `.reginfo` in raw and projected-unstripped group objects. It requires authenticated payload evidence and removal before linking. Every other uncontracted allocation still rejects.

The addendum specifies uniqueness, full section shape, symbol/reference restrictions, proof order, downstream stage checks and causally required controls. Its payload is source-dependent, so the design does not whitelist the pose metadata bytes.

The existing art-native contract and all unaffected group design requirements remain unchanged. Native alignment, public function entries, relocation anchors, function counts, owner boundaries and complete-wave verification retain their previous rules.

## Evidence and method

Read the current canonical and parent instructions, worker workflow, source policy, structural audit, correction assignment and frozen review. Rechecked the r1 proposal identity and inspected current native allocation and ancillary-removal code.

Parsed the designated preserved raw and stripped pose objects with the project ELF reader. The raw `.reginfo` has type `0x70000006`, flags `2`, size `24`, alignment `4`, address `0`, entry size `1`, link `0` and info `0`.

The raw section has one unnamed local section symbol and no relocation targets or references. The stripped artifact has no `.reginfo`. All 832 text bytes, five function records and 17 normalized load-relocation records agree across removal.

Exact artifact identities and source locations are recorded in `docs/Plans/compilation-groups-design-r2.md`. Production tool/config/test paths had no changes between the frozen predecessor and starting HEAD.

## Activity and verification limits

This report reconstructs the short read-only analysis sequence. One inline Node comparison had an extra closing brace and failed before reading artifacts. Removing that syntax error produced the stated comparison results. This was an analysis-command error, not a producer failure.

No compiler, assembler, objcopy, linker, build, verifier, audit, emulator or tests ran. No new generated evidence files were needed. The direct artifact comparisons substantiate the corrected design boundary; proposed group projection and mutation controls remain unimplemented.

The frozen r1 proposal SHA-256 remained `BE38B960535FA8EAA710358168F6281FDC2AB0263D0B6661368EE2B42A96B379`. No predecessor, production or unrelated file was edited. No agent, branch, worktree, commit or push was created.

## Changed surfaces and handoff

Only the revision-2 claim, `docs/Plans/compilation-groups-design-r2.md`, and this report were written. The correction claims design completion, not independent acceptance.

The reviewer must rerun `CGDR-001` against the explicit raw allowance and inspect its affected removal/stage controls. A later implementation retains the required changed-input structural audit and independent review.

No new canonical policy change is proposed beyond the predecessor's future implementation documentation. All assigned writes are released with terminal handoff. Preserve these completed records unchanged.
