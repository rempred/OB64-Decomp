# Compilation-group workbench correction

Completed. Valid active compilation groups now load in the workbench without blocking unrelated targets. All sixteen routine suites and the required real-chain integration pass. Director `/root` must freeze this result and route independent Material review. This implementation does not accept W8 or complete-group scratch compilation.

Worker `/root/db10_allocation_trace`, Astra Medium; activation `cbde574b9870026e4a2348dfa093405e63a9e5da`. The fresh claim was created atomically and read back. Evidence root: `build/compilation-group-workbench-implementation-r1/` (R). The accepted assessment and all three mandatory review corrections were applied.

## Resulting behavior

`tools/lib/matching/target_model.js` consumes the unchanged strict active model. It does not duplicate or relax group-registry validation. Each accepted function remains a separate target with its original retail bytes and structural identity.

Live `activeMatchingProducer` metadata distinguishes standalone sources from compilation groups. Group views expose the actual shared source, producer identifier, complete ordered membership and selected member index. `activeMatchingSource` remains the real source path for existing consumers. These fields and `scratchCompilation` are excluded from stored structural target records. No database schema or machine target identity contract changed.

All five pose members remain visible. R/producer-views.json binds their public views. They share `src/lib/combat_pose_metadata.c`; none is relabeled as an independent source. Activity remains workflow state, not a matching-acceptance claim.

Ranking can include active entries with `--include-solved`; explicit `rank --explain` also includes them. Inspection, context and history retain individual member views. Scratch capability remains distinct from the existing owner-prefix eligibility field.

Automatic sweeps exclude unsupported group candidates, including the smallest-leaves preset and include-solved mode. Reports retain `excludedTargets` with the complete-group reason. Explicit grouped selections reject before sweep setup. No group member is removed from the underlying model.

Prepare, watch and probe CLI requests reject before database initialization, source reads or analyzer launch. Direct candidate storage, compilation, m2c generation and probe library calls also check admission before their side effects. Stored target records resolve live capability through the workbench's strict active bindings. Supplied compiler/probe contexts also participate in early admission.

The independent final `text_contract.bindWorkbenchTarget` guard is unchanged. Complete-group scratch candidates remain unsupported. Canonical whole-group `diff <member>` remains the supported source workflow.

Admission functions reside in `target_model.js`, which already belongs to the diagnostic comparison-algorithm identity. R/comparison-algorithm.json proves the final file hash is sealed. An initially separate helper was folded into this file under Director direction. No diagnostic-link file or identity contract expansion was needed.

## Review corrections and file scope

**CGWA-C01:** Compilation-group fixtures now use fresh directories under `build/tests/compilation-groups/`. They no longer append to the frozen implementation root. They authenticate the production target/group configuration and every active source before and after fixture execution. The permanent empty-registry assertion was removed; all existing malformed-object and producer controls remain.

The active-target test admits `compilation-group` relocation provenance only with complete producer/member/source checks. It separately checks owner-relative and group-relative relocation places, type, symbol/value, section and encoded word. Canonical/legacy checks and unreviewed-provenance rejection remain intact. Active-target reports now use fresh `build/tests/active-targets/run-*` directories.

**CGWA-C02:** `probe.js` checks scratch capability before preparing context or writing source. It rechecks the resolved context before compiler execution. Direct-library tests cover both live workbench bindings and a supplied grouped compiler context. CLI routing is not its only protection.

**CGWA-C03:** The existing real-chain integration suite ran successfully on the final implementation. It now creates a fresh database and artifact root under `build/tests/matching-workbench-integration/run-*`. The m2c consumer forwards the existing `matchingRoot` and `storeOptions` through generation and candidate compilation. Its default production behavior is unchanged.

The fourteen changed implementation/test/documentation files are listed in R/changed-files.json. Scope includes five workbench consumers, the target model, bounded CLI routing, five test files and two documentation files. No strict active/group validator, text contract, production source/configuration, canonical build/verifier or compiler contract was edited.

## Validation

| Check | Result and retained evidence |
|---|---|
| Focused active-targets, matching-context, matching-workbench | Passed. `active-targets.log`, `matching-context.log`, `matching-workbench.log`. |
| Compilation-group fixtures | Passed with live production group present. `compilation-groups.log` identifies the fresh fixture root and retained malformed-input results. |
| Final routine manifest | **16/16 passed in 46.4 seconds.** `routine-final.log`. All four previously failing suites pass. |
| Final real-chain integration | **Passed.** `integration-final.log`; isolated root `build/tests/matching-workbench-integration/run-plA04o`. |
| Final changed-file whitespace check | Passed. R/final-audit.json. |
| Source/shared input preservation | All twenty W8 inputs retain their R6 hashes. R/source-baseline.json and R/final-audit.json. |
| Protected assessed inputs | Twenty-one unchanged inspected dependencies/design records retain their assessment hashes. R/final-audit.json. |

New consumer tests run inside `tests/matching_workbench.js`. They verify truthful complete producer views, unchanged structural records, live resolution for metadata-stripped targets, supported standalone selection and all automatic group exclusions. Library rejection tests intercept filesystem writes, directory creation and process launch; they observe zero side effects. Explicit CLI prepare/watch/probe checks reject with the group diagnostic and preserve the normal database identity. The unchanged final session guard is also tested independently.

Integration authenticates the real m2c/KMC/GNU chain. Six rulesets produce one exact 40-byte `memcpy_bytewise` candidate. Shorter/longer candidates remain scoreable, `.rodata` relocation evidence remains present, malformed functions and COMMON storage reject, and repeated generation reuses the same candidate/compile identity. This is scratch evidence, not full-ROM acceptance.

The first real-chain integration also passed in session 89200 under `run-j9KytV`. Admission context checks and final factoring changed afterward, so final validation was repeated for those changes. An intermediate shell quoting error left the helper move incomplete. Module-load failures from that attempt remain in `routine-incomplete-refactor.log` and `integration-incomplete-refactor.log`; they are not presented as passing. Syntax checks and final suites pass after the completed refactor.

The initial focused active-target test still wrote its legacy mutable `build/workflow-migration/active-targets.json` report. Its output was redirected before the final routine run. No execution wrote into the frozen compilation-group implementation root. Other unchanged routine tests retain their existing isolated fixture behavior.

## Boundaries and release

Live producer views are recomputed from current strict inputs on each model load; no new summary cache exists. The strict active loader also authenticates the normalized ROM, even when a caller requests omission of per-target expected byte buffers. This consumer path now has the same active-configuration prerequisites as the canonical active model.

Complete-group candidate compilation, database migrations and structural validation changes remain outside scope. The workbench still supplies research evidence only. No full-ROM build, verifier or structural audit was added for these consumer changes. Any future ownership, compiler or verification change requires its separate audit/review route.

R6's terminal index is `build/combat-draw-wave8-r6/terminal-current-inputs.json`, SHA256 `3DA17CD6DC1D14E48831EFF87B7F2AD8A91364D227D91FB59E0812D40D29EE3B`. Its fourteen sources and six shared inputs remain unchanged. The full fourteen-target W8 and later-family obligations remain incomplete. Held Resolver files were untouched.

Sessions 85702, 89200, 37041 and 21673 have ended. No compiler, analyzer, test or other trial process remains live. **All production/shared-tool, test, report and evidence writes are released to Director `/root`.** Independent Material review remains pending; the worker does not self-accept this implementation. R/manifest.json binds the final report, changed files and retained test artifacts.
