# Combat draw W8 R6

In progress. Receiver `/root/combat_draw_continuation`, Astra Medium, is sole production source/build writer. Director `/root`; release cc2bef9a39752e9b6ba8c8c37c517f964da06641; launch COMBAT-DRAW-WAVE8-20260908-06. Fresh claim was created atomically and read back. All twenty starting inputs authenticate against R5 terminal index47D8B0BE and are copied separately.

All fourteen targets and one final complete-wave verifier remain required. Current bests are separate from exploration inputs. Temporary regression does not reject an underlying structure. Predecessor reports remain frozen. Existing accepted default Kuna/m2c packets are reused. The upstream adapter was initially review-pending; Director subsequently routed Accepted review0b204012/adoption1e48fa8d. The review report authenticates asBE14CE6D147D7263EE026B8D55484ECBA1164563A090B35E8D791CF0E8CC7EA1 and has been read. Its optional single-owner/no-extra-data/manual-search limits and full environment authentication requirements remain applicable. This R6 sequence has not used it.

Sequence A carries the actual overlap increment instead of full strip state. R3's shared-overlap-step input improved specific retail roles but was not selected. It retained a full strip plus a short-lived late overlap variable. This sequence tests the overlap value across real vertex, helper and packet consumers. Each strip argument is overlap+1; row, remaining and accumulation updates use overlap directly. Existing defined-path strip bounds support these integer transformations without new overflow. No dummy use, storage or extra predicate is introduced.

## Sequence A measured results (ongoing)

Six new inputs are preserved under `build/combat-draw-wave8-r6/`. `results.json` binds full authored/expanded hashes and object/policy/relocation prefixes. `sequence-a-roles.json` records actual flow statistics and global register assignments. Every input has its own pinned/diagnostic agreement. All twenty best/shared inputs are currently restored. No session remains running; these processes completed synchronously.

| Input | Bytes | Frame | Native words | companion/u1/state/row registers |
|---|---:|---:|---:|---|
| carried-overlap-state |6692|504|478|s8/s7/s6/s5|
| short-carried-overlap |6764|504|523|s7/s6/s8/s5|
| companion-primary-packed-interval |6716|504|477|s7/s8/s6/s5|
| short-overlap-packed-interval |6780|504|516|s6/s7/s8/s5|
| int-overlap-packed-interval |6716|504|472|s7/s8/s6/s5|
| short-strip-packed-interval |6776|504|518|s6/s7/s8/s5|

The fourth input is source `6E17CD42FD8A1BC9A170050D974B67ED9C8E559A6878542603B5D4AC06411752`. It gives four corresponding retail roles without an added endpoint home. Its s8 value is overlap, one less than retail strip; register correspondence alone is not value or byte equality.

The sixth input, source `57F0ACBDD4ECA0E0B0C98ED2F39CC34CF03E50234FAB2EE65F6105C0BBF44477`, restores actual strip state while retaining the signed16 representation and companion-to-primary packed interval. It also gives companion/u1/strip/row as s6/s7/s8/s5. This is a useful multi-step intermediate despite its extra36 bytes and worse aggregate score. The selected D037 source remains separately preserved; no candidate has been selected for final linking.

The independent reader's proposed packed interval starts at the existing companion F4 endpoint definition and ends at primary size consumption. The trial changes only primaryEnd's value source to endField, leaving vertex, optional/fallback and WIDTH calls intact. No row/strip update intervenes. The reader noted contrary retail evidence: primary endpoint arithmetic is recomputed, and s2 is reused for a cursor between endpoint intervals. Therefore source sharing would require appropriate compiler rematerialization to match retail. This input does not assert retail carried the endpoint.

On the reader's R5 short-vertex/distinct-end context, the shared endField pseudo907 has14 references across184 instructions and8 calls, with no endpoint home. The short-overlap combination changes the competing allocation enough to recover the four named register roles. Returning that combination to int overlap loses those roles. This establishes a real type/lifetime interaction for these inputs, not a general allocator rule.

Next work separates the short state's emitted conversions, packed-end reuse, cursor assignments and extra operations. The sequence remains open. No immediate score improvement is required to continue, and no structural hypothesis is closed by this table.

The seventh input, `definition-narrowed-strip-packed-interval`, preserves the sixth input's actual narrowing at the three existing strip assignments but carries its result in an int. Authored SHA256 `3A202306E03D332EAC008E3FD77C896EB0F8ECC9142CF88F1C30DA3A7C2C9168`; native extent6724, frame504, native differing words316. It has its own successful pinned/tracer agreement and the same29 accessed homes without an endpoint home. All twenty best inputs are restored. Results/role indexes now contain seven inputs.

This controlled representation change removes repeated strip-consumer sign extensions, but companion/u1/strip/row return to s7/s8/s6/s5. The sixth input carries an HI strip in s8, including `sll`/`sra` extension pairs before packet consumers and a final `lhu` of the remaining low halfword. Retail instead carries full-width strip and uses it directly. The shared packed endpoint also survives into the primary packet in the sixth input, rather than reproducing retail's primary endpoint calculation. These are two concrete remaining discrepancies; matching four named register roles does not imply matching their complete interference or packet-phase contents. The seventh input tests moving conversion to the actual definitions, not a new dummy reference or comparison. Its improved aggregate score does not select it or close the sixth input's lifetime hypothesis.

Three further steps preserve the evolving context rather than restarting from best:

| Input | Authored SHA256 | Bytes/frame/native words | companion/u1/strip/row |
|---|---|---|---|
| definition-narrowed-strip-shared-late-step |73133717AA59277D935360089DE34E19FA9A77798AC93C9623E20535F678CEC1|6708/504/295|s6/s8/s7/s5|
| definition-narrowed-u1-strip-shared-late-step |C8F831A15A0E66AB30F800A1FCD6E3EA005E9ABA2A3C878E6576EFFD03105C60|6708/504/343|s6/s8/s7/s5|
| definition-narrowed-strip-late-step-distinct-ends |E7580C130DD96C8B380360610A4206ED01D7EF775AB38F212AB94C069A1917A1|6724/504/268|s7/s6/s8/s5|

The eighth input explicitly shares the real late overlap increment among accumulated/row/remaining updates. This reduces strip115 flow references38 to32 and moves companion100 to s6. The ninth independently moves u1 narrowing to its existing definition, carrying a full-width result into rightU; it preserves narrowing behavior and changes emitted scheduling but not the four assignments. It is retained separately rather than selected by score.

The tenth returns from the eighth to distinct primary endpoint arithmetic. This restores the real recomputation missing from the shared packed interval and increases extent16 bytes. Strip now uses retail s8, while companion/u1 become s7/s6. Thus the shared interval's effect on competitors remains evident after both real strip-lifetime changes; recomputation cannot simply be reintroduced while assuming the previous assignment will persist. All ten inputs have their own pinned agreement, frame504 and no added endpoint home; all twenty best inputs remain restored. Current source selection is unchanged. The sequence is still open, with companion/u1 competition in this latest full-width-strip/distinct-end context the next observation.

In the tenth input, the earlier allocator occupant of s6 is tileOffset924, not u1. It has12 references/122 instructions/4 calls/2 deaths. Its real raw tile division/mask definition is flow2672, the nonzero branch definition2905, with final branch uses2812/3056. Companion100 allocates before u1 but conflicts with this tile value; u1 later reuses its disjoint s6 lifetime. This narrows the next question to the actual tile expression and consumers.

The eleventh input `definition-narrowed-strip-distinct-branch-tiles`, SHA256 `336C3A1F5C057C9ECB23C99DF8CE10626ECF2673A4BA23C1B170313BFE8B839A`, separates the two real format branches' tile locals. It emits6736 bytes/frame512/native322 and retains companion/u1/strip/row s7/s6/s8/s5. It adds an accessed combined F588/tile temporary1045 at1C4 (6refs/60instructions/2calls); decoded-address home moves to1CC. This is preserved as an intermediate, not discarded merely for its spill.

The twelfth input `definition-narrowed-strip-carry-branch-fulltile`, SHA256 `E1F4AF989770BCC2AD6F7EAAAB61CC58830425A36FF156FAD68EB5A0BC335A1C`, continues from that branch split by consuming the already computed fullTileOffset at the final nonzero companion tile store instead of reassigning its same expression. This is real value reuse across existing consumers. It removes the accessed spill, yields6720 bytes/frame504/native310 and retains the same four assignments. All twelve have fresh pinned/tracer agreement; the index authenticates all twenty best inputs restored. Director's read-only historical tile-form retrieval is now coordinated with these two exact controls before further tile source changes. No full verifier or new shared tooling ran.

Exact native-region readback corrects the earlier progress shorthand for input11: its spilled1045 is the combined F588/tile word, not only the raw nine-bit tile index. Initial RTL2908 ORs the mask result with constantF5880000;2910 copies that value to1045. Native1604 stores it at1C4 and16DC reloads it. `tile-native-regions.json`/`.txt` bind complete section hashes plus offsets1400–193C for retail and inputs10–12 with actual relocation identities. These are diagnostic excerpts, not a linked-equality proof. Retail section hash matches458A6CB3 as required.

Input12's branch-local tiles (zero930/nonzero1015, both s1) leave a different s6 competitor: shared packet row922,16refs/160instructions/6calls. Input13 `definition-narrowed-strip-branch-tiles-primary-row`, source `E4318B160FD7552129E6CAF0B78E3C1FB6277679D8E98023995EBDCA63F76646`, separates only the primary packet row consumer interval. In this new tile/strip/endpoint context it yields6720/frame504/native306, with companion/u1/strip/geometry row s6/s8/s7/s5. The retained older primary-row split addressed different competitors; this control is not a repeat of its compiler input.

Input14 `definition-narrowed-strip-branch-tiles-early-rightu`, source `F4D83B27F71B4B0162C7E26E981011F16BFF4649F6C77563B49C38657172CD38`, continues from13 and moves the existing sole rightU narrowing assignment immediately before accumulated initialization/guScale, leaving all real operations intact. This removes one call from u1's lifetime; result6720/frame504/native341, companion/u1/strip/geometry row s6/s1/s7/s5. It establishes the targeted earlier consumption without obtaining retail u1 placement. Both inputs have pinned agreement and no extra home; all twenty bests authenticate restored. The report/index contain fourteen experiments, distinct from the fourteen assigned wave targets. No target has been newly selected or accepted by these experiments.
