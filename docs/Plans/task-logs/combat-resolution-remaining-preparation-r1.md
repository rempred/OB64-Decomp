# Remaining resolution owners preparation r1

Completed read-only preparation. This assignment preserves all five complete owners and the original seven-member production wave.
Director /root must bind the preceding complete seventeen-member action-mode wave before future implementation.

Task combat-resolution-remaining-preparation, revision 1; launch COMBAT-RESOLUTION-REMAINING-PREPARATION-20260907-01.
Receiver /root/sequential_scope; Director native 01a07262-aeca-7341-ad10-2dba705ff988; local; Astra Medium.
Ready 17e7baf13148fd64b71fb11119745cd11b7decb8; activation/starting HEAD 0104458a846a19bfe4fb802b531952766f577fd5.
Coordination c2f547aa8ff3705ea7675dd748d7eb94157959b0; accepted source W7 469a1416918592749d61dc34e3e079796f7b673c.

Fresh complete claim created atomically and read back before other writes. Report and ignored root were absent.
Required unchanged guides and accepted original-assembly research-aide routing are reused from the preceding preparations.
Current prompt, resolution draft and sequential route were read. W8 remains sole production; all disjoint changes are preserved.
Completed: authenticated relevant corrected inputs, compared every assigned ROM word, and mapped complete ABI/storage/control/helper/data obligations.
Main hypothesis: real copied states and escaped outputs support reconstruction within existing complete owners.
Alternative: scalar guesses or partial control flow omit required stores and calls. Direct instructions and helper contracts distinguish them.
Any byte, owner or controlling mapping mismatch stops preparation. No source, compiler or new acceptance is authorized.

## Completed result and evidence

Completed read-only preparation of all five owners. Every assigned instruction equals the canonical normalized Rev 0 ROM.
The group buffer has six concrete records; other dynamic list declarations retain explicit count and availability prerequisites.
Director must bind current preceding-wave contracts before future implementation. This report grants no source activation or new acceptance.

| Neutral owner | Role in this note | ROM extent, exclusive end | Accepted VMA extent, exclusive end | Bytes / instructions |
|---|---|---|---|---:|
| `func_002317C8` | Side-selected publication path | `2317C8..231CC0` | `801EE4F8..801EE9F0` | 1,272 / 318 |
| `func_00231CC0` | Six-record builder | `231CC0..232280` | `801EE9F0..801EEFB0` | 1,472 / 368 |
| `func_00232280` | Record reorder/retry path | `232280..232458` | `801EEFB0..801EF188` | 472 / 118 |
| `func_00232458` | Descriptor dispatch path | `232458..232D7C` | `801EF188..801EFAAC` | 2,340 / 585 |
| `func_00232D7C` | Resolution coordinator | `232D7C..2331AC` | `801EFAAC..801EFEDC` | 1,072 / 268 |

These are neutral descriptions of observed operations, not new gameplay names.
All VMAs use accepted `resource-loader-0022a280`, delta `0x7FFBCD30`; original comment addresses remain decoding aids.
Canonical ROM is 41,943,040 bytes, SHA256 `571E83396BC81E70DA4C0A20313D82DBD7DFE685F2C37418C8E27F927E2CC67A`.
The controlling corrected package has SHA256 `0EFA3D1990C347FBCCFCA32666FE6430BFDD0491E780B43D4F91CC68787BC1B1`.
Its correction report has SHA256 `0CF8D2C24D4894C79A1109A9C71D5F47A8AE1A662C75053684008804D6E453AB`, frozen at `740732e18b1f3e5808b167572662473918d35c2d`.
The original empty-alias claims remain superseded; corrected per-site arrays were reused without repeating alias research.

Each owner JSON records original ASM paths/hashes, accepted mapping, every call and argument context, direct transfers, delay slots, memory references and stack-address formation.
Each text map preserves every instruction. Each helper map distinguishes accepted placement candidates from same-address aliases.
The evidence index records exact Git input identities, current draft and frozen shared-interface reports, and separate working-registry snapshots.
The package's older draft is historical retrieval provenance; the separately authenticated coordination draft controls future routing.

## Public ABI and returns

All stack offsets in this report use adjusted `sp`. No owner consumes an incoming stack argument or floating-point public argument.

| Owner | Public input and observable result | Frame / saved registers / restore ROM |
|---|---|---|
| `2317C8` | `a0` mutable word pointer; `a1` raw zero/nonzero selector. Explicit `v0=0/1`; normal path updates caller word and objects. | `0x200`; `s0..s8` at `1D8..1F8`, `ra=1FC`; restore `231C90`, return `231CB8`, delay restores frame. |
| `231CC0` | `a0` points to at least `0xD8` writable bytes. Result is buffer mutation; no normalized public return is established. | `0x48`; `s0..s2` at `38..40`, `ra=44`; restore `232268`, return `232278`. |
| `232280` | `a0` points to the same six-record buffer. Mutation/retry effect; no normalized public return is established. | `0x48`; same save layout; restore `232440`, return `232450`. |
| `232458` | `a0` byte record with bytes 1 and 2 read; `a1` mutable word pointer. Explicit zero/one return; temporary global record is cleared on both normal and empty-descriptor exits. | `0x198`; `s0..s8` at `170..190`, `ra=194`; restore `232D4C`, return `232D74`. |
| `232D7C` | No consumed public arguments. Initial helper overwrites argument registers before use. Global orchestration effect; no normalized return is established. | `0x140`; `s0..s7` at `118..134`, `ra=138`; restore `233180`, return `2331A4`. |

Every return's delay instruction restores the exact frame. Incidental `v0` values in the three effect-only routines must not become invented status contracts.

## Side-selected path: complete 2317C8

Real storage includes a `0xF8`-byte object at `sp+0x20`, explicitly cleared and passed to `20C908` or `20D870` with raw index zero.
Both original helpers independently clear `0xF8` bytes and populate fields. This is an object buffer, not one scalar.
The same object goes to five-argument `237750(object,target,0,0,0)` and two-argument `2344B4(object,15)`.
Three regions at `sp+0x150`, `0x168`, `0x180` are each explicitly cleared for 20 bytes.
The owner records selected pointers at `0x128+4i`, prior words at `0x150+4i`, calculated words at `0x168+4i`, and helper words at `0x180+4i`.
Allocated record pointers use `0x118+4i`; their raw indices are stored at `0x140+4i` even though later code does not read them.

The selection loop scans 20 slots and does not locally cap accepted count below 20.
Frame spacing and 20-byte clears do not establish the smaller domain bound required by concrete parallel-array declarations.
The future writer must establish that domain bound or preserve actual storage aliasing; do not silently declare twenty independent entries over neighboring locals.
The free-record scan stores at most the requested count, from one through four, but later loops use requested count rather than found count.
A sufficient-free-record contract remains a prerequisite. No exhaustion check or synthetic fallback is authorized by this preparation.

| ROM interval, exclusive end | Complete behavior to preserve |
|---|---|
| `2317C8..2318EC` | Gate on global context byte `+82E`, bit 1 or 2. Query `2335E0(selector)`; zero rejects. Scan 20 objects, filtering `20C2C0` and selector-dependent `20BFF8/20C014`. Zero selected count rejects. Derive requested records from raw quantity ranges 1..3, 4..6, 7..9, otherwise four. |
| `2318EC..2319E8` | Clear all four buffers, initialize object, set corresponding context bit. Call `00043B2C(1,1)` and retain narrowed result in object `+7C`. Preserve signed division `(count+5)/(2*count)`, explicit trap instructions, single-precision conversion/multiply/divide by 12.0, then truncation to integer. The 12.0 constant is instruction-encoded, not a ROM table. |
| `2319E8..231AC8` | Signed maximum caller/object words. Per-target helper product clamped to 1..9999. Increment context byte `+818`; scan 20 global records at `context_base+1C4+F8*i`, retaining records whose word `+48` is zero. |
| `231AC8..231B48` | Sort selected pointer list with four-byte elements and callback VMA `801EFEDC`, accepted ROM owner `2331AC`. Publish seven-word `3B` and four-word `3C` records for requested allocation count. Preserve decrements of raw quantity by three. |
| `231B48..231C14` | Add 45 to time; object `+58` becomes 14 or -6. Query helper duration for object and selected targets. Eight-argument ADFC receives target, zero, calculated time, calculated value, zero, byte-zero, one, zero. Publish `3D` records and retain first target's duration in difference calculation. |
| `231C14..231CC0` | Update caller word, reconcile selected object times through signed differences and unsigned comparison, then return one. Preserve branch-likely store behavior and complete epilogue. |

The sort wrapper `2101DC(base,count,width,callback)` rearranges count/width before its downstream call.
The callback dereferences two element pointers and subtracts object `+58` in a helper-dependent direction.
This literal function-pointer argument is not a dispatcher table or permission to add the comparator to this five-owner assignment.

## Six-record builder and reorder: complete 231CC0 and 232280

The builder clears exactly `0xD8` bytes: six records of stride `0x24`.
Record fields `+0/+4/+8` hold object pointers, `+0C` holds a calculated word, and `+10` holds raw codes `0,1,2,6,7,8`.
Other cleared fields remain part of each copied record; do not shrink records to used fields.
The builder and reorder routine each use a real `0x24`-byte temporary at `sp+0x10..0x34` for two 16-byte copies and a final word.
Those temporaries do not escape into a helper. All moves rotate whole records.

| Owner / ROM interval, exclusive end | Complete behavior to preserve |
|---|---|
| Builder `231CC0..231DF0` | Clear/init records; scan 20 objects. Require non-null, helper filter, context equality and signed `+6C < +70`. Compute slot from raw object `+58`: unchanged below six, otherwise subtract three. No range guard proves that raw value is a valid six-record index. Insert via unsigned `+2C` comparisons into first/second pointer positions; third receives remaining/displaced candidate. Do not replace this with an invented general sort. |
| Builder `231DF0..232074` | For each record, independently qualify three pointers using null, `+40 & 3`, signed limits and context equality. Three qualified values produce `third + ((first+second)>>1)`; two produce their sum, one its value, none zero. Preserve unsigned halfword loads and exact branch combinations. |
| Builder `232074..232188` | Choose among populated records: largest raw code below six and smallest code at least six. If both exist, compare calculated words; ties choose the latter category. If neither exists, default index zero. |
| Builder `232188..232280` | Rotate chosen record to the front by adjacent full-record exchanges. Restore and return without an invented status. |
| Reorder `232280..2322FC` | Gate on `20BF6C()` and global byte `+6074`. Scan 20 objects for helper-qualified candidate; no candidate returns immediately. |
| Reorder `2322FC..232424` | Search three pointer positions in all six records. On a match, rotate its whole record to front and clear global byte `+6074`. Matching index zero still clears the byte. |
| Reorder `232424..232458` | If absent, decrement candidate `+6C`, rebuild through `231CC0`, recursively invoke `232280`, then restore. No local recursion-depth bound exists. Preserve this recursion and its side effects; do not add a retry limit. |

The six-record shape is directly established; valid raw slot-code and recursion-termination domains remain upstream requirements.
They are not new structural defects or permissions to change boundaries.

## Descriptor dispatch: complete 232458

The owner clears a real `0x80`-byte descriptor at `sp+0x28`.
It repeatedly reads input byte 2 and loads bytes from VMA `8018AA80 + 16*index + offsets 1..9`.
The accepted cold-boot slab projects the base to ROM `60980`; the unsigned-byte arithmetic domain does not prove 256 valid records.
Preserve repeated input loads and actual byte fields. Do not fabricate a larger table or a runtime bound from the arithmetic alone.

The descriptor's count is at `sp+34`, IDs at `38+4i`, halfwords at `60+2i`, primary flags at `74+i`, bytes at `7E+i`, signed values at `88+2i`, and secondary flags at `9C+i`.
Invalid-entry deletion shifts IDs, halfwords, bytes and signed values, decrements count, and retries the same index.
Null object results take a different skip path; do not merge null and invalid handling.
Shared producer `237E2C` and consumer `239338` retain the preceding F580 descriptor/count limits; no universal count bound is added here.

`sp+A8` is a single mutable byte passed to B06C, whose byte load/store contract is retained.
`sp+B0/B4` is one ID followed by `-1`. Lists at `sp+B8` and `sp+E8` contain descriptor IDs and zero flags, respectively, each followed by `-1`.
Their required footprints are `4*(count+1)` bytes. Twelve-word spacing does not prove capacity; settle producer bounds before declarations.
Outgoing slots `10..20` carry fifth through ninth arguments, including list pointers and narrowed bytes.
The local word at `144` is tested whole but its low big-endian byte at `147` is published; preserve that distinction.

| ROM interval, exclusive end | Complete behavior to preserve |
|---|---|
| `232458..232638` | Populate descriptor header. Scan 20 global `0xF8` records for zero `+48/+4C`, initialize chosen record through `20D870(record,1)`, then set fields `+58=7`, `+54=1`, byte `+78=3`, `+7C=input[2]`. Derive descriptor with `233F94/20C014/23887C/237E2C`. Zero count clears the temporary record and returns zero. No local exhaustion branch protects the scan's last candidate. |
| `232638..2326C4` | `239338(descriptor,temp,temp,temp)`; `25FD00(input[1])` narrowed to signed halfword. Increment context bytes `+818/+823`, publish two-word `40`, initialize local time to one and maximum to zero. |
| `2326C4..232840` | Resolve each ID, distinguish null skip from invalid deletion. Query threshold helpers and three ordered random calls; load signed halfword value and store primary flag. |
| `232840..2329C4` | Guarded ten-entry dispatch described below; preserve default and post-dispatch `2379AC` qualification. Store secondary flag. |
| `2329C4..232BE4` | Primary success calls six-argument A7B8. Conditional raw-type 10/2 logic counts repeated IDs and calls `237AF8` only for the last occurrence. Nonzero modifier with helper-qualified target publishes nine-word `1F` subtype 13 and invokes `23422C`; otherwise optional B06C and subtype zero. Primary failure halves signed value toward zero, clamps 1..9999, publishes subtype one and calls A7B8. Preserve `+94` updates and outgoing ninth byte. |
| `232BE4..232C64` | Maintain signed maximum, advance/retry descriptor loop. Then scan 20 objects with three filters and temporary-record exclusion. Preserve current scan behavior if no candidate qualifies; no new fallback is inferred. |
| `232C64..232D4C` | Build terminated lists, publish seven-word `16` and four-word `3E`. Increment caller word by `0x200`, store global `+828`, publish `20`, clear temporary `0xF8` record, return one. The earlier maximum load is immediately overwritten; do not turn it into an invented caller-word maximum update. |
| `232D4C..232D7C` | Complete restore and return delay slot. |

## Guarded dispatcher and shared data prerequisite

The sole dispatcher in these five owners is at ROM `23287C`, VMA `801EF5AC`.
It requires descriptor byte `+4 == 8`, then unsigned `(byte[+7]-2)<10`.
Ten table words at ROM `[239F78,239FA0)`, VMA `[801F6CA8,801F6CD0)`, lead to six internal destinations.

| Raw selector | Destination ROM | Local obligation |
|---|---|---|
| 2 | `23291C` | Set secondary flag one. |
| 3,7,8,9,10 | `23299C` | Default flag remains zero. |
| 4 | `232884` | Primary success gates another random triple against signed threshold halved toward zero. |
| 5 | `2328FC` | Copy primary flag. |
| 6 | `232904` | Primary success plus signed value versus unsigned target halfword `+20` can set one. |
| 11 | `232924` | Primary success gates another random triple against signed threshold divided by four toward zero. |

Out-of-range also reaches `23299C`. Outer gate failure bypasses qualification with the modifier byte cleared in its delay slot.
The three literal random triples are ROM `2327D0/D8/E0`, `23288C/94/9C`, `23292C/34/3C`.
They use the existing combined-word construction and unsigned modulo 100. Preserve conditional order and signed threshold rounding.
No direct random call appears in the other four owners; helper-internal random effects are not excluded.

The table remains inside existing row 4248, `primary:bb297c4c5ff7ddb23a65`, original `table_00239ec0.s`, ROM `[239EC0,23A3A0)`.
It follows F580's table immediately and ends before a later helper's data. Exact consumed and complete-row hashes are in the evidence index.
W7 has no overlapping auxiliary contract or active C/linkage/multi-owner records for these five targets.
Working snapshots retain those target absences separately from accepted authority.
Future ownership must bind preceding action-mode contracts and preserve complete row coverage, including F580's reads.
This footprint grants no partition, row merge, padding definition or overlapping C owner. Any demonstrated representation requirement needs separate routing.

## Resolution coordinator: complete 232D7C

This owner supplies direct cross-member ABI evidence. `sp+10..E8` is the exact `0xD8` group buffer used by builder and reorder.
`sp+F0..114` is a copied whole `0x24` record; its first three pointer words go to A9C, F580 and individual F2BC calls.
`sp+E8` is a mutable word passed to the descriptor path, side-selected path and selected-object resolution paths.
No array count is inferred from the remainder of its frame.

| ROM interval, exclusive end | Complete behavior to preserve |
|---|---|
| `232D7C..232E7C` | Initialize resources through no-argument `21088C`. Reset context bytes/words, zero context `+10..810`, scan 20 objects and clear non-null `+94`. Optional halfword `+6076` selects helper data using its low byte; update mutable byte VMA `801939CC` with a shifted mask, call `232458(record,&word)`, then clear that halfword. |
| `232E7C..232FA8` | Stop/query gates precede builder and reorder. Missing first record increments context word and checks two helpers, then queries both raw sides. Invoke `2317C8` for selected side and optionally the other side, preserving intervening completion queries. Retain the counter initialized to three and tested before decrement: up to four missing-head passes. |
| `232FA8..233090` | Copy full front record. Call A9C first; call F580 only on its zero return. On success, query completion and restart if needed. Otherwise try each non-null pointer through F2BC with a completion query after each. Preserve outer restart branches and stop gates; do not flatten this into one pass. |
| `233090..233180` | Scan 20 object times for signed maximum, then compare unsigned with context `+828`. Clear/set global byte `+6086` according to repeated helper/global conditions. Publish two-word `41` at maximum plus 20; call `210930`; reset final context words/bytes. |
| `233180..2331AC` | Complete restore and return. |

The mutable byte at VMA `801939CC` and global pointers at VMA `801CE8BC/801CE8C0` are runtime state references.
No ROM projection or frozen runtime contents are invented for them.
At call VMA `801EF188`, accepted projections also include an unrelated data start; same-slab `232458` is retained as contextual navigation.
Likewise VMA `801F4B5C` retains both recorded placement candidates. Aliases alone resolve neither execution context nor ownership.

## Resumption, verification and limits

Suggested future reconstruction order is the six-record builder, reorder, descriptor path, side-selected path, then coordinator.
This is an implementation proposal, not a change to the original seven-member wave or matching gate.
First bind accepted preceding seventeen-member action-mode results, current baseline and sole source ownership.
Settle raw slot-code, selected-count, descriptor-count, free-record and retry-termination prerequisites before choosing declarations or adding behavior.
Keep F580/A9C outstanding count, aliasing and table limits unchanged; reuse only their directly shared interfaces.

Accepted publisher contracts retain two (`D1CC`), four (`D230`), seven (`D2C0`) and nine (`D334`) ABI words.
ADFC's eighth word and byte-valued sixth argument, A7B8's six words, B06C's one-byte output and the two-argument zero helper remain distinct.
The complete call contexts and authenticated original/accepted helper sources support these counts.
No compiler-shape workaround, source-policy exception, missing initializer or new padding is proposed.

The bounded extractor authenticated inputs, checked every owner word, conserved 6,628 bytes, and required all non-call direct transfers to remain internal.
Maps cover 105 literal calls, 308 direct transfers and 522 memory references, plus five returns and one dispatcher.
Exact original ASM hashes and complete owner ROM hashes are recorded per owner; output hashes follow.
No compiler/build/diff/source-policy/verifier command, runtime/database access, source/config/shared-tool edit or Git mutation occurred.
A text search without literal matches returned exit one; it supplied no negative semantic evidence. No failed byte check occurred.

These are exact static observations and bounded reconstruction interpretations, not new accepted semantic or structural claims.
Unproved input-domain conditions are explicit prerequisites, not evidence that existing owners are incorrect.
No canonical-document edits are proposed. Director performs intake; all assigned writes are released at terminal handoff.

## Output hashes

All artifacts are under ignored build/combat-resolution-remaining-preparation-r1/. They are preparation evidence, not matching proof reports.

| Artifact | SHA256 |
|---|---|
| dispatch-map.md | E0171031F10B8F561FA8FC347578FDD7407308D24FD4444E0CA910809C47765D |
| evidence.json | 09C9D1032E83150BC38BE126D1668C41FAEBC44353E22CAA72ACF97F576CF240 |
| func_002317C8-helpers.md | BB4EC068047383B2A49788E37A5A0E20B872F1C47E1321B23888770FBD7042E1 |
| func_002317C8.json | 4DDE11F5DBD9686105DEF4A28B7A69F65ADECAC8F4997D21596DF36A5600E73A |
| func_002317C8.txt | 024F21C28E646F9F3752E808FBCEFCCDB9FFEAABD81EBEB49E7ACE20213C51C9 |
| func_00231CC0-helpers.md | D2D3B9E4F8A3E61E99A1648330DFEDDD00DDB6D7A0ACDB3A2CAA50CE3326A3D4 |
| func_00231CC0.json | 73FBDA1473A8CCA7ED64BFF41D68FBBA022AB7B7926C89BD0B22AF01048526F6 |
| func_00231CC0.txt | 9BA1B143CC7402DA2590CCD9347316CD50DA4A14EDB89E28A7052E5B213B8C4F |
| func_00232280-helpers.md | 34B6E7FA379B8E9534EF116603C47EEDB56164037980D7E534A37ADBCDAAB7CD |
| func_00232280.json | 39BCD77745CD4C1DA09C9C3A4C25BAC2363318D9CBBDB3A79BE9DC0CDA15AEFF |
| func_00232280.txt | 6C5CE681F0F0D472E281FE5BFB9AD537420C840D9E8DC0BA9E252B154BBA91C4 |
| func_00232458-helpers.md | 52194515483DE9E24F380B3783335F69AA440C6DB02C49BEBE94616CE1B880C3 |
| func_00232458.json | 6B5C0F6D7CD022C1222163492610D6D65D882E0FB41B16B0B4C34B05447EE924 |
| func_00232458.txt | ED4B5E76CA21AD8A6094001FA4ABF1B43850D1F0CEE2CB6D79F75F74F5573C1F |
| func_00232D7C-helpers.md | 6150964B5F762D684132E9540264807B289E634A667D58C0897B64F24AF7292C |
| func_00232D7C.json | 26F9FC5FE90216992755A60A6311A6FF4E57B74EA5DA1B35A087813C19429A92 |
| func_00232D7C.txt | 050A0F0D150F05CD2F65CF6E55AC49DFE7F3D37A9E22438307EEC5DD8BA8030B |
| prepare.py | 79A4AE8F91542AEFC123C36631F0F18FC1E7A9EDF5AC28FBE0DFD19D70F08AE2 |
