# Combat mode-owner retry preparation

Completed. Both owners have authenticated nonexact controls and concrete next evidence experiments. Neither has a justified new source recipe yet. Director `/root` should route the bounded probes below when source/build ownership becomes available. New interpretations are review pending; proposed experiments are untested. This preparation accepts no candidate or partition.

## Identity and scope

Assignment `combat-mode-owner-retry-preparation` r1; launch `COMBAT-MODE-OWNER-RETRY-PREPARATION-20260907-01`. Worker `/root/compilation_groups_design`, Astra Medium, local. Director native task `01a07262-aeca-7341-ad10-2dba705ff988`. The complete claim was created atomically and read back before other writes.

Starting main HEAD was `6a1cf5f3bb953a11526d23de751288fc641f3daf`; accepted source baseline is `0e1191013aeebed2929c9caff7c1f139169ad7f3`. Current Combat W7 provisional sources were not research inputs. Relevant accepted owner assembly, auxiliary implementation and fixture paths have no diff against that baseline.

Method: read frozen r7/r8/r9 reports, archived sources, original owners, existing candidate-bound reports/dumps, accepted boot lifetime evidence and reviewed table preparation. Parse existing JSON and hash exact inputs. No C source, compiler output, production tool/configuration or prior record was changed. No compilation, linked diff, build, verifier, source-policy generator, runtime, database, GUI, bridge, Git mutation or agent operation occurred.

## Strongest retained controls

“Strongest” means a useful causal control, not highest score or closest proven whole-owner match. Existing reports are historical mechanical observations, not current acceptance.

| Owner/control | Authenticated source | Recorded result | Remaining boundary |
|---|---|---|---|
| B1F4 r7 E16 | `func_0022B1F4-bdd3b1a20df1.c` | PURE_C; 3,576 bytes; frame 272; 103 text relocations | Retail is 3,588/frame 280; scheduling and tail remain different |
| B1F4 r9 E04 | `func_0022B1F4-25c2b19614e7.c` | Same extent/frame/relocations; only 14 tail words change against E16 | Pre-branch timestamp still survives event 8 in s0; not retail recomputation |
| D14C r9 E07 | `func_0022D14C-78dd9d9325ec.c` | PURE_C; 6,744 bytes; frame 576; 181 text relocations | Retail is 6,844/frame 616; whole function remains nonexact |
| D14C r7 E39 | `func_0022D14C-f1209a3616dd.c` | Same 6,744/frame 576/181; restored first-filter roles | Artificial wrapper weighting; retain only as comparison control |

Paths above are relative to `docs/archive/matching-c-candidates/resumption-20260906/sources/`.

B1F4's accepted complete owner is ROM `[0x0022B1F4,0x0022BFF8)`, loaded entry `0x801E7F24`. D14C's is ROM `[0x0022D14C,0x0022EC08)`, loaded entry `0x801E9E7C`. Both use accepted loader slab `resource-loader-0022a280`, delta `+0x7FFBCD30`. Historical assembly PC comments are not placement authority.

The E04 and E07 workbench reports explicitly record `rawExactBytes: false` and `acceptanceEligible: false`. Their extents and frame deficits reproduce by parsing the retained reports. No current source classification was generated.

## B1F4 residual and reopen question

E16 preserves the improved ordinary loop: native base-plus-0x50 loads, count reload at record offset 0x94 then subtraction, and s0/base, s1/index, s3/next-pointer roles. E04 isolates timestamp placement without changing the preceding E16 output. Its 14 changed words lie at function-relative `0xD08..0xD40`; this is equality to E16, not a claim of retail equality before that interval.

The native tail visibly computes the first event time before code selection. After event 8, it recomputes base sums and reloads stack components for event 9. Relevant original words are ROM `0x0022BF08..0x0022BF60`. E04 source computes `tailTime` before selection, then writes the later expression separately; the compiler still retains the sum across the call.

The r8 P04 probe binds E16 and accounts for twelve directly occupied spill slots: decimal offsets `140,148,156,164,172,180,191,199,207,215,220,228`. Four are byte accesses. Native has no direct access at stack `0xE8/0xEC`; saved registers start at `0xF0`, versus candidate `0xE8`. Thus an omitted accessed spill does not explain the eight-byte frame deficit. Compiler-eliminated allocation remains possible, not proven.

**Proposed B experiment, untested:** bind E04 and E16 to their existing assembly and E16 P04 dumps; obtain E04 diagnostic pass dumps only if absent in the authorized later task. Track both event-time expressions from RTL through CSE/combine, schedule1, local/global allocation and final scheduling. Locate the first pass that shares their value across event 8. Record definitions, uses, call-crossing live ranges and frame allocations separately.

Competing predictions: early common-subexpression reuse creates one sum before allocation; alternatively, separate sums survive until allocation or final scheduling. A stage where both sums remain independent falsifies an earlier-reuse claim. An earlier prefix/register change falsifies the claim that a future source variant isolates only this tail. A frame explanation must identify an actual compiler allocation event; eight unused bytes alone cannot justify filler.

Do not yet prescribe duplicated branches or arithmetic identities. After the responsible stage is known, permit one legal source-lifetime variant that predicts a distinct change there. Preserve memory accesses, calls, integer behavior and the corrected loop. Reject unchanged retention, additional runtime work or unrelated allocation drift. This is a concrete diagnostic reopen, not a claim that a source fix is already known.

## D14C residual and reopen question

E07 is preferable for the next causal study because it removes artificial wrapper weighting. P03 binds its exact source. Target pseudo 216 has 167 weighted references/live length 1,186 and occupies s3; output-word pseudo 114 occupies s4. Mode pseudo 97 has 33/721 versus index 179 at 33/725; mode occupies fp and index spills at 516. These IDs are candidate-specific.

All 24 occupied slots persist. The recovered role map does not restore retail's 616-byte frame or 6,844-byte extent. E07 and E39 differ in 263 same-offset words, first at relative `0xBF4`; their preceding bytes agree with each other. That comparison is not a retail mismatch localization. E05/E07 have different extents, so same-offset totals cannot measure block alignment quality.

**Proposed D experiment, untested:** first use preserved E07 P03 assembly/pass dumps and original complete owner to construct correspondence for the later action loop, mode switch and emission/join regions. Anchor on call targets, branch destinations, table cases and memory operations; retain relocation semantics. Find the earliest retail divergence after corresponding blocks are aligned, then trace that block backward through existing RTL, combine, schedule1, allocation and schedule2.

Competing predictions: matching block operations diverge only through register allocation/scheduling; alternatively, missing, merged or reordered source control flow already differs before allocation. A preallocation edge or memory-operation discrepancy falsifies an allocation-only explanation. Equal edges/operations with the first discrepancy at allocation falsify a missing-control-flow explanation for that block. Preserve side exits and complete owner coverage; a longest-common-subsequence score alone is insufficient.

Only after that result should a source experiment change one identified branch/join or scalar live range. Require the predicted block change, preserved early roles and full spill accounting. If no unambiguous block correspondence or responsible pass emerges, report that evidence gap instead of a spelling sweep. This is new discrimination beyond the already solved E39/E40 priority comparison.

## Negative controls to preserve

- B1F4 r7 E20 couples timestamp and duration carriers; E04 deliberately separates that change. Do not repeat the coupled carrier as a new hypothesis.
- B1F4 r9 E06's unsigned-negation/subtraction form reaches 3,588 bytes but remains frame 272 with 554 nonrelocated word differences and earlier drift. Correct extent is not progress toward acceptance.
- B1F4's twelve-slot census rejects inventing an omitted accessed local. No filler, inline assembler or padded owner is justified.
- D14C E32's labeled exit and E39's wrapper-weighted role recovery do not compose automatically. E40 loses target/mode priorities; E05 only partly recovers them. E07 resolves that specific threshold question already.
- D14C E36/E38 created USE-only stack allocations without machine operations; they were rejected as frame corrections. E39/E07's existing USE-only slot is not evidence for another semantic local.
- Restoring wrappers, repeating broad variable-name/identity sweeps, or optimizing only the first-filter score repeats known weak evidence. Byte aliases inside occupied word slots are not additional slots.

## Relevant accepted lifetime result

Accepted boot conversion at `523d460` provides a specific compiler-stage precedent. In `func_00009EFC`, a pointer dies at its final load and shares register 2 with the output. An ordinary condition with identical stores keeps the pointer live through allocation; late-jump removes the condition. The final exact code uses registers 2 and 3 without added runtime effects.

This supports stage-specific lifetime investigation. It is not a universal matching recipe. B1F4 needs less retention of a derived sum across a call, while D14C has long overlapping lifetimes and control-flow differences. Blindly copying the boot condition could move behavior in the wrong direction. No pointer conversion, undefined-behavior identity, artificial storage or compiler-policy exception is proposed.

## Representation and acceptance boundary

B1F4 has demonstrated source/allocation/scheduling residuals; the retained evidence does not show an auxiliary representation blocker. D14C has both nonexact source output and a separate production-table ownership gate. Solving either one does not solve the other.

Accepted shared-table preparation at `2d41fe3`, with its Accepted review, says current contracts can retain D14C's middle 64 bytes as assembly between B06C and EF50. The D14C placement proxy expresses five entries, four compiler alignment bytes and ten entries through two `compilerOccurrences`. It proves placement support, not exact D14C C. Later D14C activation must replace the retained interior rather than overlap it. Compilation-group support excludes these auxiliary shapes.

No new representation limitation is demonstrated. Do not alter tools to pass either owner. Changed production auxiliary ownership still requires the separately assigned structural evidence, one changed-input audit and independent review. Ordinary source matching under accepted contracts retains its complete-wave verifier without an added source-review gate.

The original seventeen-owner shared W6 remains: A280, A414, A4E0, A7B8, A964, ADFC, B06C, B1F4, BFF8, C78C, D14C, EC08, EDD4, EF50, F2BC, plus bridges `func_0022257C` and `func_00222604`; the fifteen short names use prefix `func_0022`. High Attack W5's complete eight members, including B438/B894, remain preceding obligations. Its seven-member W7 and fifteen-member W8, including bridge `func_0021EAF0`, remain later obligations. Combat's active W7 and remaining W8, shared completion prerequisites, and unresolved research gates remain intact. This is not a two-owner substitute wave or concurrent High Attack production.

## Exact evidence and verification

The ignored `build/combat-mode-owner-retry-preparation-r1/inputs.json` records 27 exact input paths, sizes and hashes. Key SHA-256 identities:

| Artifact | SHA-256 |
|---|---|
| B1F4 E16 source | `BDD3B1A20DF1B8F7D2219775F665C93A4318238B00D09EE391F882C4965D6B18` |
| B1F4 E04 source | `25C2B19614E7C99A026750897005399A09B71796C2652FFB338530C10AAB64ED` |
| B1F4 E04 run report | `A8D94AF53396ABB768144DE06EAB6B05F40304078C0D6276D5A0E8F245A88AE1` |
| B1F4 E16 P04 probe report | `99B1171BBA74A9395D187ABA568878BB4807D91C7B15D2A1EC2CFA6FFB452E23` |
| D14C E07 source | `78DD9D9325EC0AD875768041E9517A7D224D92F947B68C7DEE4D15CD922986E4` |
| D14C E39 source | `F1209A3616DD326B2C7CF1E698C02E3518F6194D7B29153396AA767ABF77DA86` |
| D14C E07 run report | `40622E763E3C0F692DBAC6710EA296C50307F8EC072CF6052D006F2AF31B830A` |
| D14C P03 report | `9B32A54B830CF0BFFAADC28F3AA389DF06A6F6E8354257CA53BAF2C827EF3E05` |
| D14C P03 lreg | `BC22248966357CA4D2E87F01AC2FF56EC4AF59C857CA4DA6BBC3C1C213E60D48` |
| D14C P03 greg | `DC7BF4851387CB221C9CF4DB76548CD9B0F133530B86FC65E1EEC875BA6939FF` |

Historical root is `../high-attack-wave-5/build/matching/`. B1F4 run is `FCF102FE514D73F54E8864EC937275388742347221848819B0310EB7CCA812D2`; D14C run is `3E7D083FC2A6606EC8A9CAD42524DA84E9985CEDDC7DAD002781B039D1CE8E64`. Run reports are under `runs/<id>/workbench-report.json`. P03 is under `targets/func_0022D14C/probes/0F97294BD42563E766A0AC830133FDADF7F99F6D13E081E22582CF2EFB002DD8/`. P04 is under `targets/func_0022B1F4/probes/93A46B0D3C89F37023582D452B8DEEAC22AB66883F37AAE4ADA2C9827923C108/`.

The historical reported compiler is SHA-256 `F3F1C99A322F5B3D8C108C2A44AF1D6D084DD27575C5D60BF0F0D33FFF34B1C6`. No compiler authentication or new ROM measurement occurred here. Parsing/hashing confirms preserved artifact identity, not current reproducibility. Broad nested JSON output was truncated; scalar follow-up reads recovered the needed fields. One parent-path Git diff failed because the path belonged to the parent repository; the corrected canonical-only query passed without mutation.

Evidence grade for the proposed causal interpretation is Supported, review pending. New experiments remain untested. Only the fresh claim, this report and the ignored input manifest were written. All writes are released at terminal handoff.
