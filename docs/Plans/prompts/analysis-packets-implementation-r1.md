---
task_id: analysis-packets-implementation
revision: 1
status: completed
role: worker
review_level: Material
inventory_profile: NORMAL
human_gate: none
launch_id: ANALYSIS-PACKETS-IMPLEMENTATION-20260908-01
workspace_claim: C:/Users/Joe/Projects/OgreBattlel64/OB64 Decomp/docs/Plans/task-logs/analysis-packets-implementation-r1.claim.json
---

# Standalone Kuna and m2c analysis packets

Director `/root` assigns existing Astra Medium `/root/db10_allocation_trace` a separate tooling task implementing Joe's standing goal addition in sequential-main-program.md. Automating prepared packets should remove repeated manual input preparation while preserving their evidentiary limits. R4 has released all writes and its unresolved source work is frozen at `8251e8cf`. This assignment grants no matching-source ownership and does not waive W8's complete fourteen-target gate.

Read parent/canonical AGENTS.md, parent Worker-workflow.md, canonical WORKFLOW.md, SOURCE_POLICY.md, NEXT_STEPS.md and AUDIT.md. Read the accepted Kuna trial and review, the cost/history assessment, and the completed R4/DB10 packets. These establish the permitted second-interpretation role, exact analysis-only ELF method, tool identities and observed limitations. Do not modify any frozen report, trial binary/specs or evidence root.

Deliver a small standalone command under `tools/analysis_packet/` that prepares an analysis packet for an accepted function and automatically includes default Kuna and m2c results. It is invoked at large/difficult-function analysis intake, independently of the normal compile, linked diff and verifier commands. m2c remains primary; both results are explicitly hypotheses. Alternate transformations must be requested for a specific ambiguity rather than run as a default sweep.

Use the accepted model and authenticated normalized retail bytes. Bind owner, runtime mapping, extent and supplied metadata in the packet. Preserve accurate treatment of supplied tables, types, unsupported instructions and ambiguous or unsupported owner shapes. Do not ask either decompiler to infer N64 ROM structure, manufacture missing data or silently present partial output as complete. Reuse existing generic project readers where appropriate without changing their contracts. The packet must expose commands, statuses, useful timing, input/tool/options identities and output locations, and permit safe cache reuse only for the same authenticated inputs and configuration. Failed or missing analysis tools must remain visible and must not affect the ordinary build. Keep large/generated analysis files under ignored build paths.

Use the existing local pinned tools for actual validation. Do not hard-code Joe's absolute installation paths into reusable source, install global dependencies, vendor external decompiler code, or download/upload ROM or source. Any local tool discovery/configuration should be explicit and documented. No automatic compilation or adoption of generated C, no scheduler/service, and no modification to match.js/diff.js/build.js/verify.js/audit.js or production compiler inputs. This is optional analysis tooling outside the canonical toolchain.

Own only new `tools/analysis_packet/` source, its meaningful tests and documentation, `docs/ANALYSIS_PACKETS.md`, the fresh claim, `docs/Plans/task-logs/analysis-packets-implementation-r1.md` and ignored `build/analysis-packets-implementation-r1/`. Create the claim atomically and read it back before other writes. You are not alone in the repository; preserve unrelated edits, especially held Resolver files. No production C/headers/configuration, accepted mapping/owner changes, runtime, Resolver, GUI, database, agent creation, staging, commits, push, branch or worktree operations.

Validate the reusable command against representative solved and hard accepted owners, including a supplied-table case when supported, and retain exact comparisons with the existing accepted packets. Exercise consequential rejection/cache/status behavior rather than tests that merely mirror code. Run the required routine tooling manifest and targeted new checks. Do not run a full-ROM verifier merely to test this isolated tool. If implementation requires a change covered by AUDIT.md, stop and route that structural requirement rather than weakening the scope.

Return a coherent tested implementation, concise usage/limitations and exact changed-file/evidence identities to Director `/root`; explicitly release every write surface. This tooling result requires independent Material review before it becomes the normal analysis-intake command. Do not issue your own acceptance verdict. Report concrete unresolved dependencies instead of expanding into general decompiler integration or altering matching gates.
