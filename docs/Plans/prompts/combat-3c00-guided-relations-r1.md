---
task_id: combat-3c00-guided-relations
revision: 1
status: active
role: worker
review_level: Focused
inventory_profile: NORMAL
human_gate: none
launch_id: COMBAT-3C00-GUIDED-RELATIONS-20260908-01
workspace_claim: none
---

# Assess a bounded joint source-relation search for W8

Director `/root` assigns existing Astra Medium `/root/combat_draw_continuation` a new read-only preparation task. Your R6 source assignment is terminal at `fba8ef7c94e3c2da548a8a8546aebc7ac29df113`; all its records remain frozen. The generic workbench implementation now owns the sole production shared-tool/build surface. This task restores no production source authority and creates no artifacts.

Read parent/canonical AGENTS.md, parent Worker-workflow.md, canonical WORKFLOW.md and SOURCE_POLICY.md. Read the frozen R6 report and final indexes. The report SHA256 is `85EAF26F58FFF954B35442312908A26CCABD553E27E100310EE3EB12172898F4`; results SHA256 is `D1A7426710EB52CD554CDDCD749B64E5B71E9047B4118A5477FF7336351ADCA4`. Read the accepted guided-permuter trial/review at `d7e87753` and `0b204012`. Use accepted mappings and immutable source/pass evidence, not live shared-tool behavior during its correction.

Determine whether the best source and preserved R6 intermediates support a small meaningful combination search for the unresolved 3C00 packet-value arrangement. Identify real source relations, their compatibility constraints, the evidence supporting each, which joint combinations are already tested, and which combinations answer a new question. Choose the proposed dimensions independently. A bounded joint search may retain worse intermediate forms; do not prune by immediate score or mistake a score for acceptance.

The proposal must preserve actual value domains, control flow, calls, stores and defined behavior. No arbitrary references, dummy storage, fabricated padding, invalid pointers, assembler escape or altered toolchain. Do not assume independent choices can be combined when their dataflow or scopes depend on each other. Distinguish original retail facts from current compiler behavior and proposed source hypotheses. Treat original allocation history as unknown where it is absent.

If justified combinations exist, return a concrete bounded search proposal usable by the next ordinary W8 source writer through the already reviewed manual upstream path. Identify its source seeds, relation set, dependency constraints, finite size and what observations would distinguish hypotheses. Do not implement a harness, generated candidate or new scoring method. If the retained evidence supplies no justified joint choices beyond completed inputs, report that exact limitation without inventing an experiment count or an impossibility claim.

This is ordinary matching preparation, not a new tool or independent source-acceptance gate. It cannot shrink the fourteen-target W8 or advance to another family. The next production source assignment remains separate and follows accepted tooling release.

You are not alone. All production source, configuration, tools, tests, reports and evidence are read-only. No file/artifact writes, compiles, tests, decompiler runs, new instrumentation, runtime, Resolver, GUI, database, agents, staging, commits, branches/worktrees or push. Use `workspace_claim: none` and return message-only findings to `/root`. Release the read-only assignment explicitly when finished.
