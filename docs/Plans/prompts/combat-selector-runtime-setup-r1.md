---
task_id: combat-selector-runtime-setup
revision: 1
status: ready
role: worker
review_level: Focused
inventory_profile: NORMAL
human_gate: separate-exact-run-capture-approval
launch_id: COMBAT-SELECTOR-RUNTIME-SETUP-20260907-01
workspace_claim: C:/Users/Joe/Projects/OgreBattlel64/OB64 Decomp/docs/Plans/task-logs/combat-selector-runtime-setup-r1.claim.json
---

# Set up the selector runtime through existing controls

Use Astra Medium /root/combat_discovery. Director /root, native task 01a07262-aeca-7341-ad10-2dba705ff988, local.
Joe replied "Are you able to do this" to the concrete startup/open-ROM/load-prepared-state/pause request. This authorizes performing that setup using supported existing controls; it does not authorize the separately described capture run.
Read both AGENTS.md files, parent docs/Worker-workflow.md, tools/total_resolver/AGENTS.md and its capture/maintenance reading, the applicable debugger/live-watch/startup SOP, accepted observer review 19049cd, frozen preparation 9323e90 and verified working-copy result 2686971.

Own the sole runtime setup operation on local port 64656. Use existing authenticated launcher/bridge/CLI controls to start the intended interactive Project64, open the verified vanilla Rev 0 ROM in interpreter mode, load the exact prepared working copy, leave it paused, and perform the documented immediate readback/readiness checks.
The prepared working copy is parent Test Savestates/combat-selector-explore-20260907-01/Battle Scene Loaded FIGHT IT OUT gone ANIMATIONS ON.pj.zip, SHA-256 464BCC227F8C8151F78572ED92F40F5CD0F4692308C837B8762FEDEC28CEE583.
Input originals and ROM remain read-only; no state edits, ROM modifications, arbitrary RAM writes, controller play, watch installation, capture, session start/stop or database mutation. Preserve other emulator instances and any unexpected active owner; do not seize or kill them.
The user requested interactive setup. Use its documented interactive launch behavior; no hidden-mode workaround or raw GUI-automation helper. Do not initialize Computer Use. Prefer the existing task-specific launcher/bridge controls; if they cannot perform a required operation, return that exact capability limit after completing safe independent setup.
Do not invent unsupported commands, change shared tooling, replace runtime binaries, weaken authentication, or bypass a denied tool/approval. Use ordinary tool approval handling if an actual environment gate occurs and report it exactly.

Authenticate source/copy/tool/runtime identity and record actual actions, bridge epoch, current owner/paused/interpreter state, selected knowledge/frontier and the six supported field readbacks from the preparation packet. Saved PC expectation is unavailable; preserve that limit.
Using the accepted adapter without changing it, prepare a new exact proposed configuration and canonical hash if all runtime prerequisites are actually established. Keep approval explicitly unapproved and include a concrete future capture-assignment reference for Director staging. This setup cannot begin capture.
If load readbacks fail or an existing runtime conflicts, preserve evidence and report the concrete blocker; do not improvise gameplay or broaden the route.

Own only the fresh claim, docs/Plans/task-logs/combat-selector-runtime-setup-r1.md and ignored build/total-resolver/combat-selector-runtime-setup-r1/, plus the named supported runtime setup actions.
Create the complete claim atomically before other writes/actions. You are not alone: boot writer owns production source/build and reviewer owns shared-dispatch evidence. Preserve all their work and frozen records.
No agents, production changes, builds, staging, commits, push, branches or worktrees. Report only through collaboration.send_message to /root; no Codex-app native callback/readback. Return setup/config readiness or precise missing capability, then release writes/runtime setup ownership explicitly.
