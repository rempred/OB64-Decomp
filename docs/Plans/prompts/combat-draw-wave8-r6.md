---
task_id: combat-draw-wave8
revision: 6
status: active
role: worker
review_level: Focused
inventory_profile: NORMAL
human_gate: none
launch_id: COMBAT-DRAW-WAVE8-20260908-06
workspace_claim: C:/Users/Joe/Projects/OgreBattlel64/OB64 Decomp/docs/Plans/task-logs/combat-draw-wave8-r6.claim.json
supersedes: combat-draw-wave8-r5
---

# Continue the complete W8 from preserved source alternatives

Director `/root` assigns existing Astra Medium `/root/combat_draw_continuation` the same complete fourteen-target source wave. R5 is frozen at `066580a4af3d852ff7ab4d90ab5b786ce7fb097b`. R5 and the isolated guided-search worker have released all writes/processes. You receive sole production source/build ownership upon explicit activation.

Read parent/canonical AGENTS.md, parent Worker-workflow.md, canonical WORKFLOW.md, SOURCE_POLICY.md, NEXT_STEPS.md and the current sequential program. Read the frozen R5 report and its source/result indexes. Starting inputs remain bound by `build/combat-draw-wave8-r5/terminal-current-inputs.json`, SHA256 `47D8B0BE5D0622771662B5AF52853A893CE90797AA0806B4CD5A97FB8092F656`. Director intake authenticated all twenty current entries unchanged. Authenticate these inputs before source work. Preserve all frozen predecessor reports and evidence.

Finish func_001F3C00, func_001F5654, func_001F6098, func_001F7ADC, func_001F89B4, func_001FCB28, func_001FD56C, func_0020BFF8, func_0020C014, func_0020C034, func_0020C448, func_0020C4B8, func_002103EC and func_0020DB10 as one wave. Preserve the accepted W7 baseline, ten new focused-exact W8 candidates and accepted BFF8. The remaining three nonexact functions and final complete-wave verification are still required.

Joe's latest clarification is mandatory: every experiment need not immediately improve its score. Preserve the current best separately while exploring supported sequences through temporary regressions. "Rejected" means not selected as current best unless stronger evidence contradicts the underlying structure. Reassess preserved intermediate forms on that basis. Record the source/instruction question, what related experiments establish, and the evidence for continuing or closing a hypothesis. Choose the technical approach independently. A worse score alone is not a drift trigger or a reason to abandon a supported sequence.

R5 provides coordinated endpoint, address-history and texture-row alternatives, including a full-extent 6098 candidate with useful register correspondences. These are evidence, not required winners. Avoid unchanged repetitions that answer no new question. When a line of work lacks a supported next change, reassess the source model or request bounded independent research. Do not replace reasoning with arbitrary storage, dummy references, fabricated padding, invalid pointer arithmetic or assembler mechanisms.

Default Kuna and m2c outputs must accompany prepared analysis packets for large/difficult targets. Keep m2c primary and both outputs as hypotheses. Reuse authenticated unchanged packets; request alternate transforms only for specific ambiguities. The standalone packet command in `docs/ANALYSIS_PACKETS.md` is accepted at `4f4ce3d6` and adopted at `1b1d8b81`. Its analysis outputs remain outside compilation and acceptance. The separate upstream guided-permuter trial at `d7e87753` is pending Material review. Do not use that new adapter or findings as a dependency before the Director transmits acceptance. Independent source work from previously accepted evidence may proceed meanwhile.

After activation, own the fourteen assigned C sources, narrowly necessary existing combat draw headers and corresponding target/linkage records under existing contracts. Own the fresh claim, `C:/Users/Joe/Projects/OgreBattlel64/OB64 Decomp/docs/Plans/task-logs/combat-draw-wave8-r6.md` and ignored `build/combat-draw-wave8-r6/`. Create the claim atomically and read it back before other writes. Work on one target at a time on main. You are not alone; preserve all other edits. Independent review owns separate reports/evidence and no production writes.

Shared tools, compiler identities/flags, diagnostic instrumentation, boundaries/mappings, source policy and linker contracts must remain unchanged. Every new traced input requires its own pinned production-compiler agreement. Route independently justified structural/tooling defects separately. Prefer readable structured C and evidence-backed shared types/fields. Explain necessary compiler workarounds beside the affected source.

No shared tooling edits, runtime, Resolver, GUI, bridge/database work, agents, staging, commits, push, branches or worktrees. Held Resolver files remain untouched. Keep production bests separately recoverable during exploratory compilation. Preserve unselected experiments and their hypothesis status.

Use focused linked comparisons, mechanical source classification and actual relocation evidence for selected candidates. Once all fourteen targets are ready, run one final normal verifier without a redundant preceding full build. Require every target PURE_C, sole intended C ownership, correct placement, exact relocations and target bytes, plus full Rev0 ROM equality. Do not shrink or split the wave. Ordinary matching requires no independent source review.

Return exact final inputs and complete-wave proof, or a precise unresolved handback distinguishing unselected candidates, open hypotheses and stronger contradictions. Release all production/report writes and processes explicitly to Director `/root`.
