---
task_id: combat-draw-wave8
revision: 8
status: ready
role: worker
review_level: Focused
inventory_profile: NORMAL
human_gate: none
launch_id: COMBAT-DRAW-WAVE8-20260908-08
workspace_claim: C:/Users/Joe/Projects/OgreBattlel64/OB64 Decomp/docs/Plans/task-logs/combat-draw-wave8-r8.claim.json
supersedes: combat-draw-wave8-r7
---

# Continue the complete W8 from the preserved best source context

Director `/root` assigns existing Astra Medium `/root/combat_draw_continuation` the same complete fourteen-target W8. R7 is terminal unresolved at `a9f3f873f23943d8a56187f5c460eed14a2d6d9f`. All former production/build/report/evidence writes and processes are released. Upon explicit activation, you are the sole production source/build writer on main. Do not create a claim or write files before that activation.

Read current parent/canonical AGENTS.md, parent Worker-workflow.md, canonical WORKFLOW.md, SOURCE_POLICY.md, NEXT_STEPS.md and the current sequential program. Read the R7 terminal handback and the source-sequence evidence needed for this continuation. Its report hashes to `E9F4516ACC6BACF650C222D659FE77AF2EAD65376F70635AA708C64580702E69`. `build/combat-draw-wave8-r7/terminal-current-inputs.json`, SHA256 `2B51C3564A03B09165EE3679A5D9DCADCAFEE257FE674D332AE4F243EAAACA60`, binds all twenty current source/shared inputs. Authenticate them before source work. R7's terminal manifest is `ABBA2B1FC1AB41CBC21BC2F60EE2705E1F735EE2EC35E202DCB4DE5913AA7560`.

Finish func_001F3C00, func_001F5654, func_001F6098, func_001F7ADC, func_001F89B4, func_001FCB28, func_001FD56C, func_0020BFF8, func_0020C014, func_0020C034, func_0020C448, func_0020C4B8, func_002103EC and func_0020DB10 as one wave. Preserve accepted BFF8, the ten new focused-exact candidates including7ADC, and the exact W7 baseline. The three nonexact targets remain required. Do not split the wave, accept a partial result or advance to another family as though W8 passed.

Use R7's observed lifetime interactions to assess a distinct source context: the preserved best 3C00 reconstruction D037. R7 used D037 as a fidelity control; its searches instead retained a different R6 strip, overlap and vertex context. R7 observed that localizing the real final size-command cursor removed an endpoint spill even after restoring int endField. Determine whether useful lifetime changes transfer to D037's existing packet and loop value histories. Check the relevant preserved earlier controls before calling a combination untested. Do not infer that a source transformation transfers unchanged or must improve output. Choose the actual source sequence and subsequent work independently.

The immutable best source is `build/combat-draw-wave8-r7/starting-inputs/src/lib/func_001F3C00.c`, SHA256 `D03797FD04EEE60592E0644A837E09308568D3E82448883997A10A85DA7F91F5`. R7's restoreend controls and fresh trace bindings document the recovered cursor interaction. Its later branchjoint results document late call-tail sharing; smaller extents and fewer static helper-call occurrences did not establish matching. Read those facts as candidate-specific observations, not original source or allocation history.

The completed allocation-trace assessment recommends existing evidence instead of more instrumentation. It found no specific missing allocator event that would select another source experiment. This does not prohibit legitimate source hypotheses or require a proposed C form to be proven original before testing. Physical register reuse alone does not prove shared source-variable identity. Preserve values, required operations and defined behavior when testing alternative representations or lifetimes.

Experiments need not improve score at every step. Preserve the current best separately while pursuing supported combinations through temporary regressions. "Rejected" means not selected as current best unless stronger evidence contradicts the structure. Record candidate selection and hypothesis elimination separately. Avoid unchanged repetitions without a new question, arbitrary references, dummy storage, fabricated padding, invalid pointers, invented callee effects and assembler mechanisms.

Automatically include default Kuna and m2c outputs in prepared analysis packets for large/difficult targets. Reuse authenticated unchanged packets. Keep m2c primary and both outputs as hypotheses; alternate Kuna transformations address specific ambiguities. Use the accepted standalone packet command outside canonical compilation/linking/acceptance. The reviewed optional upstream manual-search method remains available within its single-owner/no-extra-data scope. Use a new owned private copy, authenticate its full tool/input closure and require faithful controls plus nonempty scoring coverage. Heuristic scores do not replace complete bytes, actual relocations or canonical gates. Every newly traced input requires its own pinned production-output agreement.

After activation, own only the fourteen assigned C sources, narrowly necessary existing combat draw headers, and corresponding target/linkage records under existing contracts. Own the fresh claim, `C:/Users/Joe/Projects/OgreBattlel64/OB64 Decomp/docs/Plans/task-logs/combat-draw-wave8-r8.md`, and ignored `build/combat-draw-wave8-r8/`. Existing matching tools may write normal development outputs and candidate stores in their configured locations. Keep new private analysis/search evidence under the R8 root. Create the claim atomically and read it back before other writes. You are not alone; preserve unrelated changes and every frozen R7/predecessor record.

Compiler identities/flags, shared tools, diagnostic instrumentation, accepted boundaries/mappings, source policy and linker rules remain unchanged. Route independently justified structural/tooling defects separately. Prefer readable structured C, evidence-backed shared types/fields/constants and explicit uncertainty. Explain necessary compiler workarounds beside the affected source. No runtime, Resolver, GUI, bridge/runtime-database work, global installs, agents, staging, commits, push, branches or worktrees. Ordinary matching candidate stores remain allowed as described above.

Use focused linked comparisons, mechanical source classification and actual relocation evidence for selected candidates. After all fourteen targets are ready, run one final normal verifier without a redundant preceding build. Require every target PURE_C, sole intended C ownership, correct placement, exact relocation contracts and target bytes, plus complete normalized Rev0 ROM equality. Ordinary matching requires no independent source-review gate.

Return exact final inputs and complete-wave proof, or a precise unresolved handback preserving useful intermediates and all wave obligations. The terminal summary must clearly distinguish current status from earlier progress entries. Explicitly release all production/build/report/evidence writes and processes to Director `/root` at terminal handback. The complete Combat→Squad→High Attack goal remains required.
