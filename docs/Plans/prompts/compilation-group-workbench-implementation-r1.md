---
task_id: compilation-group-workbench-implementation
revision: 1
status: completed
role: worker
review_level: Material
inventory_profile: NORMAL
human_gate: none
launch_id: COMPILATION-GROUP-WORKBENCH-IMPLEMENTATION-20260908-01
workspace_claim: C:/Users/Joe/Projects/OgreBattlel64/OB64 Decomp/docs/Plans/task-logs/compilation-group-workbench-implementation-r1.claim.json
---

# Correct generic workbench handling of accepted compilation groups

W8 R6 has released all production/build/report writes and processes. Its terminal handback is frozen at `fba8ef7c94e3c2da548a8a8546aebc7ac29df113`. Director intake authenticated all 746 evidence entries and twenty unchanged current source/shared inputs. Preserve `build/combat-draw-wave8-r6/terminal-current-inputs.json`, SHA256 `3DA17CD6DC1D14E48831EFF87B7F2AD8A91364D227D91FB59E0812D40D29EE3B`, as the source/configuration baseline. The wave remains incomplete; no source writer may resume before your production-tool release. Do not create a claim or write files before explicit Director activation and its fresh claim/report/root absence check.

Upon activation, Director `/root` assigns existing Astra Medium `/root/db10_allocation_trace` sole production shared-tool/build ownership for this bounded correction. It fixes the independently observed reader/consumer defect and stale tests; it is not a workaround for any nonmatching function.

Read parent/canonical AGENTS.md, parent Worker-workflow.md, canonical WORKFLOW.md, SOURCE_POLICY.md and AUDIT.md. Read the frozen assessment at `71ed03c71261927a1ca11ed347f0245dac9abb7f` and its independent review at `cfd31e1bee6c1f8d1ed43a1fc8785ae8b1c36ef6`. The review report `docs/Plans/task-logs/compilation-group-workbench-assessment-review-r1.md` has SHA256 `4311F46C312FCB01AAC961462D06AEEDDC6EB57F3B62B31152F1BB102FC682A7`. Its verdict is Accepted with corrections; all three corrections below are mandatory. Preserve the accepted group implementation/design contracts and all frozen records.

Implement the reviewed generic workbench behavior. Valid active groups must not prevent unrelated standalone targets from loading. Derive live producer/member state from existing strict validation. Keep every accepted member visible with its actual shared source, complete ordered producer membership and selected member identity. Preserve each function's retail owner/bytes and immutable target identity. Do not invent per-member sources, duplicate a producer as a function, filter out group members or weaken registry validation.

Separate activity, inspection eligibility and scratch-compilation capability. Active grouped members remain inspectable and visible in requested active rankings. Automatic compilation sweeps must exclude unsupported group members and explain that exclusion. Explicit grouped prepare/watch/probe and equivalent candidate requests must reject before decompiler/compiler launch, candidate storage or source snapshots. Library callers need the same protection as CLI callers. Preserve the independent final session guard. Complete-group scratch compilation remains unsupported; canonical whole-group diff remains the supported source path.

Keep live producer/activity/capability metadata outside stored structural target records and target identity. Refresh it against current complete producer/configuration inputs. Any separate summary cache needs explicit complete identity and invalidation. Preserve existing standalone, empty-registry, continuation, native-tail, diagnostic cache and source-identity behavior. Choose factoring and implementation details independently.

Apply review correction CGWA-C01: move compilation-group fixture output out of frozen `build/compilation-groups-implementation-r1/` before removing the stale assertion. Use a fresh assigned or generic isolated test root. Preserve and authenticate production source/configuration around fixture execution.

Apply CGWA-C02: protect the probe compiler library path before source writes or compiler launch. Do not rely solely on CLI routing or on the normal candidate compiler's final guard.

Apply CGWA-C03: run the existing `tests/matching_workbench_integration.js` after the completed reader/admission correction. It covers the actual compiler/decompiler chain and is not included in the routine manifest.

Correct the two stale test assumptions without weakening their meaning. Group relocation provenance requires positive complete producer/member/source/relocation binding assertions. Replace the permanent empty-production-registry assumption with fixture isolation. Preserve malformed, missing, partial, duplicate, reordered, mixed-source and unsupported-contract rejection.

Own only the relevant workbench reader/consumer files under `tools/lib/matching/` (`target_model.js`, `rank.js`, `sweep.js`, `compiler.js`, `m2c.js`, `probe.js`, and a small new consumer helper if needed), bounded CLI reporting in `tools/match.js`, affected matching/group test files under `tests/`, and `docs/MATCHING_WORKBENCH.md` with narrowly necessary test-usage documentation. Own the fresh claim, `C:/Users/Joe/Projects/OgreBattlel64/OB64 Decomp/docs/Plans/task-logs/compilation-group-workbench-implementation-r1.md`, ignored `build/compilation-group-workbench-implementation-r1/`, and fresh isolated test outputs. Do not write into frozen evidence roots. Create the claim atomically and read it back before other writes.

Production C/ASM/headers/configuration, strict active/group validators, text-contract rules, canonical compilation/build/verifier logic, compiler identities/flags and structural mappings remain read-only. Do not change database schemas or canonical cache/ownership contracts for this consumer correction. If the accepted result cannot be achieved without expanding that boundary, stop affected writes and return the concrete reason for a separate assignment. User authorization does not permit weakening a generic invariant to make one target pass.

Run meaningful focused validation of the reviewed behavior, the affected suites, the required workbench integration suite and `node tools/test.js` on the completed correction. Preserve all failures and verify that tests use isolated outputs. Prove rejection before side effects for direct and CLI/library paths, truthful producer views, stable structural identity and continued standalone behavior. Do not add full-ROM verification solely for these analysis-consumer changes. Structural/build/acceptance changes remain outside scope and would require the applicable separate audit.

You are not alone in the codebase. Preserve the exact baseline, all unfinished W8 inputs and held Resolver files. No ordinary matching source writer may run alongside this assignment. Read-only research or independent review may use disjoint evidence roots. No runtime, Resolver, GUI, database mutation outside fresh test fixtures, global installs, agents, staging, commits, branches/worktrees or push.

Return the completed change, exact changed inputs, reproducible validation and scoped limitations, or a concrete blocker. This implementation remains pending independent Material review; it does not accept a source wave. Release all production/tool/report writes and processes explicitly to Director `/root`.
