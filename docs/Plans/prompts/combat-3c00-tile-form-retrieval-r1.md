---
task_id: combat-3c00-tile-form-retrieval
revision: 1
status: completed
role: worker
review_level: Focused
inventory_profile: NORMAL
human_gate: none
launch_id: COMBAT-3C00-TILE-FORM-RETRIEVAL-20260908-01
workspace_claim: none
---

# Locate previously tested 3C00 tile-value source forms

Director `/root` assigns existing Sol High `/root/db10_alias_inventory` a bounded retrieval/parsing task. Active Astra Medium W8 writer `/root/combat_draw_continuation` owns all production source/build work. This task creates no files or artifacts.

Read parent/canonical AGENTS.md, parent Worker-workflow.md, canonical WORKFLOW.md and SOURCE_POLICY.md. Use frozen W8 R1 through R5 authored-source/result indexes and their retained source inputs. Do not substitute mutable generic compiler outputs for retained candidate identity. Current R6 source and evidence may change; the writer supplies the current technical interpretation.

Locate previously tested func_001F3C00 inputs that change the real packed tile value's declarations, definitions or branch-specific uses. The current writer identifies the value as `(tileBytes / 8) & 511` and its combined tile value. Return literal source forms, exact source paths/hashes, associated experiment names and recorded size/frame/score metrics where bound. Distinguish authored inputs from expanded duplicates and simple renames. Identify the searched corpus and any unavailable artifacts. Do not infer validity, causation, a preferred experiment, or the absence of forms outside that corpus.

The purpose is to avoid repeating source experiments while the writer analyzes a newly observed tile-value/companion register conflict. No technical diagnosis or implementation is assigned. Old unselected scores do not disprove structures; retain that distinction when quoting historical labels.

You are not alone. All files are read-only. No source/configuration/tool/report/evidence writes, compiles, tests, decompiler runs, new instrumentation, runtime, Resolver, GUI, database, agents, staging, commits, branches/worktrees or push. Use `workspace_claim: none` and return message-only findings.

Send the bounded literal index to `/root` and `/root/combat_draw_continuation`. Explicitly release the read-only assignment when finished.
