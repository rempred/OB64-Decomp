---
task_id: combat-3c00-cursor-context-guidance
revision: 1
status: ready
role: worker
review_level: Focused
inventory_profile: NORMAL
human_gate: none
launch_id: COMBAT-3C00-CURSOR-CONTEXT-GUIDANCE-20260908-01
workspace_claim: none
---

# Assess the remaining packet-value lifetimes after cursor localization

Director `/root` assigns existing Astra Medium `/root/combat_6098_neighbor_expression` one read-only source question. Your earlier packet-lifetime and 6098 assignments are terminal. R7's source worker has since removed an endpoint spill through coordinated source changes and then restored the endpoint's original width. Determine whether those completed controls support one distinct next source-lifetime question for func_001F3C00.

Read current parent/canonical AGENTS.md, parent Worker-workflow.md, canonical WORKFLOW.md, SOURCE_POLICY.md and the standing sequential program. Use the immutable controls below, accepted retail assembly and the relevant preserved earlier controls. Read only the earlier evidence necessary to distinguish this question from completed experiments. Do not use the mutable R7 report or rerun its searches.

All following paths are relative to `C:/Users/Joe/Projects/OgreBattlel64/OB64 Decomp/build/combat-draw-wave8-r7/`:

- `restoreend-authored/i1t0.c`: SHA256 `4ED007DE4897C89A67C691A6D4F4C6493BA6A94C8F967FC7A7768F741A31962C`.
- `restoreend-authored/i1t1.c`: SHA256 `4C5079DB5B248A891426D63DD978591A29E26728A767E6DEB76CD976D796DA74`.
- `restoreend-results.json`: SHA256 `446806BB25F700DFD7216CB9FF56A0110B6191C80EA939541462F2D29C9F459F`; identifies upstream inputs, objects, complete private comparisons and actual relocations.
- `restoreend-trace-bindings.json`: SHA256 `AAE1F13E256F7275333014BFEF8D68926982E4AC3E7E239242B4450C0F475294`; binds per-input compiler agreements and homes.
- `3c00-traces/i1t0/` and `3c00-traces/i1t1/`: complete flow/local/global passes, authored inputs, pinned assembly and analysis.
- `starting-inputs/src/lib/func_001F3C00.c`: preserved best, SHA256 `D03797FD04EEE60592E0644A837E09308568D3E82448883997A10A85DA7F91F5`.

The current writer already completed the endpoint recompute/reuse, primary-row shared/separate and final-tile recompute/reuse product in this localized-cursor context. Treat that product as covered. The writer also owns investigation of its changed static helper-call count; do not duplicate that work. Your question concerns a remaining actual packet-value lifetime or consumer relation beyond those choices. Choose the reasoning independently.

Compare the remaining compiler quantities and retail register/value histories with the preserved source forms. Return at most one concrete bounded source question, including compatible seeds, supporting and contrary instruction evidence, and the observation it would distinguish. Distinguish actual source-variable lifetimes from physical register reuse by unrelated values. If no new supported relation emerges, identify the missing evidence precisely. Do not invent a finite search merely from untried spellings.

Preserve actual values, calls, stores, control flow and defined behavior. No dummy storage, arbitrary references, padding, invalid pointers, assembler mechanisms or altered compiler contract. Keep original retail allocation history unknown where evidence does not supply it. A worse score alone must not eliminate a structure; completed controls and useful intermediate paths remain available.

This is ordinary matching preparation, not independent review or source acceptance. The sole production source/build writer remains `/root/combat_draw_continuation`. All fourteen W8 targets and the final combined verifier remain required.

You are not alone. This assignment permits no file or artifact writes, so `workspace_claim` is none. Do not compile, run tests or decompilers, instrument tools, access runtime/Resolver/GUI/databases, create agents, change Git, or use the network. Send message-only findings to Director `/root` and explicitly release this read-only assignment when finished. The production writer chooses and executes any source continuation.
