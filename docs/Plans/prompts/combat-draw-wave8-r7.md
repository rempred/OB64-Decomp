---
task_id: combat-draw-wave8
revision: 7
status: active
role: worker
review_level: Focused
inventory_profile: NORMAL
human_gate: none
launch_id: COMBAT-DRAW-WAVE8-20260908-07
workspace_claim: C:/Users/Joe/Projects/OgreBattlel64/OB64 Decomp/docs/Plans/task-logs/combat-draw-wave8-r7.claim.json
supersedes: combat-draw-wave8-r6
---

# Continue the complete W8 with supported joint source experiments

This assignment is prepared but not activated. Do not create a claim or write files until Director `/root` explicitly activates it. The generic workbench correction at `9e253e2402a6acd0b7e41472870780d1445fb3e3` passed independent review at `f3a568681960bf77c63ecd565dd804aff592eae4`. The review report is `docs/Plans/task-logs/compilation-group-workbench-review-r1.md`, SHA256 `D3A811BE20AC7D58ED47B7DFC76395A0473A71CAEB1AD19D09AA46FBACBDEE31`. Implementation and review writes are released. An exclusive source/build ownership check remains required before activation.

Upon activation, existing Astra Medium `/root/combat_draw_continuation` resumes sole production source/build ownership for the same complete fourteen-target W8. R6 is terminal at `fba8ef7c94e3c2da548a8a8546aebc7ac29df113`. Its report SHA256 is `85EAF26F58FFF954B35442312908A26CCABD553E27E100310EE3EB12172898F4`; `build/combat-draw-wave8-r6/terminal-current-inputs.json` has SHA256 `3DA17CD6DC1D14E48831EFF87B7F2AD8A91364D227D91FB59E0812D40D29EE3B`. Authenticate all twenty source/shared inputs before work. All prior reports and evidence remain frozen.

Read parent/canonical AGENTS.md, parent Worker-workflow.md, canonical WORKFLOW.md, SOURCE_POLICY.md, NEXT_STEPS.md and the current sequential program. Read the completed R6 report/indexes and the final workbench correction/review supplied at activation. Preserve the accepted W7 baseline, accepted BFF8 and the ten new focused-exact W8 candidates. No W8 final verifier has run.

Finish func_001F3C00, func_001F5654, func_001F6098, func_001F7ADC, func_001F89B4, func_001FCB28, func_001FD56C, func_0020BFF8, func_0020C014, func_0020C034, func_0020C448, func_0020C4B8, func_002103EC and func_0020DB10 as one wave. The three nonexact targets remain required. Do not shrink or split the wave or advance to another family as though this wave passed.

Use the completed read-only guided-relations proposal as a concrete next source lead. It defines one compatible eight-combination product on R6 `func_001F3C00.definition-narrowed-strip-shared-late-step.c`, SHA256 `73133717AA59277D935360089DE34E19FA9A77798AC93C9623E20535F678CEC1`. The binary relations are primary endpoint recomputation/reuse, shared/distinct primary packet row, and nonzero final full tile recomputation/reuse. Hold its strip representation, late overlap update, vertex forms, cursor histories, outer tile declaration and zero-format tile form fixed. Preserve complete declarations and consumers for each choice. R6 inputs 8, 10, 15 and 17 are four known joint controls; four combinations remain untested in that fixed context. D037 remains a separate current-best control. Choose the implementation and subsequent source reasoning independently; no combination is required or predicted to win.

The actual upstream guided-search path is accepted at `0b204012`, within its documented single-owner/no-extra-data limits. Read that trial/review and use a new owned private copy. Reauthenticate the complete adapter/upstream/launcher/environment/scorer/compiler/Binutils set. Establish faithful retained-control round trips and nonempty scoring coverage. Retain complete bytes and actual relocations separately from heuristic scores. Do not change shared tools or present a custom enumerator as an upstream run. Every newly traced input requires its own pinned production-compiler agreement.

Experiments need not improve score immediately. Preserve the best source separately while exploring supported combinations and temporary regressions. "Rejected" means not selected as current best unless stronger evidence contradicts the structure. Record what each sequence establishes and what remains open. A source-reasoning limit is not pure-C impossibility. Avoid unchanged repetitions without a new question and do not add arbitrary references, dummy storage, padding, invalid pointers or assembler mechanisms.

Automatically include default Kuna and m2c outputs in prepared packets for large/difficult targets. Reuse authenticated unchanged packets; keep m2c primary and both outputs as hypotheses. Request alternate transformations only for specific ambiguities. Packet generation and permutation scores remain outside canonical compilation/linking/acceptance gates.

After activation, own only the fourteen assigned C sources, narrowly necessary existing combat draw headers and corresponding target/linkage records under existing contracts. Own the fresh claim, `C:/Users/Joe/Projects/OgreBattlel64/OB64 Decomp/docs/Plans/task-logs/combat-draw-wave8-r7.md` and ignored `build/combat-draw-wave8-r7/`. Existing matching tools may write normal development outputs and candidate stores in their configured locations. Keep private permuter copies and new assignment evidence under the R7 root; preserve frozen outputs. Create the claim atomically and read it back before other writes. Work on one target at a time on main. You are not alone; preserve other edits and all frozen evidence.

Compiler identities/flags, shared tools, diagnostic instrumentation, accepted boundaries/mappings, source policy and linker rules must remain unchanged. Route independently justified structural/tooling defects separately. Prefer readable structured C and evidence-backed shared types/fields. Explain necessary compiler workarounds beside source. Original retail allocation history remains unknown where evidence does not supply it.

No runtime, Resolver, GUI, bridge or runtime-database work, global installs, agents, staging, commits, push, branches or worktrees. Ordinary matching and analysis candidate stores remain permitted within the output authority above. Held Resolver files remain untouched. No other production source/build writer may run alongside this assignment.

Use focused linked comparisons, mechanical source classification and actual relocation evidence for selected candidates. Once all fourteen targets are ready, run one final normal verifier without a redundant preceding build. Require every target PURE_C, sole intended C ownership, correct placement, exact relocations and target bytes, plus full Rev0 ROM equality. Ordinary source work requires no independent source-review gate.

Return exact final inputs and complete-wave proof, or a precise unresolved handback preserving candidate/hypothesis distinctions and every wave obligation. Release all production/build/report writes and processes explicitly to Director `/root`.
