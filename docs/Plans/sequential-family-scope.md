# Sequential family scope reconciliation

Completed reconciliation; review is pending. Every listed family member and retained gate remains represented in the [target manifest](sequential-family-targets.json).
The Director must bind complete execution waves and resolve shared dependencies before claiming family completion.
This report does not accept new membership, source matches, structural changes, or semantic claims.

## Evidence boundary

The inspected main baseline is `46d78ded786669e306bc3ca80c6f2d9e752a81a5`.
Accepted production inputs remain those consolidated at `497181d8a6ba4bc32c35a465fccd261abe2e0c69`.
Git reports no intervening changes under `src`, `tools`, `config`, or `tests`.
All 1,150 available accepted-input records matched their recorded SHA-256 values; no recorded input path was missing.

The accepted generated proof is `C:/Users/Joe/.codex/ob64-consolidated-intake-20260906/work/final-snapshots-02/3-verification.json`.
Its SHA-256 is `2A837B56D02452A333704C31DBB70334831F7F7C6BD181A0F58FF1F4F64A6FAF`.
It reports passing ownership, placement, relocation, source-object, target-byte, and complete-ROM gates.
Its ROM SHA-256 is `571E83396BC81E70DA4C0A20313D82DBD7DFE685F2C37418C8E27F927E2CC67A`.
Per-target classes come from its mechanical source-object evidence, not active registry presence or historical plan labels.
No new build or verifier ran for this reconciliation.

The manifest preserves exact source-plan rows, line references, input hashes, accepted proof references, candidate origins, execution ownership, and acceptance gates.
It preserves all 78 Combat members, 113 Squad functions and one data owner, and 74 High Attack functions.
It also preserves Combat boundary candidates, shared interfaces, Squad's 97 external direct-call dispositions, and direct-data exclusions.
These are inventory identities, not independently maintained progress counters.
The three frozen parent manifests and the later High Attack donor plan remain historical read-only evidence.

## Program requirements

- [ ] Accept the process correction for sequential main, internal agents, and readable independently derived C.
- [ ] Implement and independently review sound compilation-group support with the required heavyweight audit.
- [ ] Preserve public entries, accepted boundaries, compiler identity, relocations, source classification, sole ownership, target bytes, and complete-ROM equality.
- [ ] Reject symbol-specific bypasses, fabricated padding, and source-policy exceptions.
- [ ] Complete a bounded readability experiment with linked comparisons and one complete-wave verifier.
- [ ] Investigate and attempt independent PURE_C conversion of all three boot hybrid leads below.
- [ ] Complete Combat's supported members, discovery, and required shared dependencies.
- [ ] Complete Squad's released source scopes and retained research, structural, interface, and deferred-scope decisions.
- [ ] Complete High Attack's original waves and every retained PURE_C retry.
- [ ] Audit every manifest requirement against final source and authoritative proof. The Director records closure and coherent local integration.

| Boot lead | Canonical registry key | Current source | Required gate |
|---|---|---|---|
| Frame shutdown | `func_00003798` | `src/boot/boot_resource_state_reset.c` | Independent PURE_C attempt; preserve concrete blocker and reopen condition when unresolved |
| Asset unpacking | `func_00009EFC` | `src/boot/boot_resource_node_lzss_context_materialize.c` | Same gate; filename does not establish stronger behavior claims |
| Recursive payload clear | `func_0000A1F8` | `src/boot/boot_resource_node_recursive_payload_clear.c` | Same gate; accepted hybrid bytes are not matching C |

The Director supplied these lead identities, and the current active registry confirms their source paths.
All three remain mechanically HYBRID_C in accepted proof.
External work supplies leads only; new source must be independently derived from retail and accepted project evidence.

## Combat execution and completion

W1–W4 already passed accepted combined proof and remain completed source context.
The inventory's old ASM labels do not reopen those matches.
W5 retains eight pending targets: seven provisional archived sources and the pose parser `func_002050AC`.
The four existing return stubs remain completed members of the twelve-member inventory wave.
Do not verify seven targets and call W5 complete.

The pose parser requires its complete accepted 372-byte owner.
The exact 364-byte body and bounded tail diagnosis do not establish complete sole-C ownership.
Compilation-group research must resolve that representation without inventing padding or changing accepted boundaries.
The group's accepted stubs retain their public entries and matching evidence.

- [ ] Finish W5 after accepted group support, using all eight pending targets in its final combined gate.
- [ ] Execute W6's complete pool lifecycle/accessor membership, preserving the already accepted member.
- [ ] Execute W7's complete descriptor/compositing membership.
- [ ] Execute W8's complete draw/body integration membership, preserving the already accepted member.
- [ ] Qualify all eight boundary candidates before inclusion or exclusion; their exact identities and questions remain in the manifest.
- [ ] Resolve renderer-call ambiguity between `func_00159FD4` and `func_0025FD90` through accepted runtime-owner evidence.
- [ ] Enumerate and classify all eight inventoried dispatch owners' jump-table destinations.
- [ ] Identify selector function-pointer/data consumers, especially `func_00201778`.
- [ ] Resolve registration target live `0x800EA604` from `func_00208DC8` before excluding its callback.
- [ ] Reassess shared owners and stream/carrier integrations, including `func_002267A8`; retain evidenced general-service exclusions.

The inventory preserves `func_00201D24` and its internal `func_00201D2C` label, and the accepted 3,800-byte `func_001F7ADC` owner.
Accepted matches do not automatically close the separate discovery gates.
Newly supported members require explicit inventory evidence and applicable review before supplementary matching scopes.

## Sequential shared-dependency resolution

There is a scheduling cycle if Combat waits for High Attack while High Attack waits for Squad and Combat closure.
This is a plan dependency, not evidence of a runtime call cycle.
The current program authorizes Combat to execute required shared members once and High Attack to reuse their accepted results.
Original family membership and original High Attack complete-wave gates remain unchanged.

Sixteen unfinished High Attack owners occur in Combat's shared ledger:

| Original High Attack wave | Shared owners requiring work or retry |
|---|---|
| W3 | `func_001FFE80` |
| W4 | `func_002159D0`, `func_00215CF0` |
| W5 | `func_0021B894` |
| W6 | `func_0022257C`, `func_00222604` |
| W8 | `func_0021D7F0`, `func_0021DCA4`, `func_0021E070`, `func_0021E50C`, `func_0021EBBC`, `func_002215D0`, `func_00222190`, `func_0022222C`, `func_00222344`, `func_002223E0` |

First qualify which shared interfaces are required family implementations under the accepted inventory boundary.
An unresolved relationship remains a Combat discovery requirement; do not silently classify it as an excluded utility.
The Director then binds required shared work to Combat execution while retaining each original High Attack wave membership.
If an owner needs later-wave type or dispatch evidence, investigate that bounded dependency within Combat's shared prerequisite scope.
Do not launch a concurrent High Attack family or duplicate source implementation.

The original High Attack wave cannot become exact-complete until every original member passes its complete-wave gate.
When a full prerequisite wave is needed, retain all original targets in that wave and close its dependencies explicitly.
When only shared members are ready, retain provisional evidence without claiming the original High Attack wave complete.
If this prevents Combat's all-supported-members closure, the Director must expand the sequential prerequisite package or resolve the membership question with reviewed evidence.
The Director must not silently skip the dependency or redefine an existing wave as single-function waves.

## Squad execution and completion

The constructor, normal loader, and map/update dispatcher are accepted opening-wave PURE_C results.
The historical nine-PURE_C/five-HYBRID_C snapshot predates that opening-wave acceptance.
Current proof retains four Squad hybrids: `func_001072B8`, `func_0010DDB4`, `func_0011B344`, and `func_0012FBB8`.
Their preserved interface status is not matching-C completion; release conversion or record the supported retained-interface disposition explicitly.

The frozen four-target continuation is unfinished: `func_00129068`, `func_001291B4`, `func_0012967C`, and `func_00128E80`.
Its archived focused results do not complete the broader construction/deployment wave.
The Director must define the complete continuation boundary before execution, preserving those four targets together.

| Planned wave | Complete requirement |
|---|---|
| W2 construction/deployment/records | Preserve all named construction and record groups, formation mapper, and gated supply initializer. Include ledger-only route maintenance/query `func_0012F140` and `func_0012F530`. |
| W3 wake/conditions/lifecycle | Preserve both listed groups, route flag uncertainty, and finalizer return-store behavior. |
| W4 scheduler/pursuit | Preserve scheduler, candidate, eligibility, selection, reset, target-writing, teardown, and membership owners. Research all three terrain-policy helpers first. |
| W5 movement/routes | Close `func_0012B1C4` and `func_0012D6FC`; review the four retained route interfaces together. Preserve every movement owner and accepted extent through z64 `0x0012C788`. |
| W6 supplies | Preserve capacity, membership, three consumers, compactor, controller, and two branch helpers. Resolve supply policy and the encoded-item writer gate. |

The target manifest gives each ledger row a recommended wave or explicit interface, hybrid, placement, or deferred gate.
These recommendations reconcile the accepted ledger; they do not authorize a new structural owner or membership change.

- [ ] Resolve placement entry for `func_00120BBC` and `func_001307D4` through heavyweight structural audit and independent review.
- [ ] Preserve the known three callers and six calls targeting owner start plus four.
- [ ] Resolve `func_00130E60` across accepted owners `p2449` and `p2450` before conversion.
- [ ] Validate supply initializer ownership using the accepted 136-byte original prefix, 24-byte compiler table, and eight-byte original tail.
- [ ] Preserve `data_00197738` as one data owner; internal switch labels are not six functions.
- [ ] Preserve the unnamed finalizer return stored at source-record `+0x18` and literal route flag `0x00020000`.
- [ ] Resolve all nine deferred scripted/site owners' complete scope and benefit before release.
- [ ] Keep core site owners `func_00109C04` and `func_0013A3D8` with that deferred decision.
- [ ] Keep broad hybrid `func_0010DDB4` outside ordinary waves until its complete owner scope is justified.
- [ ] Review shared interfaces, including broad terminal/supply teardown `func_0012A958`; do not omit them or assume conversion authority.
- [ ] Preserve all 97 external direct-call dispositions, direct-data exclusions, and indirect-call limits.
- [ ] Reinspect affected call/data boundaries when later accepted source invalidates the static inventory.

Deferred scope is unresolved program work until its condition is decided with evidence.
Retained engine interfaces can remain assembly where the accepted boundary explicitly permits it.
Normal-drop initialization remains preserved; direct-event and creature rewards remain outside this family.

## High Attack execution and completion

The donor's later plan supersedes stale progress descriptions as historical evidence, while canonical accepted proof controls current source status.
W1 and W2 are accepted PURE_C.
W3 retains `func_001FFE80`; W4 retains four hybrid retries, including `func_002158E4` and `func_00217BA8` beyond Combat's shared subset.
Every retry remains open until PURE_C acceptance or Joe's explicit exception decision at program closure.

- [ ] Reattempt W3/W4 using new compiler lifetime, neighboring-source, or source-shape evidence; preserve concrete failed hypotheses and reopen conditions.
- [ ] Finish W5's complete eight-member boundary, including B438 and B894.
- [ ] Use the accepted prefix, repeated-section, aligned-object, and provenance mechanisms before claiming a missing tool capability.
- [ ] Preserve B438's measured saved-register allocation blocker; donor acceptance of B894 does not accept the incomplete wave on main.
- [ ] Finish all seventeen W6 owners; fourteen archived donor-active sources remain provisional.
- [ ] Finish nonexact B1F4 and D14C; inspect the inactive EF50 candidate under reviewed combined production ownership.
- [ ] Preserve B06C's structural partition evidence separately from ordinary source activation and complete-wave acceptance.
- [ ] Resolve W6 shared prototypes and prefer the committed A414 source calling `func_0020C014`; do not invent the older unresolved alias.
- [ ] Map the four dispatch modes before tuning large owners.
- [ ] Finish all seven W7 members using supported W6 types and mode relationships.
- [ ] Finish all fifteen W8 owners, including bridge `func_0021EAF0`.
- [ ] Preserve terminal `func_0021EBBC` as one logical 10,272-byte owner with a 9,284-byte head and 988-byte continuation.
- [ ] Attempt supported naming evidence for each match; uncertain semantics do not block byte matching.

The donor parked B438 under its documented blocker policy and advanced to W6; that action did not complete W5.
New evidence must trigger a practical retry before another parking decision.
Compilation difficulty, function size, exact hybrids, and archive presence do not establish completion.
All five shared bridges retain their original complete-wave commitments.

## Verification, limits, and handoff

Use focused linked diff and mechanical classification during source iteration.
Run the canonical verifier once after the complete assigned wave is ready; it builds CURRENT when needed.
Confirm every assigned target's PURE_C classification and all matching gates in that same final report.
Verify changed completed-wave integration inputs; do not repeat solely for an unchanged commit or handoff.
Ordinary matching adds no independent source reviewer gate.
Structural, tooling, semantic, and new inventory claims retain applicable independent review.

This reconciliation is Supported, review pending, for planning coverage and sequencing.
Its source-status observations are bounded to authenticated accepted proof and unchanged production inputs.
It does not prove exhaustive indirect membership, unresolved runtime meaning, or future candidate exactness.
The ignored extraction script is `build/sequential-scope/reconcile.js`; the tracked JSON remains self-contained.
The Director's next action is scope intake, then concrete prerequisite and complete-wave assignments.
No source, tool, configuration, original plan, history, branch, worktree, or commit was changed by this task.
